# Handoff — 2026-05-22 (Phase 05.7.9d branch)

> Created at context near-exhaustion. Next session: open this file first.
> Alternative: invoke `/gsd-resume-work` and let GSD orchestrate.

---

## Where the codebase stands (HEAD = main, commit `2e1606f`)

Today shipped **9 atomic commits** on top of `ae4a625`:

```
2e1606f  feat(campaigns): hide/show columns menu + new ROAS Shopify · פלטפורמה column
efd628c  fix(trust): extend trust chip to TikTok + reward Meta↔mapping agreement
c22954d  fix(campaigns): request ?lineItems=true so the new per-platform columns get data
625415a  feat(campaigns): split Shopify columns into 4 — deterministic per-platform + total across-all
b31539e  feat(attribution): deterministic-first per-platform allocation + total denominator in tooltip
28795ce  ui(campaigns): round 'יח׳ Shopify' display to integer + add * mark for fractional shares
a83d72f  ui(reconciliation): switch Shopify line to dashed pattern (was thicker solid)
d2460fc  feat(reconciliation): add TikTok to channel-correlation chart + product breakdown
7244fff  feat(campaigns): TikTok product-mapping + drilldown + refresh UX
c426011  fix(apis): bump Meta v23→v25 + Shopify 2024-10→2026-04 + add tiktok to platform CHECK
```

All pushed to `https://github.com/dor77777-prog/script-roas/tree/main`.
Vercel auto-deployed each. Production alias `roas-dashboard-smoky.vercel.app`.
413 tests pass; `npx tsc --noEmit` clean.

## Pending work — ordered by user priority

### A. Phase 05.4 — Unmapped active campaigns indicator (RESEARCH DONE)

**Research artifacts (read these first, both ~ready-to-plan):**
- `.planning/phases/05.4-unmapped-active-campaigns-indicator/05.4-RESEARCH.md` (29KB, canonical GSD format, gsd-phase-researcher output)
- `.planning/phases/05.4-unmapped-active-campaigns-indicator/05.4-RESEARCH-2026-05-22.md` (general-purpose diff doc)
- `.planning/phases/05.4-unmapped-active-campaigns-indicator/05.4-CONTEXT.md` (locked decisions — partially invalidated, see RESEARCH.md §2)
- `.planning/phases/05.4-unmapped-active-campaigns-indicator/05.4-DISCUSSION-LOG.md`

**Key facts the research surfaced:**
- D-02 / D-03 in CONTEXT.md (write status to Apps Script Sheets) are **DEAD** — Postgres is permanent since 05.7.0. Replace with `ALTER TABLE campaigns_daily ADD COLUMN status TEXT;` migration.
- TikTok status endpoint: **`GET /open_api/v1.3/campaign/get/`** with `fields=["campaign_id","campaign_name","operation_status","secondary_status"]`. Use `operation_status` (values `ENABLE | DISABLE | DELETE`), NOT `secondary_status` (which excludes just-toggled campaigns). Verified via TikTok Python SDK on GitHub.
- 9 chips max (3 stores × 3 platforms). Google chips appear but its rows can't be mapped — same gate as existing `CampaignsTableRow.tsx:318-320`.
- `ProductPickerModal.tsx` per-store invariant intact at lines 122 + 139.
- ~6 weeks of legacy `campaigns_daily` rows have NULL status. Aggregator treats NULL as "not active" (D-10). On first deploy, strip will under-report until next cron-daily — recommended to fire sync-now manually after the migration.

**3 open questions for the user (block planning until answered):**
1. Show Google chips in the strip even though Google can't be mapped (PMax)? Or exclude Google entirely?
2. Refresh status on every cron-live tick (10min) or only on cron-daily (24h)?
3. Legacy NULL handling — sync-now manual trigger after deploy, or accept 24h ramp?

**Plan-execution flow (when answers in):**
```
1. gsd-planner spawns with research + answers → produces 05.4-PLAN.md (4 sub-plans)
2. (Optional) gsd-review for codex cross-AI critique → converge
3. gsd-executor runs each sub-plan in order
4. Plan 1: schema migration + cronDaily writer + fetcher diffs
5. Plan 2: aggregator + types
6. Plan 3: <UnmappedCampaignsStrip> component + filter wiring
7. Plan 4: tests + manual update
```

### B. Phase 05.3 — In-dashboard searchable user manual (RESEARCH DONE)

**Research artifacts:**
- `.planning/phases/05.3-in-dashboard-searchable-user-manual-with-live-component-exam/05.3-RESEARCH.md` (canonical GSD, 597 lines)
- `.planning/phases/05.3-in-dashboard-searchable-user-manual-with-live-component-exam/05.3-RESEARCH-2026-05-22.md` (general-purpose, 401 lines)
- `.planning/phases/05.3-in-dashboard-searchable-user-manual-with-live-component-exam/05.3-CONTEXT.md`
- `.planning/phases/05.3-in-dashboard-searchable-user-manual-with-live-component-exam/05.3-UI-SPEC.md`

**Key facts:**
- Project is **Next 15.5.18 + React 19.2.6** (not 14/18 as the prompt assumed).
- `next-mdx-remote` is **archived as of 2026-04-09** → use **`next-mdx-remote-client@2.1.10`** (active fork, React ≥19.1.0).
- Fuse.js@7.3.0 confirmed (8.3 KB gz, Hebrew-tested).
- Pitfall #2: every component in `dashboard-web/src/components/` is `'use client'` (41/41). `<MDXRemote/>` from `/rsc` is server. Required topology: `app/page.tsx` (server) renders MDX → passes as `manualSlot: ReactNode` to `<Dashboard>` (client). **Non-obvious arch rewrite to `app/page.tsx`.**
- Pitfall #3: `urlState.ts:24-26` TAB_VALUES Set MUST be updated alongside `Dashboard.tsx:77` and `CommandPalette.tsx:55` or `?tab=manual` silently falls back to `home`.
- Manual has TWO version strings (line 10: v1.4, line 2790: v1.8) and NO YAML frontmatter. Plan 1 must add frontmatter.
- CPM chart is IIFE in `CampaignDrawer.tsx:555-700`, not exported. `ManualCPMChartExample` must rebuild a 200px chart inline.
- `<ROASChip>` referenced in original SPEC doesn't exist — must build from `roasLabel()` + local `TONE_BG` lookup.

**3 open questions:**
1. Bump manual to v1.9 (Recommended), or keep v1.8?
2. Accept Hebrew↔English transliteration gap, or build a glossary (`ROAS↔רואס`, `CPM↔עלות ל-1000 חשיפות`)?
3. Esc focus behavior — return to search input, or to last-focused row?

### C. Smaller pending asks (operator surfaced during today's session)

1. **Phase B of column customization** (deferred): drag-reorder + per-column color customization (header bg, text color, highlight rules). Stage A (hide/show) shipped in `2e1606f`.
2. **Rename `metaClaim` → `platformClaim` across codebase**: cosmetic, mentioned in passing today. The field semantically holds any platform's claim now. ~30 file touch (analyzer + hook + types + tests). Low priority.
3. **Open questions from the 05.3 + 05.4 research** (above) — needs user decision.

### D. Things confirmed working in production (don't re-check)

- TikTok creds in Vercel (`UZOSHOP_TIKTOK_ACCESS_TOKEN` + `UZOSHOP_TIKTOK_ADVERTISER_ID`). Verified via 3 live API calls earlier today (test results in conversation, not persisted).
- `dashboard-web/.env.local` has the same creds locally (gitignored, chmod 600). For ad-hoc TikTok API testing.
- Supabase migration `20260522102151_add_tiktok_platform_check.sql` applied to prod (`'tiktok'` added to platform CHECK constraint).

## How next session should resume

**Option 1 (cleanest):** Open new session in this repo. First action:
```
/gsd-resume-work
```
GSD will read its state files + this HANDOFF and orchestrate.

**Option 2 (manual):** Open new session. Tell Claude:
> Read `.planning/HANDOFF-2026-05-22.md` first. We're picking up Phase 05.3 + 05.4 with the research already done in `.planning/phases/05.3-...` and `.planning/phases/05.4-...`. Need answers to the open questions before planning.

**Option 3 (if you want to do small asks first):**
> Read `.planning/HANDOFF-2026-05-22.md` § C. Knock out items 1-2 before the bigger phase work.

## What NOT to do in the next session

- ❌ Re-run the 4 research agents — done, written to disk.
- ❌ Re-bump API versions — Meta v25, Shopify 2026-04, Google v24, TikTok v1.3 are locked in.
- ❌ Re-explain the trust chip / 4-column / agreement-upgrade design — implemented and shipped.
- ❌ Try to read this conversation's history — the user only sees the diff; this file is the canonical record.

---

*Created by Opus 4.7 (1M context) on 2026-05-22 ~14:55 IL.*
*Last commit: `2e1606f` on `main`.*
