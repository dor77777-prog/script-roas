---
phase: 05.1-per-store-live-trigger-split
plan: 01
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - DailyUpdate.gs
  - Main.gs
autonomous: true
requirements: [P5.1-LIVE-TRIGGER-SPLIT]

must_haves:
  truths:
    - "3 wrappers חדשים קיימים ב-DailyUpdate.gs: runLiveUpdateUzoshop, runLiveUpdateZolplus, runLiveUpdateUsmile"
    - "כל wrapper קורא ל-runUpdateForSingleStore_(storeId, todayStr_()) בדיוק כמו ה-daily wrappers שכבר נבדקו ב-Phase 5"
    - "installLiveTrigger מתקין 3 triggers, אחד לכל wrapper, כל אחד everyMinutes(15)"
    - "removeLiveTrigger מנקה גם את runLiveUpdate (legacy) וגם את 3 ה-handlers החדשים, כדי שניתן יהיה לעבור deployment ישן->חדש באופן idempotent"
    - "runLiveUpdate המקורי נשאר ללא שינוי כ-manual entry point (לקריאה מ-editor או menu)"
  artifacts:
    - path: "DailyUpdate.gs"
      provides: "runLiveUpdateUzoshop / runLiveUpdateZolplus / runLiveUpdateUsmile"
      contains: "function runLiveUpdateUzoshop"
    - path: "Main.gs"
      provides: "installLiveTrigger מתקין 3 triggers; removeLiveTrigger מנקה 4 handlers"
      contains: "runLiveUpdateUzoshop"
  key_links:
    - from: "runLiveUpdateUzoshop"
      to: "runUpdateForSingleStore_"
      via: "ישיר — אותו pattern של runDailyUpdateUzoshop"
      pattern: "runUpdateForSingleStore_\\('uzoshop',\\s*todayStr_"
    - from: "installLiveTrigger"
      to: "3 triggers"
      via: "ScriptApp.newTrigger(...).timeBased().everyMinutes(15)"
      pattern: "ScriptApp\\.newTrigger\\('runLiveUpdate(Uzoshop|Zolplus|Usmile)'"
---

<objective>
לפצל את ה-15-min `runLiveUpdate` trigger (שכרגע משלב את כל 3 החנויות בריצה אחת, עם 19% failure rate בגלל timeout על ה-6-min cap) ל-3 triggers נפרדים — אחד לכל חנות. אותו pattern שעבד ב-05-01 על ה-daily trigger, אבל עבור ה-live trigger.

Purpose: גילוי במהלך UAT של Phase 5 — צילום מסך של Triggers UI הראה ש-`runLiveUpdate` נכשל ב-19.12% מההרצות. הסיבה היא אותה quota cascade שתוקנה ב-05-01 עבור ה-daily trigger. רגרסיה אם נשאיר את זה: הדשבורד מאבד `live` updates ~פעם בשעה.

Output: 3 wrappers חדשים + עדכון installer/remover.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@DailyUpdate.gs
@Main.gs
@Config.gs

<interfaces>
<!-- כל ה-interfaces כבר קיימים ב-main מ-Phase 5 — לא צריך לכתוב כלום חדש -->

From DailyUpdate.gs (Phase 5):
```javascript
// helper ש-05-01 הוסיף — אנחנו פשוט קוראים לו מ-3 wrappers חדשים
function runUpdateForSingleStore_(storeId, dateStr): void;

// המקורי שצריך להישאר כ-manual entry
function runLiveUpdate(): void;  // קורא ל-runUpdateForDate(todayStr_()) — sequential על כל 3 החנויות
```

From Config.gs:
```javascript
function todayStr_(): string;  // 'YYYY-MM-DD' בזמן TZ='Asia/Jerusalem'
```

From Main.gs (current state — לפני 05.1):
```javascript
function installLiveTrigger(): void;  // מתקין trigger אחד ל-runLiveUpdate, everyMinutes(15)
function removeLiveTrigger(): void;   // מסיר רק את ה-handler 'runLiveUpdate'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: הוסף 3 live wrappers ל-DailyUpdate.gs</name>
  <files>DailyUpdate.gs</files>
  <behavior>
    - runLiveUpdateUzoshop() קורא ל-runUpdateForSingleStore_('uzoshop', todayStr_())
    - runLiveUpdateZolplus() קורא ל-runUpdateForSingleStore_('zolplus', todayStr_())
    - runLiveUpdateUsmile() קורא ל-runUpdateForSingleStore_('usmile360', todayStr_())
    - runLiveUpdate המקורי נשאר ללא שינוי
    - הקובץ נשאר syntactically valid (`node -c` ירוק אחרי patch)
  </behavior>
  <action>
    1. במצא את הבלוק של הפונקציות הקיימות `runDailyUpdateUzoshop / runDailyUpdateZolplus / runDailyUpdateUsmile` (Phase 5 additions, סביב שורות 35-60).

    2. **מתחת** לבלוק הזה (אחרי `runDailyUpdateUsmile`'s closing brace, ולפני `ensureSummaryMonthBlock_`), הוסף:

    ```javascript
    /**
     * Per-store LIVE trigger entry points (Phase 5.1).
     *
     * Mirrors the daily per-store split (Phase 5) — each gets its OWN
     * 6-min budget so a slow Shopify/Meta response on one store can't
     * cascade into a timeout for the others. Triggers run independently
     * every 15 minutes via installLiveTrigger().
     *
     * The original runLiveUpdate() is retained as a MANUAL entry point
     * for full-sequential runs (e.g., from the editor / spreadsheet menu).
     * Triggers no longer call it directly.
     */
    function runLiveUpdateUzoshop() {
      runUpdateForSingleStore_('uzoshop', todayStr_());
    }
    function runLiveUpdateZolplus() {
      runUpdateForSingleStore_('zolplus', todayStr_());
    }
    function runLiveUpdateUsmile() {
      runUpdateForSingleStore_('usmile360', todayStr_());
    }
    ```

    3. **אל תיגע** ב-`runLiveUpdate()`, `runDailyUpdate*`, `runUpdateForDate`, `updateStoreForDate_`, `runUpdateForSingleStore_`, `ensureSummaryMonthBlock_`, `archiveOlderThan`, או כל פונקציה אחרת. רק 3 שורות חדשות + comment block.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('DailyUpdate.gs','utf8');['runLiveUpdateUzoshop','runLiveUpdateZolplus','runLiveUpdateUsmile'].forEach(fn=>{if(!new RegExp('function '+fn+'\\\\b').test(s))throw new Error('missing '+fn)});if(!/function runLiveUpdate\(\)/.test(s))throw new Error('runLiveUpdate removed by mistake');if(!/runUpdateForSingleStore_\('uzoshop',\s*todayStr_/.test(s))throw new Error('uzoshop wrapper not wired to runUpdateForSingleStore_');console.log('OK')"</automated>
  </verify>
  <done>
    - 3 פונקציות חדשות קיימות ב-DailyUpdate.gs
    - runLiveUpdate המקורי קיים ולא נגעו בו
    - אין שינוי בחתימה של פונקציות קיימות
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: עדכון installLiveTrigger + removeLiveTrigger ב-Main.gs</name>
  <files>Main.gs</files>
  <behavior>
    - installLiveTrigger מתקין 3 triggers (אחד לכל wrapper) everyMinutes(15), במקום אחד ל-runLiveUpdate
    - הרצה מיידית בהתקנה נעלמת (כמו ב-installDailyTrigger ב-05-01) כדי שלא ייצור burst של 3 קריאות כפולות שיכשילו את ההתקנה עצמה
    - הלוג של ההתקנה אומר "3 live triggers installed: uzoshop / zolplus / usmile (every 15 minutes)"
    - removeLiveTrigger מסיר את 4 ה-handlers (legacy `runLiveUpdate` + 3 חדשים), כדי שניתן יהיה לרוץ installLiveTrigger מהגרסה החדשה גם אם הישן מותקן (idempotent)
    - לא נגעו ב-installDailyTrigger, removeDailyTrigger, או setupAll
  </behavior>
  <action>
    החלף את 2 הפונקציות `installLiveTrigger` ו-`removeLiveTrigger` ב-Main.gs (שורות ~95-124) ב:

    ```javascript
    /**
     * Installs THREE live triggers (Phase 5.1 split) — one per store.
     *
     * Each runs every 15 minutes in its own execution context, so each
     * gets its own 6-min budget. Mirrors the daily split (Phase 5 — 05-01)
     * that fixed the same quota-cascade problem for the 00:05 daily
     * trigger. Without this split, the historical 19% failure rate of
     * the single combined trigger continues.
     *
     * Apps Script's everyMinutes(15) doesn't offer a minute-offset knob,
     * so all 3 fire at the same scheduled instant. That's fine: each
     * execution is INDEPENDENT (its own 6-min budget, its own Sheets
     * API context). Short-window quota contention is bounded because
     * each execution only handles 1 store's worth of API calls.
     *
     * Unlike the old installer, this one does NOT run any of the
     * functions immediately. The first per-store live update happens at
     * the next 15-minute boundary. Rationale: an immediate sequential
     * burst would defeat the very quota separation we're setting up,
     * and the next firing is at most 15 minutes away.
     */
    function installLiveTrigger() {
      removeLiveTrigger();

      ScriptApp.newTrigger('runLiveUpdateUzoshop')
        .timeBased().everyMinutes(15).create();
      ScriptApp.newTrigger('runLiveUpdateZolplus')
        .timeBased().everyMinutes(15).create();
      ScriptApp.newTrigger('runLiveUpdateUsmile')
        .timeBased().everyMinutes(15).create();

      Logger.log('3 live triggers installed: uzoshop / zolplus / usmile (every 15 minutes)');
      Logger.log('First run at next 15-min boundary. To refresh today now, run runLiveUpdate from the editor.');
    }

    /**
     * Removes ALL handlers that the live-trigger installer knows about,
     * including the legacy single-trigger handler ('runLiveUpdate') so a
     * re-install after deploying Phase 5.1 doesn't leave the old trigger
     * orphaned. installDailyTrigger remains untouched.
     */
    function removeLiveTrigger() {
      const HANDLERS = new Set([
        'runLiveUpdate',          // legacy — pre-Phase-5.1
        'runLiveUpdateUzoshop',
        'runLiveUpdateZolplus',
        'runLiveUpdateUsmile',
      ]);
      const triggers = ScriptApp.getProjectTriggers();
      let removed = 0;
      for (const t of triggers) {
        if (HANDLERS.has(t.getHandlerFunction())) {
          ScriptApp.deleteTrigger(t);
          removed++;
        }
      }
      Logger.log(`Removed ${removed} live trigger(s)`);
    }
    ```

    אל תיגע ב-`setupAll`, `installDailyTrigger`, `removeDailyTrigger`, `onOpen`, או בשום פונקציה אחרת.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('Main.gs','utf8');['runLiveUpdateUzoshop','runLiveUpdateZolplus','runLiveUpdateUsmile'].forEach(fn=>{if(!new RegExp(\"newTrigger\\\\('\"+fn+\"'\\\\)\").test(s))throw new Error('trigger missing for '+fn)});const live=s.match(/function installLiveTrigger\(\)[\\s\\S]+?function removeLiveTrigger/);if(!live)throw new Error('installLiveTrigger or removeLiveTrigger missing');if(!/'runLiveUpdate',\s+\/\/ legacy/.test(s))throw new Error('legacy runLiveUpdate not in remove set');if(!/everyMinutes\(15\)/.test(s))throw new Error('15-min interval missing');console.log('OK')"</automated>
  </verify>
  <done>
    - installLiveTrigger מתקין בדיוק 3 triggers
    - removeLiveTrigger מנקה 4 handlers (3 חדשים + 1 legacy)
    - אף פונקציה אחרת ב-Main.gs לא השתנתה
    - הרצה מיידית הוסרה (אין `runLiveUpdate()` קריאה מתוך ה-installer)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apps Script live trigger → updateStoreForDate_ | פנימי — אין input חיצוני (todayStr_ הוא דטרמיניסטי) |
| External APIs → updateStoreForDate_ | כבר מוגן ע"י fetchWithRetry_ + try/catch קיימים |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.1-01 | Denial of Service | per-store live trigger | mitigate | Apps Script `everyMinutes(15)` בלי offset → 3 ה-executions עלולות לרוץ בו זמנית. אבל כל אחת בקונטקסט נפרד עם 6-min budget משלה. Short-window API quota שמורה כי כל execution = 1 חנות. אם בעתיד נראה contention — אפשרי להוסיף sleep רנדומלי קצר בתחילת כל wrapper. |
| T-05.1-02 | Repudiation | מי רץ מתי | accept | Apps Script Executions log מתעד אוטומטית; אין PII |
</threat_model>

<verification>
- בדיקה אוטומטית: `node -c` ירוק על שני הקבצים אחרי ה-patch
- בדיקה ידנית (post-deploy): אחרי שמריצים `installLiveTrigger`, ה-Triggers UI מציג 3 live triggers + 4 daily triggers (מ-Phase 5) = 7 triggers סך הכל
- אחרי 1 שעה (4 הרצות): failure rate ב-Apps Script Executions tab אמור להיות 0% או קרוב לכך (ירידה דרסטית מ-19%)
- אחרי 24 שעות: ה-Executions tab מראה ~288 הרצות (3 × 96 כל 15 דק) עם failure rate <2%
</verification>

<success_criteria>
- 3 wrappers חדשים קיימים ב-DailyUpdate.gs
- installLiveTrigger מתקין 3 triggers נפרדים
- removeLiveTrigger מנקה 4 handlers (idempotent re-install עובד)
- אין שינוי בחתימה של runLiveUpdate המקורי
- node -c עובר על שני הקבצים
</success_criteria>

<output>
After completion, create `.planning/phases/05.1-per-store-live-trigger-split/05.1-01-SUMMARY.md`
</output>
