---
phase: 05.2.2.1-campaigns-deep-audit-fixes-codex-via-gsd-cross-ai
verified: 2026-05-20T16:12:00Z
status: passed
score: 32/32 must-haves verified
overrides_applied: 0
gates:
  tsc_noemit: pass
  vitest_run: pass (21 files / 222 tests passing)
  files_scope_dashboard_web_src: pass (zero files outside dashboard-web/src/ + .planning/)
fix_commit_inventory:
  fix_01_to_07_plan_01: 7/7 atomic commits present
  fix_08_to_23_plan_02: 16/16 atomic commits present
  test_01_to_08_plan_03: 8/8 atomic commits present
  fix_24_post_review: 1/1 (commit bd7f1d0)
  review_fixes_wr_01_to_06: 6/6 commits present
  review_fixes_in_01_to_08: 8/8 commits present
  total_atomic_commits: 46/46
---

# Phase 05.2.2.1: Campaigns Deep-Audit Fixes (codex via gsd-cross-ai) — Verification Report

**Phase Goal:** Apply the 31 actionable findings from the Phase 5.2.2 cross-AI deep audit of the Campaigns tab (Opus + codex synthesis) — split into 3 plans (7 P0/P1 critical fixes, 16 P2 secondary fixes, 8 regression tests). All fixes delegated to codex via cross-AI; FIX-24 + 14 code-review fixes (WR-01..WR-06 + IN-01..IN-08) applied by Claude Code on top.

**Verified:** 2026-05-20T16:12:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

#### Plan 01 truths (7)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `isOrganicSource` returns false for `'other-paid'` AND `''` | VERIFIED | `MetaShopifyReconciliation.tsx:245-246` adds the two explicit exclusions with the documented policy comment |
| 2 | Shopify + Organic accumulators use `hitMapped` boolean | VERIFIED | `MetaShopifyReconciliation.tsx:188-203` (Shopify) and `:254-271` (Organic); each has `let hitMapped = false`, `hitMapped = true`, `if (!hitMapped) continue` |
| 3 | Drilldown carries `storeId` through Row → Table → Drawer with strict `r.storeId === drillStoreId` | VERIFIED | Row signature `onDrillCampaign(campaignId, platform, storeId)` at `CampaignsTableRow.tsx:63,107`; Table has `drillStoreId` state at `:319` + memoized `drillRows` at `:580-589`; Drawer Props.storeId required at `CampaignDrawer.tsx:77`; strict equality is in `filterDrillRows` (`drillFilter.ts:15`) |
| 4 | AdsDrawer fetches `/api/orders-attribution` via `buildDateRangeKey` with range params | VERIFIED | `AdsDrawer.tsx:93` `buildDateRangeKey('/api/orders-attribution', drawerRange)`; the import is at `:26` |
| 5 | CampaignDrawer summary uses `latestAdSetBudgetDate` Map with strict `>` | VERIFIED | `CampaignDrawer.tsx:228` declares Map; `:265-269` gates overwrite on `!prev || r.date > prev`; seed at `:257` |
| 6 | `aggregate()` runs second-pass normalization on `map.values()` (ABO→campaignBudgetCad=null, CBO→adSetBudgetCad=null) | VERIFIED | `campaignsAggregator.ts:123-126` runs the second pass after the per-row loop; the function was extracted to `lib/` per TEST-03 |
| 7 | `/api/ads` accepts range params; `fetchAdsData` filters server-side; AdsDrawer SWR key uses `buildDateRangeKey('/api/ads', drawerRange)` | VERIFIED | `route.ts:5,17` imports + uses `parseRangeParams`; `route.ts:29` passes `{ range }` to fetcher; `ads.ts:71-72,107` accepts `opts.range` + filters via `isInRange`; `AdsDrawer.tsx:84` builds the keyed URL |

#### Plan 02 truths (16)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 8 | `useCampaignTrueRevenue.ts` hoists `dailyMetaByCampaign` Map from `allCampaignRows` outside per-campaign loop | VERIFIED | `useCampaignTrueRevenue.ts:191-204` declares the `useMemo` builder; `:325` consumes via `.get(campaignKey(...))` |
| 9 | `analyzeAttribution` checks `o.storeId !== campaign.storeId` as defense-in-depth | VERIFIED | `attributionAnalysis.ts:312` adds the explicit guard with the FIX-09 comment referencing the mirrored adset/ad analyzers |
| 10 | CampaignDrawer previous-period filter includes storeId + platform + campaignId | VERIFIED | `CampaignDrawer.tsx:583-585` filters by all three fields strict equality |
| 11 | AdsDrawer Props.platform: 'Meta' \| 'Google'; aggregation + dailyMetaByAd filter by platform | VERIFIED | `AdsDrawer.tsx:43` type; `:73` destructure; `:165` and `:218` apply `if (r.platform !== platform) continue` filters |
| 12 | AttributionAnalysisPanel widths wrapped in `Math.max(0, Math.min(100, ...))` | VERIFIED | `AttributionAnalysisPanel.tsx:92,96` show the double-clamp on both bar widths |
| 13 | `aggregate()` uses strict `>` at 3 sites | VERIFIED | `campaignsAggregator.ts:100,107,114` all use `r.date > prev`; grep confirms zero `r.date >= prev` occurrences anywhere |
| 14 | Pearson r computed on `activeSeries` (filtered to active days) | VERIFIED | `MetaShopifyReconciliation.tsx:289-296` builds `activeSeries` then uses it in all 4 Pearson calls; `series` still drives the chart and dark-traffic computation |
| 15 | Pre-built `productToCampaigns` Map in `useMemo([productMap])` | VERIFIED | `useCampaignTrueRevenue.ts:206-208` declares the index; `:309` consumes via `.get(a.storeId)` |
| 16 | Per-day-table renders three distinct labels: 'Shopify only' / 'Channels only' / ±X% | VERIFIED | `MetaShopifyReconciliation.tsx:345-346` (extracted as `computeDayDelta` pure helper for testability per TEST-04) |
| 17 | `ordersForPlatform` deleted from `attributionAnalysis.ts`; docstring updated | VERIFIED | `grep -rn ordersForPlatform dashboard-web/src/` returns zero matches |
| 18 | `parseLineItems` normalizes productId via `String(it.p ?? '').trim()` | VERIFIED | `ordersAttribution.ts:157` with the FIX-18 comment |
| 19 | CampaignsTable + CampaignDrawer render fallback banner when `cpmAnalysisMode === 'prev' && analysis.mode === 'half-over-half'` | VERIFIED | `CampaignsTable.tsx:952` and `CampaignDrawer.tsx:793` both gate the banner correctly (with WR-01's `!isLoadingPrev` added to suppress during SWR loading) |
| 20 | `lib/dateRange.ts` exports `parseDate` strict UTC-anchored; campaigns.ts / ads.ts / ordersAttribution.ts consume it | VERIFIED | `dateRange.ts:139` declares; all three lib files import `from './dateRange'` (verified via grep) |
| 21 | `useCampaignAttribution.ts` dailyMetaByAdSet useMemo has Meta/null gate at top | VERIFIED | `useCampaignAttribution.ts:81,104` both gate with `if (!summary \|\| summary.platform !== 'Meta')` |
| 22 | `CampaignsTable.tsx` memoizes `drillRows` in useMemo including `drillStoreId` in deps | VERIFIED | `CampaignsTable.tsx:580-589` declares the useMemo with all 6 deps including `drillStoreId` |
| 23 | `lib/dateRange.ts` exports `getPreviousPeriod(range)` with regex validation | VERIFIED | `dateRange.ts:219` declares with ISO regex validation + WR-05 `from > to` guard at `:234-235`; consumed by `CampaignsTable.tsx:436` + `CampaignDrawer.tsx:151` |

#### Plan 03 truths (8)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 24 | All 8 new/extended tests pass | VERIFIED | `npx vitest run` shows 222 passing across 21 files; per-file runs (buildReconciliation 22 tests, campaignsAggregator 4 tests, etc.) all pass |
| 25 | Test count rises by ~15-25 new tests | VERIFIED | Plan 01 baseline was 178; final count is 222 (+44 tests over the phase, including 2 extra smoke tests from WR-03) |
| 26 | Test files at exact location `dashboard-web/src/lib/__tests__/` | VERIFIED | All 8 expected files present at the exact path |
| 27 | Tests use vitest pattern (describe/it/expect, explicit imports, globals: false) | VERIFIED | Spot-checked `buildReconciliation.test.ts:1` `import { describe, it, expect, vi } from 'vitest';` — explicit imports throughout |
| 28 | TEST-03 requires `aggregate` extraction to `lib/campaignsAggregator.ts` | VERIFIED | `lib/campaignsAggregator.ts` exists (5.3KB) with full `aggregate()` function; consumed by `CampaignsTable.tsx:28` + `CampaignsTableRow.tsx:8` |
| 29 | TEST-05 requires `filterDrillRows` extraction to `lib/drillFilter.ts` | VERIFIED | `lib/drillFilter.ts` exists (530B) with `filterDrillRows`; consumed by `CampaignsTable.tsx:30` |
| 30 | TEST-06 requires chart hex literals extracted to `lib/chartColors.ts` | VERIFIED | `lib/chartColors.ts` exists with `CHART_COLORS` const; `grep '#[0-9a-fA-F]{6}'` in the 3 chart components returns zero leftover hex literals |
| 31 | TEST-08 requires `PRODUCT_MAP_CHIP_KEY` extracted to `lib/sessionKeys.ts` | VERIFIED | `lib/sessionKeys.ts` exists; both `MetaShopifyReconciliation.tsx:19` and `ProductChannelBreakdown.tsx:6` import the constant; the WR-03 fix added smoke tests that read each component source and assert no inline literal `'roas:productMapChipHidden'` remains |

#### Post-review / FIX-24 truth

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 32 | FIX-24: CPM analysis box renders unconditionally with placeholder when n<5 / `analysis.hasData === false` | VERIFIED | `CampaignsTable.tsx:968-980` removes `analysis.hasData &&` outer gate; placeholder explainer text renders regardless; commit bd7f1d0 |

**Score:** 32/32 must-haves verified

### Required Artifacts

#### Plan 01 artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `dashboard-web/src/components/MetaShopifyReconciliation.tsx` | hitMapped pattern | VERIFIED | Contains 8 `hitMapped` references (2 Shopify + 2 Organic, declarations + assignments) |
| `dashboard-web/src/components/CampaignsTableRow.tsx` | onDrillCampaign signature carries storeId | VERIFIED | Line 63 type, line 107 call site both include the storeId arg |
| `dashboard-web/src/components/CampaignsTable.tsx` | drillStoreId state, strict storeId filter, budget normalization | VERIFIED | drillStoreId at line 319; memoized drillRows + storeId filter via filterDrillRows at lines 580-589; budget normalization extracted to campaignsAggregator.ts |
| `dashboard-web/src/components/CampaignDrawer.tsx` | storeId prop, latestAdSetBudgetDate Map | VERIFIED | storeId in Props at line 77; latestAdSetBudgetDate Map at 228, strict `>` overwrite at 265-269 |
| `dashboard-web/src/components/AdsDrawer.tsx` | Range-keyed SWR for both endpoints | VERIFIED | Lines 84 + 93 both wrap URLs with buildDateRangeKey |
| `dashboard-web/src/app/api/ads/route.ts` | parseRangeParams | VERIFIED | Line 5 import, line 17 call with RangeParamError handler |
| `dashboard-web/src/lib/ads.ts` | fetchAdsData accepts {range} | VERIFIED | Line 71 signature; line 107 uses `isInRange(dateStr, range)` (post-IN-05 refactor moved the filter inside the parse loop) |

#### Plan 02 artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `useCampaignTrueRevenue.ts` | dailyMetaByCampaign + productToCampaigns | VERIFIED | Both hoisted useMemo builders present; IN-06 added Meta-only gate at the dailyMetaByCampaign builder loop |
| `attributionAnalysis.ts` | storeId guard + ordersForPlatform deleted | VERIFIED | Guard at line 312; ordersForPlatform fully removed (zero grep matches) |
| `CampaignDrawer.tsx` | namespace-strict previous-period filter | VERIFIED | Lines 583-585 filter by all three fields strict equality; WR-01 added `!isLoadingPrev` to FIX-19 banner |
| `AdsDrawer.tsx` | platform prop + filter | VERIFIED | Multiple `platform`-related lines (43 type, 73 destructure, 165 + 218 filter sites) |
| `AttributionAnalysisPanel.tsx` | Width clamp [0,100] | VERIFIED | Lines 92 + 96 both wrap in Math.max(0, Math.min(100, ...)) |
| `MetaShopifyReconciliation.tsx` | activeSeries + three-label day-table | VERIFIED | activeSeries at 289; computeDayDelta (with 3 distinct labels) at 345-346 |
| `ordersAttribution.ts` | parseLineItems trims productId | VERIFIED | Line 157: `String(it.p ?? '').trim()` |
| `dateRange.ts` | parseDate + getPreviousPeriod | VERIFIED | parseDate at 139, getPreviousPeriod at 219; WR-04 added calendar round-trip validation, WR-05 added `from > to` guard |

#### Plan 03 artifacts (test files + extracted modules)

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `__tests__/buildReconciliation.test.ts` | OrderSource sweep | VERIFIED | 22 tests including 11-case sweep with `satisfies Record<OrderSource, boolean>` + 2 fbclid/gclid override tests |
| `__tests__/adsDrawer.test.ts` | buildDateRangeKey URL assertions | VERIFIED | 3 tests pinning URL shape for both endpoints + non-collision across ranges |
| `lib/campaignsAggregator.ts` | aggregate() pure function | VERIFIED | 128-line module with full aggregate() + second-pass normalization |
| `__tests__/campaignsAggregator.test.ts` | strict > + latest-date + CBO/ABO | VERIFIED | 4 tests covering FIX-05/06/13 |
| `__tests__/reconciliationDayTable.test.ts` | three distinct labels | VERIFIED | 6 tests pinning Shopify-only / Channels-only / numeric / fallback cases |
| `lib/drillFilter.ts` | filterDrillRows | VERIFIED | 20-line module with the pure filter helper |
| `__tests__/campaignsTableDrilldown.test.ts` | two-store fixture | VERIFIED | 4 tests with two stores sharing `camp-shared` — drilldown isolation confirmed |
| `lib/chartColors.ts` | CHART_COLORS | VERIFIED | 17-line module with 10-color CHART_COLORS const |
| `__tests__/chartColors.test.ts` | theme match assertions | VERIFIED | 6 tests pinning meta/google/organic/shopify hex values + readonly contract |
| `__tests__/campaignsTableRowTrustLevel.test.ts` | trustLevel excludes 'unknown' | VERIFIED | 4 tests; WR-02 rewrote to import production binding (computeTrustTone + CampaignsTableRowTrustLevel) |
| `__tests__/cpmRoasAnalysis.test.ts` | N=2/N=3 threshold | VERIFIED | 20 tests including `describe('Pearson N=3 threshold — locks TEST-08')` block at line 209 |
| `lib/sessionKeys.ts` | PRODUCT_MAP_CHIP_KEY | VERIFIED | 2-line module; both consumer components import it |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| CampaignsTableRow | CampaignsTable drillStoreId state | onDrillCampaign(id, platform, storeId) | WIRED | Signature at row line 63, call at row line 107, parent callback at table line 1161 |
| CampaignsTable drilldown filter | CampaignDrawer rows prop | r.storeId === drillStoreId strict equality | WIRED | Via filterDrillRows pure helper (drillFilter.ts:15); consumer at CampaignsTable.tsx:582 with drillStoreId in deps |
| AdsDrawer SWR fetcher | /api/orders-attribution + /api/ads | buildDateRangeKey wraps both URLs | WIRED | Lines 84 (ads) + 93 (orders-attribution) of AdsDrawer.tsx |
| CampaignsTable aggregate() second pass | CBO/ABO budget shape on map.values() | post-loop normalization clears stale-typed budget | WIRED | campaignsAggregator.ts:123-126 (extracted) |
| useCampaignTrueRevenue per-campaign loop | pre-built dailyMetaByCampaign + productToCampaigns | hoisted useMemo([allCampaignRows, productMap]) | WIRED | dailyMetaByCampaign at line 191 (with IN-06 Meta-only filter at 194); productToCampaigns at line 208 |
| CampaignsTable.tsx drilldown mount | memoized drillRows array (FIX-22) | useMemo deps include drillStoreId | WIRED | Lines 580-589 — full 6-deps array including drillStoreId |
| CampaignDrawer CPM previous-period block | campaignsDataPrev.rows filtered by full namespace | filter chain storeId + platform + campaignId | WIRED | Lines 583-585: all three strict equalities |
| CampaignsTable + CampaignsTableRow + CampaignDrawer | lib/campaignsAggregator.ts | imports aggregate + Aggregated type | WIRED | Three import statements verified |
| CampaignsTable drilldown | lib/drillFilter.ts | imports filterDrillRows | WIRED | Import at CampaignsTable.tsx:30 |
| MetaShopifyReconciliation + ProductChannelBreakdown | lib/sessionKeys.ts | both import PRODUCT_MAP_CHIP_KEY | WIRED | Imports at lines 19 + 6 respectively; WR-03 smoke tests pin via fs.readFileSync that no inline literal remains |

### Data-Flow Trace (Level 4)

Verified for the most user-visible artifact (CampaignDrawer):

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| CampaignDrawer (drilldown) | rows | CampaignsTable.drillRows useMemo → filterDrillRows(data.rows) → /api/campaigns via SWR | Yes (real DB rows) | FLOWING |
| CampaignDrawer (CPM prev) | campaignsDataPrev | SWR fetch of /api/campaigns?from=&to= with namespace-strict filter | Yes | FLOWING |
| AdsDrawer | data | useSWR<AdsResponse>(buildDateRangeKey('/api/ads', drawerRange)) | Yes — /api/ads applies parseRangeParams + fetchAdsData filters server-side | FLOWING |
| AdsDrawer | ordersAttrData | useSWR<OrdersAttributionResponse>(buildDateRangeKey('/api/orders-attribution', drawerRange)) | Yes — server defaults overridden by user range | FLOWING |
| MetaShopifyReconciliation | activeSeries (Pearson input) | filtered from `series` (built from ordersData + campaignsData) | Yes — paused days dropped, real days kept | FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| TypeScript compiles cleanly | `cd dashboard-web && npx tsc --noEmit` | exit 0, zero output | PASS |
| Test suite passes | `cd dashboard-web && npx vitest run` | 21 files / 222 tests passing | PASS |
| Plan 03 TEST-01 (OrderSource sweep) | `npx vitest run buildReconciliation.test` | 22 passed | PASS |
| Plan 03 TEST-03 (campaignsAggregator) | `npx vitest run campaignsAggregator.test` | 4 passed | PASS |
| Phase scope: no files outside dashboard-web/src/ | `git log 2f19562..HEAD --name-only \| grep -v dashboard-web/src/ \| grep -v .planning/` | empty | PASS |
| Atomic commit count | `git log --oneline \| grep 05.2.2.1 \| wc -l` | 46 commits (31 fix + 14 review-fix + 1 FIX-24) | PASS |
| No hex literals in chart components | `grep -nE '#[0-9a-fA-F]{6}' MetaShopifyReconciliation/CampaignDrawer/CampaignsTable` | zero matches | PASS |
| No ordersForPlatform anywhere | `grep -rn ordersForPlatform dashboard-web/src/` | zero matches | PASS |
| No `r.date >= prev` in aggregator | `grep -E 'r\.date >= prev' campaignsAggregator/CampaignsTable` | zero matches | PASS |
| Three `r.date > prev` in aggregator | `grep -c 'r\.date > prev' campaignsAggregator.ts` | 3 | PASS |

### Anti-Patterns Found

None at blocker level. All known issues identified by the code review (REVIEW.md, 14 findings) were addressed by REVIEW-FIX.md and the corresponding commits (WR-01..WR-06, IN-01..IN-08). The phase ends with zero TODO/FIXME/placeholder markers introduced by this phase's commits.

### Requirements Coverage

(Skipped per the task instructions: REQUIREMENTS.md doesn't exist in this project. Requirement IDs FIX-01..FIX-23, TEST-01..TEST-08, FIX-24, WR-01..WR-06, IN-01..IN-08 are tracked inline against must_haves above.)

### Human Verification Required

None for this verification. All must_haves verified via grep-based static analysis + automated test execution. The production smoke-tests called out in each PLAN's `<verification>` block (e.g. "open the campaigns tab and click into a Meta campaign's drawer") are production deployment validation steps for the developer, not items that block phase completion. They are bookmarked in each PLAN.md for the deploy-time check after Vercel finishes the auto-deploy.

### Gaps Summary

None.

---

## Inversion + Confirmation Bias Counter — secondary checks

**Inversion pass (3 ways this could be wrong):**

1. SUMMARY claims 222 tests but actual count differs → **CHECKED:** `npx vitest run` output shows `Tests 222 passed (222)` matches exactly.
2. A must_have artifact `contains:` pattern shows the literal, but the surrounding code is a stub or commented-out → **CHECKED:** each `contains:` pattern was confirmed by reading 5-30 line code context around the match; all are real, active code paths with descriptive comments referencing FIX-NN IDs.
3. Phase touches files outside `dashboard-web/src/` despite the scope claim → **CHECKED:** `git log 2f19562..HEAD --name-only` lists only `dashboard-web/src/` and `.planning/` paths; zero files in other directories.

**Confirmation Bias Counter (one partial / one misleading / one uncovered):**

1. **Partially met requirement** — Plan 02 FIX-20 says "inline `new Date(string)` fallbacks removed from the three files". Both campaigns.ts and ads.ts and ordersAttribution.ts now `import { parseDate } from './dateRange'`. Spot-checked: zero inline `new Date(rawCell)` constructs remain in those three files for date parsing. Fully met.
2. **Misleading test** — TEST-07 (trustLevel) originally was misleading (REVIEW.md WR-02 flagged it tested a local alias, not the production binding). REVIEW-FIX commit dd3ea26 corrected the test to import `CampaignsTableRowTrustLevel` + `computeTrustTone` from `@/components/CampaignsTableRow` so assertions now run against production. Verified.
3. **Uncovered error path** — WR-04 (DD/MM/YYYY validation) was flagged; the fix at dateRange.ts:174-178 round-trips through Date and rejects calendar-invalid strings. This closes the previously-uncovered "Feb 31 silently becomes Mar 03" path. Verified.

---

_Verified: 2026-05-20T16:12:00Z_
_Verifier: Claude (gsd-verifier)_
