---
phase: 05.2.3.0-shopify-revenue-refunds-bug-fix
verified: 2026-05-21T02:13:00Z
status: passed
score: 14/14 must-haves verified
source: ".planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/ (10 plans + 10 summaries; CONTEXT.md D-A*..D-D* decisions; REVIEW.md CR-01..CR-04; PROBE-EVIDENCE.md empirical contract)"
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 5/9
  gaps_closed:
    - "Gap 1 / CR-01 — Cross-day refund double-deducted when prior-day rows re-fetched (model swap to total_price + drop cross-day filter)"
    - "Gap 2 / CR-02 — Per-product refundByLineId missing processed_at filter (Plan 08 Task 3 added the filter)"
    - "Gap 3 / CR-03 — Math.max(0, …) clamp violating D-D3 (Plan 08 Task 3 removed clamp)"
    - "Gap 4 / CR-04 — KEEP-IN-SYNC drift between Apps Script and TS mirror (Plan 08 Task 5 rewrote TS mirror with LOAD-BEARING INVARIANTS docstring)"
    - "Gap 5 / WR-01 — Test suite never exercised multi-day reconciliation (Plan 09 added invariant 5 period-sum + invariant 6 cross-day lock-in)"
    - "Gap 6 / WR-04 — getShopifyRefundsForDay_ called twice per (store, day) (Plan 08 Task 4 refactored updateStoreForDate_ to one-fetch-two-consumers)"
    - "Gap 7 / WR-05 — User Manual §16.11.1 false 'שורת D-3' claim (Plan 10 Task 1 removed the false claim, refreshed §16.11.2 example, extended §16.11.5 clamp claim)"
  gaps_remaining: []
  regressions: []
---

# Phase 05.2.3.0: Shopify Revenue Refunds Bug Fix — Verification Report

**Phase Goal:** Eliminate the load-bearing data-integrity bug where store-level and per-product Shopify revenue did not reflect refunds processed on a different day than the original order. After the phase: a refund processed today on yesterday's order MUST appear as a deduction in today's revenue row across every dashboard surface — without double-counting and without silently rewriting prior-day rows.

**Verified:** 2026-05-21T02:13:00Z
**Status:** passed
**Re-verification:** Yes — post gap-closure (Plans 08/09/10) after the initial 7-plan ship surfaced 4 critical findings (CR-01..CR-04) and 1 blocking warning (WR-01)

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `probeRefundContract` exists in Main.gs, captures empirical contract, populated PROBE-EVIDENCE.md | VERIFIED | `Main.gs:232` (function), `Main.gs:183` (menu item), PROBE-EVIDENCE.md populated with 3 stores' real data including the empirically-corrected `current_total_price == total_price − Σ refund_line_items[].subtotal` invariant |
| 2 | Store-level gross uses `total_price` (immutable), NOT `current_total_price` | VERIFIED | `Shopify.gs:160` — `total += parseFloat(o.total_price || 0)`; URL fields=`id,total_price,financial_status,test` at line 123. Single remaining `parseFloat(o.current_total_price` at line 799 is in `getShopifyOrdersAttribution` — out of scope per Plan 08 Deviation D2 (orders-attribution path not in this phase per REVIEW.md WR-08) |
| 3 | `getShopifyRefundsForDay_` has NO `orderCreatedDay === dateStr` short-circuit (cross-day filter dropped) | VERIFIED | `Shopify.gs:686` — only `if (processedDay !== dateStr) continue` remains. grep `'orderCreatedDay === dateStr' Shopify.gs` returns no match. Line 685 comment cites gap-closure 08 model change |
| 4 | Per-product `refundByLineId` filters by `processed_at == dateStr` (intra-order filter) | VERIFIED | `Shopify.gs:259-262` — `for (const refund of (o.refunds \|\| [])) { if (!refund.processed_at) continue; const processedDay = Utilities.formatDate(...); if (processedDay !== dateStr) continue; ... }`. Matches TS mirror at `shopifyRevenueRefunds.ts:205-208` (KEEP-IN-SYNC) |
| 5 | `Math.max(0, gross - lineDiscount - lineRefund)` clamp REMOVED on per-product net | VERIFIED | `Shopify.gs:287` — `const net = gross - lineDiscount - lineRefund` (no clamp). grep `'Math.max(0, gross - lineDiscount - lineRefund)' Shopify.gs` returns no match. Comment at line 284-286 cites CR-03 fix + D-D3 |
| 6 | `DailyUpdate.gs:updateStoreForDate_` calls `getShopifyRefundsForDay_` exactly once and passes to both consumers | VERIFIED | `DailyUpdate.gs:252` — `const refunds = getShopifyRefundsForDay_(store.id, dateStr)` (only call site). `DailyUpdate.gs:253` passes to `getShopifyRevenue(store.id, dateStr, refunds)`. `DailyUpdate.gs:315` passes to `getShopifyProductSalesForDay(store.id, dateStr, refunds)`. `grep -c 'getShopifyRefundsForDay_(' DailyUpdate.gs` == 1 |
| 7 | TS mirror `shopifyRevenueRefunds.ts` is KEEP-IN-SYNC with Apps Script + docstring lists 3 LOAD-BEARING INVARIANTS | VERIFIED | `dashboard-web/src/lib/shopifyRevenueRefunds.ts:8` — `=== LOAD-BEARING INVARIANTS (gap-closure 08, 2026-05-21) ===` block at lines 1-48 enumerating: (1) total_price gross, (2) no cross-day filter, (3) intra-order processed_at filter; line 174 `isSameDayOrder = dayInTz(o.created_at, tz) === dateStr` matches Apps Script same-day branch; line 180 `parseNum(o.total_price)` matches; lines 204-208 intra-order processed_at filter matches; lines 224-227 refund-day attribution has no cross-day filter |
| 8 | 6 D-C3 invariant tests pass in `shopifyRevenueRefunds.test.ts` (invariants 1..4 reworked + 5..6 added) | VERIFIED | `npx vitest run shopifyRevenueRefunds` → `Test Files 1 passed (1) / Tests 6 passed (6)`. Tests at lines 331 (inv 1), 340 (inv 2), 373 (inv 3), 417 (inv 4), 455 (inv 5 period-reconciliation), 520 (inv 6 cross-day lock-in). Both new tests embed DUAL-ALGORITHM RED GATE comment blocks proving they would have been RED against pre-Plan-08 algorithm |
| 9 | Rolling 3-day backfill in DailyUpdate.gs (D-D1) — 3 per-store live wrappers iterate D-2, D-1, D | VERIFIED | `DailyUpdate.gs:80-115` — `runLiveUpdateUzoshop` / `Zolplus` / `Usmile` each `for (let n = 2; n >= 0; n--)` with `previousDayStr_(today, n)`. Comment block at lines 80-95 cites Phase 05.2.3.0 D-D1 and ~46s runtime estimate |
| 10 | 3 per-store 20-day cleanup menu items in Main.gs (D-D2) with verbatim Hebrew | VERIFIED | `Main.gs:165-167` — `addItem('רענן הכנסות (20 ימים) — uzoshop', 'refreshRevenue20DaysUzoshop_')` + Zol Plus + 360usmile. Handlers at `Main.gs:354-362`. Implementation at `refreshRevenue20DaysImpl_` line 368 |
| 11 | `previousDayStr_` helper exists in Config.gs | VERIFIED | `Config.gs:94` — `function previousDayStr_(dateStr, n)` with Date.UTC arithmetic mirroring `nextDayStr_`. Comment block at lines 84-93 cites Phase 05.2.3.0 D-D1 |
| 12 | User Manual §16.11 documents the corrected total_price model — false claim REMOVED | VERIFIED | `docs/ROAS-Dashboard-User-Manual.md:1506-1580` — §16.11 + 6 subsections. §16.11.1 (line 1512) caveat box explicitly states `total_price` is constant. §16.11.2 (lines 1525-1543) refreshed canonical example shows +68.80 / −68.80 / sum=0 for uzoshop order 10803404276006. §16.11.5 (line 1572) extended no-clamping claim covers all 3 levels (aggregate, per-product, per-line). The false claim `'שורת D-3 תישאר עם הסכום שהיה לפני ההחזר'` is no longer present (grep returns no match) |
| 13 | CONTEXT.md decisions D-A1/D-A2/D-A3/D-C3 revised with REVISED + ORIGINAL/SUPERSEDED markers | VERIFIED | `05.2.3.0-CONTEXT.md` — D-A1 (line 27 REVISED, line 28 ORIGINAL superseded), D-A2 (line 29 REVISED, line 30 ORIGINAL superseded), D-A3 (line 31 REVISED, line 32 ORIGINAL), D-C3 (line 46 REVISED + invariants 5/6 appended at lines 51-52). Header (lines 3-5) carries two `Revised:` lines distinguishing `(probe)` from `(gap-closure 08)`. 8 grep hits for `REVISED 2026-05-21 \| ORIGINAL, superseded` |
| 14 | Cross-day refund attributed correctly with NO double-counting (the phase goal — load-bearing) | VERIFIED | Mechanism: (a) Store-level gross uses immutable `total_price` (truth 2) → re-fetching prior-day rows produces identical value, no silent drift. (b) `getShopifyRefundsForDay_` deducts every refund on its `processed_at` day exactly once with no cross-day filter (truth 3). (c) Period invariant 5 (truth 8) sums storeNetCad across all 5 fixture days and asserts equality with `Σ total_price − Σ refund_line_items.subtotal = 85.00 CAD`, GREEN under HEAD. Under pre-Plan-08 algorithm same fixtures produced `-190.51`, off by `−275.51` (the double-deduction magnitude). (d) Per-product invariant 6 locks the processed_at filter — synthetic `sameDayOrderWithFutureRefund` fixture, GREEN under HEAD. (e) Worked example with real probe data (uzoshop 10803404276006, total_price=68.80, refund_line_items[0].subtotal=68.80): pre-Plan-08 period sum −68.80 (off by 68.80); post-Plan-08 period sum 0.00 (correct, fully-refunded). Per Plan 08 SUMMARY §"Worked Example" and User Manual §16.11.2 |

**Score:** 14/14 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `Shopify.gs` | getShopifyRevenue uses total_price, getShopifyRefundsForDay_ no cross-day filter, getShopifyProductSalesForDay no clamp + processed_at filter | VERIFIED | All 3 functions confirmed; 1001 lines total; 5 commits 9a1a262 / 1cc1d43 / 0781f81 land on main |
| `DailyUpdate.gs` | updateStoreForDate_ has one refund fetch passed to both consumers + rolling 3-day backfill in live wrappers | VERIFIED | Line 252 single fetch site; lines 96-115 three rolling-3-day wrappers; 963 lines total |
| `Main.gs` | probeRefundContract + 3 per-store 20-day cleanup menu items | VERIFIED | Line 232 probe function; lines 165-167 three menu items; line 183 probe menu item |
| `Config.gs` | previousDayStr_ helper | VERIFIED | Line 94 function defined with Date.UTC arithmetic |
| `dashboard-web/src/lib/shopifyRevenueRefunds.ts` | TS mirror with LOAD-BEARING INVARIANTS docstring + total_price + no cross-day filter + processed_at filter | VERIFIED | Lines 1-48 docstring with 3 enumerated invariants; line 180 parseNum(o.total_price); lines 204-208 processed_at intra-order filter; lines 224-227 no cross-day filter on refund-day attribution |
| `dashboard-web/src/lib/__tests__/shopifyRevenueRefunds.test.ts` | 6 D-C3 invariants (4 reworked + 2 new) | VERIFIED | 6 it() blocks; both new tests embed DUAL-ALGORITHM RED GATE comment blocks; 6/6 pass under HEAD |
| `docs/ROAS-Dashboard-User-Manual.md` | §16.11 rewritten for total_price model; false claim removed; §16.11.5 no-clamping claim covers all 3 levels | VERIFIED | 2043 lines; §16.11 spans lines 1506-1580; 6 subsections all present; false claim absent; canonical example uses 68.80 with sum=0 |
| `.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-CONTEXT.md` | D-A1/D-A2/D-A3/D-C3 revised with REVISED + ORIGINAL/SUPERSEDED markers | VERIFIED | 158 lines; 8 REVISED/ORIGINAL marker hits; header dual `Revised:` lines |
| `.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-PROBE-EVIDENCE.md` | Empirical refund contract from 3 stores | VERIFIED | 3 stores populated with real data; Finding 1 documents `current_total_price == total_price − Σ refund_line_items[].subtotal` invariant matched on 3/3 stores |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `Shopify.gs:getShopifyRevenue` | `Shopify.gs:getShopifyRefundsForDay_` | Optional `refunds` 3rd param + fallback fetch | WIRED | Line 182: `const r = (refunds !== undefined) ? refunds : getShopifyRefundsForDay_(storeId, dateStr); total -= r.storeRefundCad;` |
| `Shopify.gs:getShopifyProductSalesForDay` | `Shopify.gs:getShopifyRefundsForDay_` | Optional `refunds` 3rd param + fallback fetch | WIRED | Line 328: `const crossDayRefunds = (refunds !== undefined) ? refunds : getShopifyRefundsForDay_(storeId, dateStr);` + lines 330-345 byProduct refund subtraction |
| `DailyUpdate.gs:updateStoreForDate_` | `Shopify.gs:getShopifyRefundsForDay_` | Direct call (single invocation) | WIRED | Line 252 single fetch; lines 253 + 315 pass result to both consumers; `grep -c 'getShopifyRefundsForDay_(' DailyUpdate.gs` == 1 |
| `DailyUpdate.gs:runLiveUpdateUzoshop\|Zolplus\|Usmile` | `DailyUpdate.gs:runUpdateForSingleStore_` | `for n=2..0` loop with `previousDayStr_(today, n)` | WIRED | Lines 96-115 — 3 wrappers, identical shape, oldest-first ordering |
| `Main.gs:onOpen menu` | `Main.gs:refreshRevenue20Days{Uzoshop\|Zolplus\|Usmile}_` | addItem(label, handler) | WIRED | Lines 165-167 menu items + lines 354-362 handlers + line 368 refreshRevenue20DaysImpl_ |
| `Main.gs:onOpen menu` | `Main.gs:probeRefundContract` | addItem('בדיקת חוזה החזרים (Shopify probe)', 'probeRefundContract') | WIRED | Line 183 menu item, line 232 function |
| `dashboard-web/.../shopifyRevenueRefunds.ts:computeRevenueWithCrossDayRefunds` | (consumed by test only) | Import in test file | WIRED | Test file imports + uses in 6 `it()` blocks |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
|----------|--------------|--------|--------------------|--------|
| `Shopify.gs:getShopifyRevenue` | `total` accumulator | Shopify Admin REST `orders.json` (created_at window) — fields=id,total_price,financial_status,test | YES — real HTTP call, paginated, 401 auto-bootstrap, 429 backoff | FLOWING |
| `Shopify.gs:getShopifyRefundsForDay_` | `storeRefundCad` / `byProduct` | Shopify Admin REST `orders.json` (updated_at window) — fields=id,created_at,financial_status,test,refunds | YES — same fetch pattern; refund_line_items[].subtotal extracted per Plan 02 | FLOWING |
| `Shopify.gs:getShopifyProductSalesForDay` | `byProduct[pid].netRevenueCad` | Shopify Admin REST `orders.json` (created_at window) — fields=id,financial_status,test,line_items,refunds | YES — per-product gross from line_items × price × qty minus refund_line_items subtotal (intra-order processed_at-filtered) and minus cross-day refunds | FLOWING |
| `DailyUpdate.gs:updateStoreForDate_` | `revenueCad`, `products` | Calls into all 3 Shopify.gs functions + writes to Sheets via `writeDayRow` / `writeProductSalesForDay_` | YES — writes to `data-daily` tab consumed by `/api/data` route + Looker | FLOWING |
| `dashboard-web/.../shopifyRevenueRefunds.ts:computeRevenueWithCrossDayRefunds` | `storeNetCad`, `byProduct` | Pure function over fixtures from PROBE-EVIDENCE.md | YES (test surface) — real probe fixtures, not fictional. Sibling test imports drive the algorithm. Production dashboard reads `data-daily` written by Apps Script (not this TS file) | FLOWING (test-only) |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| 6 D-C3 invariants pass under HEAD | `cd dashboard-web && npx vitest run shopifyRevenueRefunds` | `Test Files 1 passed (1) / Tests 6 passed (6)` | PASS |
| TypeScript compiles clean | `cd dashboard-web && npx tsc --noEmit` | exit 0 (no errors) | PASS |
| `parseFloat(o.total_price)` in Shopify.gs | `grep -c 'parseFloat(o.total_price' Shopify.gs` | 1 | PASS |
| `parseFloat(o.current_total_price)` confined to out-of-scope orders-attribution | `grep -n 'parseFloat(o.current_total_price' Shopify.gs` | line 799 only (getShopifyOrdersAttribution, per Plan 08 D2 deviation) | PASS (scope) |
| Cross-day filter dropped | `! grep -q 'orderCreatedDay === dateStr' Shopify.gs` | exit 0 (not found) | PASS |
| Per-line clamp removed | `! grep -q 'Math.max(0, gross - lineDiscount - lineRefund)' Shopify.gs` | exit 0 (not found) | PASS |
| One-fetch-two-consumers | `grep -c 'getShopifyRefundsForDay_(' DailyUpdate.gs` | 1 | PASS |
| LOAD-BEARING INVARIANTS docstring present | `grep -q 'LOAD-BEARING INVARIANTS' dashboard-web/src/lib/shopifyRevenueRefunds.ts` | exit 0 | PASS |
| Probe menu item present | `grep -q 'בדיקת חוזה החזרים (Shopify probe)' Main.gs` | exit 0 | PASS |
| uzoshop 20-day cleanup menu item present | `grep -q 'רענן הכנסות (20 ימים) — uzoshop' Main.gs` | exit 0 | PASS |
| previousDayStr_ helper present | `grep -q 'function previousDayStr_' Config.gs` | exit 0 | PASS |
| getShopifyRefundsForDay_ function present | `grep -q 'function getShopifyRefundsForDay_' Shopify.gs` | exit 0 | PASS |
| Rolling 3-day backfill documented | `grep -q 'rolling 3-day backfill' DailyUpdate.gs` | exit 0 | PASS |
| User Manual §16.11 chapter present | `grep -q '### 16.11' docs/ROAS-Dashboard-User-Manual.md` | exit 0 | PASS |
| Old false claim removed | `! grep -q 'שורת D-3 תישאר עם הסכום שהיה לפני ההחזר' docs/ROAS-Dashboard-User-Manual.md` | exit 0 (not found) | PASS |
| SUPERSEDED markers in CONTEXT.md | `grep -c 'REVISED 2026-05-21\|ORIGINAL, superseded' CONTEXT.md` | 8 | PASS |

All 16 behavioral spot-checks PASS.

### Requirements Coverage

This phase uses D-IDs from CONTEXT.md instead of formal REQUIREMENTS.md IDs.

| Decision | Description | Status | Evidence |
|----------|-------------|--------|----------|
| D-A1 (REVISED) | Store-level uses `total_price` (immutable), NOT `current_total_price` | SATISFIED | truth 2 + Shopify.gs:160 |
| D-A2 (REVISED) | Every refund attributes to its `processed_at` day, no cross-day filter | SATISFIED | truth 3 + Shopify.gs:686 |
| D-A3 (REVISED) | One HTTP call, two consumers — structurally enforced in updateStoreForDate_ | SATISFIED | truth 6 + DailyUpdate.gs:252-315 |
| D-A4 | Asia/Jerusalem TZ for all day comparisons | SATISFIED | Shopify.gs uses `Utilities.formatDate(..., TZ, 'yyyy-MM-dd')`; TS mirror uses `Intl.DateTimeFormat` with `'Asia/Jerusalem'` |
| D-B1..D-B4 | Empirical probe before TS mirror lock-in; D-B4 checkpoint gate fired | SATISFIED | truth 1 + PROBE-EVIDENCE.md populated; CONTEXT.md decisions revised after probe |
| D-C1 (REVISED) | Store-level deducts `refund_line_items[].subtotal` | SATISFIED | Shopify.gs:697 — `parseFloat(rli.subtotal || rli.total || 0)`; TS mirror lines 234-240 |
| D-C2 | Per-product deducts `refund_line_items[].subtotal` | SATISFIED | Shopify.gs:266 — same field |
| D-C3 (REVISED) | 6 invariants mandatory (4 reworked + 2 new) | SATISFIED | truth 8 + 6/6 vitest passing |
| D-C4 | Tests use fixtures derived from PROBE-EVIDENCE.md (no fictional) | SATISFIED | Real probe fixtures retained; synthetic `sameDayOrderWithFutureRefund` clearly labeled and used only for invariant 6 |
| D-D1 | Rolling 3-day backfill in live trigger | SATISFIED | truth 9 + DailyUpdate.gs:96-115 |
| D-D2 | 3 per-store 20-day cleanup menu items with verbatim Hebrew | SATISFIED | truth 10 + Main.gs:165-167 |
| D-D3 | Negative revenue shown as-is (no clamping) | SATISFIED | truth 5 + Shopify.gs:287 + User Manual §16.11.5 |
| D-D4 | Same algorithm across 3 stores (uzoshop / zolplus / usmile360) | SATISFIED | Shared `getShopifyRefundsForDay_` + 3 identical-shape per-store wrappers |

13/13 D-IDs satisfied.

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `Shopify.gs` | 641-643 | Stale doc-comment on `getShopifyRefundsForDay_` still references the pre-gap-closure-08 cross-day filter (`JS-filter to keep only refunds whose processed_at is on day D AND whose underlying order's created_at is NOT on day D`) — the algorithm at line 686 implements only `processedDay !== dateStr`, no longer the AND clause | INFO | Documentation drift only. The actual algorithm is correct (filter is processed_at-only at line 686 with a clarifying comment at line 685). This is a pre-existing comment that Plan 08 did not rewrite (Plan 08 SUMMARY Deviation D3 explicitly defers file-header rewrites to Plan 10; Plan 10 covered the file-level docs not this function header). Not blocking; recommend cleanup in a future micro-doc-fix. |
| `Shopify.gs` | 799 | `parseFloat(o.current_total_price || 0)` in `getShopifyOrdersAttribution` | INFO | Out-of-scope per Plan 08 Deviation D2 + REVIEW.md WR-08. The orders-attribution path was pre-existing and is not part of the bug-fix scope. WR-08 is informational, not blocking. Not a regression from this phase. |

No BLOCKER or WARNING anti-patterns. Two INFO items documented above for traceability.

### Gaps Summary

All 7 gaps from the prior verification (2026-05-21T15:35:00Z, score 5/9) are CLOSED:

- **Gap 1 (CR-01)** — Store-level double-deduction → fixed by Plan 08 model swap (total_price + drop cross-day filter). Verified by truth 14 + invariant 5.
- **Gap 2 (CR-02)** — Per-product double-deduction → fixed by Plan 08 Task 3 (processed_at filter on `refundByLineId`). Verified by truth 4 + invariant 6.
- **Gap 3 (CR-03)** — Per-product Math.max clamp → fixed by Plan 08 Task 3 (clamp removed). Verified by truth 5.
- **Gap 4 (CR-04)** — KEEP-IN-SYNC drift → fixed by Plan 08 Task 5 (TS mirror rewritten with LOAD-BEARING INVARIANTS docstring enumerating 3 invariants). Verified by truth 7.
- **Gap 5 (WR-01)** — Test suite blind to multi-day reconciliation → fixed by Plan 09 (invariant 5 period-sum + invariant 6 cross-day lock-in, both with DUAL-ALGORITHM RED GATE comments). Verified by truth 8.
- **Gap 6 (WR-04)** — `getShopifyRefundsForDay_` called twice per (store, day) → fixed by Plan 08 Task 4 (one fetch + dependency-injection into both consumers). Verified by truth 6.
- **Gap 7 (WR-05)** — User Manual §16.11.1 false claim about D-3 row preservation → fixed by Plan 10 (false claim removed; §16.11.2 example refreshed; §16.11.5 no-clamping extended to 3 levels). Verified by truth 12.

No regressions. Vitest suite is GREEN (6/6 invariants), tsc clean (exit 0), all 14 must-haves verified.

## Final Verdict

**status: passed**

The phase goal is achieved in the codebase. Cross-day refunds attribute to their `processed_at` day exactly once. Prior-day rows are safe to re-fetch because the gross field (`total_price`) is immutable. The double-deduction structural bug surfaced by the 2026-05-21 code review (CR-01..CR-04, WR-01/04/05) is eliminated, with regression-gate tests (invariants 5+6) embedded that would have been RED under the pre-gap-closure-08 algorithm. The corrected model is documented in CONTEXT.md (with full historical chain preserved via REVISED + ORIGINAL/SUPERSEDED markers) and the User Manual §16.11 (with the prior false claim removed). The operator-facing surfaces (3 per-store 20-day cleanup menu items, probe diagnostic, rolling 3-day live trigger backfill) are wired and present.

---

*Verified: 2026-05-21T02:13:00Z*
*Verifier: Claude (gsd-verifier)*
*Re-verification mode: post-gap-closure (Plans 08/09/10)*
