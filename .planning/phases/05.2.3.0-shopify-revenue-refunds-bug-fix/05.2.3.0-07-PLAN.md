---
phase: 05.2.3.0-shopify-revenue-refunds-bug-fix
plan: 07
type: execute
wave: 4
depends_on:
  - 05.2.3.0-01
  - 05.2.3.0-02
  - 05.2.3.0-03
  - 05.2.3.0-04
  - 05.2.3.0-05
  - 05.2.3.0-06
files_modified:
  - docs/ROAS-Dashboard-User-Manual.md
autonomous: true
requirements:
  - D-D2
  - D-D3
  - D-A4
  - D-A1
  - D-A2

objective: "Add new sub-section `16.11 Refund-Day Attribution` to the User Manual covering: (a) the refund-day model with the canonical D-3 order + D refund example with REAL numbers from PROBE-EVIDENCE.md; (b) the 3 Hebrew cleanup menu items verbatim per D-D2; (c) the negative-revenue policy per D-D3; (d) per-store Shopify Admin verification URLs. Satisfies the project memory rule `feedback_keep_user_manual_current.md` (User Manual MUST be updated on every behavior change)."

must_haves:
  truths:
    - "docs/ROAS-Dashboard-User-Manual.md has a new `### 16.11 Refund-Day Attribution` sub-section inside the existing `## 16. Backfill — מילוי נתונים היסטוריים` chapter"
    - "The new sub-section contains all 4 D-D2 + D-D3 elements: (1) the refund-day attribution explanation with a canonical D-3 order + D refund example using REAL numbers from PROBE-EVIDENCE.md; (2) the 3 exact Hebrew cleanup menu items verbatim per D-D2; (3) the negative-revenue explanation per D-D3 (no clamping, red color in dashboard); (4) per-store Shopify Admin > Reports > Total Sales verification URLs"
    - "All 3 cleanup menu items appear in the manual EXACTLY as: `רענן הכנסות (20 ימים) — uzoshop`, `רענן הכנסות (20 ימים) — Zol Plus`, `רענן הכנסות (20 ימים) — 360usmile`"
    - "The canonical example cites the actual numbers from PROBE-EVIDENCE.md (executor copies real values; acceptance criterion grep-binds at least one numeric value)"
    - "The negative-revenue policy is documented with the Asia/Jerusalem TZ note per D-A4 (e.g., a refund processed at 22:30 UTC is attributed to the NEXT calendar day in Israel)"
    - "The new section is reachable from the Table of Contents at lines 63-93 — the TOC line for section 16 already exists; sub-sections like 16.11 are implicitly browsable via Ctrl-F on the section number"
  artifacts:
    - path: "docs/ROAS-Dashboard-User-Manual.md"
      provides: "Section 16.11 Refund-Day Attribution"
      contains: "16.11 Refund-Day Attribution"
  key_links:
    - from: "docs/ROAS-Dashboard-User-Manual.md §16.11"
      to: "Main.gs cleanup menu items"
      via: "Verbatim Hebrew menu strings"
      pattern: "רענן הכנסות \\(20 ימים\\) — uzoshop"
    - from: "docs/ROAS-Dashboard-User-Manual.md §16.11"
      to: "05.2.3.0-PROBE-EVIDENCE.md"
      via: "Canonical example uses REAL probe numbers"
      pattern: "מבוסס על דאטה אמיתי מ-PROBE-EVIDENCE.md"
---

<objective>
Per the project memory rule `feedback_keep_user_manual_current.md` (updating the User Manual on every behavior change is a permanent pre-push gate, on par with tsc/vitest), the cross-day refund attribution change is a behavior change that requires manual documentation. This plan adds a single self-contained sub-section to the existing Backfill chapter rather than fragmenting the refund explanation across multiple chapters — the operator finds everything in one place.

The four required documentation elements are derived from CONTEXT.md `<specifics>` paragraph 3: refund-day model, exact Hebrew menu strings, negative-revenue policy, verification URLs. Per D-C4, the canonical example uses REAL numbers from PROBE-EVIDENCE.md (no fictional values in code, no fictional values in operator-facing docs either).

Output: ~80-120 lines of new Hebrew + English Markdown content inserted between the existing 16.10 Checklist sub-section and the `---` + `## 17. עשה ואל תעשה` chapter boundary.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-CONTEXT.md
@.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-PROBE-EVIDENCE.md
@.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-03-SUMMARY.md
@.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-06-SUMMARY.md
@docs/ROAS-Dashboard-User-Manual.md

<interfaces>
Existing User Manual structure (from prior Read):
- Lines 1394-1505: Chapter 16 with sub-sections 16.1 through 16.10
- Line 1506: `---` separator
- Line 1508: `## 17. עשה ואל תעשה` (next chapter)

The new sub-section is inserted between lines 1505 (end of 16.10) and 1506 (the `---`). It does NOT replace any existing content; it appends a new sub-section to chapter 16.

The existing manual uses Hebrew prose with English code snippets, formatted Markdown lists, and bolded labels (`**עקרון:**`, `**אפשרות א: לכל החנויות יחד**`). Match this voice.

Existing sub-section heading style: `### 16.{N} {Title}` (no period after the number). The new heading: `### 16.11 Refund-Day Attribution (ייחוס החזרים לפי יום עיבוד)`.
</interfaces>

<probe_evidence>
The canonical example numbers are sourced from `.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-PROBE-EVIDENCE.md`. The executor picks ONE refund from the probe file (uzoshop's data preferred for example clarity) and uses its real `total_price`, `current_total_price`, `processed_at`, `transactions[0].amount` values in the example. Acceptance criterion below grep-binds at least one numeric value back to the probe file.
</probe_evidence>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Insert section 16.11 Refund-Day Attribution into the User Manual</name>
  <files>docs/ROAS-Dashboard-User-Manual.md</files>
  <read_first>
    - docs/ROAS-Dashboard-User-Manual.md lines 1490-1510 (the end of 16.10 + the chapter-16/17 boundary)
    - .planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-PROBE-EVIDENCE.md (for the canonical example numbers — REQUIRED, halt if absent)
    - .planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-CONTEXT.md `<decisions>` D-D2, D-D3, D-A1, D-A2, D-A4 (the spec for the documentation content)
  </read_first>
  <behavior>
    Insert a new sub-section at line 1506 (or wherever the `---` separator is, depending on minor line shifts from prior plans). The new sub-section uses the User Manual's established Hebrew-first style with bolded English headings where helpful. Structure:

    1. Heading `### 16.11 Refund-Day Attribution (ייחוס החזרים לפי יום עיבוד)`
    2. Sub-heading "מודל הייחוס" — explains: each refund counts on the day its `processed_at` falls, in Asia/Jerusalem TZ. Refund processed at 22:30 UTC is attributed to NEXT calendar day in Israel (D-A4 example).
    3. Sub-heading "דוגמה קנונית" — uses REAL numbers from PROBE-EVIDENCE.md. Shows a D-3 order + D refund, and what the dashboard displays on each affected row.
    4. Sub-heading "ניקוי ידני — 3 פריטי תפריט" — lists the 3 Hebrew menu items VERBATIM with usage instructions (run one at a time, wait 30s between clicks).
    5. Sub-heading "הכנסה שלילית — תופעה מותרת" — explains D-D3: a day can show negative revenue legitimately; no clamping; dashboard color-coding handles it.
    6. Sub-heading "אימות מול Shopify Admin" — 3 verification URLs (Shopify Admin > Reports > Total Sales per store), instructions to cross-check.
  </behavior>
  <action>
    Use the Edit tool. Find this existing text near the end of chapter 16:

    ```
    ### 16.10 Checklist אחרי Backfill
    - [ ] Apps Script Executions log: ירוק (Completed, לא Failed/Timed out).
    - [ ] Logs ב-Apps Script: אין שגיאות מטרידות.
    - [ ] Sheets `data-daily`: הטווח מופיע עם נתונים סבירים.
    - [ ] Sheets `campaigns-daily`: בדומה, נתונים סבירים.
    - [ ] דשבורד אחרי hard-refresh: הטווח מציג נתונים.
    - [ ] Per-store check: כל אחת מ-3 החנויות מציגה.
    - [ ] Per-platform check (ב-Campaigns tab): Meta, Google, All.

    ---

    ## 17. עשה ואל תעשה
    ```

    Insert the new sub-section between the existing `[ ] Per-platform check` bullet and the `---` separator. The replacement should be:

    ```
    ### 16.10 Checklist אחרי Backfill
    - [ ] Apps Script Executions log: ירוק (Completed, לא Failed/Timed out).
    - [ ] Logs ב-Apps Script: אין שגיאות מטרידות.
    - [ ] Sheets `data-daily`: הטווח מופיע עם נתונים סבירים.
    - [ ] Sheets `campaigns-daily`: בדומה, נתונים סבירים.
    - [ ] דשבורד אחרי hard-refresh: הטווח מציג נתונים.
    - [ ] Per-store check: כל אחת מ-3 החנויות מציגה.
    - [ ] Per-platform check (ב-Campaigns tab): Meta, Google, All.

    ### 16.11 Refund-Day Attribution (ייחוס החזרים לפי יום עיבוד)

    > **שינוי התנהגות חשוב (Phase 05.2.3.0, 2026-05-20).** המערכת מנכה כעת החזרים ביום שבו הם **עובדו** (ולא ביום ההזמנה המקורית). זה משפיע על כל מקום שמראה הכנסה לפי יום: KPI cards בבית, PerStoreCards, GoalTracker, P&L יומי, וטאב Detail. הסבר מלא להלן.

    #### 16.11.1 מודל הייחוס

    החזר נספר ביום שבו השדה `refund.processed_at` נופל ב-Asia/Jerusalem TZ — לא ביום שבו ההזמנה המקורית נוצרה. אם הזמנה נוצרה ב-D-3 וההחזר עליה עובד ב-D, ההחזר ינוכה משורת ההכנסה של D. שורת D-3 תישאר עם הסכום שהיה לפני ההחזר.

    **דוגמה ל-TZ (D-A4):**
    - החזר שעובד ב-`2026-05-20T22:30:00Z` (UTC) → מיוחס ליום `2026-05-21` (Israel) — כי 22:30 UTC = 01:30 בלילה של 21 במאי בישראל (חורף) או 01:30 בלילה של 21 במאי (קיץ, +03:00).

    #### 16.11.2 דוגמה קנונית — מבוסס על דאטה אמיתי מ-PROBE-EVIDENCE.md

    הסבר זה משתמש בערכים אמיתיים שצולמו מ-Shopify Admin REST ביום הפרובה. ראה `.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-PROBE-EVIDENCE.md` לפרטים מלאים.

    **תרחיש:**
    - **D-3** (יום יצירת ההזמנה): הזמנה ב-uzoshop, `total_price = __REPLACE_FROM_PROBE__ CAD`. שורת `data-daily` של D-3 נכתבה עם הכנסה זו.
    - **D** (יום עיבוד ההחזר): הלקוח החזיר את המוצר. Shopify רשם `refund.processed_at = __REPLACE_FROM_PROBE__` עם `refund.transactions[0].amount = __REPLACE_FROM_PROBE__ CAD`.

    **מה הדשבורד מציג:**
    | יום | הכנסה לפני התיקון | הכנסה אחרי התיקון |
    |---|---|---|
    | D-3 | `__REPLACE_FROM_PROBE_TOTAL_PRICE__` | `__REPLACE_FROM_PROBE_TOTAL_PRICE__` (ללא שינוי — D-3 נשאר עם הסכום המקורי) |
    | D | מכירות היום | מכירות היום `−` `__REPLACE_FROM_PROBE_AMOUNT__` (החזר מ-D-3 מנוכה מ-D) |

    שני הסעיפים יחד שווים לסכום שראית ב-Shopify Admin > Reports > Total Sales עבור הטווח D-3..D.

    #### 16.11.3 ניקוי ידני — 3 פריטי תפריט (D-D2)

    הטריגר Live (כל 15 דקות) מבצע backfill מתגלגל ל-3 ימים אחרונים (D-2, D-1, D) — כך החזרים שנעשו ב-48 השעות האחרונות נספרים אוטומטית. עבור החזרים ישנים יותר (>48 שעות מ-יצירת ההזמנה), השתמש בתפריט הניקוי הידני:

    1. **`רענן הכנסות (20 ימים) — uzoshop`** — מרענן 20 ימים אחורה לחנות uzoshop. הרצה ~5 דקות.
    2. **`רענן הכנסות (20 ימים) — Zol Plus`** — אותו דבר לחנות Zol Plus.
    3. **`רענן הכנסות (20 ימים) — 360usmile`** — אותו דבר לחנות 360usmile.

    **כללי שימוש:**
    - הרץ אחד-אחד, **לא** במקביל.
    - חכה **~30 שניות** בין לחיצות (אחרת Apps Script עלול לדחות את הריצה השנייה בגלל מכסת ריצות מקבילות).
    - בכל ריצה תראה התראת BUT (תיבת הודעה בעברית) המאשרת התחלה + סיום.
    - אחרי כל 3 הריצות, hard-refresh את הדשבורד וודא שהשורות מתעדכנות.

    #### 16.11.4 הכנסה שלילית — תופעה מותרת (D-D3)

    יום שבו ההחזרים על הזמנות מימים קודמים גדולים מהמכירות של אותו היום יראה **הכנסה שלילית**. זו חשבונאות נכונה — לא באג.

    - הדשבורד מציג את המספר השלילי בצבע אדום (חוקי הצבעים הקיימים).
    - אל תנסה "לתקן" יום שלילי — הסכום הכולל לחודש/שנה עדיין תקין.
    - אם רוב הימים שליליים בטווח קצר → ייתכן שהיו החזרים גדולים על קמפיין שעצרת לאחרונה. בדוק את `{store}-orders-attribution` לאיתור.

    #### 16.11.5 אימות מול Shopify Admin

    לוודא שהמערכת מסכמת נכון, השווה את `data-daily` ל-Shopify Admin > Reports > Total Sales:

    - **uzoshop:** `https://uzo-d-s-2.myshopify.com/admin/reports/total_sales` (טווח רלוונטי)
    - **zolplus:** `https://2x1gqx-y0.myshopify.com/admin/reports/total_sales`
    - **usmile360:** `https://360usmile.myshopify.com/admin/reports/total_sales`

    שני המקורות אמורים להתאים ב-`±0.50 CAD` (פערים קטנים בשל עיגול ו-FX). אם הפער עולה על 5 CAD ביום בודד או על 50 CAD בטווח 30 ימים → פתח Issue ב-GitHub.

    ---

    ## 17. עשה ואל תעשה
    ```

    EXECUTOR INSTRUCTION: After inserting the block above, you MUST replace every `__REPLACE_FROM_PROBE__` and `__REPLACE_FROM_PROBE_TOTAL_PRICE__` and `__REPLACE_FROM_PROBE_AMOUNT__` placeholder with the actual values from `05.2.3.0-PROBE-EVIDENCE.md`. Pick ONE refund record from PROBE-EVIDENCE.md (uzoshop's preferred — easiest for the operator to verify) and use its real values throughout the canonical example. The phrase `מבוסס על דאטה אמיתי מ-PROBE-EVIDENCE.md` is canonical — keep it verbatim.

    DO NOT alter the Hebrew copy of the 3 menu items (verbatim per D-D2). DO NOT shorten the negative-revenue explanation (per D-D3). DO NOT remove the verification URLs (per CONTEXT.md `<specifics>`).
  </action>
  <verify>
    <automated>cd /Users/dorperetz/script-roas && grep -q "### 16.11 Refund-Day Attribution" docs/ROAS-Dashboard-User-Manual.md && grep -q "רענן הכנסות (20 ימים) — uzoshop" docs/ROAS-Dashboard-User-Manual.md && grep -q "רענן הכנסות (20 ימים) — Zol Plus" docs/ROAS-Dashboard-User-Manual.md && grep -q "רענן הכנסות (20 ימים) — 360usmile" docs/ROAS-Dashboard-User-Manual.md && grep -q "מבוסס על דאטה אמיתי מ-PROBE-EVIDENCE.md" docs/ROAS-Dashboard-User-Manual.md && grep -q "הכנסה שלילית — תופעה מותרת" docs/ROAS-Dashboard-User-Manual.md && grep -q "uzo-d-s-2.myshopify.com/admin/reports/total_sales" docs/ROAS-Dashboard-User-Manual.md && grep -q "2x1gqx-y0.myshopify.com/admin/reports/total_sales" docs/ROAS-Dashboard-User-Manual.md && grep -q "360usmile.myshopify.com/admin/reports/total_sales" docs/ROAS-Dashboard-User-Manual.md && ! grep -q "__REPLACE_FROM_PROBE__" docs/ROAS-Dashboard-User-Manual.md && ! grep -q "__REPLACE_FROM_PROBE_TOTAL_PRICE__" docs/ROAS-Dashboard-User-Manual.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - The new sub-section header exists: `grep -q "### 16.11 Refund-Day Attribution" docs/ROAS-Dashboard-User-Manual.md`.
    - All 3 Hebrew menu items appear VERBATIM (same exact strings as Plan 06): `grep -q "רענן הכנסות (20 ימים) — uzoshop" docs/ROAS-Dashboard-User-Manual.md` AND `grep -q "רענן הכנסות (20 ימים) — Zol Plus" docs/ROAS-Dashboard-User-Manual.md` AND `grep -q "רענן הכנסות (20 ימים) — 360usmile" docs/ROAS-Dashboard-User-Manual.md`.
    - PROBE-EVIDENCE.md is cited: `grep -q "מבוסס על דאטה אמיתי מ-PROBE-EVIDENCE.md" docs/ROAS-Dashboard-User-Manual.md`.
    - Negative-revenue policy documented: `grep -q "הכנסה שלילית — תופעה מותרת" docs/ROAS-Dashboard-User-Manual.md`.
    - 3 verification URLs present (one per store): `grep -q "uzo-d-s-2.myshopify.com/admin/reports/total_sales" docs/ROAS-Dashboard-User-Manual.md` AND `grep -q "2x1gqx-y0.myshopify.com/admin/reports/total_sales" docs/ROAS-Dashboard-User-Manual.md` AND `grep -q "360usmile.myshopify.com/admin/reports/total_sales" docs/ROAS-Dashboard-User-Manual.md`.
    - All `__REPLACE_*__` placeholders replaced: `! grep -q "__REPLACE_FROM_PROBE__" docs/ROAS-Dashboard-User-Manual.md && ! grep -q "__REPLACE_FROM_PROBE_TOTAL_PRICE__" docs/ROAS-Dashboard-User-Manual.md && ! grep -q "__REPLACE_FROM_PROBE_AMOUNT__" docs/ROAS-Dashboard-User-Manual.md`.
    - At least one numeric value used in the example is grep-bindable to PROBE-EVIDENCE.md: `for token in $(awk '/### 16.11 Refund-Day Attribution/,/^## 17\\./' docs/ROAS-Dashboard-User-Manual.md | grep -oE '[0-9]+\\.[0-9]+' | sort -u | head -10); do grep -q "$token" .planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-PROBE-EVIDENCE.md && echo "BOUND" && break; done | grep -q "^BOUND"`.
    - The chapter-16/17 boundary is preserved — the existing `## 17. עשה ואל תעשה` header still exists exactly once: `grep -c "## 17. עשה ואל תעשה" docs/ROAS-Dashboard-User-Manual.md` outputs `1`.
  </acceptance_criteria>
  <done>
    The new sub-section is in place, all probe placeholders replaced with real values, all 3 Hebrew menu items exact, negative-revenue policy + verification URLs documented. Manual reads coherently in operator-context.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator -> User Manual | Operator-facing documentation; no executable surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2.3.0-07-01 | I (Information disclosure) | User Manual could leak Shopify PII via the canonical example | mitigate | The canonical example sources values from PROBE-EVIDENCE.md, which was already gated against PII at Plan 01 (probe `fields=` excludes customer fields). The example only references monetary amounts + processed_at timestamps + order IDs — no customer identifiers. Acceptance criterion below grep-rejects PII tokens. INFO. |
| T-05.2.3.0-07-02 | T (Tampering) | User Manual could drift from production behavior | mitigate | (a) The 3 menu items in §16.11 are grep-verified verbatim against Plan 06's Main.gs edits. (b) The negative-revenue rule is grep-verified against Plan 03's header docstring (D-D3 cited in both). (c) Pre-push memory rule (`feedback_keep_user_manual_current.md`) makes manual updates a mandatory gate going forward. LOW. |
| T-05.2.3.0-07-03 | R/D/S/E | N/A | accept | Markdown documentation. No new auth surface. INFO. |
</threat_model>

<verification>
- `grep -q "### 16.11 Refund-Day Attribution" docs/ROAS-Dashboard-User-Manual.md` passes.
- All 3 verbatim menu strings present.
- Probe-evidence binding holds (numeric value grep-bindable).
- No `__REPLACE_*__` placeholders left.
- Chapter 17 boundary intact.
- (Optional, operator-side) Visual check: open the manual in any Markdown viewer, navigate to §16, verify §16.11 renders Hebrew-RTL correctly and the verification URLs are clickable.
</verification>

<success_criteria>
1. New sub-section §16.11 added with 5 sub-sub-sections (model, canonical example, cleanup menu, negative revenue, verification URLs).
2. All 3 Hebrew menu items verbatim per D-D2.
3. Canonical example uses REAL probe numbers.
4. Negative-revenue policy + verification URLs documented.
5. Pre-push gate satisfied (the memory rule `feedback_keep_user_manual_current.md`).
</success_criteria>

<output>
After completion, create `.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-07-SUMMARY.md` recording:
- The line number where §16.11 starts in the User Manual.
- Which store's probe data was used for the canonical example.
- The grep-binding witness value (the numeric token grep-bindable to PROBE-EVIDENCE.md).
- Confirmation that no PII (email, address, name) appears in the new section.
</output>
</content>
</invoke>