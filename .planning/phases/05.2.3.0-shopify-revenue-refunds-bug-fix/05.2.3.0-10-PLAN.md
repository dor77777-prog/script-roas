---
phase: 05.2.3.0-shopify-revenue-refunds-bug-fix
plan: 10
type: execute
wave: 2
depends_on:
  - 05.2.3.0-08
files_modified:
  - docs/ROAS-Dashboard-User-Manual.md
  - .planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-CONTEXT.md
autonomous: true
gap_closure: true
requirements:
  - D-A1
  - D-A2
  - D-A3
  - D-D2
  - D-D3
subsystem: operator-documentation
tags: [gap-closure, user-manual, context-revision, refund-day-attribution, hebrew, docs-only]

must_haves:
  truths:
    - "User Manual §16.11.1 no longer claims 'שורת D-3 תישאר עם הסכום שהיה לפני ההחזר' (the false invariant from WR-05). The new text states that prior-day rows ARE re-fetched but `total_price` is immutable so their value does not change as a result of refunds."
    - "User Manual §16.11.2 worked example uses the same uzoshop probe data but the dashboard before/after table reflects the new attribution: 2026-05-02 shows +68.80 (gross, immutable, refresh-safe) and 2026-05-20 shows −68.80 (refund deduction on processed_at day), summing to 0 across the period."
    - "User Manual §16.11.5 (no-clamping claim) is verified accurate after Plan 08's Math.max(0, ...) removal."
    - "CONTEXT.md D-A1, D-A2, D-A3, D-C3 are revised to document the 2026-05-21 gap-closure model change. Original text is preserved (marked 'superseded') so the historical reasoning chain is traceable."
    - "The User Manual remains fully Hebrew and the existing chapter boundaries (§16.10 above, §17 below) are preserved."
  artifacts:
    - path: "docs/ROAS-Dashboard-User-Manual.md"
      provides: "§16.11.1 rewritten to describe the post-Plan-08 attribution model + §16.11.2 example refreshed + §16.11.5 confirmed accurate"
      contains: "אחרי תיקון מודל הייחוס"
    - path: ".planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-CONTEXT.md"
      provides: "D-A1, D-A2, D-A3 revised + Critical correction subsection added for the 2026-05-21 model change"
      contains: "REVISED 2026-05-21 (gap-closure 08)"
  key_links:
    - from: "docs/ROAS-Dashboard-User-Manual.md §16.11.1"
      to: "Plan 08's total_price + drop-cross-day-filter model"
      via: "describes attribution semantics in terms operators can verify"
      pattern: "total_price.*immutable"
    - from: "CONTEXT.md D-A1/D-A2/D-A3"
      to: "Plan 08 source code"
      via: "Decision-ID anchors that match the grep gates in Plan 08"
      pattern: "REVISED 2026-05-21"
---

<objective>
Close Gap 7 (WR-05) by rewriting User Manual §16.11.1 to match the post-Plan-08 model and refreshing §16.11.2's canonical example. Verify §16.11.5's no-clamping claim is accurate after Plan 08 Task 3. Revise CONTEXT.md D-A1, D-A2, D-A3, D-C3 to document the 2026-05-21 gap-closure model change with the historical reasoning preserved (so a future reader can trace the chain: original hypothesis → first probe revision → gap-closure model change).

Purpose: The User Manual is the operator-facing contract. Right now §16.11.1 contains a load-bearing false claim that misdirects the operator. CONTEXT.md is the planning-layer contract. Right now D-A1/D-A2/D-A3 describe an algorithm the codebase no longer implements (Plan 08 has shipped its replacement). Both must be brought into truth.

Output: 1 commit modifying the User Manual + 1 commit modifying CONTEXT.md (or 1 combined commit if the executor prefers — splitting is cleaner). No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-CONTEXT.md
@.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-VERIFICATION.md
@.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-REVIEW.md
@.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-PROBE-EVIDENCE.md
@.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-07-SUMMARY.md
@.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-08-PLAN.md
@docs/ROAS-Dashboard-User-Manual.md

<interfaces>
Existing User Manual structure (post-Plan-07, pre-Plan-10):

```
§16.10 Checklist (lines 1497-1504)
§16.11 Refund-Day Attribution (lines 1506-1578)
  §16.11.1 מודל הייחוס (lines 1510-1517) ← REWRITE
  §16.11.2 דוגמה קנונית (lines 1519-1534) ← REFRESH dashboard table
  §16.11.3 ניקוי ידני (lines 1536-1550) ← unchanged
  §16.11.4 פרובה אבחונית (lines 1552-1554) ← unchanged
  §16.11.5 הכנסה שלילית (lines 1556-1563) ← verify accurate post-Plan-08
  §16.11.6 אימות מול Shopify Admin (lines 1565-1575) ← unchanged
§17 עשה ואל תעשה (line 1579+)
```

Existing CONTEXT.md decisions (lines 19-49):

```
### Area A - Revenue field source & deduction scope
- D-A1: ... uses current_total_price ...                       ← REVISE
- D-A2: ... refund.processed_at on D AND order.created_at NOT on D ...   ← REVISE
- D-A3: per-product reuses same updated_at-windowed HTTP call ...        ← KEEP (matches Plan 08 Task 4)
- D-A4: Asia/Jerusalem TZ ...                                            ← KEEP

### Area C - Refund magnitude field & reconciliation invariant
- D-C1 REVISED 2026-05-21 ... refund_line_items[].subtotal ...           ← KEEP (Plan 08 uses same field)
- D-C2 ... refund_line_items[].subtotal per-product ...                  ← KEEP
- D-C3 REVISED 2026-05-21 ... 4 mandatory invariants ...                 ← UPDATE to 6 invariants
- D-C4 ... fixtures derived from PROBE-EVIDENCE.md ...                   ← KEEP
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite User Manual §16.11.1 + refresh §16.11.2 dashboard table + verify §16.11.5</name>
  <files>docs/ROAS-Dashboard-User-Manual.md</files>
  <read_first>
    - docs/ROAS-Dashboard-User-Manual.md lines 1506-1578 (full §16.11 section)
    - .planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-REVIEW.md §WR-05 (the specific false claim to fix)
    - .planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-PROBE-EVIDENCE.md ##uzoshop (the canonical example's source data)
    - .planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-VERIFICATION.md §"Gap 7" (acceptance criteria for §16.11.1 + §16.11.2)
  </read_first>
  <action>
Open `docs/ROAS-Dashboard-User-Manual.md`. Locate §16.11 (currently at lines 1506-1578).

**Step 1: Rewrite §16.11.1 (מודל הייחוס) — currently at lines 1510-1517.**

The current text reads (verbatim, line 1512):
```
החזר נספר ביום שבו השדה `refund.processed_at` נופל ב-Asia/Jerusalem TZ — לא ביום שבו ההזמנה המקורית נוצרה. אם הזמנה נוצרה ב-D-3 וההחזר עליה עובד ב-D, ההחזר ינוכה משורת ההכנסה של D. שורת D-3 תישאר עם הסכום שהיה לפני ההחזר (פר D-D3 — לא משכתבים שורות עבר).
```

The bolded clause **שורת D-3 תישאר עם הסכום שהיה לפני ההחזר** is factually false (the rolling 3-day live trigger and 20-day cleanup BOTH re-fetch prior-day rows every cycle). The reason it appeared safe in Plan 07 is the old algorithm used `current_total_price` which the cleanup-driven re-fetch silently mutated.

Replace §16.11.1 (the entire sub-sub-section, from `#### 16.11.1 מודל הייחוס` through the example at line 1517 — but keep the §16.11.1 heading) with:

```markdown
#### 16.11.1 מודל הייחוס

> **אחרי תיקון מודל הייחוס (Phase 05.2.3.0 gap-closure 08, 2026-05-21):** שורות עבר מתעדכנות שוב ושוב ע"י ה-Live Trigger המתגלגל ו-תפריט הניקוי (20 ימים), אבל הערך שלהן לא משתנה — כי המערכת מסכמת את `order.total_price` (שדה **קבוע** במטבע ההזמנה, שלא משתנה אחרי החזר) ולא את `order.current_total_price` (שדה **חי** שמשתקף בכל החזר שעבר על ההזמנה).

החזר נספר ביום שבו השדה `refund.processed_at` נופל ב-Asia/Jerusalem TZ — לא ביום שבו ההזמנה המקורית נוצרה. אם הזמנה נוצרה ב-D-3 וההחזר עליה עובד ב-D:
- שורת `data-daily` של D-3 ממשיכה להציג את `total_price` של ההזמנה (סכום הברוטו הקבוע) — backfills חוזרים על השורה ולא משנים את ערכה.
- שורת `data-daily` של D מציגה את מכירות D פחות הסכום של אותו החזר (`refund_line_items[].subtotal`).

**שדה הניכוי:** המערכת מסכמת `refund_line_items[].subtotal` (סחורה בלבד, במטבע התצוגה של ההזמנה). זהו השדה ש-Shopify עצמו משתמש בו פנימית כדי לחשב את `current_total_price` — אומת אמפירית על 3/3 חנויות בפרובה של 2026-05-21 (ראה PROBE-EVIDENCE.md §Finding 1). השדה `refund.transactions[].amount` **אינו** משמש לניכוי (הוא רץ פי 2–4 מסכום ההזמנה בפועל בגלל הפרשי מטבע ו-artifacts של החזרים כפולים).

**למה total_price ולא current_total_price?** עד 2026-05-20 המערכת השתמשה ב-`current_total_price` בתוספת מסנן cross-day. הוסכמה זו יצרה **double-deduction** מבני: כל פעם שה-Live Trigger או תפריט הניקוי שיחזרו לשלוף את שורת ההזמנה המקורית, ה-`current_total_price` שלה היה נופל לערך הנטו-אחרי-החזר ו-המסנן cross-day היה עדיין מנכה את אותו החזר ביום העיבוד. שני ניכויים על אותו החזר. החלפת השדה ל-`total_price` (קבוע) מסירה את המלכודת הזו: שורות עבר תמיד מציגות את הברוטו, וההחזר נספר פעם אחת — ביום `processed_at` שלו.

**דוגמה ל-TZ (D-A4):**
- החזר שעובד ב-`2026-05-20T22:30:00Z` (UTC) → מיוחס ליום `2026-05-21` (Israel) — כי 22:30 UTC = 01:30 בלילה של 21 במאי בישראל.
```

**Step 2: Refresh §16.11.2 (דוגמה קנונית) — currently at lines 1519-1534.**

Locate the table (currently at lines 1530-1532):
```
| יום | הכנסה לפני התיקון | הכנסה אחרי התיקון |
|---|---|---|
| 2026-05-02 (D-3, יום ההזמנה) | `68.80` | `68.80` (ללא שינוי — שורת D-3 נשארת קפואה) |
| 2026-05-20 (D, יום ההחזר) | מכירות היום | מכירות היום `−` `68.80` (החזר על הזמנה מ-D-3 מנוכה כאן) |
```

Replace the entire §16.11.2 sub-sub-section (from `#### 16.11.2` through the closing paragraph at line 1534) with:

```markdown
#### 16.11.2 דוגמה קנונית — מבוסס על דאטה אמיתי מ-PROBE-EVIDENCE.md

הסבר זה משתמש בערכים אמיתיים שצולמו מ-Shopify Admin REST ביום הפרובה (2026-05-21, חנות uzoshop). ראה `.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-PROBE-EVIDENCE.md` לפרטים מלאים על כל 3 החנויות.

**תרחיש:**
- **D-18** (יום יצירת ההזמנה, `2026-05-02T14:30:21+03:00`): הזמנה ב-uzoshop, `order.id = 10803404276006`, `total_price = 68.80 CAD` (שדה **קבוע** — לא משתנה אחרי החזר). שורת `data-daily` של 2026-05-02 נכתבת עם הכנסה של 68.80.
- **D** (יום עיבוד ההחזר, `2026-05-20T15:27:19+03:00`): הלקוח החזיר את המוצר במלואו. Shopify רשם `refund.processed_at = 2026-05-20T15:27:19+03:00` עם `refund.refund_line_items[0].subtotal = 68.80 CAD` (סחורה בלבד, מטבע ההזמנה). שדה `current_total_price` של ההזמנה ירד ל-`0.00` אבל **המערכת לא קוראת את current_total_price יותר** — היא קוראת רק את `total_price`, שערכו 68.80 קבוע.

**מה הדשבורד מציג (אחרי gap-closure 08):**

| יום | הכנסה | הסבר |
|---|---|---|
| 2026-05-02 (D-18, יום ההזמנה) | `+68.80 CAD` | `total_price` הקבוע. גם אחרי שה-Live Trigger או תפריט הניקוי ירעננו את השורה הזו, הערך לא משתנה — `total_price` קבוע. |
| 2026-05-20 (D, יום ההחזר) | `(מכירות היום) − 68.80 CAD` | ההחזר מנוכה ביום `processed_at` שלו. אם אין מכירות אחרות ביום הזה, השורה תהיה `−68.80` — הכנסה שלילית מותרת (D-D3). |
| סכום שני הימים | `+68.80 − 68.80 = 0.00 CAD` | סך הנטו עבור הזמנה שהוחזרה במלואה הוא אפס — תואם ל-Shopify Admin > Reports > Net sales עבור אותו טווח. |

**מה היה לפני gap-closure 08 (לתיעוד היסטורי):** המערכת הייתה מסכמת `current_total_price` ומבצעת מסנן cross-day. שורת 2026-05-02 הייתה מציגה +68.80 בהתחלה, ואז ב-backfill הראשון של ה-Live Trigger או תפריט הניקוי הייתה נופלת ל-0.00 (כי `current_total_price` של ההזמנה ירד ל-0). שורת 2026-05-20 הייתה מציגה −68.80 (מסנן cross-day). סכום הימים: `0 − 68.80 = −68.80` (off by 68.80 — הbag של double-deduction).

שני הסעיפים בדוגמה החדשה (אחרי gap-closure 08) יחד שווים בסכום ל-Shopify Admin > Reports > Net sales עבור הטווח 2026-05-02..2026-05-20 (פערים של עד `±0.50 CAD` בשל עיגול ו-FX מקובלים).
```

**Step 3: Verify §16.11.5 (הכנסה שלילית) is still accurate.** Currently at lines 1556-1563. The key claim (line 1563) is:
```
הקוד ב-`Shopify.gs` (`getShopifyRevenue` + `getShopifyProductSalesForDay`) **אינו** מבצע `Math.max(0, …)` על הסכום הסופי — זה תיעוד-של-עיצוב, לא באג שצריך לתקן.
```

This claim was FALSE before Plan 08 (the per-product path had `Math.max(0, gross - lineDiscount - lineRefund)` at Shopify.gs:274). Plan 08 Task 3 REMOVED that clamp. So the claim is now TRUE.

**However**, the manual says "אינו מבצע Math.max(0, …) על **הסכום הסופי**" — that wording is ambiguous (could be read as: aggregate net is not clamped, but individual lines might be). To preempt confusion, add a clarifying sentence. Modify the line at 1563 to:

```markdown
הקוד ב-`Shopify.gs` (`getShopifyRevenue` + `getShopifyProductSalesForDay`) **אינו** מבצע `Math.max(0, …)` באף שלב — לא על הסכום הסופי, לא על נטו פר-מוצר, ולא על נטו פר-שורה (line-item). אחרי gap-closure 08 (Plan 08 Task 3) הוסר ה-clamp גם ב-`getShopifyProductSalesForDay`. זה תיעוד-של-עיצוב, לא באג שצריך לתקן.
```

**Step 4: Verify other sub-sub-sections are accurate.**
- §16.11.3 (ניקוי ידני, lines 1536-1550): the 3 menu items still work the same; the description of the rolling 3-day Live Trigger backfill is still accurate. **No changes needed**.
- §16.11.4 (פרובה אבחונית, lines 1552-1554): the probe function is unchanged. **No changes needed**.
- §16.11.6 (אימות מול Shopify Admin, lines 1565-1575): the URLs and the "compare against Net sales" guidance are still accurate. **No changes needed**.

**Step 5: Commit** with message: `docs(05.2.3.0-10): user manual §16.11.1/§16.11.2 reflect total_price model + verify §16.11.5 clamp claim`
  </action>
  <verify>
    <automated>cd /Users/dorperetz/script-roas &amp;&amp; \
      grep -q "אחרי תיקון מודל הייחוס" docs/ROAS-Dashboard-User-Manual.md &amp;&amp; \
      grep -q "gap-closure 08" docs/ROAS-Dashboard-User-Manual.md &amp;&amp; \
      ! grep -q "שורת D-3 תישאר עם הסכום שהיה לפני ההחזר" docs/ROAS-Dashboard-User-Manual.md &amp;&amp; \
      grep -q "total_price.*קבוע" docs/ROAS-Dashboard-User-Manual.md &amp;&amp; \
      grep -q "double-deduction" docs/ROAS-Dashboard-User-Manual.md &amp;&amp; \
      grep -q "10803404276006" docs/ROAS-Dashboard-User-Manual.md &amp;&amp; \
      grep -q "68.80" docs/ROAS-Dashboard-User-Manual.md &amp;&amp; \
      grep -q "לא על נטו פר-שורה" docs/ROAS-Dashboard-User-Manual.md &amp;&amp; \
      [ "$(grep -c '^## 17. עשה ואל תעשה' docs/ROAS-Dashboard-User-Manual.md)" = "1" ]</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'אחרי תיקון מודל הייחוס' docs/ROAS-Dashboard-User-Manual.md` returns 0 (the new model is named)
    - `grep -q 'gap-closure 08' docs/ROAS-Dashboard-User-Manual.md` returns 0 (provenance)
    - `! grep -q 'שורת D-3 תישאר עם הסכום שהיה לפני ההחזר' docs/ROAS-Dashboard-User-Manual.md` (the false claim is GONE)
    - `grep -q 'total_price.*קבוע' docs/ROAS-Dashboard-User-Manual.md` returns 0 (the new field is named as immutable)
    - `grep -q 'double-deduction' docs/ROAS-Dashboard-User-Manual.md` returns 0 (the historical reasoning is preserved)
    - `grep -q '10803404276006' docs/ROAS-Dashboard-User-Manual.md` returns 0 (canonical order ID still present)
    - `grep -q '68.80' docs/ROAS-Dashboard-User-Manual.md` returns 0 (canonical value still present)
    - `grep -q 'לא על נטו פר-שורה' docs/ROAS-Dashboard-User-Manual.md` returns 0 (§16.11.5 clarification added)
    - `grep -c '^## 17. עשה ואל תעשה' docs/ROAS-Dashboard-User-Manual.md == 1` (chapter 17 boundary preserved)
    - PII gate: 0 matches for `email|customer|billing_address|shipping_address` inside §16.11 (verify by `awk '/### 16.11/,/^---$/' | grep -E 'customer\|email\|billing_address\|shipping_address' | wc -l == 0`)
  </acceptance_criteria>
  <done>
    User Manual §16.11.1 describes the post-Plan-08 model with the rolling-3-day-trigger + 20-day-cleanup re-fetch invariant explained. §16.11.2's table reflects the new attribution (2026-05-02 = +68.80 immutable, 2026-05-20 = −68.80). §16.11.5's no-clamping claim is reinforced after Plan 08's clamp removal.
  </done>
</task>

<task type="auto">
  <name>Task 2: Revise CONTEXT.md D-A1/D-A2/D-A3/D-C3 to document the 2026-05-21 gap-closure model change</name>
  <files>.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-CONTEXT.md</files>
  <read_first>
    - .planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-CONTEXT.md (FULL file, 148 lines)
    - .planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-VERIFICATION.md §"Strategy: Model Change"
    - .planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-08-PLAN.md (the implementation contract)
  </read_first>
  <action>
Open `.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-CONTEXT.md`.

**Step 1: Update the file header (lines 1-5)** to flag the new revision:

Currently:
```markdown
# Phase 05.2.3.0: shopify-revenue-refunds-bug-fix - Context

**Gathered:** 2026-05-20
**Revised:** 2026-05-21 — D-B2, D-C1, D-C3 corrected after empirical probe (PROBE-EVIDENCE.md). The D-B4 checkpoint gate fired exactly as designed: original `transactions[].amount`-based invariant was falsified on 3/3 stores; corrected `refund_line_items[].subtotal`-based invariant matched 3/3 stores exactly. See [05.2.3.0-PROBE-EVIDENCE.md](05.2.3.0-PROBE-EVIDENCE.md) and [05.2.3.0-01-SUMMARY.md](05.2.3.0-01-SUMMARY.md) §"Deviation D2".
**Status:** Ready for Wave 2 execution (probe complete; contract revised)
```

Replace with:
```markdown
# Phase 05.2.3.0: shopify-revenue-refunds-bug-fix - Context

**Gathered:** 2026-05-20
**Revised:** 2026-05-21 (probe) — D-B2, D-C1, D-C3 corrected after empirical probe (PROBE-EVIDENCE.md). See [05.2.3.0-01-SUMMARY.md](05.2.3.0-01-SUMMARY.md) §"Deviation D2".
**Revised:** 2026-05-21 (gap-closure 08) — D-A1, D-A2, D-A3, D-C3 updated for the model change (total_price + drop cross-day filter). Code-review (REVIEW.md) surfaced a structural double-deduction bug in the prior algorithm; this revision documents the corrected model. See [05.2.3.0-REVIEW.md](05.2.3.0-REVIEW.md) §CR-01 and [05.2.3.0-08-PLAN.md](05.2.3.0-08-PLAN.md).
**Status:** Post-gap-closure 08 (model change shipped). Plans 09 (test gate) and 10 (this doc revision) close the remaining gaps.
```

**Step 2: Update the `<domain>` block (lines 7-14)** — append a new "Critical correction (gap-closure 08)" subsection after the existing "Critical correction from the failed first attempt" paragraph.

Currently the `<domain>` block ends at line 14 with:
```
**Critical correction from the failed first attempt (rolled back 2026-05-20):** Prior planning assumed `current_total_price` was gross at order-creation time, then subtracted ALL refunds processed on day D. This caused double-deduction of same-day refunds (because `current_total_price` IS net of refunds applied to that order — confirmed by AUDIT-P0-03). The corrected algorithm subtracts only refunds whose underlying order was created on a different day.

</domain>
```

Replace the closing of `<domain>` (the last paragraph + closing tag) with:
```
**Critical correction from the failed first attempt (rolled back 2026-05-20):** Prior planning assumed `current_total_price` was gross at order-creation time, then subtracted ALL refunds processed on day D. This caused double-deduction of same-day refunds (because `current_total_price` IS net of refunds applied to that order — confirmed by AUDIT-P0-03). The corrected algorithm subtracts only refunds whose underlying order was created on a different day.

**Critical correction (gap-closure 08, 2026-05-21):** The Wave 1+2+3 algorithm (current_total_price + cross-day filter) ALSO caused double-deduction — but in a more subtle way. `current_total_price` is a LIVE field that reflects every refund on the order, including refunds processed days later. The rolling 3-day live trigger and 20-day cleanup menu BOTH re-fetch prior-day rows on every cycle, so a refund processed on day D against an order created on day D-2 would:
1. Be subtracted from the D row via the cross-day filter (`getShopifyRefundsForDay_`).
2. Silently drop the D-2 row when it's re-fetched (because `current_total_price` had since dropped to net-of-refund).
Net effect: every cross-day refund within the backfill window was deducted TWICE in normal operation. The gap-closure 08 model change switches the gross field from `current_total_price` (live, refund-reflecting) to `total_price` (immutable, gross) and drops the cross-day filter so every refund attributes to its `processed_at` day exactly ONCE. This makes prior-day row re-fetches safe by construction (total_price never changes after order creation).

</domain>
```

**Step 3: Update D-A1 (line 20)**. Currently:
```markdown
- **D-A1:** Store-level revenue uses `current_total_price` (NOT `total_price`). This field IS net of refunds applied to the order — confirmed empirically by [.planning/phases/05.2.1-algorithm-correctness-audit-opus-codex-cross-ai-review/05.2.1-AUDIT.md](.planning/phases/05.2.1-algorithm-correctness-audit-opus-codex-cross-ai-review/05.2.1-AUDIT.md) AUDIT-P0-03 which observed `current_total_price` going negative when refunds exceed the initial total. The 15-min sync lag is mitigated by the rolling 3-day backfill (D-D1).
```

Replace with:
```markdown
- **D-A1 (REVISED 2026-05-21 gap-closure 08):** Store-level revenue uses `total_price` (immutable, gross) — NOT `current_total_price`. The original D-A1 (`current_total_price`) is **superseded** because backfills re-fetching prior-day rows caused double-deduction: `current_total_price` is a live field that drops as refunds are applied, so a refund processed on day D against an order created on D-2 was simultaneously (a) subtracted from D via the cross-day filter and (b) silently subtracted from D-2 when D-2's row was re-fetched (because `current_total_price` had since dropped). Under the new model, `total_price` is constant per Shopify Admin REST 2024-10 (confirmed by PROBE-EVIDENCE.md — the value is fixed at order creation and does not change for refunds), so re-fetching is mathematically safe. The 15-min `current_total_price` sync lag is no longer relevant because the field is not read anymore.
- **D-A1 (ORIGINAL, superseded):** Store-level revenue uses `current_total_price`. This field IS net of refunds applied to the order — confirmed empirically by AUDIT-P0-03 which observed `current_total_price` going negative when refunds exceed the initial total. The 15-min sync lag is mitigated by the rolling 3-day backfill (D-D1).
```

**Step 4: Update D-A2 (line 21)**. Currently:
```markdown
- **D-A2:** Cross-day refund detection: query `orders.json` with `updated_at_min/max` covering day D (Asia/Jerusalem 00:00–23:59), then iterate each order's `refunds[]` client-side and keep only refunds where `refund.processed_at` falls on day D AND `order.created_at` does NOT fall on day D. This catches refunds processed on D against orders created on any prior day, since refunding an order advances its `updated_at`.
```

Replace with:
```markdown
- **D-A2 (REVISED 2026-05-21 gap-closure 08):** Refund attribution: query `orders.json` with `updated_at_min/max` covering day D (Asia/Jerusalem 00:00–23:59), then iterate each order's `refunds[]` client-side and keep ALL refunds where `refund.processed_at` falls on day D. The `order.created_at !== dateStr` short-circuit is **removed** — under the new D-A1 model (total_price gross, immutable), same-day and cross-day refunds both deduct on the refund-processing day exactly ONCE. The `updated_at` window still catches every order touched by a same-day or cross-day refund (refunding advances `updated_at`).
- **D-A2 (ORIGINAL, superseded):** Cross-day refund detection: keep only refunds where `refund.processed_at` falls on day D AND `order.created_at` does NOT fall on day D. The cross-day filter existed because the original D-A1 (`current_total_price`) already accounted for same-day refunds — but it caused double-deduction when backfills re-fetched prior-day rows. Both D-A1 and D-A2 are revised together as a single model change.
```

**Step 5: Update D-A3 (line 22)**. Currently:
```markdown
- **D-A3:** Per-product path (`getShopifyProductSalesForDay`) reuses the SAME `updated_at`-windowed HTTP call as the store-level cross-day fetch. One fetch, two consumers: store-level sums `transactions[].amount`; per-product accumulates `refund_line_items[].subtotal` by `product_id` into `byProduct[pid].netRevenueCad`. Avoids duplicate API calls and API quota burn.
```

Replace with:
```markdown
- **D-A3 (REVISED 2026-05-21 gap-closure 08):** Per-product path (`getShopifyProductSalesForDay`) reuses the SAME `updated_at`-windowed HTTP call as the store-level refund fetch. One HTTP call, two consumers — **enforced structurally** in `DailyUpdate.gs:updateStoreForDate_` which fetches `getShopifyRefundsForDay_(storeId, dateStr)` once and passes the result to both `getShopifyRevenue(storeId, dateStr, refunds)` and `getShopifyProductSalesForDay(storeId, dateStr, refunds)` via an optional third parameter (WR-04 / Gap 6 fix). Both paths use `refund_line_items[].subtotal` (per D-C1 revised + D-C2). Per-day Shopify refund-fetch API call count: 1 (was 2 before gap-closure 08).
- **D-A3 (ORIGINAL):** Per-product path reused the SAME HTTP call as the store-level cross-day fetch. The "one fetch, two consumers" intent existed but was violated in practice — both consumers fetched the refunds independently. The store-level path used `transactions[].amount` originally (later corrected to `refund_line_items[].subtotal` in the 2026-05-21 probe revision); per-product used `refund_line_items[].subtotal` throughout.
```

**Step 6: Update D-C3** at the start of the bullet list at lines 37-41. Currently the D-C3 block specifies 4 mandatory invariants. Add invariants 5 and 6 to align with Plan 09's test gate. Locate the existing `D-C3 (REVISED 2026-05-21)` block and append after the "4. Full reconciliation" line:

After the existing line 41 (the 4th invariant block ending with "if `> 1 CAD`, log as WARNING and surface in the next probe re-run for investigation."), insert two new bullets:

```markdown
  5. **Period reconciliation (added gap-closure 08, 2026-05-21):** Σ `computeRevenueWithCrossDayRefunds(orders, day, tz).storeNetCad` summed over EVERY unique day touched by the fixture set (order-creation days + refund-processing days) MUST equal `Σ total_price(non-test, non-voided)` − `Σ refund_line_items[].subtotal(all refunds)`. This invariant catches the structural double-deduction bug that the original 4 invariants were blind to (they all run against a single day). Implemented as Vitest "D-C3 invariant 5" in `shopifyRevenueRefunds.test.ts` (Plan 09).
  6. **Per-product cross-day same-day-order with future-day refund (added gap-closure 08, 2026-05-21):** A same-day order with a refund processed on a FUTURE day must have its same-day per-product net equal to the line gross (no future-refund deduction); the refund deducts on its `processed_at` day. Locks the `processed_at` filter on the intra-order `refundByLineId` map (Apps Script + TS mirror). Implemented as Vitest "D-C3 invariant 6" (Plan 09).
```

**Step 7: Commit** with message: `docs(05.2.3.0-10): revise CONTEXT.md D-A1/D-A2/D-A3/D-C3 for gap-closure 08 model change`
  </action>
  <verify>
    <automated>cd /Users/dorperetz/script-roas &amp;&amp; \
      grep -q "Revised:** 2026-05-21 (gap-closure 08)" .planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-CONTEXT.md &amp;&amp; \
      grep -q "D-A1 (REVISED 2026-05-21 gap-closure 08)" .planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-CONTEXT.md &amp;&amp; \
      grep -q "D-A1 (ORIGINAL, superseded)" .planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-CONTEXT.md &amp;&amp; \
      grep -q "D-A2 (REVISED 2026-05-21 gap-closure 08)" .planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-CONTEXT.md &amp;&amp; \
      grep -q "D-A3 (REVISED 2026-05-21 gap-closure 08)" .planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-CONTEXT.md &amp;&amp; \
      grep -q "Period reconciliation (added gap-closure 08" .planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-CONTEXT.md &amp;&amp; \
      grep -q "Per-product cross-day same-day-order with future-day refund" .planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-CONTEXT.md &amp;&amp; \
      grep -q "Critical correction (gap-closure 08, 2026-05-21)" .planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-CONTEXT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'Revised:\*\* 2026-05-21 (gap-closure 08)' CONTEXT.md` returns 0 (new revision header line present)
    - `grep -q 'D-A1 (REVISED 2026-05-21 gap-closure 08)' CONTEXT.md` returns 0 (new D-A1 wording)
    - `grep -q 'D-A1 (ORIGINAL, superseded)' CONTEXT.md` returns 0 (historical chain preserved)
    - `grep -q 'D-A2 (REVISED 2026-05-21 gap-closure 08)' CONTEXT.md` returns 0
    - `grep -q 'D-A3 (REVISED 2026-05-21 gap-closure 08)' CONTEXT.md` returns 0
    - `grep -q 'Period reconciliation (added gap-closure 08' CONTEXT.md` returns 0 (invariant 5 added to D-C3)
    - `grep -q 'Per-product cross-day same-day-order with future-day refund' CONTEXT.md` returns 0 (invariant 6 added to D-C3)
    - `grep -q 'Critical correction (gap-closure 08, 2026-05-21)' CONTEXT.md` returns 0 (the new domain subsection is present)
    - D-A4, D-B*, D-C1, D-C2, D-C4, D-D1, D-D2, D-D3, D-D4 sections REMAIN UNCHANGED (verify by line count + grep for each: each should appear exactly once with its original wording)
  </acceptance_criteria>
  <done>
    CONTEXT.md documents the 2026-05-21 gap-closure 08 model change with the historical reasoning chain preserved (`ORIGINAL, superseded` markers). D-A1/D-A2/D-A3 describe the algorithm Plan 08 shipped. D-C3 includes invariants 5+6 matching Plan 09's tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation - operator action | The User Manual is the operator-facing contract; misleading text leads to wrong cleanup actions and lost trust in the dashboard. |
| CONTEXT.md - future planning | CONTEXT.md is consumed by future `/gsd-discuss-phase` and `/gsd-plan-phase` runs; stale wording leads to plans that don't match implementation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information disclosure | New User Manual text could leak per-customer data | accept | Canonical example uses only order.id (numeric), timestamps, and aggregate monetary values. PII gate via grep in acceptance criteria. |
| T-10-02 | Tampering | Future refactor changes algorithm without updating §16.11 | mitigate | Pre-push memory rule `feedback_keep_user_manual_current.md` enforces this; the new §16.11.1 text explicitly cites the load-bearing invariant ("total_price is immutable") which a future change must invalidate to re-introduce the bug. |
| T-10-03 | Repudiation | Operator reads stale §16.11.1 claim and believes prior rows are frozen | mitigate | Plan 10 IS the fix for this — the false claim is grep-deleted via acceptance criteria. |
| T-10-04 | Documentation drift between code and CONTEXT.md | medium | mitigate | Task 2 preserves the ORIGINAL text with `superseded` markers so the historical chain is auditable. Future planners can grep `REVISED.*gap-closure 08` to see the latest decision. |
</threat_model>

<verification>
Run these checks before declaring the plan complete:

```bash
cd /Users/dorperetz/script-roas

# Task 1 — User Manual updates
grep -q "אחרי תיקון מודל הייחוס" docs/ROAS-Dashboard-User-Manual.md
grep -q "gap-closure 08" docs/ROAS-Dashboard-User-Manual.md
! grep -q "שורת D-3 תישאר עם הסכום שהיה לפני ההחזר" docs/ROAS-Dashboard-User-Manual.md
grep -q "double-deduction" docs/ROAS-Dashboard-User-Manual.md
grep -q "לא על נטו פר-שורה" docs/ROAS-Dashboard-User-Manual.md
# Canonical example numbers still present:
grep -q "10803404276006" docs/ROAS-Dashboard-User-Manual.md
grep -q "68.80" docs/ROAS-Dashboard-User-Manual.md
# Chapter 17 boundary preserved:
[ "$(grep -c '^## 17. עשה ואל תעשה' docs/ROAS-Dashboard-User-Manual.md)" = "1" ]

# Task 2 — CONTEXT.md updates
CTX=.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-CONTEXT.md
grep -q "Revised:\*\* 2026-05-21 (gap-closure 08)" "$CTX"
grep -q "D-A1 (REVISED 2026-05-21 gap-closure 08)" "$CTX"
grep -q "D-A1 (ORIGINAL, superseded)" "$CTX"
grep -q "D-A2 (REVISED 2026-05-21 gap-closure 08)" "$CTX"
grep -q "D-A3 (REVISED 2026-05-21 gap-closure 08)" "$CTX"
grep -q "Period reconciliation (added gap-closure 08" "$CTX"
grep -q "Per-product cross-day same-day-order with future-day refund" "$CTX"
grep -q "Critical correction (gap-closure 08, 2026-05-21)" "$CTX"

# Unchanged decisions still present (smoke check):
grep -q "D-A4:\*\* All" "$CTX" || grep -q "D-A4:" "$CTX"
grep -q "D-B1:" "$CTX"
grep -q "D-B2" "$CTX"
grep -q "D-C1 (REVISED 2026-05-21)" "$CTX"
grep -q "D-C2:" "$CTX"
grep -q "D-C4:" "$CTX"
grep -q "D-D1:" "$CTX"
grep -q "D-D2:" "$CTX"
grep -q "D-D3:" "$CTX"
grep -q "D-D4:" "$CTX"

# PII gate on §16.11
awk '/### 16.11 /,/^---$/' docs/ROAS-Dashboard-User-Manual.md | grep -E "customer|email|billing_address|shipping_address" | wc -l
# Expect: 0
```

All gates must pass.
</verification>

<success_criteria>
- 2 atomic commits with `docs(05.2.3.0-10):` prefix (one for the User Manual, one for CONTEXT.md)
- All grep gates in `<verification>` PASS
- §16.11.1's false claim is GONE; the new model is documented in Hebrew
- §16.11.2's table shows the new attribution (+68.80 / −68.80 / sum=0)
- §16.11.5's no-clamping claim explicitly covers all 3 levels (aggregate, per-product, per-line)
- CONTEXT.md preserves original D-A1/D-A2/D-A3 wording with `superseded` markers (auditable history)
- D-C3 lists 6 invariants total (4 from Plan 04 + 2 from Plan 09)
- File line count grew by no more than ~80 lines for the User Manual and ~30 lines for CONTEXT.md (pure additions + targeted rewrites; no large deletions)
- Pre-push gate per `feedback_keep_user_manual_current.md` is satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05.2.3.0-shopify-revenue-refunds-bug-fix/05.2.3.0-10-SUMMARY.md` capturing:
- Tasks completed with commit hashes
- Before/after line counts for both files
- Verbatim §16.11.1 new text (so the SUMMARY.md doubles as a contract record)
- List of CONTEXT.md decisions revised + decisions preserved as `superseded`
- Verification gate results
- Deviations from plan (if any)
- Confirmation that the rolling 3-day Live Trigger (§16.11.3) and probe diagnostic (§16.11.4) are unchanged
</output>
