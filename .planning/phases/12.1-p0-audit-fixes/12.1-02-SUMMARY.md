---
phase: 12.1-p0-audit-fixes
plan: 12.1-02
subsystem: ai-report
tags:
  - audit-fix
  - ai-report
  - cross-store
  - storeId-threading
  - p0
dependency-graph:
  requires:
    - "12.1-01 (Inngest retry + idempotency baseline — TSC + vitest green)"
    - "Aggregated.storeId already on campaignsAggregator.ts:11 (verified)"
  provides:
    - "Per-store isolation for Currently-Off, Health Score, Pixel↔Shopify det, ad-set drill-down, creative drill-down in operator's default storeName='All' view"
    - "TG-02 golden cross-store fixture for future regression"
  affects:
    - "dashboard-web/src/lib/aiReport.ts (consumer of campaignsAggregator)"
    - "No downstream Aggregated consumers — fix is purely INTERNAL to aiReport"
tech-stack:
  added: []
  patterns:
    - "storeId-scoped composite keys (`${storeId}::${platform}::${campaignId}`) across all aiReport internal maps"
    - "Direct map.get() instead of suffix-match Array.from().find() for cross-store correctness AND O(N²) → O(1) lookup"
key-files:
  created:
    - dashboard-web/src/lib/__tests__/aiReportCrossStoreCollision.test.ts
  modified:
    - dashboard-web/src/lib/aiReport.ts
decisions:
  - "Composite commit per AUDIT.md / operator-approved D-08 exception — all 3 findings share root cause + file + test"
  - "Removed ALG-09 dead conditional (`c.store === c.platform ? '' : ''`) as a bonus during the ALG-06 site #2 edit since the variable was unused anyway"
  - "Fixture intentionally does NOT mix non-chronological campaign rows — that's ALG-03 territory (separate finding, out of scope for 12.1.2)"
metrics:
  duration: ~28 minutes
  completed: 2026-05-24
---

# Phase 12.1 Plan 12.1-02: aiReport cross-store cleanup Summary

One-liner: ALG-04 + ALG-05 + ALG-06 + ALG-09 fixed atomically via storeId-scoped composite keys across every internal map in `aiReport.ts`, pinned by TG-02 golden two-store fixture.

## What shipped

**Composite commit**: `51c0de4` — `fix(aiReport): ALG-04 + ALG-05 + ALG-06 storeId-scoped keys (Phase 12.1.2)`

**5 edits applied to `dashboard-web/src/lib/aiReport.ts` in one working-tree pass:**

1. **Edit A (Foundation)** — line ~777-810. Surfaced `storeId` on the `campaignsList` aggregated value shape (typed as `storeId: string`, populated from `c.storeId`). This is the load-bearing change all subsequent edits build on.

2. **Edit B (ALG-05 site #1)** — line ~842-863. `ordersByCampaignId` / `ordersByCampaignName` keys switched to `${o.storeId}::${id}` / `${o.storeId}::${name.toLowerCase()}`. Pixel↔Shopify consumer lookup at line ~880 updated to use the storeId-scoped composite.

3. **Edit B (ALG-05 site #2)** — line ~1095-1115. `detById` / `detByName` parallel maps same treatment. Health Score consumer lookup at line ~1170 updated.

4. **Edit C (ALG-04)** — line ~1320-1340. `statusByCampaign` set side keyed by `${c.storeId}::${c.platform}::${c.campaignId}`; consumer get side at line ~1340 symmetric.

5. **Edit D (ALG-06 + ALG-09)** — three sites:
   - Line ~1145-1170: replaced `Array.from(dailyByKey.keys()).find(k => k.endsWith(...))` with direct `dailyByKey.get(composedKey)`. Also updated the synthesized `Aggregated` shape at line ~1226 to pass the real `c.storeId` and `composedKey` instead of pre-fix placeholder `''` and suffix-derived key.
   - Line ~1545-1560: ad-set drill-down — replaced suffix scan with direct `adsetsByCampaign.get(adsetKey)`. **Removed ALG-09 dead conditional** `c.store === c.platform ? '' : ''` (always returned empty string regardless of the comparison; the resulting `cKey` was never read).
   - Line ~1620-1635: ad creative drill-down — replaced `adAgg.values().filter(a => a.campaignKey.endsWith(...))` with exact-match `a.campaignKey === adKey` since `adAgg.campaignKey` is set to the campaign-level composite (no ad-set / ad-id suffix to worry about).

## Verification

| Gate | Result |
|------|--------|
| TG-02 fixture (RED gate, pre-fix) | 4/5 RED — confirmed bug-pinning |
| TG-02 fixture (GREEN gate, post-fix) | 5/5 GREEN |
| Revert sanity (`git checkout HEAD~1 -- aiReport.ts`) | 4/5 RED — fixture genuinely catches the bug |
| Full project test suite (`npm test`) | 971/971 passing (was 955 baseline; +5 TG-02 net for this plan, +11 from concurrent Wave-1 commits that landed test files in the same directory) |
| TypeScript gate (`npx tsc --noEmit`) | exit 0, no errors |
| Downstream Aggregated consumers | Zero modifications needed (per 12-CHANNELS.md §1.3 — `Aggregated.storeId` already existed on `campaignsAggregator.ts:11`, so the fix is purely additive to aiReport.ts's INTERNAL maps) |
| Per-finding annotations | grep `AUDIT ALG-04`: 3 hits · `AUDIT ALG-05`: 4 hits · `AUDIT ALG-06`: 3 hits |
| Dead conditional removed | grep `c.store === c.platform` in executable code: 0 (1 remaining hit is inside an annotation comment) |

## Downstream Aggregated consumer impact

**NONE.** The 8 downstream consumers per 12-CHANNELS.md §1.3 (CampaignsTable, CampaignsTableRow, ProductCentricView, campaignHealthScore, multiMappingCohort, productCentricView, useCampaignTrueRevenue, plus aiReport itself) all continue to compile + pass their existing tests unchanged. The fix is **additive to aiReport's INTERNAL map keys only** — the public `Aggregated` type contract was already correct (storeId surfaced in earlier work).

## Test file assertion updates

**None needed.** The pre-existing `aiReportStoreId.test.ts` and `aiReportStatistics.test.ts` did not assert pre-fix key shapes — they tested orthogonal concerns (storeName-vs-storeId filter, MAD anomaly math). Both passed unmodified after the fix.

## Deviations from Plan

### Inline fixture refinement (not a Rule deviation — within plan latitude)

**1. Removed non-chronological campaign row stress from `makeBrandFixture`**
- **Found during:** Task 2 GREEN gate (initial run failed 2 of 5 tests with status=ACTIVE on a "should be PAUSED" row)
- **Issue:** The original fixture per the plan's Step 2 added 2-date / non-chronological-order rows specifically to also stress the ALG-03 latest-wins ordering bug. With ALG-04/05/06 fixed (per-store isolation restored), ALG-03's bug surfaced (which was previously masked by the cross-store collision). ALG-03 is a separate finding NOT in scope for 12.1.2.
- **Fix:** Simplified the fixture to a single-status-per-store / single-date design. The TG-02 specification (per 12-tests-needed.md) only requires the cross-store collision shape — the non-chronological cross-ref was an additive optimization that ended up coupling two independent bugs.
- **Files modified:** `dashboard-web/src/lib/__tests__/aiReportCrossStoreCollision.test.ts` (header doc + fixture body)
- **Rationale:** Test-scope cleanup, not a code change. Pure focus on the 3 plan-scoped findings.

### None other.

The plan was executed exactly as specified — 5 edits applied in one working-tree pass, atomic composite commit per CONTEXT.md D-08 operator-approved exception, full verification gate green.

## Authentication gates

None — pure code-fix plan.

## Self-Check: PASSED

| Claim | Verification |
|-------|-------------|
| `aiReportCrossStoreCollision.test.ts` exists | FOUND: dashboard-web/src/lib/__tests__/aiReportCrossStoreCollision.test.ts |
| `aiReport.ts` modified | git diff confirms +107 / -41 lines |
| Commit `51c0de4` exists | FOUND in `git log` |
| Commit message references ALG-04 + ALG-05 + ALG-06 | git log -1 confirms ("ALG-04 + ALG-05 + ALG-06 storeId-scoped keys (Phase 12.1.2)") |
| Full suite green (npm test) | 971/971 passing |
| TSC green (npx tsc --noEmit) | exit 0 |
| Revert-sanity test passes (TG-02 red on revert, green on restore) | Verified via `git checkout HEAD~1 -- aiReport.ts` cycle |

## Recommended follow-up

- **Per CONTEXT.md D-07** — Codex critique pass on the post-fix aiReport.ts is recommended (not blocking). Operator decision.
- **ALG-03 (latest-wins / sort-before-iterate)** is a separate MAJOR finding NOT scoped to 12.1; should be picked up in a future wave. Its bug surfaced during this plan's TG-02 RED-then-GREEN cycle (previously masked by the cross-store collision); confirmed not to affect production usage today because campaigns rows from fetchers happen to land in chronological order. Recommend planning into Phase 12.2.
