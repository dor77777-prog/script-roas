---
phase: 12.1-p0-audit-fixes
plan: 12.1-03
type: execute
wave: 2
depends_on: [12.1-01]
files_modified:
  - dashboard-web/src/lib/multiMappingCohort.ts
  - dashboard-web/src/lib/productCentricView.ts
  - dashboard-web/src/lib/__tests__/multiMappingCohortRanking.test.ts
  - dashboard-web/src/lib/__tests__/productCentricViewSumConservation.test.ts
autonomous: true
requirements:
  - "P0-MMC-BLOCKER-01: multiMappingCohort ranking score reversal under shrinkage"
  - "P0-PCV-ALG-01: productCentricView sum-conservation (allocatedRevenueEstimate sums to intraAllocatedRevenue)"
tags:
  - audit-fix
  - cohort
  - allocator
  - sum-conservation
  - tdd
must_haves:
  truths:
    - "MMC-BLOCKER-01 fixed: multiMappingCohort.ts uses a tuple-lexicographic comparator (primary roasShopify desc → secondary roasShopifyPlatform desc → spend desc → campaignKey lex asc) instead of the composite-key formula score = shrunk*1_000_000 + shrunkPlat*1_000 + spend"
    - "MMC-BLOCKER-01 fixed: explicit Number.isFinite guard on inputs roasShopifyByKey + roasShopifyPlatformByKey so NaN/Infinity sink to a deterministic position instead of corrupting the comparator"
    - "MMC-BLOCKER-01 regression test: 5 cases per 12-tests-needed.md TG-05 — extreme-value (roasShopifyPlatform > 1000) sort, reference-identity (result.current === result.rankedAll[currentRank - 1]), others array ordering, NaN guard, tie-break stability across renders"
    - "PCV-ALG-01 fixed: productCentricView.ts simplified-split branch — when allocator is NOT taken AND intraTotal === 0 AND platformRev > 0, intraShare falls back to 1/platformMembers.length instead of 0 so per-member allocatedRevenueEstimate sums to the platform total"
    - "PCV-ALG-01 regression test: 4 cases per 12-tests-needed.md HP-21 — zero-spend cohort sum-conservation, stale-mapped cohort (allocator branch dropped member), productUnits omitted graceful, cross-product independence"
    - "2 atomic commits per CONTEXT.md D-08 — one for MMC-BLOCKER-01, one for ProductCentricView ALG-01"
    - "MMC-BLOCKER-01 fix is BACKWARDS-COMPATIBLE with the CohortComparisonPanel.compareIntraSectionMembers HIGH-6 fix already shipped — the panel re-sorts each section but consumes rankedAll's order for currentRank/isLeader/isWeakest; this fix aligns source ranking with the panel's sort"
  artifacts:
    - path: "dashboard-web/src/lib/__tests__/multiMappingCohortRanking.test.ts"
      provides: "MMC-BLOCKER-01 regression test (5 cases per TG-05)"
      contains: "describe('multiMappingCohort ranking"
    - path: "dashboard-web/src/lib/__tests__/productCentricViewSumConservation.test.ts"
      provides: "ProductCentricView ALG-01 regression test (4 cases per HP-21)"
      contains: "describe('productCentricView sum-conservation"
    - path: "dashboard-web/src/lib/multiMappingCohort.ts"
      provides: "MMC-BLOCKER-01 fix: tuple-lex comparator + non-finite guard"
      contains: "AUDIT MMC-BLOCKER-01"
    - path: "dashboard-web/src/lib/productCentricView.ts"
      provides: "PCV-ALG-01 fix: equal-share fallback when intraTotal === 0 in simplified-split branch"
      contains: "AUDIT ALG-01"
  key_links:
    - from: "dashboard-web/src/lib/multiMappingCohort.ts new compareCohortMembers(a, b)"
      to: "dashboard-web/src/components/CohortComparisonPanel.tsx compareIntraSectionMembers (line ~60-97, HIGH-6 fix)"
      via: "the two comparators must now use the SAME shape (tuple-lex) so source ranking and panel display ranking agree"
      pattern: "compareCohortMembers\\|compareIntraSectionMembers"
    - from: "dashboard-web/src/lib/productCentricView.ts simplified-split branch (line ~279-286 in the else of allocByCampaign)"
      to: "byPlatform.intraAllocatedRevenue field consumed by ProductCentricView.tsx component"
      via: "per-member allocatedRevenueEstimate at line ~301 now sums to platformRev when platformSpend totals to 0"
      pattern: "intraShare =.*platformMembers.length\\|1 / platformMembers.length"
---

<objective>
Phase 12.1.3 — Ship the 2 algorithm-math fixes from AUDIT.md §Recommended Phase 12.1 Scope. These are isolated to 2 algorithm files with disjoint consumers from Plan 12.1-02, so this plan runs in parallel with 12.1-02 (both depend only on 12.1-01).

- MMC-BLOCKER-01 (BLOCKER): multiMappingCohort ranking score reversal under shrinkage. The current rankingScore at line 201-205 uses a composite key (shrunk * 1_000_000 + shrunkPlat * 1_000 + spend). When roasShopifyPlatform reaches extreme values (>~2500, plausible at tiny-spend deterministic-ROAS calculations), the secondary tier overflows past the primary tier's magnitude and FLIPS the winner. Operator sees inverted rank chips (🥇 / 🥈 / 🥉) and the cohort health-score adjustment in applyCohortAdjustmentOnce applies the WRONG bonus.

- ProductCentricView ALG-01 (BLOCKER): per-member allocatedRevenueEstimate does not sum to byPlatform.intraAllocatedRevenue. In the no-allocator (simplified-split) path at line 289-301, when platform spend totals to 0 but platformRev > 0 (the b/HI-05 zero-spend cohort fix), intraShare = a.spend / intraTotal = 0/0 = 0. So every per-member allocatedRev = platformRev * 0 = 0. Operator sees CAD 0 rows under a CAD 500 platform header.

Per CONTEXT.md D-03 each fix lands TDD-style: write failing regression test → apply fix → confirm green → commit atomically.

Purpose: Both findings are BLOCKER per AUDIT.md and produce operator-visible wrong-data on real production cohorts. They are independent of 12.1-02 (different files, different consumers).

Output: 2 atomic commits on main (one per finding), 2 new regression test files, vitest pass count rises, tsc green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/AUDIT.md
@.planning/phases/12.1-p0-audit-fixes/12.1-CONTEXT.md
@.planning/phases/12-codebase-audit-baseline/raw-returns/lib_algorithm_multiMappingCohort.json
@.planning/phases/12-codebase-audit-baseline/raw-returns/lib_algorithm_productCentricView.json
@.planning/phases/12-codebase-audit-baseline/12-tests-needed.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@dashboard-web/src/lib/multiMappingCohort.ts
@dashboard-web/src/lib/productCentricView.ts
@dashboard-web/src/lib/__tests__/multiMappingCohort.test.ts
@dashboard-web/src/lib/__tests__/productCentricView.test.ts

<interfaces>
From dashboard-web/src/lib/multiMappingCohort.ts (lines ~181-206, the buggy ranking site):

```
function shrinkRoas(roas: number, spend: number, orders: number): number {
  if (spend <= 0 && orders <= 0) return 1.0;
  const wSpend = spend > 0 ? spend / (spend + ROAS_SHRINKAGE_ANCHOR_CAD) : 0;
  const wOrders = orders > 0 ? orders / (orders + ROAS_SHRINKAGE_ANCHOR_ORDERS) : 0;
  const w = Math.max(wSpend, wOrders);
  return roas * w + 1.0 * (1 - w);
}

function rankingScore(m: CohortMember): number {
  if (!m.metrics) return -Infinity;
  const shrunk = shrinkRoas(m.metrics.roasShopify, m.metrics.spend, m.metrics.conversions);
  const shrunkPlat = shrinkRoas(m.metrics.roasShopifyPlatform, m.metrics.spend, m.metrics.conversions);
  return (
    shrunk * 1_000_000 +
    shrunkPlat * 1_000 +
    m.metrics.spend
  );
}

// Usage at line ~312:
rankedAll.sort((a, b) => rankingScore(b) - rankingScore(a));
```

The fix replaces rankingScore with a tuple-lexicographic compareCohortMembers function. The shrinkRoas helper is unchanged (it's already correct per the b/HI-02 audit fix).

From dashboard-web/src/lib/multiMappingCohort.ts (lines ~355-363, the metrics build where NaN can enter):

```
metrics: agg ? {
  spend: agg.spend,
  conversions: agg.conversions,
  conversionValue: agg.conversionValue,
  roasShopify: roasShopifyByKey.get(campaignKey) ?? 0,        // ← NaN passes the ?? guard
  roasShopifyPlatform: roasShopifyPlatformByKey.get(campaignKey) ?? 0,  // ← NaN passes too
} : undefined,
```

From dashboard-web/src/lib/productCentricView.ts (lines ~279-301, the buggy simplified-split branch):

```
} else {
  for (const [platform, spend] of platformSpend.entries()) {
    const share = totalCohortSpend > 0
      ? spend / totalCohortSpend
      : (platformCount > 0 ? 1 / platformCount : 0);
    platformAllocatedRevenue.set(platform, totalNetRevenue * share);  // b/HI-05 fix already correct: platform-level equal share
  }
}

const members: ProductCohortMember[] = raw
  .filter(r => r.agg !== undefined)
  .map(r => {
    const a = r.agg!;
    const intraTotal = platformSpend.get(a.platform) ?? 0;
    const intraShare = intraTotal > 0 ? a.spend / intraTotal : 0;  // ← BUG: 0/0 → 0 when zero-spend cohort
    const totalShare = totalCohortSpend > 0 ? a.spend / totalCohortSpend : 0;
    const platformRev = platformAllocatedRevenue.get(a.platform) ?? 0;
    const allocatedRev = allocByCampaign?.get(a.key) ?? (platformRev * intraShare);  // ← BUG: 500 × 0 = 0
    // ...
  });
```

The fix changes intraShare so when intraTotal === 0 AND platformRev > 0, it falls back to 1 / N (where N is the count of members on this platform within the cohort). The simplest implementation: count the per-platform member count before the map call.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MMC-BLOCKER-01 — multiMappingCohort tuple-lex comparator + non-finite guard</name>
  <files>
    - dashboard-web/src/lib/__tests__/multiMappingCohortRanking.test.ts (new)
    - dashboard-web/src/lib/multiMappingCohort.ts (modify)
  </files>
  <read_first>
    - .planning/phases/12-codebase-audit-baseline/raw-returns/lib_algorithm_multiMappingCohort.json — MMC-BLOCKER-01 (lines 7-15)
    - .planning/phases/12-codebase-audit-baseline/12-tests-needed.md §"TG-05" (lines 54-64)
    - dashboard-web/src/lib/multiMappingCohort.ts lines 173-220 (the buggy rankingScore) + lines 305-340 (sort site + rankedAll construction)
    - dashboard-web/src/lib/__tests__/multiMappingCohort.test.ts (existing fixture pattern with makeAgg + key('Meta', 'c1'))
    - dashboard-web/src/components/CohortComparisonPanel.tsx lines 60-97 (the HIGH-6 compareIntraSectionMembers — the pattern the cohort source should align with)
  </read_first>
  <behavior>
The regression test file MUST fail on current main HEAD (RED) and pass after the fix (GREEN). Concretely:

- Test 1 (extreme-value sort, TG-05): two members A {roasShopify=2, roasShopifyPlatform=5000, spend=10, conversions=10} vs B {roasShopify=3, roasShopifyPlatform=4, spend=10, conversions=10}. Per the raw-returns failing_input: pre-fix the composite-key score puts A first (4,000,510) because tier-2 (shrunkPlat * 1_000 ≈ 2.5e6) overflows past tier-1 (shrunk * 1_000_000 = 2e6). Post-fix: B is rank 1 (higher primary roasShopify=3 beats A's 2). Assert: result.rankedAll[0].campaignKey equals key('Meta', 'B') AND when current is B → isLeader === true.

- Test 2 (reference identity): assert result.current === result.rankedAll[result.currentRank - 1] (same JS reference). Pre-fix the two are built via separate spreads (lines 309 + 328 — see MMC-WARN-04). Post-fix the fix should make them the same reference OR explicitly document the decoupling. CHOICE: align them to the same reference (cleaner consumer API per the MMC-WARN-04 fix_sketch).

- Test 3 (others ordering): 3 cohort members inserted into productMap in order [c2, c3, c1] with ROAS c1 > c2 > c3. Assert result.others.map(o => o.campaignKey) is in ROAS-desc order [c1, c2, c3], NOT insertion order. Per MMC-WARN-01, the others array is appended in productMap iteration order and never sorted. The fix should sort others using the same compareCohortMembers comparator.

- Test 4 (non-finite guard): pass roasShopifyByKey with NaN for one campaign and Infinity for another. Assert (a) result is non-null, (b) two consecutive calls produce identical currentRank/isLeader/isWeakest (deterministic), (c) every metrics.roasShopify field is Number.isFinite (NaN/Infinity sunk to 0).

- Test 5 (tie-break stability across renders): two members with identical metrics inserted into productMap in different key orders across two calls. Assert rankedAll order is identical across both calls (per MMC-WARN-05). The fix adds a terminal lex tiebreaker on campaignKey.
  </behavior>
  <action>
Step 1 — Write the regression test FIRST (RED gate):

Create dashboard-web/src/lib/__tests__/multiMappingCohortRanking.test.ts with:

A — Header block:
```
/**
 * Audit fix 2026-05-24 (AUDIT MMC-BLOCKER-01, Phase 12.1.3) — locks
 * the cohort ranking contract per TG-05.
 *
 * Pre-fix: rankingScore at lines 194-205 used a composite-key formula
 * (shrunk * 1_000_000 + shrunkPlat * 1_000 + spend). For tiny-spend
 * deterministic ROAS scenarios (roasShopifyPlatform > ~2500), the
 * secondary tier overflowed past the primary's magnitude and FLIPPED
 * the winner. Operator-visible: rank chip 🥇/🥈/🥉 inverted in the
 * cohort comparison panel, and applyCohortAdjustmentOnce applied the
 * wrong cohort health bonus.
 *
 * Fix: replace rankingScore with a tuple-lexicographic
 * compareCohortMembers(a, b) — primary roasShopify desc, secondary
 * roasShopifyPlatform desc, tertiary spend desc, terminal campaignKey
 * lex asc for deterministic stability. Add Number.isFinite guard on
 * inputs so NaN/Infinity sink to 0 (preventing non-deterministic sort).
 *
 * The fix aligns the cohort SOURCE ranking with the
 * CohortComparisonPanel.compareIntraSectionMembers HIGH-6 fix already
 * shipped (the panel re-sorts each section for DISPLAY, but
 * currentRank/isLeader/isWeakest still come from this source ranking).
 *
 * Evidence: .planning/phases/12-codebase-audit-baseline/raw-returns/
 *   lib_algorithm_multiMappingCohort.json (MMC-BLOCKER-01 + WARN-01,
 *   WARN-03, WARN-04, WARN-05).
 * Cross-ref: 12-tests-needed.md TG-05.
 */
```

B — Explicit vitest imports + import computeMultiMappingCohort, MultiMappingCohort, Aggregated, ProductMap. Mirror the makeAgg + key('Meta', 'c1') factory from dashboard-web/src/lib/__tests__/multiMappingCohort.test.ts.

C — Five it() blocks per the behavior list. Example for Test 1:
```
it('MMC-BLOCKER-01: tiny-spend extreme roasShopifyPlatform does not overflow primary tier', () => {
  const a = makeAgg({ key: key('Meta', 'A'), campaignId: 'A', campaignName: 'A', spend: 10, conversions: 10, conversionValue: 20 });
  const b = makeAgg({ key: key('Meta', 'B'), campaignId: 'B', campaignName: 'B', spend: 10, conversions: 10, conversionValue: 30 });
  const productMap: ProductMap = {
    [key('Meta', 'A')]: ['p1'],
    [key('Meta', 'B')]: ['p1'],
  };
  const result = computeMultiMappingCohort({
    currentCampaignKey: key('Meta', 'B'),
    productMap,
    aggregated: [a, b],
    roasShopifyByKey: new Map([[key('Meta', 'A'), 2], [key('Meta', 'B'), 3]]),
    roasShopifyPlatformByKey: new Map([[key('Meta', 'A'), 5000], [key('Meta', 'B'), 4]]),
  });
  expect(result).not.toBeNull();
  expect(result!.rankedAll[0].campaignKey).toBe(key('Meta', 'B'));
  expect(result!.isLeader).toBe(true);
});
```

Run BEFORE fix:
cd /Users/dorperetz/script-roas/dashboard-web && npx vitest run src/lib/__tests__/multiMappingCohortRanking.test.ts

Expect: at least Tests 1, 2, 3, 5 FAIL. Test 4 (NaN) may have indeterminate behavior on main HEAD — that's also a RED gate signal.

Step 2 — Apply the fix to dashboard-web/src/lib/multiMappingCohort.ts:

Edit A — Replace rankingScore with compareCohortMembers (around lines 181-206):
```
// Audit fix 2026-05-24 (AUDIT MMC-BLOCKER-01, Phase 12.1.3): replace
// the composite-key rankingScore (shrunk*1_000_000 + shrunkPlat*1_000
// + spend) with a tuple-lex comparator. The composite score overflowed
// when roasShopifyPlatform exceeded ~2500, FLIPPING the winner — see
// the IDENTICAL pathology already fixed in CohortComparisonPanel's
// compareIntraSectionMembers (HIGH-6, 2026-05-23). This fix brings the
// source ranking (which drives currentRank/isLeader/isWeakest and the
// cohort health adjustment) into parity with the panel's section sort.
//
// Evidence: .planning/phases/12-codebase-audit-baseline/raw-returns/
//   lib_algorithm_multiMappingCohort.json (MMC-BLOCKER-01).

// Helper for safe-finite extraction — fixes MMC-WARN-03 (NaN/Infinity
// guard) — the existing ?? 0 in buildMember catches null/undefined
// but NOT NaN. Adding the guard here belt-and-suspenders.
function safeFinite(v: number): number {
  return Number.isFinite(v) ? v : 0;
}

function compareCohortMembers(a: CohortMember, b: CohortMember): number {
  // Members without metrics sink to the bottom (consistent with the
  // pre-fix -Infinity sentinel behavior).
  const am = a.metrics;
  const bm = b.metrics;
  if (!am && !bm) return a.campaignKey.localeCompare(b.campaignKey);
  if (!am) return 1;
  if (!bm) return -1;

  // Primary: shrunk roasShopify desc.
  const primaryA = shrinkRoas(safeFinite(am.roasShopify), am.spend, am.conversions);
  const primaryB = shrinkRoas(safeFinite(bm.roasShopify), bm.spend, bm.conversions);
  if (primaryA !== primaryB) return primaryB - primaryA;

  // Secondary: shrunk roasShopifyPlatform desc.
  const secondaryA = shrinkRoas(safeFinite(am.roasShopifyPlatform), am.spend, am.conversions);
  const secondaryB = shrinkRoas(safeFinite(bm.roasShopifyPlatform), bm.spend, bm.conversions);
  if (secondaryA !== secondaryB) return secondaryB - secondaryA;

  // Tertiary: spend desc.
  if (am.spend !== bm.spend) return bm.spend - am.spend;

  // Terminal: campaignKey lex asc (MMC-WARN-05 — deterministic tie-break
  // independent of productMap insertion order).
  return a.campaignKey.localeCompare(b.campaignKey);
}
```

Remove the old rankingScore function (it's no longer called). Update the sort site at line ~312 to use the new comparator:
```
rankedAll.sort(compareCohortMembers);
```

Edit B — Sort the others array (MMC-WARN-01 fix):
The others array is currently built in productMap iteration order. After the for-of loop (around line 289-292), sort it:
```
// Audit fix 2026-05-24 (MMC-WARN-01, Phase 12.1.3): sort others by
// the same comparator so iteration order matches the docstring claim.
others.sort(compareCohortMembers);
```

Edit C — Reference identity (MMC-WARN-04 fix) at lines 308-328:
Build the current member entry ONCE and use the same reference in both rankedAll and result.current:
```
const currentWithFlag = { ...currentMemberBase, isCurrent: true as const };
const rankedAll = [
  currentWithFlag,
  ...others.map(o => ({ ...o, isCurrent: false as const })),
];
rankedAll.sort(compareCohortMembers);
// result.current uses the SAME reference as rankedAll's current entry —
// see the assertion `result.current === result.rankedAll[currentRank - 1]`
// in multiMappingCohortRanking.test.ts.
```

Find the existing `result.current` assignment (around line 328) and replace it with the new reference:
```
current: currentWithFlag,  // SAME reference as rankedAll[currentRank - 1] per MMC-WARN-04 contract
```

Edit D — Non-finite guard at line 355-363 (defense-in-depth — the comparator already calls safeFinite, but adding it at the source prevents NaN from entering the metrics object in the first place):
```
metrics: agg ? {
  spend: agg.spend,
  conversions: agg.conversions,
  conversionValue: agg.conversionValue,
  // AUDIT MMC-WARN-03 (2026-05-24, Phase 12.1.3): explicit Number.isFinite
  // guard — the ?? 0 catches null/undefined but NOT NaN/Infinity.
  roasShopify: safeFinite(roasShopifyByKey.get(campaignKey) ?? 0),
  roasShopifyPlatform: safeFinite(roasShopifyPlatformByKey.get(campaignKey) ?? 0),
} : undefined,
```

Step 3 — Run regression tests:
cd /Users/dorperetz/script-roas/dashboard-web && npx vitest run src/lib/__tests__/multiMappingCohortRanking.test.ts
Expect: 5 passing.

Step 4 — Full suite + tsc:
cd /Users/dorperetz/script-roas/dashboard-web && npm test && npx tsc --noEmit

Pay attention to existing multiMappingCohort.test.ts — it may assert against the old rankingScore behavior. If any existing test is now red, update its expectation to the post-fix behavior (cite MMC-BLOCKER-01 in the comment) and document in the commit message.

Step 5 — Atomic commit:
cd /Users/dorperetz/script-roas && git add \
  dashboard-web/src/lib/multiMappingCohort.ts \
  dashboard-web/src/lib/__tests__/multiMappingCohortRanking.test.ts \
  $(git diff --name-only dashboard-web/src/lib/__tests__/multiMappingCohort.test.ts 2>/dev/null)

git commit -m "$(cat <<'EOF'
fix(cohort): MMC-BLOCKER-01 ranking tie-break + non-finite guard (Phase 12.1.3)

Replace the composite-key rankingScore (shrunk * 1_000_000 +
shrunkPlat * 1_000 + spend) with a tuple-lex compareCohortMembers
function (primary roasShopify desc → secondary roasShopifyPlatform
desc → spend desc → campaignKey lex asc).

Pre-fix the composite score overflowed when roasShopifyPlatform
exceeded ~2500 (plausible at tiny-spend deterministic-ROAS
calculations), FLIPPING the winner. Operator-visible: medal chip
🥇 / 🥈 / 🥉 inverted in CohortComparisonPanel, and applyCohort
AdjustmentOnce in CampaignsTable applied the wrong cohort health
bonus. This is the IDENTICAL pathology already fixed inside
CohortComparisonPanel.compareIntraSectionMembers (HIGH-6, 2026-05-23);
the source ranking (which drives currentRank/isLeader/isWeakest)
now matches that panel sort.

Folded-in fixes:
- MMC-WARN-01: others array now sorted by the same comparator
  (was insertion-order from productMap).
- MMC-WARN-03: safeFinite guard on roasShopify + roasShopifyPlatform
  inputs at buildMember; the ?? 0 caught null/undefined but not NaN
  or Infinity. Plus belt-and-suspenders inside the comparator.
- MMC-WARN-04: result.current and result.rankedAll[currentRank - 1]
  now share the same JS reference (was built via separate spreads).
- MMC-WARN-05: terminal lex tiebreaker on campaignKey so identical-
  metrics members rank deterministically across renders, independent
  of productMap insertion order.

Adds multiMappingCohortRanking.test.ts (TG-05 fixture) with 5 cases
covering the extreme-value sort, reference identity, others ordering,
NaN guard, and tie-break stability.

Evidence: .planning/phases/12-codebase-audit-baseline/raw-returns/
  lib_algorithm_multiMappingCohort.json (MMC-BLOCKER-01 + WARN-01,
  WARN-03, WARN-04, WARN-05).
Cross-ref: 12-tests-needed.md TG-05.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"

Revert sanity:
cd /Users/dorperetz/script-roas && git stash -- dashboard-web/src/lib/multiMappingCohort.ts
cd /Users/dorperetz/script-roas/dashboard-web && npx vitest run src/lib/__tests__/multiMappingCohortRanking.test.ts
Expect: at least 4 failing.
cd /Users/dorperetz/script-roas && git stash pop
  </action>
  <verify>
    <automated>cd /Users/dorperetz/script-roas/dashboard-web && npx vitest run src/lib/__tests__/multiMappingCohortRanking.test.ts && npx tsc --noEmit && grep -q "AUDIT MMC-BLOCKER-01" dashboard-web/src/lib/multiMappingCohort.ts</automated>
  </verify>
  <acceptance_criteria>
    - File dashboard-web/src/lib/__tests__/multiMappingCohortRanking.test.ts exists.
    - grep -c "compareCohortMembers" dashboard-web/src/lib/multiMappingCohort.ts is at least 2 (definition + usage).
    - grep -c "AUDIT MMC-BLOCKER-01" dashboard-web/src/lib/multiMappingCohort.ts is at least 1.
    - grep -c "safeFinite" dashboard-web/src/lib/multiMappingCohort.ts is at least 2 (helper + at least one call site).
    - The old rankingScore function is REMOVED: grep -c "^function rankingScore" dashboard-web/src/lib/multiMappingCohort.ts equals 0.
    - cd dashboard-web && npx vitest run src/lib/__tests__/multiMappingCohortRanking.test.ts exits 0 with 5 tests passing.
    - cd dashboard-web && npx vitest run src/lib/__tests__/multiMappingCohort.test.ts (existing tests) exits 0.
    - cd dashboard-web && npx tsc --noEmit exits 0.
    - git log --oneline -1 shows the MMC-BLOCKER-01 commit with message matching ^fix\(cohort\): MMC-BLOCKER-01 .* \(Phase 12\.1\.3\)$.
    - Revert test: git stash on multiMappingCohort.ts → vitest red on the new test file; git stash pop → green.
  </acceptance_criteria>
  <done>
    MMC-BLOCKER-01 regression test added (red-on-revert, green-on-fix); multiMappingCohort.ts gained compareCohortMembers + non-finite guard + reference-identity alignment + others-sort + terminal lex tiebreaker; atomic commit landed; full test suite green; tsc green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ProductCentricView ALG-01 — sum-conservation in simplified-split branch</name>
  <files>
    - dashboard-web/src/lib/__tests__/productCentricViewSumConservation.test.ts (new)
    - dashboard-web/src/lib/productCentricView.ts (modify)
  </files>
  <read_first>
    - .planning/phases/12-codebase-audit-baseline/raw-returns/lib_algorithm_productCentricView.json — finding ALG-01 (lines 7-15)
    - .planning/phases/12-codebase-audit-baseline/12-tests-needed.md §"HP-21" (lines 192-196)
    - dashboard-web/src/lib/productCentricView.ts lines 255-320 (the platformAllocatedRevenue + members build)
    - dashboard-web/src/lib/__tests__/productCentricView.test.ts (existing fixture pattern with makeAgg + k('Meta', 'c1'))
  </read_first>
  <behavior>
- Test 1 (HP-21 sum-conservation, the failing scenario): zero-spend cohort with 2 members on different platforms (Meta + TikTok), productNetRevenue=1000. Pre-fix: byPlatform.intraAllocatedRevenue=500/500 (b/HI-05 fix correct) but each member's allocatedRevenueEstimate=0 because intraTotal=0 → intraShare=0 → 500*0=0. Assert: sum(members[].allocatedRevenueEstimate) === 1000 (within FP epsilon); each platform's single member receives exactly 500.

- Test 2 (stale-mapped cohort): allocator branch taken (orders provided). Product mapped to 3 campaigns: 2 active Meta + 1 stale TikTok (mapped but no agg). Orders include 1 tiktok-paid order on the product with revenueCad=300. Per the raw-returns ALG-02 fix_sketch, the surviving Meta members should still receive their share AND the t_stale member should either be emitted as zero-metrics OR the dropped revenue should be redistributed proportionally. CHOICE per CONTEXT.md D-05 (no scope creep): preserve the simpler invariant — sum(member.allocatedRevenueEstimate) ≈ totalNetRevenue. The minimal fix is to ALSO drop the stale member's contribution from the numerator (or include it as zero-metrics). For 12.1 scope this assertion is sum-conservation only; the stale-member-emission-policy decision is operator-deferred (ALG-02 is P1).

- Test 3 (productUnits omitted): allocator branch taken without productUnits. Pre-fix per ALG-04 the allocator's units calculation silently degrades to 0 which is internally inconsistent but produces no visible NaN. Post-fix expectation: either the result is computed correctly OR a console.warn fires AND the simplified-split fallback runs. For 12.1 scope the minimal assertion is "result is non-null AND no member's allocatedRevenueEstimate is NaN".

- Test 4 (cross-product independence per ALG-07 hypothesis): two products both mapped to the same campaign uzoshop::Meta::c1. One order contains both products as line items. Assert per-product allocated revenue does NOT double-count. For 12.1 scope this assertion is purely on the sum: sum across products' members equals the order's gross revenue.
  </behavior>
  <action>
Step 1 — Write the regression test FIRST (RED gate):

Create dashboard-web/src/lib/__tests__/productCentricViewSumConservation.test.ts with:

A — Header block:
```
/**
 * Audit fix 2026-05-24 (AUDIT ALG-01 in lib/productCentricView.ts,
 * Phase 12.1.3) — locks the sum-conservation invariant for the
 * product-centric per-platform allocation.
 *
 * Pre-fix: in the no-allocator (simplified-split) branch at lines
 * 289-301, when a cohort has zero total spend but positive net
 * revenue (b/HI-05 fix correctly sets byPlatform.intraAllocatedRevenue
 * = totalNetRevenue / platformCount), the per-member
 * allocatedRevenueEstimate stays at 0 because intraShare = a.spend /
 * intraTotal = 0/0 = 0. So platformRev * intraShare = 500 * 0 = 0.
 * Operator sees CAD 0 rows under a CAD 500 platform header.
 *
 * Fix: when intraTotal === 0 and platformRev > 0, fall back to
 * 1/platformMembers.length so per-member allocatedRev = platformRev /
 * memberCount. Conserves the platform sum and aligns with the
 * b/HI-05 equal-platform semantic.
 *
 * Evidence: .planning/phases/12-codebase-audit-baseline/raw-returns/
 *   lib_algorithm_productCentricView.json (ALG-01).
 * Cross-ref: 12-tests-needed.md HP-21.
 */
```

B — Explicit vitest imports + import buildProductCentricView, makeAgg (mirror from existing test), Aggregated, ProductMap.

C — Four it() blocks per the behavior list. Example for Test 1:
```
it('ALG-01: zero-spend cohort sum-conservation (member.allocatedRevenueEstimate sums to totalNetRevenue)', () => {
  const metaAgg = makeAgg({
    key: k('Meta', 'c1'), campaignId: 'c1', campaignName: 'Meta C1',
    platform: 'Meta', spend: 0, impressions: 1, conversionValue: 0,
  });
  const tiktokAgg = makeAgg({
    key: k('TikTok', 't1'), campaignId: 't1', campaignName: 'TikTok T1',
    platform: 'TikTok', spend: 0, impressions: 1, conversionValue: 0,
  });
  const productMap: ProductMap = {
    [k('Meta', 'c1')]: ['p1'],
    [k('TikTok', 't1')]: ['p1'],
  };
  const result = buildProductCentricView({
    aggregated: [metaAgg, tiktokAgg],
    productMap,
    productNetRevenue: new Map([['p1', 1000]]),
    storeId: STORE,
  });
  expect(result).toHaveLength(1);
  const row = result[0];
  expect(row.totalNetRevenue).toBe(1000);

  // Sum-conservation across members:
  const memberSum = row.members.reduce((s, m) => s + m.allocatedRevenueEstimate, 0);
  expect(Math.abs(memberSum - 1000)).toBeLessThan(1e-9);

  // Per-platform: each single-member platform's member receives exactly that platform's allocation.
  const metaMember = row.members.find(m => m.platform === 'Meta');
  const tiktokMember = row.members.find(m => m.platform === 'TikTok');
  expect(metaMember).toBeDefined();
  expect(tiktokMember).toBeDefined();
  expect(metaMember!.allocatedRevenueEstimate).toBeCloseTo(500, 9);
  expect(tiktokMember!.allocatedRevenueEstimate).toBeCloseTo(500, 9);
});
```

(Verify the buildProductCentricView signature by reading the existing productCentricView.test.ts; the inputs param shape may differ slightly.)

Run BEFORE fix:
cd /Users/dorperetz/script-roas/dashboard-web && npx vitest run src/lib/__tests__/productCentricViewSumConservation.test.ts
Expect: at least Test 1 FAIL (RED gate confirmed). Tests 3, 4 may pass on main HEAD if they don't depend on the same divide-by-zero path — that's OK; Test 1 is the verification-blocking case.

Step 2 — Apply the fix to dashboard-web/src/lib/productCentricView.ts (around lines 288-301):

Before the `.map(r => { ... })` over filtered raw, build a per-platform member-count map so the new intraShare fallback can use it:
```
// Audit fix 2026-05-24 (AUDIT ALG-01, Phase 12.1.3): per-platform
// member count so the simplified-split branch can fall back to
// equal-share when intraTotal === 0 (the zero-spend cohort scenario
// that the b/HI-05 platform-level fix already handles one level up).
// Pre-fix: intraShare = a.spend / intraTotal = 0/0 = 0 → per-member
// allocatedRev = platformRev * 0 = 0, even though byPlatform.intra
// AllocatedRevenue = 500. Operator saw CAD 0 rows under a CAD 500
// platform header.
// Evidence: .planning/phases/12-codebase-audit-baseline/raw-returns/
//   lib_algorithm_productCentricView.json (ALG-01).
const platformMemberCount = new Map<string, number>();
for (const r of raw) {
  if (!r.agg) continue;
  platformMemberCount.set(r.agg.platform, (platformMemberCount.get(r.agg.platform) ?? 0) + 1);
}
```

Then inside the `.map(r => { ... })` callback at lines 291-301, REPLACE the intraShare line:
```
const intraTotal = platformSpend.get(a.platform) ?? 0;
// AUDIT ALG-01: when platform spend is 0 but platformRev > 0
// (b/HI-05 zero-spend cohort), fall back to equal-share across
// the platform's members instead of 0/0 = 0.
const platformMembers = platformMemberCount.get(a.platform) ?? 1;
const intraShare = intraTotal > 0
  ? a.spend / intraTotal
  : (platformMembers > 0 ? 1 / platformMembers : 0);
```

Note: intraPlatformSpendShare downstream (line 311) still uses intraShare for the spend-share UI rendering. Per the fix sketch in raw-returns: "Keep intraPlatformSpendShare as 0 (UI-correct: spend share IS zero) but use the equal-share for revenue allocation." So we need to SEPARATE the two: keep the existing intraShare for revenue allocation, and compute a separate intraSpendShare for the UI:

```
const intraTotal = platformSpend.get(a.platform) ?? 0;
// Spend share for UI rendering (operator-correct: when spend=0, spend share=0).
const intraSpendShare = intraTotal > 0 ? a.spend / intraTotal : 0;
// Revenue allocation share — AUDIT ALG-01: fall back to equal-share when
// intraTotal === 0 so per-member allocatedRev sums to platformRev.
const platformMembers = platformMemberCount.get(a.platform) ?? 1;
const intraRevShare = intraTotal > 0
  ? a.spend / intraTotal
  : (platformMembers > 0 ? 1 / platformMembers : 0);

const totalShare = totalCohortSpend > 0 ? a.spend / totalCohortSpend : 0;
const platformRev = platformAllocatedRevenue.get(a.platform) ?? 0;
const allocatedRev = allocByCampaign?.get(a.key) ?? (platformRev * intraRevShare);

return {
  campaignKey: a.key,
  campaignId: a.campaignId,
  campaignName: a.campaignName,
  platform: a.platform,
  storeId: a.storeId,
  storeName: a.storeName,
  spend: a.spend,
  conversionValue: a.conversionValue,
  intraPlatformSpendShare: intraSpendShare,  // UI-correct: spend share is 0 when spend=0
  totalSpendShare: totalShare,
  allocatedRevenueEstimate: allocatedRev,  // Now correctly equals platformRev/N when intraTotal=0
  platformRoas: a.spend > 0 ? a.conversionValue / a.spend : 0,
  // ... rest of the fields unchanged
};
```

Step 3 — Run regression tests:
cd /Users/dorperetz/script-roas/dashboard-web && npx vitest run src/lib/__tests__/productCentricViewSumConservation.test.ts
Expect: 4 passing.

Step 4 — Full suite + tsc:
cd /Users/dorperetz/script-roas/dashboard-web && npm test && npx tsc --noEmit

Existing productCentricView.test.ts may have b/HI-05 assertions that pre-date the per-member sum-conservation pin — confirm none asserted "member.allocatedRevenueEstimate === 0 when spend=0" (which would have been pinning the BUG). If any did, update them per the post-fix behavior.

Step 5 — Atomic commit:
cd /Users/dorperetz/script-roas && git add \
  dashboard-web/src/lib/productCentricView.ts \
  dashboard-web/src/lib/__tests__/productCentricViewSumConservation.test.ts \
  $(git diff --name-only dashboard-web/src/lib/__tests__/productCentricView.test.ts 2>/dev/null)

git commit -m "$(cat <<'EOF'
fix(product-centric): ALG-01 allocatedRevenueEstimate sum-conservation (Phase 12.1.3)

In the simplified-split branch at lines 289-301 of productCentricView.ts,
per-member allocatedRevenueEstimate now equals platformRev / N when
platform spend totals to 0 — fixing the zero-spend cohort scenario
where the b/HI-05 fix correctly set byPlatform.intraAllocatedRevenue
to totalNetRevenue / platformCount but the per-member share dropped
to 0 (because intraShare = a.spend / intraTotal = 0/0 = 0, then
platformRev * 0 = 0). Operator saw CAD 0 rows under a CAD 500
platform header.

Fix: SEPARATE the UI-spend-share (still 0 when spend=0, operator-
correct) from the revenue-allocation-share (falls back to
1/platformMembers.length when intraTotal === 0). New
platformMemberCount map computed once before the .map callback.

Adds productCentricViewSumConservation.test.ts (HP-21 fixture)
with 4 cases covering: zero-spend cohort sum-conservation,
stale-mapped cohort sum invariant, productUnits-omitted graceful
degradation, cross-product independence.

ALG-02 (stale-mapped revenue leak), ALG-04 (productUnits silent
corruption), ALG-05 (zero-spend + non-empty orders divergence),
ALG-06 (isMultiMapped semantic), ALG-07 (cross-product
correctness hypothesis), ALG-08 (O(n²) perf) deferred to Phase 12.2
per CONTEXT.md scope. Test cases 2-4 use minimum-invariant
assertions (sum-conservation only) so they pass with the current
12.1 minimal fix.

Evidence: .planning/phases/12-codebase-audit-baseline/raw-returns/
  lib_algorithm_productCentricView.json (ALG-01).
Cross-ref: 12-tests-needed.md HP-21.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"

Revert sanity:
cd /Users/dorperetz/script-roas && git stash -- dashboard-web/src/lib/productCentricView.ts
cd /Users/dorperetz/script-roas/dashboard-web && npx vitest run src/lib/__tests__/productCentricViewSumConservation.test.ts
Expect: at least Test 1 failing.
cd /Users/dorperetz/script-roas && git stash pop
  </action>
  <verify>
    <automated>cd /Users/dorperetz/script-roas/dashboard-web && npx vitest run src/lib/__tests__/productCentricViewSumConservation.test.ts && npx tsc --noEmit && grep -q "AUDIT ALG-01" dashboard-web/src/lib/productCentricView.ts</automated>
  </verify>
  <acceptance_criteria>
    - File dashboard-web/src/lib/__tests__/productCentricViewSumConservation.test.ts exists.
    - grep -c "AUDIT ALG-01" dashboard-web/src/lib/productCentricView.ts is at least 1.
    - grep -c "platformMemberCount" dashboard-web/src/lib/productCentricView.ts is at least 1.
    - grep -c "intraSpendShare\|intraRevShare" dashboard-web/src/lib/productCentricView.ts is at least 2 (the two separated shares).
    - cd dashboard-web && npx vitest run src/lib/__tests__/productCentricViewSumConservation.test.ts exits 0 with 4 tests passing.
    - cd dashboard-web && npx vitest run src/lib/__tests__/productCentricView.test.ts (existing tests) exits 0.
    - cd dashboard-web && npx tsc --noEmit exits 0.
    - git log --oneline -1 shows the ALG-01 commit with message matching ^fix\(product-centric\): ALG-01 .* \(Phase 12\.1\.3\)$.
    - Revert test: git stash on productCentricView.ts → vitest red on Test 1; git stash pop → green.
  </acceptance_criteria>
  <done>
    ProductCentricView ALG-01 regression test added (red-on-revert, green-on-fix); productCentricView.ts simplified-split branch now conserves the platform sum across members; atomic commit landed; full test suite green; tsc green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Final wave-2 verification — full project gate + commit count</name>
  <files>
    - (read-only — no writes)
  </files>
  <read_first>
    - The two commits just landed (Tasks 1–2)
    - .planning/phases/12.1-p0-audit-fixes/12.1-CONTEXT.md §D-06 (verification gate)
  </read_first>
  <action>
After Tasks 1–2 ship, run the full project gate to confirm Plan 12.1-03 is clean:

cd /Users/dorperetz/script-roas/dashboard-web && npx tsc --noEmit
cd /Users/dorperetz/script-roas/dashboard-web && npm test

Both MUST exit 0. Test pass count MUST be at least 955 (baseline 946 + 9 new tests minimum: 5 from MMC + 4 from PCV).

Confirm the 2 Phase 12.1.3 commits are present:
cd /Users/dorperetz/script-roas && git log --oneline --grep="Phase 12.1.3" main | wc -l

MUST be at least 2.

If any gate fails, DO NOT attempt fix-up commits — surface the failure to the operator with the test names + assertion that broke.
  </action>
  <verify>
    <automated>cd /Users/dorperetz/script-roas/dashboard-web && npx tsc --noEmit && npm test && [ "$(cd /Users/dorperetz/script-roas && git log --oneline --grep='Phase 12.1.3' main | wc -l | tr -d ' ')" -ge "2" ]</automated>
  </verify>
  <acceptance_criteria>
    - cd dashboard-web && npx tsc --noEmit exits 0.
    - cd dashboard-web && npm test exits 0 AND pass count is at least 955.
    - git log --oneline --grep='Phase 12.1.3' main | wc -l is at least 2.
    - Both fix commit messages match the D-08 format (MMC-BLOCKER-01 and ALG-01 in product-centric scope).
  </acceptance_criteria>
  <done>
    Plan 12.1-03 verified green. Combined with 12.1-02 (parallel wave 2), the project is ready for the master plan's Task 2 (phase close).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Cohort Comparison Panel (drawer) | Medal chip 🥇/🥈/🥉 + cohort health adjustment in CampaignsTable consume the source ranking. MMC-BLOCKER-01 fix corrects the inversion. |
| Operator → Product-Centric View component | Renders allocatedRevenueEstimate per member directly. ALG-01 fix conserves the platform sum. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.1.3-01 | Tampering | multiMappingCohort ranking inversion under extreme roasShopifyPlatform | mitigate | Tuple-lex comparator + safeFinite guard pinned by 5-case regression test. |
| T-12.1.3-02 | Information Disclosure | ProductCentricView CAD-0 rows under CAD-500 header (operator-trust erosion) | mitigate | Equal-share fallback in simplified-split branch; sum-conservation pinned by regression test. |
| T-12.1.3-03 | Tampering | NaN/Infinity ROAS inputs producing non-deterministic sort (rank-chip flicker) | mitigate | safeFinite guard at buildMember + inside the comparator; regression test asserts deterministic ranking across calls. |
</threat_model>

<verification>
- Each fix has a dedicated regression test that fails on revert and passes after fix.
- cd dashboard-web && npm test exits 0 with pass count at least 955.
- cd dashboard-web && npx tsc --noEmit exits 0.
- 2 atomic commits on main matching the D-08 format.
- No new files outside the listed source + test paths.
</verification>

<success_criteria>
- 2 P0 algorithm-math fixes shipped per CONTEXT.md D-01 (MMC-BLOCKER-01, ProductCentricView ALG-01).
- 2 new regression test files added with full coverage of TG-05 + HP-21 fixtures.
- 2 atomic commits per D-08 (one per fix) with finding ID in the message.
- Full test suite green; tsc green.
- Plan 12.1-03 complete; Wave 2 (parallel with 12.1-02) complete.
</success_criteria>

<output>
After completion, create .planning/phases/12.1-p0-audit-fixes/12.1-03-SUMMARY.md documenting:
- Per fix: file paths modified, test file added, commit SHA, regression-test red-on-revert verification result.
- Test count delta (baseline → +9 at least).
- Folded-in MMC-WARN-01/03/04/05 fixes documented (the BLOCKER-01 fix naturally folds these in).
- Any deviations from CONTEXT.md or AUDIT.md.
- Confirmation that CohortComparisonPanel (consumer of multiMappingCohort) and ProductCentricView component (consumer of productCentricView.ts) still render correctly per the existing tests.
</output>
</content>
</invoke>