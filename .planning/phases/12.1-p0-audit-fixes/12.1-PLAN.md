---
phase: 12.1-p0-audit-fixes
plan: 12.1
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/STATE.md
autonomous: true
requirements:
  - "P0-INN-10: cronLive persist-rolling-3day retry idempotency"
  - "P0-INN-16: eventBackfill systemic-failure abort + console.warn"
  - "P0-INN-01: cronDaily return value uses persisted row (TikTok included)"
  - "P0-ALG-04: aiReport statusByCampaign storeId-scoped key"
  - "P0-ALG-05: aiReport ordersByCampaignId/Name storeId-scoped keys"
  - "P0-ALG-06: aiReport suffix-match lookups replaced with storeId-scoped direct map.get"
  - "P0-MMC-BLOCKER-01: multiMappingCohort ranking score reversal under shrinkage"
  - "P0-PCV-ALG-01: productCentricView sum-conservation (allocatedRevenueEstimate sums to intraAllocatedRevenue)"
tags:
  - audit-fix
  - bug-fix
  - p0
  - regression-test
must_haves:
  truths:
    - "All 8 P0 fixes from .planning/AUDIT.md §Recommended Phase 12.1 Scope are shipped on `main`"
    - "Every fix has a regression test that fails when the source change is reverted (`git stash` → red; `git stash pop` → green)"
    - "`cd dashboard-web && npm test` reports zero failures across all 86+ files"
    - "`cd dashboard-web && npx tsc --noEmit` reports zero errors"
    - "Each fix landed as a separate atomic commit (8 commits total), per CONTEXT.md D-08"
    - "No new files appear in `dashboard-web/src` outside the explicitly-listed source + test paths"
    - "campaignsAggregator.ts `Aggregated` type already exposes `storeId` (verified in pre-flight)"
  artifacts:
    - path: ".planning/phases/12.1-p0-audit-fixes/12.1-PLAN.md"
      provides: "Master orchestrator plan (this file)"
    - path: ".planning/phases/12.1-p0-audit-fixes/12.1-01-PLAN.md"
      provides: "Wave 1: Inngest retry + idempotency (INN-10, INN-16, INN-01)"
    - path: ".planning/phases/12.1-p0-audit-fixes/12.1-02-PLAN.md"
      provides: "Wave 2: aiReport cross-store cleanup (ALG-04, ALG-05, ALG-06)"
    - path: ".planning/phases/12.1-p0-audit-fixes/12.1-03-PLAN.md"
      provides: "Wave 2: Cohort + Allocator math (MMC-BLOCKER-01, ProductCentricView ALG-01)"
  key_links:
    - from: "12.1-01-PLAN.md (Wave 1 Inngest fixes)"
      to: "12.1-02-PLAN.md + 12.1-03-PLAN.md (Wave 2 algorithm fixes)"
      via: "depends_on: [12.1-01] — wave 2 starts only after Inngest fixes land + green"
      pattern: "depends_on:.*12\\.1-01"
    - from: "Every Phase 12.1 commit"
      to: ".planning/AUDIT.md finding ID"
      via: "commit message suffix `(Phase 12.1.X)` referencing finding ID (INN-10, ALG-04, etc.)"
      pattern: "fix\\([^)]+\\):\\s+(INN|ALG|MMC|PCV)-[0-9A-Z-]+ .* \\(Phase 12\\.1\\.[1-3]\\)"
---

<objective>
This is the **master orchestrator plan** for Phase 12.1. It owns the pre-flight verification (clean baseline before any fix lands), the project-level gate sweep after all sub-plans complete, and the STATE.md update that closes the phase.

Per CONTEXT.md D-01 the actual fixes live in three sub-plans (12.1-01 Inngest, 12.1-02 aiReport, 12.1-03 Cohort/Allocator). This plan does NOT modify source code; it ensures the working tree is in a known-good state before sub-plans start and re-verifies the project at the end.

Purpose: Per CONTEXT.md D-06, Phase 12.1 cannot mark complete until every fix has a regression test (red on revert) AND `npm test` + `npx tsc --noEmit` report zero. This master plan owns those gates.

Output: STATE.md milestone update marking Phase 12.1 complete + a final commit reading "chore(12.1): close phase — 8/8 P0 fixes shipped".
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/AUDIT.md
@.planning/phases/12.1-p0-audit-fixes/12.1-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — verify clean baseline before sub-plans start</name>
  <files>
    - (read-only — no writes)
  </files>
  <read_first>
    - .planning/AUDIT.md §Recommended Phase 12.1 Scope (the 8 fixes that must land)
    - .planning/phases/12.1-p0-audit-fixes/12.1-CONTEXT.md (full file — D-01 through D-08)
    - .planning/codebase/CONVENTIONS.md §"Atomic-commit discipline" + §"git add discipline"
    - .planning/codebase/TESTING.md §"Run commands"
  </read_first>
  <action>
Verify the working tree at HEAD is in a known-good state BEFORE any sub-plan modifies source code. Concretely:

1. Confirm the audit baseline commit is present:
   ```bash
   git log --oneline -1 --grep="Phase 12 atomic AUDIT.md synthesis"
   ```
   Expect output to include commit `3287cff` (the Phase 12 close).

2. Confirm the `Aggregated` type in `dashboard-web/src/lib/campaignsAggregator.ts` already exposes `storeId`:
   ```bash
   grep -n "^  storeId: string;" dashboard-web/src/lib/campaignsAggregator.ts
   ```
   Expect ONE match on line 11. (This is the contract foundation for 12.1-02; if it's missing, the planner mis-described the type and 12.1-02 needs a re-plan.)

3. Confirm `cd dashboard-web && npx tsc --noEmit` exits 0 (no pre-existing TS errors). Run from the repository root using absolute path:
   ```bash
   cd /Users/dorperetz/script-roas/dashboard-web && npx tsc --noEmit
   ```

4. Confirm `cd dashboard-web && npm test` exits 0 with the documented 946 tests passing:
   ```bash
   cd /Users/dorperetz/script-roas/dashboard-web && npm test
   ```
   Per .planning/codebase/TESTING.md the expected pass count is 946 and runtime ~4.6s; any failure here is a pre-existing regression that MUST be diagnosed BEFORE 12.1 fixes land, otherwise the verifier cannot distinguish "12.1 broke this" from "this was already red."

5. Confirm working tree is clean (no untracked source files outside `.planning/`):
   ```bash
   git status --porcelain | grep -v "^?? \.planning/" | grep -v "^?? dashboard-web/" || echo CLEAN
   ```
   Acceptable noise: untracked `.planning/` directories from prior planner runs (already in `git status` baseline). Anything inside `dashboard-web/src/` is a red flag — abort and ask the operator.

Do NOT commit anything in this task; the pre-flight is read-only.
  </action>
  <verify>
    <automated>cd /Users/dorperetz/script-roas/dashboard-web && npx tsc --noEmit && npm test</automated>
  </verify>
  <acceptance_criteria>
    - `git log --oneline -1 --grep="Phase 12 atomic AUDIT.md synthesis"` returns at least one line referencing commit `3287cff` (or descendant if subsequent merge commits exist).
    - `grep -n "^  storeId: string;" dashboard-web/src/lib/campaignsAggregator.ts` returns exactly one line: `11:  storeId: string;` (line number may differ ±1 if file changed, but it must exist).
    - `cd dashboard-web && npx tsc --noEmit` exits with status 0.
    - `cd dashboard-web && npm test` exits with status 0 and the summary line reports 946+ tests passing (skips=0, fails=0).
    - `git status --porcelain` shows no modified files under `dashboard-web/src/` (untracked planning files OK).
  </acceptance_criteria>
  <done>
    Working tree confirmed clean; baseline TS+vitest green; `Aggregated.storeId` confirmed present so 12.1-02 contract change is additive, not invasive.
  </done>
</task>

<task type="auto">
  <name>Task 2: Post-sub-plans — full project gate sweep + STATE.md close</name>
  <files>
    - .planning/STATE.md
  </files>
  <read_first>
    - .planning/phases/12.1-p0-audit-fixes/12.1-CONTEXT.md §D-06 (verification gate)
    - .planning/STATE.md (current state to update)
    - .planning/ROADMAP.md (to update Phase 12.1 checkbox)
    - The 8 SUMMARY.md files emitted by the three sub-plans (12.1-01, 12.1-02, 12.1-03) — they live at:
      - .planning/phases/12.1-p0-audit-fixes/12.1-01-SUMMARY.md
      - .planning/phases/12.1-p0-audit-fixes/12.1-02-SUMMARY.md
      - .planning/phases/12.1-p0-audit-fixes/12.1-03-SUMMARY.md
  </read_first>
  <action>
This task runs AFTER all three sub-plans (12.1-01, 12.1-02, 12.1-03) have shipped their commits. It is the D-06 verification gate.

1. Re-run the full TypeScript + test gate from a clean working tree:
   ```bash
   cd /Users/dorperetz/script-roas/dashboard-web && npx tsc --noEmit
   cd /Users/dorperetz/script-roas/dashboard-web && npm test
   ```
   Both MUST exit 0. Test pass count MUST equal 946 + N where N = number of new regression tests added by 12.1 (expected: 8 tests for the 8 fixes + the supplementary fixtures from TG-02 + TG-05 + HP-21, expected N range = [8, 20]).

2. Confirm the commit log contains the 8 expected fix commits (one per P0 finding). Per CONTEXT.md D-08, each commit follows the pattern `fix(<area>): <finding-id> <brief> (Phase 12.1.X)`:
   ```bash
   git log --oneline --grep="Phase 12.1" main
   ```
   Expected entries (in some order):
   - `fix(inngest): INN-10 cronLive persist-rolling-3day retry idempotency (Phase 12.1.1)`
   - `fix(inngest): INN-16 eventBackfill systemic-failure abort + console.warn (Phase 12.1.1)`
   - `fix(inngest): INN-01 cronDaily return value reads persisted row (Phase 12.1.1)`
   - `fix(ai-report): ALG-04 statusByCampaign storeId-scoped key (Phase 12.1.2)`
   - `fix(ai-report): ALG-05 ordersByCampaignId/Name storeId-scoped keys (Phase 12.1.2)`
   - `fix(ai-report): ALG-06 storeId-scoped suffix-match replaced with direct map.get (Phase 12.1.2)`
   - `fix(cohort): MMC-BLOCKER-01 ranking tie-break + non-finite guard (Phase 12.1.3)`
   - `fix(product-centric): ALG-01 allocatedRevenueEstimate sum-conservation (Phase 12.1.3)`

   Tolerance: commit message wording may differ slightly but every `(Phase 12.1.N)` suffix MUST be present (8 occurrences).

3. Update `.planning/STATE.md` to mark Phase 12.1 complete:
   - Bump `progress.completed_phases` from 10 to 11.
   - Bump `progress.completed_plans` from 54 to 58 (+4: 12.1 master + 12.1-01 + 12.1-02 + 12.1-03).
   - Set `last_activity` to a one-line summary describing the 8 fixes shipped + Phase 12.1 complete.
   - Update `## Current Position`:
     - Phase: "12.1 (P0 Audit Fixes) — COMPLETE 8/8 fixes shipped"
     - Status: "Awaiting operator decision on Phase 12.2 (P1, 11 fixes from AUDIT.md) and Phase 12.3 (P2, 8 fixes)."

4. Update `.planning/ROADMAP.md` Phase 12.1 entry — change `[ ]` to `[x]` for each of the 3 sub-plans listed under "Sub-plans:" and append `— COMPLETE 2026-05-24 (8/8 P0 fixes shipped)` to the phase heading.

5. Commit the STATE.md + ROADMAP.md update as a single phase-close commit:
   ```bash
   cd /Users/dorperetz/script-roas && git add .planning/STATE.md .planning/ROADMAP.md
   git commit -m "$(cat <<'EOF'
chore(12.1): close phase — 8/8 P0 fixes shipped

INN-10, INN-16, INN-01 (Inngest retry + idempotency, Phase 12.1.1).
ALG-04, ALG-05, ALG-06 (aiReport cross-store cleanup, Phase 12.1.2).
MMC-BLOCKER-01, ProductCentricView ALG-01 (Cohort + Allocator math,
Phase 12.1.3). All fixes have regression tests; vitest + tsc green.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
   ```

NO `git add -A` per CONTEXT.md D-08 and CONVENTIONS.md `git add` discipline.
  </action>
  <verify>
    <automated>cd /Users/dorperetz/script-roas/dashboard-web && npx tsc --noEmit && npm test && cd /Users/dorperetz/script-roas && [ "$(git log --oneline --grep='Phase 12.1' | wc -l | tr -d ' ')" -ge "8" ]</automated>
  </verify>
  <acceptance_criteria>
    - `cd dashboard-web && npx tsc --noEmit` exits 0.
    - `cd dashboard-web && npm test` exits 0 AND the pass-count is ≥ 954 (= 946 baseline + 8 new regression tests minimum).
    - `git log --oneline --grep='Phase 12.1' main | wc -l` returns at least 8 (the 8 fix commits) plus 1 (the chore close commit) = 9 minimum.
    - `grep -c 'Phase 12.1.1' <(git log --format=%s main)` ≥ 3 (INN-10, INN-16, INN-01).
    - `grep -c 'Phase 12.1.2' <(git log --format=%s main)` ≥ 3 (ALG-04, ALG-05, ALG-06).
    - `grep -c 'Phase 12.1.3' <(git log --format=%s main)` ≥ 2 (MMC-BLOCKER-01, ProductCentricView ALG-01).
    - `grep -q "completed_phases: 11" .planning/STATE.md`.
    - `grep -q "Phase 12.1.*COMPLETE" .planning/ROADMAP.md`.
    - `git status --porcelain` shows no uncommitted changes in `.planning/STATE.md` or `.planning/ROADMAP.md`.
  </acceptance_criteria>
  <done>
    All 8 P0 fixes verified shipped; project-level gates (tsc + vitest) green; STATE.md + ROADMAP.md updated; phase-close commit recorded.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator → Inngest cron handlers | Cron schedules are operator-defined; no user input crosses this boundary. The handler reads from Shopify/Meta/Google/TikTok APIs and Supabase service-role, but the cron trigger itself is not an attack surface. |
| Operator → `/api/operator/backfill` | The only external trigger for `eventBackfill`. URL-obscurity trust model per CONVENTIONS.md (single-operator) — no auth layer, no rate limit. Already audited as ✅ in Phase 12. |
| Supabase service-role → data_daily / campaigns_daily / orders_attribution | Writer = Inngest functions; reader = `lib/postgresReaders.ts`. RLS off (operator-managed). Phase 12.1 fixes do not touch RLS or write-path authorization. |

## STRIDE Threat Register

Phase 12.1 is a bug-fix-only phase. It does NOT introduce new endpoints, new auth surfaces, new data flows, or new external dependencies. Every fix is an internal correctness patch on already-audited code. No new threats are introduced; existing threats already documented in Phase 12 AUDIT.md (API-10/18/23/26/32/37/38 — all deferred to Phase 12.3) are unchanged.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12.1-01 | Tampering | Phase 12.1 commit message format (D-08) | mitigate | Each fix commit message MUST include the finding ID (`INN-10`, `ALG-04`, etc.) — enables future audits to grep commits→AUDIT.md mappings. Verified by Task 2 grep gate. |
| T-12.1-02 | Repudiation | Atomic commits per fix | mitigate | One fix = one commit per CONTEXT.md D-08. If a future bisect identifies regression in cron-live, the commit log immediately points at the fix that caused it. Verified by Task 2 commit count gate (≥ 8 fix commits). |
| T-12.1-03 | Denial of Service | Inngest free-tier exec budget | accept | INN-10 fix adds ONE new `step.run` (`select-prior-spend-{date}-{storeId}`) per cron-live tick = +432 execs/day = +12,960/month. Free tier is 50K/month; cron-live currently consumes ~25.9K/month per `cronLive.ts:18-25` documentation. Post-fix: ~38.9K/month. Still under cap by 22% headroom. Documented in 12.1-01 PLAN. |
| T-12.1-04 | Information Disclosure | Cross-store data bleed (ALG-04/05/06) | mitigate | Pre-fix, operator in default `storeName='All'` view saw store A's data attributed to store B's campaigns when campaign IDs collided. This is itself a STRIDE-I bug (the operator is the receiver; the bug exposes wrong data). The fix IS the mitigation. Verified by 12.1-02 TG-02 golden fixture (two-store collision → assert per-store isolation). |
</threat_model>

<verification>
- All 8 fixes from `.planning/AUDIT.md` §Recommended Phase 12.1 Scope shipped with a regression test each.
- `cd dashboard-web && npm test` reports zero failures.
- `cd dashboard-web && npx tsc --noEmit` reports zero errors.
- `git log --grep="Phase 12.1" main` returns at least 8 commits matching the D-08 message format.
- `.planning/STATE.md` reflects Phase 12.1 complete (`completed_phases: 11`).
- `.planning/ROADMAP.md` reflects Phase 12.1 complete (sub-plans checked, heading appended with COMPLETE marker).
</verification>

<success_criteria>
- All 8 P0 fixes shipped, each in its own atomic commit per D-08.
- Every fix has a regression test that pins the bug; reverting the source change (via `git stash`) flips the test red.
- vitest pass count rises from 946 → 954+ (8 new regression tests minimum; supplementary fixtures from TG-02 + TG-05 + HP-21 may add more).
- TypeScript compiles cleanly across the modified surface (campaignsAggregator.ts type already exposes storeId; storeId-threading in aiReport.ts produces zero new errors).
- `.planning/STATE.md` and `.planning/ROADMAP.md` reflect Phase 12.1 closure.
</success_criteria>

<output>
After completion, create `.planning/phases/12.1-p0-audit-fixes/12.1-SUMMARY.md` summarizing:
- The pre-flight gate result (Task 1)
- The post-sub-plans gate result (Task 2)
- Total commits shipped across all three sub-plans (count + IDs)
- Final test count delta (946 → N)
- Any deviations from the locked decisions in CONTEXT.md
- Recommended next step (likely `/gsd-phase add 12.2` for the 11 P1 fixes from AUDIT.md)
</output>
</content>
</invoke>