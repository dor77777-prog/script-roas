---
phase: 04-component-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard-web/src/lib/hooks/useCampaignTrueRevenue.ts
  - dashboard-web/src/lib/hooks/useCampaignAttribution.ts
  - dashboard-web/src/lib/hooks/useBillingRecurring.ts
  - dashboard-web/src/lib/hooks/useBillingOneTime.ts
  - dashboard-web/src/components/CampaignsTableRow.tsx
  - dashboard-web/src/components/AttributionAnalysisPanel.tsx
  - dashboard-web/src/components/MetaShopifyReconciliation.tsx
  - dashboard-web/src/components/ProductChannelBreakdown.tsx
  - dashboard-web/src/components/AdSetTable.tsx
  - dashboard-web/src/components/BillingCsvImport.tsx
  - dashboard-web/src/components/CampaignsTable.tsx
  - dashboard-web/src/components/CampaignDrawer.tsx
  - dashboard-web/src/components/BillingSettings.tsx
autonomous: false
requirements:
  - PH4-CT-A      # useCampaignTrueRevenue hook (CampaignsTable group)
  - PH4-CT-B      # useCampaignAttribution hook (CampaignsTable group + drawer)
  - PH4-CT-C      # CampaignsTableRow sub-component
  - PH4-CT-D      # CampaignsTable shell ≤500L
  - PH4-CD-E      # AttributionAnalysisPanel sub-component
  - PH4-CD-F      # MetaShopifyReconciliation sub-component + pearson exports
  - PH4-CD-G      # ProductChannelBreakdown sub-component
  - PH4-CD-H      # AdSetTable sub-component
  - PH4-CD-I      # CampaignDrawer shell ≤500L
  - PH4-BS-J      # useBillingRecurring + useBillingOneTime hooks
  - PH4-BS-K      # BillingCsvImport sub-component
  - PH4-BS-L      # BillingSettings shell ≤500L
  - PH4-SMOKE     # Phase-wide manual smoke (per UI-SPEC §Manual Smoke Checklist)

must_haves:
  truths:
    # Goal-backward: what MUST be observably true after this plan ships
    - "All 3 source components measure ≤500 lines (or ≤600 with documented override per D-04)"
    - "`npm run build` exits 0 after every task lands (TypeScript catches missing exports + type drift)"
    - "`npm run test` exits 0 after every task lands (84 Phase 2 tests are the regression net)"
    - "`useCampaignTrueRevenue` hook returns the same `Map<string, TrueRevenueInfo>` shape with byte-identical dep array `[mode, data, productsResp, ordersAttrResp, productMap, aggregated, localRange]`"
    - "`useCampaignAttribution` hook preserves null (does NOT normalize to undefined) for ad-sets with no usable data"
    - "Pearson r values match pre-refactor outputs byte-for-byte (Math.max(-1, Math.min(1, r)) clamp preserved)"
    - "Trust chip ladder renders all 4 levels + fallback (`'אמין'`/`'חלקי'`/`'לא אמין'`/`'אין המרות'`/`'·מיפוי'`) in both CampaignsTable + AdSetTable"
    - "Both `useBillingRecurring` and `useBillingOneTime` listen to the SAME custom event `'roas-billing-changed'` — NOT separate `'roas-billing-onetime-changed'` (does not exist in codebase)"
    - "Drawer panel render order is preserved exactly: AttributionAnalysisPanel → ProductChannelBreakdown → MetaShopifyReconciliation → AdSetTable"
    - "Hebrew RTL string literals (D-05) move byte-identical from parent → sub-component (UI-SPEC §Copywriting tables anchor the spot-check inventory)"
    - "Sub-components are dumb / presentational: zero `useSWR`, zero `localStorage` direct access, zero `roas-*-changed` event listeners in any new sub-component (UI-SPEC §Components critical contract)"
    - "AdSetTable.tsx greps clean for `analyzeAttributionForAdSet` (zero direct calls — must consume via Map prop)"
    - "BillingSettings tab content uses conditional render (NOT `display:none`) — preserves React unmount semantics + form state"
    - "Recharts SVG hex literals (`#dc2626`, `#15803d`, `#d97706`, `#64748b`) move verbatim with chart JSX — cannot become Tailwind classes (SVG attrs)"
    - "Explicit `<div dir=\"rtl\">` on the reconciliation chart tooltip moves with the tooltip JSX (not silently dropped)"
    - "Manual smoke (UI-SPEC §Manual Smoke Checklist) passes end-to-end after all 12 tasks land"
    - "D-01: All 4 new hook files live under `dashboard-web/src/lib/hooks/` (the new directory introduced in this phase). This is the convention for all future React hooks (phases 5/6/7)."
    - "D-02: All 6 new sub-component files live flat in `dashboard-web/src/components/` — no subdirs per-parent. Matches the existing ~30-file flat convention."
    - "D-03: Regression confidence = `npm run build` (per task) + `npm run test` (84 Phase 2 tests, per task) + manual smoke (per checkpoint). No new tests are written in this phase."
    - "D-06: Tasks execute sequentially in 3 groups (CampaignsTable → CampaignDrawer → BillingSettings), gated by 2 in-phase human-verify checkpoints + 1 final phase-wide smoke checkpoint. No two groups in flight at once."
    - "D-07: Each of the 12 implementation tasks (T-A..T-L) lands as ONE atomic commit. 12 tasks = 12 commits across the phase."
  artifacts:
    - path: "dashboard-web/src/lib/hooks/useCampaignTrueRevenue.ts"
      provides: "Memoized Map<campaignKey, TrueRevenueInfo> with byte-identical body lift from CampaignsTable.tsx:552-682"
      max_lines: 200
      exports: ["useCampaignTrueRevenue", "TrueRevenueInfo", "ConfidenceLevel"]
    - path: "dashboard-web/src/lib/hooks/useCampaignAttribution.ts"
      provides: "Memoized Map<adSetKey, AttributionAnalysis|null> with byte-identical body lift from CampaignDrawer.tsx:278-325"
      max_lines: 120
      exports: ["useCampaignAttribution"]
    - path: "dashboard-web/src/lib/hooks/useBillingRecurring.ts"
      provides: "localStorage ↔ React state ↔ 'roas-billing-changed' event for recurring costs"
      max_lines: 80
      exports: ["useBillingRecurring"]
    - path: "dashboard-web/src/lib/hooks/useBillingOneTime.ts"
      provides: "localStorage ↔ React state ↔ 'roas-billing-changed' event for one-time costs"
      max_lines: 60
      exports: ["useBillingOneTime"]
    - path: "dashboard-web/src/components/CampaignsTableRow.tsx"
      provides: "Single <tr> with 14 <td> cells: toggle, name+meta+chip, spend, budget, value, ROAS, ROAS-Shopify trust chip, Shopify value, units, conversions, CTR, CPC, CPA, external link"
      max_lines: 400
      exports: ["CampaignsTableRow"]
    - path: "dashboard-web/src/components/AttributionAnalysisPanel.tsx"
      provides: "Trust verdict callout: score header, det/meta ROAS, breakdown bar, reasons, recommendation, stability + outlier footer"
      max_lines: 250
      exports: ["AttributionAnalysisPanel"]
    - path: "dashboard-web/src/components/MetaShopifyReconciliation.tsx"
      provides: "Pearson r header + 3-branch interpretation + lag banner + ComposedChart + day-by-day disclosure; named exports `pearson` + `pearsonWithLag` for downstream consumers (Phase 5/6/7)"
      max_lines: 350
      exports: ["MetaShopifyReconciliation", "pearson", "pearsonWithLag"]
    - path: "dashboard-web/src/components/ProductChannelBreakdown.tsx"
      provides: "Phase 1 channel breakdown: heading, summary line, 4-segment bar, conditional recommendation chip (green ≥60% / amber <30%)"
      max_lines: 180
      exports: ["ProductChannelBreakdown"]
    - path: "dashboard-web/src/components/AdSetTable.tsx"
      provides: "Ad-sets table: 7-col sort headers, per-row optimization toggle, drill-to-ads, per-row trust chip (consumed from prop Map)"
      max_lines: 400
      exports: ["AdSetTable", "AdSetSortKey", "AdSetSortDir"]
    - path: "dashboard-web/src/components/BillingCsvImport.tsx"
      provides: "4-stage CSV import: paste/file → parse → preview with per-row override → confirm"
      max_lines: 400
      exports: ["BillingCsvImport"]
    - path: "dashboard-web/src/components/CampaignsTable.tsx"
      provides: "Orchestration shell: toolbar, KPI summary, AttributionGapPanel, thead, conditional states, drawer mounts. Shrunk from 1740 to ≤500 lines."
      max_lines: 600
      contains: "function CampaignsTable"
    - path: "dashboard-web/src/components/CampaignDrawer.tsx"
      provides: "Drawer shell: aside, sticky header, KPI rows, daily trend chart, mapped products section, panel mounts in order, drawer-local state. Shrunk from 1443 to ≤500 lines."
      max_lines: 600
      contains: "function CampaignDrawer"
    - path: "dashboard-web/src/components/BillingSettings.tsx"
      provides: "Modal shell: trigger pill, modal header, 3-tab nav, conditional tab body mount. Shrunk from 1328 to ≤500 lines."
      max_lines: 600
      contains: "function BillingSettings"
  key_links:
    - from: "dashboard-web/src/components/CampaignsTable.tsx"
      to: "dashboard-web/src/lib/hooks/useCampaignTrueRevenue.ts"
      via: "import + invocation in shell render"
      pattern: "import.*useCampaignTrueRevenue.*from '@/lib/hooks/useCampaignTrueRevenue'"
    - from: "dashboard-web/src/components/CampaignsTable.tsx"
      to: "dashboard-web/src/components/CampaignsTableRow.tsx"
      via: "display.map((a, i) => <CampaignsTableRow ... />)"
      pattern: "<CampaignsTableRow"
    - from: "dashboard-web/src/components/CampaignDrawer.tsx"
      to: "dashboard-web/src/lib/hooks/useCampaignAttribution.ts"
      via: "import + invocation in drawer render"
      pattern: "import.*useCampaignAttribution.*from '@/lib/hooks/useCampaignAttribution'"
    - from: "dashboard-web/src/components/CampaignDrawer.tsx"
      to: "dashboard-web/src/components/AttributionAnalysisPanel.tsx"
      via: "<AttributionAnalysisPanel analysis={...} ... /> mounted in panel position #6"
      pattern: "<AttributionAnalysisPanel"
    - from: "dashboard-web/src/components/CampaignDrawer.tsx"
      to: "dashboard-web/src/components/ProductChannelBreakdown.tsx"
      via: "<ProductChannelBreakdown breakdown={...} /> in panel position #7"
      pattern: "<ProductChannelBreakdown"
    - from: "dashboard-web/src/components/CampaignDrawer.tsx"
      to: "dashboard-web/src/components/MetaShopifyReconciliation.tsx"
      via: "<MetaShopifyReconciliation reconciliation={...} /> in panel position #8"
      pattern: "<MetaShopifyReconciliation"
    - from: "dashboard-web/src/components/CampaignDrawer.tsx"
      to: "dashboard-web/src/components/AdSetTable.tsx"
      via: "<AdSetTable adSets={...} attributionByAdSet={...} ... /> in panel position #9"
      pattern: "<AdSetTable"
    - from: "dashboard-web/src/components/BillingSettings.tsx"
      to: "dashboard-web/src/lib/hooks/useBillingRecurring.ts"
      via: "shell consumes recurring + setRecurring + totalMonthly from hook"
      pattern: "useBillingRecurring\\("
    - from: "dashboard-web/src/components/BillingSettings.tsx"
      to: "dashboard-web/src/lib/hooks/useBillingOneTime.ts"
      via: "shell consumes oneTime + setOneTime from hook"
      pattern: "useBillingOneTime\\("
    - from: "dashboard-web/src/components/BillingSettings.tsx"
      to: "dashboard-web/src/components/BillingCsvImport.tsx"
      via: "<BillingCsvImport ... /> mounted when tab === 'import'"
      pattern: "<BillingCsvImport"
    - from: "dashboard-web/src/lib/hooks/useBillingRecurring.ts"
      to: "dashboard-web/src/lib/billing.ts"
      via: "writeRecurring → safeWrite → dispatchEvent('roas-billing-changed') (lib/billing.ts:79)"
      pattern: "writeRecurring"
    - from: "dashboard-web/src/lib/hooks/useBillingOneTime.ts"
      to: "dashboard-web/src/lib/billing.ts"
      via: "writeOneTime → safeWrite → dispatchEvent('roas-billing-changed') (SAME event as recurring)"
      pattern: "writeOneTime"
---

<objective>
Split three 1300+ line React components into focused ≤500-line modules with extracted hooks and presentational sub-components. **This is a mechanical refactor — no behavior change, no UI change, no logic change.** Every Hebrew literal, every Tailwind className, every memo dep array moves byte-identical. The 84-test Vitest suite from Phase 2 + `npm run build` are the regression net.

Purpose:
- Reduce cognitive load + IDE pressure on three files totaling 4511 lines (CampaignsTable 1740L + CampaignDrawer 1443L + BillingSettings 1328L) by extracting 10 new files (4 hooks + 6 sub-components) and shrinking each parent shell to ≤500 lines.
- Establish `dashboard-web/src/lib/hooks/` as the convention for future hooks (Phase 5/6/7 will add more there).
- Preserve byte-identical the three load-bearing logic chunks: `trueRevenueByKey` memo, `attributionByAdSet`/`dailyMetaByAdSet` memos, Pearson r + lag detection.

Output:
- 4 new hook files in `dashboard-web/src/lib/hooks/` (D-01 establishes the directory)
- 6 new sub-component files flat in `dashboard-web/src/components/` (D-02 keeps the flat layout)
- 3 parent shells shrunk in place
- Phase-wide manual smoke per UI-SPEC §Manual Smoke Checklist (D-03 regression confidence)
- Per-task atomic commits (D-07): one commit per task, 12 commits total
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-component-decomposition/04-CONTEXT.md
@.planning/phases/04-component-decomposition/04-UI-SPEC.md
@.planning/phases/04-component-decomposition/04-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/02-foundations/02-VERIFICATION.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from existing codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From dashboard-web/src/lib/billing.ts (the storage primitives the billing hooks wrap):
```typescript
export type RecurringCost = { id: string; label: string; monthlyCAD: number; active: boolean; source: CostSource; storeId?: string; /* etc */ };
export type OneTimeCost = { id: string; date: string; label: string; amountCAD: number; source: CostSource; storeId?: string; /* etc */ };
export type CostSource = 'shopify-plan' | 'meta-ads' | 'app' | 'service' | 'other';
export type ParsedBillLine = { /* CSV row shape */ };

export function readRecurring(): RecurringCost[];
export function readOneTime(): OneTimeCost[];
export function writeRecurring(items: RecurringCost[]): void;   // dispatches 'roas-billing-changed' + pushes to cloud
export function writeOneTime(items: OneTimeCost[]): void;        // dispatches SAME 'roas-billing-changed' event
export function generateId(): string;
export function parseShopifyBillsCsv(text: string): ParsedBillLine[];
export function findMatchingRecurring(line: ParsedBillLine, list: RecurringCost[]): RecurringCost | undefined;

// safeWrite (lib/billing.ts:73-85) — used by writeRecurring + writeOneTime
//   localStorage.setItem(key, JSON.stringify(value));
//   window.dispatchEvent(new CustomEvent('roas-billing-changed'));
//   pushCloudKey(key, value);
```

From dashboard-web/src/lib/attributionAnalysis.ts (consumed by hooks + panels — DO NOT re-implement):
```typescript
export type AttributionAnalysis = {
  deterministicRevenue: number;
  modeledRevenue: number;
  coverage: number;
  trust: { level: 'high' | 'medium' | 'low' | 'unknown'; label: string; score: number };
  roasInterval: { lower: number; upper: number } | null;
  windowStability: { sigma: number; verdict: 'stable' | 'mixed' | 'volatile'; weeks: number } | null;
  outlierDays: Array<{ date: string; metaValue: number; baseline: number; zScore: number }>;
  reasons: string[];
  recommendation: string | null;
};
export type ProductChannelBreakdown = {
  totalOrders: number;
  facebookOrders: number;
  facebookShare: number;
  bySource: Record<string, { orders: number; revenueCAD: number }>;
};
export function analyzeAttribution(opts: { ... }): AttributionAnalysis | null;
export function analyzeAttributionForAdSet(adSet, ordersRows, dateFrom, dateTo, dailyMeta): AttributionAnalysis | null;
export function analyzeProductChannel(opts: { productIds; orders; storeId; dateFrom; dateTo }): ProductChannelBreakdown;
```

From dashboard-web/src/lib/campaignProductMap.ts (consumed by useCampaignTrueRevenue):
```typescript
export type ProductMap = Record<string, string[]>;   // campaignKey → mapped productIds
export function readProductMap(): ProductMap;
export function campaignKey(storeId: string, campaignId: string): string;   // `${storeId}::${campaignId}`
export function allocateProductRevenue(opts: { ... }): { value: number; units: number; sharedCampaigns: number };
```

From dashboard-web/src/lib/campaigns.ts (CampaignRow + analytics shapes):
```typescript
export type CampaignRow = {
  date: string; storeId: string; platform: string;
  campaignId: string; campaignName: string;
  adSetId: string; adSetName: string;
  spend: number; conversionValue: number; conversions: number;
  impressions: number; clicks: number; dailyBudgetCad: number | null;
  budgetType: 'CBO' | 'ABO' | null;
};
```

From dashboard-web/src/lib/cloudSync.ts (event names + state keys):
```typescript
export const STATE_KEYS = [
  'roas-dashboard:billing-recurring',
  'roas-dashboard:billing-onetime',
  /* ...5 more keys */
] as const;
export const CHANGE_EVENTS: Record<StateKey, string> = {
  'roas-dashboard:billing-recurring': 'roas-billing-changed',
  'roas-dashboard:billing-onetime': 'roas-billing-changed',   // SAME event name
  /* etc */
};
```

From dashboard-web/src/components/AnnotationsPanel.tsx:51-80 (the canonical hook-shape analog for cloud-sync state):
```typescript
useEffect(() => {
  setItems(readAnnotations());
  function onChange() { setItems(readAnnotations()); }
  window.addEventListener('roas-annotations-changed', onChange);
  return () => window.removeEventListener('roas-annotations-changed', onChange);
}, []);
```

From dashboard-web/src/lib/drawerStack.ts:
```typescript
export function useDrawerEsc(open: boolean, onClose: () => void): void;
// Single shared keydown listener — pops only top-most open drawer (WR-01).
// CampaignDrawer + AdsDrawer + ProductPickerModal all use this; the refactored
// drawer shell MUST keep calling it (do NOT register a private keydown listener).
```
</interfaces>

<source_excerpts>
<!-- The three byte-identical lift blocks. These MUST be preserved verbatim. -->

CampaignsTable.tsx:680-682 (the dep array — MUST stay byte-identical when lifted to useCampaignTrueRevenue.ts):
```typescript
    return out;
  }, [mode, data, productsResp, ordersAttrResp, productMap, aggregated, localRange]);
```

CampaignDrawer.tsx:294 + 325 (TWO dep arrays — both lift to useCampaignAttribution.ts byte-identical):
```typescript
  }, [rows]);    // dailyMetaByAdSet dep array
  // ...
  }, [summary, ordersAttrData, rows, dailyMetaByAdSet]);    // attributionByAdSet dep array
```

CampaignDrawer.tsx:1402-1443 (pearson + pearsonWithLag — move to MetaShopifyReconciliation.tsx as named exports):
```typescript
function pearson(xs: number[], ys: number[]): number {
  const n = Math.min(xs.length, ys.length);
  if (n < 2) return 0;
  let sx = 0, sy = 0;
  for (let i = 0; i < n; i++) { sx += xs[i]; sy += ys[i]; }
  const mx = sx / n;
  const my = sy / n;
  let cov = 0, vx = 0, vy = 0;
  for (let i = 0; i < n; i++) {
    const dx = xs[i] - mx;
    const dy = ys[i] - my;
    cov += dx * dy;
    vx += dx * dx;
    vy += dy * dy;
  }
  const denom = Math.sqrt(vx * vy);
  if (denom === 0) return 0;
  const r = cov / denom;
  return Math.max(-1, Math.min(1, r));    // clamp — byte-identical preservation required
}

function pearsonWithLag(xs: number[], ys: number[], lag: number): number {
  if (lag === 0) return pearson(xs, ys);
  const xsShift: number[] = [];
  const ysShift: number[] = [];
  for (let i = 0; i < xs.length; i++) {
    const j = i + lag;
    if (j < 0 || j >= ys.length) continue;
    xsShift.push(xs[i]);
    ysShift.push(ys[j]);
  }
  return pearson(xsShift, ysShift);
}
```
</source_excerpts>
</context>

<tasks>

<!-- ============================================================ -->
<!-- WAVE 1 — CampaignsTable group (sequential per D-06)         -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task A: Create `useCampaignTrueRevenue` hook (byte-identical lift of trueRevenueByKey memo)</name>
  <requirement>PH4-CT-A</requirement>

  <read_first>
**Required reading (in order):**
1. `.planning/phases/04-component-decomposition/04-CONTEXT.md` — D-01 (hooks under `lib/hooks/`), D-04 (line-cap discretion), D-07 (per-task atomic commits), `<specifics>` first bullet (byte-identical lift)
2. `.planning/phases/04-component-decomposition/04-PATTERNS.md` — §"`useCampaignTrueRevenue.ts`" (FULL section — has imports template, type-export pattern, hook signature, the "What's different" notes about `Aggregated` placement)
3. `.planning/codebase/CONVENTIONS.md` — §"Function Design" (object args pattern from `analyzeProductChannel`), §"Module Design" (named exports, no `'use client'` on hook files), §"Module Design" (pure-function JSDoc marker)
4. `dashboard-web/src/components/CampaignsTable.tsx` — lines 1-44 (imports — pick subset the lifted memo references), 50-87 (the `TrueRevenueInfo` + `ConfidenceLevel` type exports — copy verbatim with docstring), 100-173 (the `computeConfidence` helper + `applyDowngrade` inner — keep module-private in hook file), 216-238 (the `Aggregated` type — decide (a) export-from-CampaignsTable or (b) hoist-to-lib/campaigns.ts; recommend (a) for smaller diff), 540-551 (docstring above the memo — move with the memo), 552-682 (THE MEMO — copy 553-681 verbatim, replace `useMemo(() => {` opener with hook wrapper, preserve dep array at line 682 exactly)
5. `dashboard-web/src/lib/attributionAnalysis.ts` — lines 1-25 (canonical module-header shape for hook file imports), 798-805 (`analyzeProductChannel` opts-object pattern for the hook signature)

**Critical preservation contract:**
- Dep array MUST stay `[mode, data, productsResp, ordersAttrResp, productMap, aggregated, localRange]` byte-identical (CONTEXT `<code_context>` 3rd Established Pattern bullet: *"Dep array drift is the single most likely regression source."*)
- `TrueRevenueInfo` + `ConfidenceLevel` type definitions move byte-identical including their docstrings (CampaignsTable.tsx:50-87)
- `computeConfidence` body byte-identical (CampaignsTable.tsx:100-173), exported as **module-private** (no `export` keyword)
- The `'אמין'` / `'חלקי'` / `'לא אמין'` Hebrew labels referenced inside `computeConfidence` (lines 122, 142, 162) stay verbatim (D-05)
- No `'use client';` directive (hook files are imported by client components; hooks themselves don't opt in)
  </read_first>

  <files>
dashboard-web/src/lib/hooks/useCampaignTrueRevenue.ts (new — ~150-200 lines)
dashboard-web/src/components/CampaignsTable.tsx (modified — re-export `Aggregated` if option (a) chosen, remove TrueRevenueInfo+ConfidenceLevel+computeConfidence+trueRevenueByKey local code; NOT yet shrunk to ≤500 lines — that's Task D)
  </files>

  <action>
1. Create the directory if needed: `mkdir -p dashboard-web/src/lib/hooks`
2. Create `dashboard-web/src/lib/hooks/useCampaignTrueRevenue.ts`:
   - **No `'use client'`** at top (hook files don't need it; CONVENTIONS.md §React Patterns)
   - Imports per PATTERNS.md §"`useCampaignTrueRevenue.ts` → Imports pattern" (8-step order from CONVENTIONS.md §Import Organization)
   - Export `type TrueRevenueInfo` + `type ConfidenceLevel` — copy CampaignsTable.tsx:56-87 verbatim including docstring at lines 50-55
   - Module-private `function computeConfidence(...)` — copy CampaignsTable.tsx:100-173 verbatim (no `export` keyword, keep `applyDowngrade` nested)
   - Export `function useCampaignTrueRevenue(opts: { ... }): Map<string, TrueRevenueInfo>`:
     - Use opts-object pattern per CONVENTIONS.md §Function Design + `analyzeProductChannel` analog
     - Body = `return useMemo(() => { ... CampaignsTable.tsx:553-681 verbatim ... }, [mode, data, productsResp, ordersAttrResp, productMap, aggregated, localRange]);`
     - The docstring above the memo (CampaignsTable.tsx:540-551) moves above the hook export
   - Decide `Aggregated` placement:
     - **Recommend (a):** Add `export` to `type Aggregated = {...}` at CampaignsTable.tsx:216-238. Import it in the hook via `import type { Aggregated } from '@/components/CampaignsTable'`. This keeps the diff small. **OR**
     - (b) Move `Aggregated` to `@/lib/campaigns.ts` if cleaner — but only if also reused elsewhere. Document the choice in the task commit message.
3. In `dashboard-web/src/components/CampaignsTable.tsx`:
   - If option (a): add `export` to `type Aggregated = {...}` line.
   - Remove the now-duplicated local declarations: `TrueRevenueInfo`, `ConfidenceLevel`, `computeConfidence`, and the entire `trueRevenueByKey = useMemo(...)` block at lines 552-682.
   - Replace the removed memo with: `const trueRevenueByKey = useCampaignTrueRevenue({ mode, data, productsResp, ordersAttrResp, productMap, aggregated, localRange });`
   - Add `import { useCampaignTrueRevenue, type TrueRevenueInfo } from '@/lib/hooks/useCampaignTrueRevenue';` at the top (drop the now-unused imports `allocateProductRevenue`, `campaignKey` from `@/lib/campaignProductMap` IF nothing else in the file still uses them — grep to confirm; the file still consumes `ProductMap` so keep that import).
   - **DO NOT yet shrink CampaignsTable to ≤500 lines.** That happens in Task D after Row + the Attribution hook also exist.
4. Verification:
   - `cd dashboard-web && npm run build` exits 0
   - `cd dashboard-web && npm run test` exits 0 (all 84 tests pass)
   - `grep -c "useCampaignTrueRevenue" dashboard-web/src/components/CampaignsTable.tsx` ≥ 1
   - `grep -c "useCampaignTrueRevenue" dashboard-web/src/lib/hooks/useCampaignTrueRevenue.ts` ≥ 1
   - **Manual dep-array audit (CRITICAL):** open the hook file in editor, locate the dep array on the last line of the inner `useMemo`. Confirm it reads exactly `[mode, data, productsResp, ordersAttrResp, productMap, aggregated, localRange]` — same 7 names, same order. If even one name was added/removed/reordered, this task is incomplete.
5. Atomic commit per D-07: `git add` only the 2 files touched; commit message `refactor(04): extract useCampaignTrueRevenue hook (T-A)`.
  </action>

  <verify>
    <automated>cd dashboard-web && npm run build && npm run test 2>&1 | tail -10</automated>
    <automated>grep -c "useCampaignTrueRevenue" dashboard-web/src/components/CampaignsTable.tsx</automated>
    <automated>grep -c "useCampaignTrueRevenue" dashboard-web/src/lib/hooks/useCampaignTrueRevenue.ts</automated>
    <automated>grep -F "[mode, data, productsResp, ordersAttrResp, productMap, aggregated, localRange]" dashboard-web/src/lib/hooks/useCampaignTrueRevenue.ts</automated>
    <automated>test ! -f dashboard-web/src/lib/hooks/useCampaignTrueRevenue.ts && echo MISSING || echo OK</automated>
  </verify>

  <acceptance_criteria>
- `npm run build` exits 0 (no TypeScript errors)
- `npm run test` exits 0 (all 84 tests pass — no behavioral regression in attributionAnalysis helpers)
- Hook file exists at `dashboard-web/src/lib/hooks/useCampaignTrueRevenue.ts`
- Hook exports `useCampaignTrueRevenue`, `TrueRevenueInfo`, `ConfidenceLevel` (3 named exports)
- Hook file has NO `'use client'` directive (hooks consumed by client components don't need it)
- Dep array byte-identical: grep returns 1+ for `[mode, data, productsResp, ordersAttrResp, productMap, aggregated, localRange]` in hook file
- CampaignsTable.tsx imports `useCampaignTrueRevenue` (grep returns 1+)
- CampaignsTable.tsx no longer contains the local `trueRevenueByKey = useMemo(...)` block at lines 552-682 (grep for `trueRevenueByKey = useMemo` returns 0 in CampaignsTable.tsx)
- CampaignsTable.tsx still contains `trueRevenueByKey` as a binding (now from hook call) — grep for `const trueRevenueByKey = useCampaignTrueRevenue` returns 1
- Manual dep-array audit confirms preservation
- Single atomic commit `refactor(04): extract useCampaignTrueRevenue hook (T-A)` per D-07
  </acceptance_criteria>

  <done>
Task A is done iff: build green, tests green, hook file exists with verified dep array, CampaignsTable still works (imports hook, no duplicate logic), atomic commit made. CampaignsTable shrinkage to ≤500L is deferred to Task D.
  </done>
</task>

<task type="auto">
  <name>Task B: Create `useCampaignAttribution` hook (byte-identical lift of attributionByAdSet + dailyMetaByAdSet memos)</name>
  <requirement>PH4-CT-B</requirement>

  <read_first>
**Required reading (in order):**
1. `.planning/phases/04-component-decomposition/04-CONTEXT.md` — D-01 (hooks under `lib/hooks/`), `<specifics>` 2nd bullet (null preserved, do NOT normalize to undefined), `<code_context>` "Reusable Assets" `analyzeAttribution` consumed (do NOT re-implement)
2. `.planning/phases/04-component-decomposition/04-PATTERNS.md` — §"`useCampaignAttribution.ts`" (FULL section — imports, hook signature with duck-typed summary, dep array preservation, the "What's different" decision between summary-type-promotion (a) vs (b))
3. `dashboard-web/src/components/CampaignDrawer.tsx` — lines 33-37 (the `analyzeAttributionForAdSet` import — only this from attributionAnalysis), 190-269 (the anonymous `summary` shape that the memos consume — promote to a named type `CampaignDrawerSummary` exported from the hook file per PATTERNS.md option (b)), 278-294 (`dailyMetaByAdSet` — copy verbatim INCLUDING dep array `[rows]`), 299-325 (`attributionByAdSet` — copy verbatim INCLUDING dep array `[summary, ordersAttrData, rows, dailyMetaByAdSet]`), comments at 273-298 + 295-298 (the IN5-01 docstring stays with the lifted code)

**Critical preservation contract:**
- BOTH dep arrays MUST stay byte-identical: `[rows]` for `dailyMetaByAdSet` and `[summary, ordersAttrData, rows, dailyMetaByAdSet]` for `attributionByAdSet`
- `null` preservation: the inner `Map<string, ReturnType<typeof analyzeAttributionForAdSet>>` type ALREADY captures null because `analyzeAttributionForAdSet` returns `AttributionAnalysis | null`. Do NOT change the Map value type to non-null.
- The key formula `r.adSetId || r.adSetName || '(אחר)'` is load-bearing — `(אחר)` is Hebrew for "(other)", a Hebrew literal D-05 protects. Move verbatim.
- The summary shape's `(אחר)` mirror at line 308 (`a.id || a.name || '(אחר)'`) must also stay verbatim.
- IN5-01 comment (CampaignDrawer.tsx:296-298) explains the performance reason for the memo — keep it as the JSDoc above the hook export.
- No `'use client';` directive.
  </read_first>

  <files>
dashboard-web/src/lib/hooks/useCampaignAttribution.ts (new — ~80-120 lines)
dashboard-web/src/components/CampaignDrawer.tsx (modified — remove the 2 lifted memos, replace with hook call; NOT yet shrunk to ≤500 lines — that's Task I)
  </files>

  <action>
1. Create `dashboard-web/src/lib/hooks/useCampaignAttribution.ts`:
   - No `'use client'` directive
   - Imports per PATTERNS.md §"`useCampaignAttribution.ts` → Imports pattern":
     ```typescript
     import { useMemo } from 'react';
     import {
       analyzeAttributionForAdSet,
       type AttributionAnalysis,
     } from '@/lib/attributionAnalysis';
     import type { CampaignRow } from '@/lib/campaigns';
     import type { OrdersAttributionResponse } from '@/app/api/orders-attribution/route';
     ```
   - Export `type CampaignDrawerSummary` — promote the anonymous summary shape from CampaignDrawer.tsx:190-269 to a named type. Per PATTERNS.md option (b) recommendation: declare here, import in the drawer. The shape includes at minimum the fields the two memos read: `platform: string` + `adSets: Array<{ id: string; name: string; storeId: string; platform: string; value: number; spend: number }>`. To be safe and minimize drawer-shell drift, include all fields the drawer already declares anonymously — the executor reads `CampaignDrawer.tsx:190-269` to determine the full shape, then declares `type CampaignDrawerSummary = { ... }` matching it byte-identical.
   - Export `function useCampaignAttribution(opts: { summary: CampaignDrawerSummary | null; rows: CampaignRow[]; ordersAttrData: OrdersAttributionResponse | undefined; }): Map<string, AttributionAnalysis | null>`:
     - Inside: declare hook-local `const dailyMetaByAdSet = useMemo(() => { ... CampaignDrawer.tsx:279-293 verbatim ... }, [rows]);`
     - Then: `return useMemo(() => { ... CampaignDrawer.tsx:300-324 verbatim ... }, [summary, ordersAttrData, rows, dailyMetaByAdSet]);`
     - Pre-memo docstring is the IN5-01 explanation from CampaignDrawer.tsx:296-298.
2. In `dashboard-web/src/components/CampaignDrawer.tsx`:
   - Add `import { useCampaignAttribution, type CampaignDrawerSummary } from '@/lib/hooks/useCampaignAttribution';` per the import-order convention.
   - If the `summary` variable is currently typed via inferred return of an inline `useMemo`, refactor that memo's return type annotation to `CampaignDrawerSummary | null`. (The memo body itself is NOT lifted in this task — it stays in the drawer shell because it consumes drawer-local props/state.)
   - Remove the two `useMemo` blocks: `dailyMetaByAdSet` (lines 278-294) and `attributionByAdSet` (lines 299-325).
   - Add: `const attributionByAdSet = useCampaignAttribution({ summary, rows, ordersAttrData });` — where `ordersAttrData` is the existing SWR data variable in the drawer scope (line ~157 area).
   - Drop the unused `analyzeAttributionForAdSet` import from `@/lib/attributionAnalysis` if no other call site remains in the drawer (grep to confirm before deleting).
3. **DO NOT yet shrink CampaignDrawer to ≤500 lines.** That happens in Task I.
4. Verification:
   - `cd dashboard-web && npm run build` exits 0
   - `cd dashboard-web && npm run test` exits 0
   - `grep -c "useCampaignAttribution" dashboard-web/src/components/CampaignDrawer.tsx` ≥ 1
   - `grep -c "}, \[summary, ordersAttrData, rows, dailyMetaByAdSet\]" dashboard-web/src/lib/hooks/useCampaignAttribution.ts` = 1
   - `grep -c "}, \[rows\]" dashboard-web/src/lib/hooks/useCampaignAttribution.ts` = 1
   - **Manual dep-array audit:** open hook file, confirm BOTH dep arrays match the source byte-for-byte.
5. Atomic commit: `refactor(04): extract useCampaignAttribution hook (T-B)`.
  </action>

  <verify>
    <automated>cd dashboard-web && npm run build && npm run test 2>&1 | tail -10</automated>
    <automated>grep -c "useCampaignAttribution" dashboard-web/src/components/CampaignDrawer.tsx</automated>
    <automated>grep -F "}, [summary, ordersAttrData, rows, dailyMetaByAdSet]" dashboard-web/src/lib/hooks/useCampaignAttribution.ts</automated>
    <automated>grep -cE "^  *\}, \[rows\];?$" dashboard-web/src/lib/hooks/useCampaignAttribution.ts</automated>
    <automated>test -f dashboard-web/src/lib/hooks/useCampaignAttribution.ts && echo OK || echo MISSING</automated>
  </verify>

  <acceptance_criteria>
- `npm run build` exits 0
- `npm run test` exits 0 (84 tests pass)
- Hook file exists at `dashboard-web/src/lib/hooks/useCampaignAttribution.ts`
- Exports include `useCampaignAttribution` and `CampaignDrawerSummary` type
- BOTH dep arrays byte-identical: grep returns 1+ for `[summary, ordersAttrData, rows, dailyMetaByAdSet]` and 1+ for `}, [rows]` in hook file
- CampaignDrawer.tsx imports `useCampaignAttribution` (grep returns 1+)
- CampaignDrawer.tsx no longer contains local `attributionByAdSet = useMemo` (grep returns 0)
- The Map value type stays `AttributionAnalysis | null` (null preserved, not normalized) — verify by reading the hook's return type annotation
- Hebrew `(אחר)` literal preserved byte-identical in the hook key formula
- Atomic commit `refactor(04): extract useCampaignAttribution hook (T-B)` per D-07
  </acceptance_criteria>

  <done>
Task B is done iff: build green, tests green, hook file exists with both dep arrays preserved byte-identical, CampaignDrawer consumes via hook call, null preservation verified, atomic commit made. Drawer shrinkage to ≤500L deferred to Task I.
  </done>
</task>

<task type="auto">
  <name>Task C: Create `CampaignsTableRow` sub-component (extract 14-column row JSX)</name>
  <requirement>PH4-CT-C</requirement>

  <read_first>
**Required reading (in order):**
1. `.planning/phases/04-component-decomposition/04-CONTEXT.md` — D-02 (sub-components flat in `components/`), D-05 (Hebrew literals verbatim), `<specifics>` 5th bullet (preserve trust chip behavior)
2. `.planning/phases/04-component-decomposition/04-UI-SPEC.md` — §Components inventory (CampaignsTableRow row receives 9 props listed in table), §Color "Trust chip color mapping" (4 levels + fallback ladder is byte-identical), §Copywriting "Column headers" (column ORDER preserved), §Interaction Contracts (toggle stop-propagation, external-link stop-propagation, drill behavior per mode), §Empty States §CampaignsTable rows
3. `.planning/phases/04-component-decomposition/04-PATTERNS.md` — §"`CampaignsTableRow.tsx`" (FULL section — imports, component signature with 9 props, the 3 callback-substitution points at lines 1173-1174 / 1178-1183 / 1203, stop-propagation contract, TONE_BG duplication note)
4. `dashboard-web/src/components/CampaignsTable.tsx` — lines 199-205 (`TONE_BG` constant — leave duplicated per D-04 to minimize diff, document in commit), 1145-1461 (THE WHOLE IIFE — copy lines 1146-1460 verbatim into the new component body, the `display.map((a, i) => { ... return <tr>...</tr>; })` at line 1145 becomes `display.map((a, i) => <CampaignsTableRow ... />)` after extraction)
5. `dashboard-web/src/components/DetailTable.tsx` — full file (127L) for file-header shape: `'use client'`, lucide-react imports, plain props-in, no state

**Critical preservation contract:**
- Hebrew literals D-05: every Hebrew string in CampaignsTable.tsx:1145-1461 stays byte-identical — including `'אמין'`/`'חלקי'`/`'לא אמין'`/`'אין המרות'` via `info.confidence.label`, the `·{N}` and `·מיפוי` suffixes (lines 1390 + 1396), tooltip strings (lines 1360-1380)
- Trust chip 4-level color ladder at lines 1340-1344 byte-identical (UI-SPEC §Color critical contract)
- `e.stopPropagation()` on the optimization toggle (lines 1200-1203) and external link (line 1450) byte-identical
- 14 columns in the exact same order as `<thead>` (which STAYS in CampaignsTable.tsx shell)
- The callback substitutions are the ONLY non-mechanical edits: `setDrillCampaignId/setDrillPlatform` → `onDrillCampaign(campaignId, platform)`, `setAdDrill({...})` → `onDrillAd({...})`, `toggleOptimized(a.key)` from `@/lib/campaignOptimized` → `onToggleOptimized(a.key)`
- `'use client';` directive at top (all sub-components in `components/` start with it)
  </read_first>

  <files>
dashboard-web/src/components/CampaignsTableRow.tsx (new — ~350-400 lines)
dashboard-web/src/components/CampaignsTable.tsx (modified — replace the IIFE with `<CampaignsTableRow ... />` invocation; NOT yet shrunk to ≤500 lines — that's Task D, but this task removes ~315 lines)
  </files>

  <action>
1. Create `dashboard-web/src/components/CampaignsTableRow.tsx`:
   - First line: `'use client';`
   - Imports per PATTERNS.md §"`CampaignsTableRow.tsx` → Imports pattern":
     ```typescript
     'use client';

     import { CheckCircle2, Circle, ExternalLink } from 'lucide-react';
     import { cn, formatCurrency, formatNumber } from '@/lib/utils';
     import { campaignKey } from '@/lib/campaignProductMap';
     import { buildAdsManagerLink, type AdAccountMap } from '@/lib/campaignsLinks';
     import { roasLabel } from '@/lib/analytics';
     import type { Aggregated } from './CampaignsTable';
     import type { TrueRevenueInfo } from '@/lib/hooks/useCampaignTrueRevenue';
     ```
     (Adjust based on the actual JSX block's imports — grep what `1145-1461` references and pull in only the matching subset of `CampaignsTable.tsx:1-44`.)
   - Copy the `TONE_BG` constant from CampaignsTable.tsx:199-205 into this file (per PATTERNS.md recommendation — leave duplicated for minimal diff, document in commit).
   - Declare props type:
     ```typescript
     type Props = {
       a: Aggregated;
       i: number;
       mode: 'campaign' | 'adset';
       trueRevenueByKey: Map<string, TrueRevenueInfo>;
       adAccounts: AdAccountMap;
       optimized: Set<string>;
       onToggleOptimized: (key: string) => void;
       onDrillCampaign: (campaignId: string, platform: string) => void;
       onDrillAd: (set: { storeId: string; campaignId: string; adSetId: string; adSetName: string }) => void;
     };
     ```
   - Export `function CampaignsTableRow({ a, i, mode, trueRevenueByKey, adAccounts, optimized, onToggleOptimized, onDrillCampaign, onDrillAd }: Props)`:
     - Body = CampaignsTable.tsx:1146-1460 VERBATIM
     - Substitute 3 callback sites:
       - lines 1173-1174 (`setDrillCampaignId(a.campaignId); setDrillPlatform(a.platform);`) → `onDrillCampaign(a.campaignId, a.platform);`
       - lines 1178-1183 (`setAdDrill({...})`) → `onDrillAd({...})` with the same object literal
       - line 1203 (`toggleOptimized(a.key)`) → `onToggleOptimized(a.key)`
     - Return `<tr>...</tr>` exactly as in source.
2. In `dashboard-web/src/components/CampaignsTable.tsx`:
   - Add `import { CampaignsTableRow } from './CampaignsTableRow';` per import-order convention.
   - Replace the IIFE at line 1145 starting with `{display.map((a, i) => {` and ending at the matching `})}` after line 1461 with:
     ```jsx
     {display.map((a, i) => (
       <CampaignsTableRow
         key={a.key}
         a={a}
         i={i}
         mode={mode}
         trueRevenueByKey={trueRevenueByKey}
         adAccounts={adAccounts}
         optimized={optimized}
         onToggleOptimized={toggleOptimized}
         onDrillCampaign={(campaignId, platform) => { setDrillCampaignId(campaignId); setDrillPlatform(platform); }}
         onDrillAd={(set) => setAdDrill(set)}
       />
     ))}
     ```
     (Verify the actual existing variable names — `toggleOptimized` may be a local wrapper around the imported one from `@/lib/campaignOptimized`; preserve whatever the shell uses today.)
3. Verification:
   - `npm run build` exits 0
   - `npm run test` exits 0
   - `wc -l dashboard-web/src/components/CampaignsTableRow.tsx` returns ≤400
   - `wc -l dashboard-web/src/components/CampaignsTable.tsx` returns < 1500 (i.e. dropped from 1740 by ~315 lines)
   - `grep -c "CampaignsTableRow" dashboard-web/src/components/CampaignsTable.tsx` ≥ 2 (import + usage)
   - `grep -F '·מיפוי' dashboard-web/src/components/CampaignsTableRow.tsx` returns ≥1 (the fallback suffix from CampaignsTable.tsx:1396 — proves the JSX moved verbatim per D-05; the `'אמין'`/`'חלקי'`/`'לא אמין'` strings flow in via `info.confidence.label` prop and live in attributionAnalysis.ts/the hook, not the row file)
   - `grep -c "stopPropagation" dashboard-web/src/components/CampaignsTableRow.tsx` ≥ 2 (toggle + external link)
4. **Manual smoke (UI-SPEC §Manual Smoke Checklist → "After T-C"):** open dashboard, confirm: row hover bg-surfaceMuted, optimized rows opacity 0.5, 1-based circle, CBO/ABO chip on Meta, drill behavior per mode, trust chip 4 levels render. **Report any smoke deviations as a re-task.**
5. Atomic commit: `refactor(04): extract CampaignsTableRow sub-component (T-C)`.
  </action>

  <verify>
    <automated>cd dashboard-web && npm run build && npm run test 2>&1 | tail -10</automated>
    <automated>wc -l dashboard-web/src/components/CampaignsTableRow.tsx</automated>
    <automated>wc -l dashboard-web/src/components/CampaignsTable.tsx</automated>
    <automated>grep -c "CampaignsTableRow" dashboard-web/src/components/CampaignsTable.tsx</automated>
    <automated>grep -c "stopPropagation" dashboard-web/src/components/CampaignsTableRow.tsx</automated>
    <automated>grep -F '·מיפוי' dashboard-web/src/components/CampaignsTableRow.tsx</automated>
    <automated>! grep -E "useSWR|window\.localStorage|window\.addEventListener" dashboard-web/src/components/CampaignsTableRow.tsx</automated>
  </verify>

  <acceptance_criteria>
- `npm run build` exits 0
- `npm run test` exits 0
- `CampaignsTableRow.tsx` exists, ≤400 lines
- `CampaignsTable.tsx` shrunk to <1500 lines (still above 500 — that's Task D)
- Imports + usage of `CampaignsTableRow` in shell verified by grep
- 2+ `stopPropagation` calls preserved
- 14 `<td>` cells in source still produce a `<tr>` with 14 cells in the new file (visual inspection)
- Trust chip color ladder + `·{N}` / `·מיפוי` suffixes preserved (manual inspection of lines copied)
- Manual smoke passes (UI-SPEC §Manual Smoke → After T-C)
- Atomic commit per D-07
  </acceptance_criteria>

  <done>
Task C is done iff: build green, tests green, Row file exists, parent file shrunk by ~315 lines, manual smoke passes, atomic commit made.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: CampaignsTable group smoke (after T-A + T-B + T-C, before T-D)</name>
  <what-built>Three extractions for the CampaignsTable group are complete: `useCampaignTrueRevenue` hook (Task A), `useCampaignAttribution` hook (Task B, primarily used by drawer but also exists), and `CampaignsTableRow` sub-component (Task C). The parent `CampaignsTable.tsx` is NOT yet ≤500L — that's Task D after this checkpoint confirms no regressions.</what-built>
  <how-to-verify>
1. Run `cd dashboard-web && npm run build` — must exit 0
2. Run `cd dashboard-web && npm run test` — must show 84 passing tests
3. Run `cd dashboard-web && npm run dev` (or use existing dev server) and visit the Campaigns tab
4. Test the following per UI-SPEC §Manual Smoke Checklist:
   - Campaigns table renders without errors
   - Pick a campaign with `spend > 0` and a click-id-attributed mapping. Trust chip shows one of `'אמין'` / `'חלקי'` / `'לא אמין'` based on its level.
   - Pick a campaign with no click-id data. Trust chip shows `'אין המרות'` or `·מיפוי` fallback.
   - Click a campaign row → CampaignDrawer opens with all panels rendering correctly (this exercises `useCampaignAttribution` from Task B).
   - Switch to ad-set mode and confirm rows still render.
   - Click the optimization toggle → row dims to 0.5 opacity; toolbar shows the count + clear-all button.
   - Click the external link icon → opens Ads Manager (does NOT trigger row drill).
   - Sort by spend → arrow flips between asc/desc.
5. Confirm UI-SPEC §Empty States CampaignsTable: filter to a range with no campaigns → empty block `'אין קמפיינים פעילים בטווח הזה.'` appears.
  </how-to-verify>
  <resume-signal>Type "approved" if all smoke items pass. If anything is broken, describe the symptom and which task to re-run (A, B, or C).</resume-signal>
  <files>none — human-verify checkpoint pauses execution; no files modified by this task</files>
  <action>This is a human-verification checkpoint. Execute the steps in `<how-to-verify>` above (build, test, dev-server smoke), then wait for the user's `<resume-signal>` reply ("approved" or a re-task description). No code changes made by this task.</action>
  <verify>
    <automated>cd dashboard-web && npm run build 2>&1 | tail -3</automated>
    <automated>cd dashboard-web && npm run test 2>&1 | tail -3</automated>
  </verify>
  <done>User has typed "approved" (or equivalent) confirming all smoke items in `<how-to-verify>` pass. If "approved" is not given, the executor must re-run the indicated upstream task(s) before proceeding.</done>
</task>

<task type="auto">
  <name>Task D: Shrink `CampaignsTable.tsx` shell to ≤500 lines (orchestration only)</name>
  <requirement>PH4-CT-D</requirement>

  <read_first>
**Required reading (in order):**
1. `.planning/phases/04-component-decomposition/04-CONTEXT.md` — D-04 (≤500L soft target, document overrides), D-04b (success criterion override pattern from Phase 2)
2. `.planning/phases/04-component-decomposition/04-UI-SPEC.md` — §DOM Structural Invariants "CampaignsTable — Top-down DOM order" (10-item sequence MUST be preserved verbatim), §Empty States §CampaignsTable
3. `.planning/phases/04-component-decomposition/04-PATTERNS.md` — §"`CampaignsTable.tsx` (modified, ≤500L shell)" classification row + §Pattern Assignments general guidance
4. `dashboard-web/src/components/CampaignsTable.tsx` — current state (after Tasks A + C landed). Read 1-100 (imports + types), 200-700 (state setup, aggregation), and the now-shrunk render block.

**Critical preservation contract:**
- 10-item DOM order from UI-SPEC §DOM Structural Invariants stays exact: toolbar → AttributionGapPanel → KPI summary → conditional error/loading/empty → `<table>` with thead + `<tbody>` mapping rows → show-more footer → CampaignDrawer mount → AdsDrawer mount
- `<thead>` STAYS in the shell (14-column header — `'קמפיין'` / `'הוצאה'` / etc.). Per UI-SPEC §Copywriting "Column headers": the column ORDER + sort-key names + Hebrew labels are pinned.
- `AttributionGapPanel` STAYS in the shell (per UI-SPEC §Copywriting "AttributionGapPanel" final note + per CONTEXT D-04b: line cap is a target, not a hard cap — the gap panel is justifiably co-located).
- `KPI summary strip` STAYS in the shell.
- After this task, the file should be ≤500L if a clean seam exists, OR ≤600L with a documented override (D-04). The executor MUST document override in the commit message if line count exceeds 500.
  </read_first>

  <files>
dashboard-web/src/components/CampaignsTable.tsx (modified — final shrink to ≤500L target)
  </files>

  <action>
1. Read current `dashboard-web/src/components/CampaignsTable.tsx` end-to-end to understand what remains after Tasks A + C.
2. Identify shrink candidates IN ORDER OF PRIORITY:
   - Dead imports (after T-A removed `allocateProductRevenue`, `campaignKey` may be dead unless still used elsewhere — grep to confirm)
   - Duplicated `TONE_BG` constant (lines ~199-205) — can be removed if `CampaignsTableRow` owns its own copy (verify via Task C completion)
   - Unused helpers that no longer have a call site
   - Comments that were load-bearing only for the lifted blocks (e.g. the `trueRevenueByKey` docstring at 540-551 already moved to the hook file; the comment-shell here can be deleted)
3. Run `wc -l dashboard-web/src/components/CampaignsTable.tsx` after each shrink. Stop when ≤500.
4. If after removing ALL safely-removable content the file is still > 500L:
   - Per D-04 + D-04b: **accept the override**. Document in commit message: "Final size: NNN lines (target 500). Override per D-04 because: [reason]". Common valid reasons:
     - "Toolbar + AttributionGapPanel + KPI strip + thead together exceed 350L; further extraction would create an artificial wrapper that adds indirection without value (per CONTEXT D-04)."
     - The shrink is a target, NOT a Success Criterion override that requires verifier sign-off — the verifier will mark it like Phase 2 truth #5 was marked.
5. Verification:
   - `cd dashboard-web && npm run build` exits 0
   - `cd dashboard-web && npm run test` exits 0
   - `wc -l dashboard-web/src/components/CampaignsTable.tsx` returns ≤500 (preferred) OR ≤600 (with documented override)
   - `grep -c "function CampaignsTable" dashboard-web/src/components/CampaignsTable.tsx` ≥ 1 (function still exists)
6. Manual smoke (UI-SPEC §Manual Smoke Checklist → After every T-*): run dev server, visit Campaigns tab, click a row → drawer opens. Confirm nothing visibly broken.
7. Atomic commit: `refactor(04): shrink CampaignsTable shell to NNN lines (T-D)` — where NNN is the actual line count (so the override is auditable from `git log`).
  </action>

  <verify>
    <automated>cd dashboard-web && npm run build && npm run test 2>&1 | tail -10</automated>
    <automated>wc -l dashboard-web/src/components/CampaignsTable.tsx</automated>
    <automated>grep -c "function CampaignsTable" dashboard-web/src/components/CampaignsTable.tsx</automated>
    <automated>grep -c "AttributionGapPanel" dashboard-web/src/components/CampaignsTable.tsx</automated>
  </verify>

  <acceptance_criteria>
- `npm run build` exits 0
- `npm run test` exits 0
- `wc -l dashboard-web/src/components/CampaignsTable.tsx` returns ≤500 (preferred) OR ≤600 with override documented in commit
- `function CampaignsTable` still defined (grep returns 1)
- `<AttributionGapPanel>` still rendered (grep returns 1+) — UI-SPEC pins it inside the shell
- `<CampaignsTableRow` still imported and rendered (grep returns 1+)
- `useCampaignTrueRevenue` still called (grep returns 1)
- DOM order preserved per UI-SPEC §DOM Structural Invariants (manual code-review inspection by executor)
- Manual smoke passes
- Atomic commit with line-count in subject line
  </acceptance_criteria>

  <done>
Task D done iff: build green, tests green, file ≤500L (or ≤600 with override), DOM order preserved per UI-SPEC, all roles still present (gap panel, KPI strip, thead, row mounting, drawer mounts), smoke green, atomic commit made.
  </done>
</task>

<!-- ============================================================ -->
<!-- WAVE 2 — CampaignDrawer group (sequential per D-06)         -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task E: Create `AttributionAnalysisPanel` sub-component (extract trust verdict callout)</name>
  <requirement>PH4-CD-E</requirement>

  <read_first>
**Required reading (in order):**
1. `.planning/phases/04-component-decomposition/04-CONTEXT.md` — `<code_context>` "Reusable Assets" 2nd bullet (already partially separated — easiest extraction; verify the existing partial seam before duplicating)
2. `.planning/phases/04-component-decomposition/04-UI-SPEC.md` — §Components "AttributionAnalysisPanel" row (props), §Copywriting "AttributionAnalysisPanel" (8 Hebrew strings), §Empty States §CampaignDrawer "AttributionAnalysisPanel: section hidden" (the parent gates with `{analysis && <Panel ... />}`)
3. `.planning/phases/04-component-decomposition/04-PATTERNS.md` — §"`AttributionAnalysisPanel.tsx`" (FULL section — Shape A vs Shape B decision; recommend Shape B "panel receives pre-computed analysis")
4. `dashboard-web/src/components/CampaignDrawer.tsx` — lines 707-857 (the entire IIFE — note the partial seam mentioned in ROADMAP), 716-724 (dateFrom/dateTo derivation — STAYS in parent), 738-739 (the early-return `if (!analysis) return null;`), 741-745 (4-level tone bg ladder — byte-identical), 754-855 (the JSX section)

**Critical preservation contract:**
- Hebrew literals (D-05): all 8 strings from UI-SPEC §Copywriting "AttributionAnalysisPanel" stay verbatim — especially `'ניתוח attribution'`, `'ציון אמינות'`, `'/100'`, `'ROAS אמיתי (click-id)'`, `'טווח 95%:'`, `'ROAS לפי Meta'`, `'מדווח'`, `'💡 המלצה:'` (emoji included), stability `'יציב'`/`'מעורב'`/`'תנודתי'`, outlier `'{N} ימי spike מ-Meta (modeled)'`
- 4-level tone bg ladder at lines 741-745 stays byte-identical
- Shape B recommended: panel receives pre-computed `analysis: AttributionAnalysis | null` + `spend: number` + `value: number` as props; parent guards with `{analysis && <Panel ... />}`
- Section heading `<h3>` with `<TrendingUp size={14}>` icon byte-identical
- `'use client';` directive at top
  </read_first>

  <files>
dashboard-web/src/components/AttributionAnalysisPanel.tsx (new — ~150-250 lines)
dashboard-web/src/components/CampaignDrawer.tsx (modified — replace IIFE with `<AttributionAnalysisPanel ... />` after parent computes `analysis = analyzeAttribution(...)`)
  </files>

  <action>
1. Create `dashboard-web/src/components/AttributionAnalysisPanel.tsx`:
   - First line: `'use client';`
   - Imports per PATTERNS.md §"`AttributionAnalysisPanel.tsx` → Imports pattern" (subset of CampaignDrawer.tsx imports — only what the JSX block references):
     ```typescript
     'use client';

     import { TrendingUp } from 'lucide-react';
     import { cn } from '@/lib/utils';
     import type { AttributionAnalysis } from '@/lib/attributionAnalysis';
     ```
   - Declare props (Shape B from PATTERNS.md):
     ```typescript
     type Props = {
       analysis: AttributionAnalysis;   // non-null — parent guards
       spend: number;
       value: number;
     };
     ```
   - Export `function AttributionAnalysisPanel({ analysis, spend, value }: Props)`:
     - Body: copy CampaignDrawer.tsx:741-855 verbatim (the JSX inside the `(() => { ... })()` from line 707, MINUS the `analyzeAttribution(...)` call and the early-return — those stay in parent).
     - Return `<section>...</section>` with the heading `<h3>` + 4-level callout.
     - Tone ladder lines 741-745 byte-identical.
2. In `dashboard-web/src/components/CampaignDrawer.tsx`:
   - Add `import { AttributionAnalysisPanel } from './AttributionAnalysisPanel';`
   - Locate the IIFE at line 707-857.
   - Replace with:
     ```jsx
     {(() => {
       if (!summary || summary.platform !== 'Meta') return null;
       const ordersRows = ordersAttrData?.rows ?? [];
       const first = rows[0];
       const dateFrom = rows.length ? rows.reduce((min, r) => (r.date < min ? r.date : min), first.date) : '';
       const dateTo = rows.length ? rows.reduce((max, r) => (r.date > max ? r.date : max), first.date) : '';
       const analysis = analyzeAttribution({
         /* same opts as source */
       });
       if (!analysis) return null;
       return <AttributionAnalysisPanel analysis={analysis} spend={summary.spend} value={summary.value} />;
     })()}
     ```
     (Match the actual existing `analyzeAttribution` opts shape — read source closely.)
   - The 3 lines (715, 716, 724) defining `analyzeAttribution`, `dateFrom`, `dateTo` STAY in the parent because the source already has them outside the panel JSX.
3. **Sanity check the "already partially separated" claim:** read CampaignDrawer.tsx:707-857 carefully. If the parent ALREADY computes `analysis` outside the IIFE and just passes it in, then this task is even simpler — replace the JSX-only block. Adjust action accordingly.
4. Verification:
   - `npm run build` exits 0
   - `npm run test` exits 0
   - `wc -l dashboard-web/src/components/AttributionAnalysisPanel.tsx` returns ≤250
   - `grep -c "AttributionAnalysisPanel" dashboard-web/src/components/CampaignDrawer.tsx` ≥ 2 (import + JSX)
   - `grep -F 'ניתוח attribution' dashboard-web/src/components/AttributionAnalysisPanel.tsx` returns 1
   - `grep -F 'ציון אמינות' dashboard-web/src/components/AttributionAnalysisPanel.tsx` returns 1
   - `grep -F '💡 המלצה:' dashboard-web/src/components/AttributionAnalysisPanel.tsx` returns 1
5. Manual smoke (UI-SPEC §Manual Smoke → After T-D in their numbering, our T-E): open drawer for a campaign with click-id data, confirm trust callout renders with score, breakdown bar, stability/outlier footer.
6. Atomic commit: `refactor(04): extract AttributionAnalysisPanel sub-component (T-E)`.
  </action>

  <verify>
    <automated>cd dashboard-web && npm run build && npm run test 2>&1 | tail -10</automated>
    <automated>wc -l dashboard-web/src/components/AttributionAnalysisPanel.tsx</automated>
    <automated>grep -c "AttributionAnalysisPanel" dashboard-web/src/components/CampaignDrawer.tsx</automated>
    <automated>grep -F 'ניתוח attribution' dashboard-web/src/components/AttributionAnalysisPanel.tsx</automated>
    <automated>grep -F 'ציון אמינות' dashboard-web/src/components/AttributionAnalysisPanel.tsx</automated>
    <automated>grep -F '💡 המלצה:' dashboard-web/src/components/AttributionAnalysisPanel.tsx</automated>
    <automated>! grep -E "useSWR|window\.localStorage|window\.addEventListener" dashboard-web/src/components/AttributionAnalysisPanel.tsx</automated>
  </verify>

  <acceptance_criteria>
- `npm run build` exits 0
- `npm run test` exits 0
- File exists at `dashboard-web/src/components/AttributionAnalysisPanel.tsx`, ≤250 lines, starts with `'use client';`
- CampaignDrawer.tsx imports + uses `AttributionAnalysisPanel` (grep returns 2+)
- 8 Hebrew anchor strings preserved (spot-checked via grep)
- 4-level tone bg ladder preserved (visual inspection during code review)
- Manual smoke passes
- Atomic commit per D-07
  </acceptance_criteria>

  <done>
Task E done iff: build green, tests green, panel file exists with Hebrew anchors preserved, parent imports + uses it, manual smoke passes, atomic commit made.
  </done>
</task>

<task type="auto">
  <name>Task F: Create `MetaShopifyReconciliation` sub-component + `pearson` + `pearsonWithLag` named exports</name>
  <requirement>PH4-CD-F</requirement>

  <read_first>
**Required reading (in order):**
1. `.planning/phases/04-component-decomposition/04-CONTEXT.md` — `<domain>` in-scope 3rd bullet (byte-identical Pearson + lag), `<code_context>` "Reusable Assets" 3rd bullet (pearson + pearsonWithLag as named exports — for Phase 5/6/7 reuse)
2. `.planning/phases/04-component-decomposition/04-UI-SPEC.md` — §Copywriting "MetaShopifyReconciliation" (12 Hebrew strings), §Color "Recharts SVG colors" critical contract (hex literals `#d97706`, `#15803d`, `#64748b` move verbatim — NOT Tailwind classes), §Typography critical contract (explicit `<div dir="rtl">` on chart tooltip moves with JSX — do NOT silently drop)
3. `.planning/phases/04-component-decomposition/04-PATTERNS.md` — §"`MetaShopifyReconciliation.tsx`" (FULL section — Option A vs Option B for the `reconciliation` builder; recommend Option A: move IIFE to a named export `buildReconciliation` so parent does `const reconciliation = buildReconciliation({...})`)
4. `dashboard-web/src/components/CampaignDrawer.tsx` — lines 413-460 (the `reconciliation = (() => {...})()` analysis IIFE — Option A moves this to a named export, Option B leaves in shell), 447-458 (lag-search loop with `effectiveN < 5` guard from #WR-03), 932-1087 (the JSX panel — copy verbatim), 1015 (the explicit `<div dir="rtl">` on chart tooltip — MUST move with JSX), 1029-1030 (Recharts stroke colors — `#d97706` Meta + `#15803d` Shopify, byte-identical), 1406-1443 (pearson + pearsonWithLag — move VERBATIM as named exports)

**Critical preservation contract:**
- Pearson clamp `Math.max(-1, Math.min(1, r))` (line 1424) byte-identical
- `pearsonWithLag` shift logic (lines 1432-1442) byte-identical
- `effectiveN < 5` guard inside `reconciliation` IIFE byte-identical (Round 2 WR-03 fix)
- 3 interpretation paragraphs (lines 958-982) — all Hebrew copy byte-identical, especially the long Hebrew sentences with `<strong>` tags inside
- Lag banner copy (lines 989-992) byte-identical including the positive/negative branch phrasing
- Explicit `<div dir="rtl">` on tooltip body (line 1015) MUST move with JSX
- Recharts hex strokes (`#d97706`, `#15803d`) + tick `fill: '#64748b'` (line 1001) move verbatim as SVG props
- Day-by-day table headers `'תאריך'`/`'Meta'`/`'Shopify'`/`'פער'` (lines 1053-1056) byte-identical
- Mark `pearson`, `pearsonWithLag`, and `buildReconciliation` (Option A) with JSDoc: `Pure function — no side effects, no IO. Safe to memoize on inputs.` per CONVENTIONS.md
  </read_first>

  <files>
dashboard-web/src/components/MetaShopifyReconciliation.tsx (new — ~250-350 lines)
dashboard-web/src/components/CampaignDrawer.tsx (modified — remove pearson + pearsonWithLag defs, remove `reconciliation` IIFE if Option A, add `<MetaShopifyReconciliation ... />` mount)
  </files>

  <action>
1. Create `dashboard-web/src/components/MetaShopifyReconciliation.tsx`:
   - First line: `'use client';`
   - Imports per PATTERNS.md (subset of CampaignDrawer imports — TrendingUp icon, Recharts components, `cn` + `formatCurrency` + `formatDate` from utils).
   - **First named export: `pearson`** — copy CampaignDrawer.tsx:1406-1425 VERBATIM. Add `export` keyword. JSDoc: `* Pure function — no side effects, no IO. Safe to memoize on inputs.` per CONVENTIONS.md.
   - **Second named export: `pearsonWithLag`** — copy CampaignDrawer.tsx:1432-1442 VERBATIM. Add `export`. Same JSDoc tag.
   - **Third named export (Option A — recommended): `buildReconciliation`** — copy CampaignDrawer.tsx:413-460 IIFE body VERBATIM into a function. Signature:
     ```typescript
     export function buildReconciliation(opts: {
       summary: { platform: string; /* etc — match source */ };
       productsData: ProductsResponse | undefined;
       mappedIds: string[];
       storeId: string;
       /* whatever else the IIFE consumes */
     }): { series: Array<{ date: string; meta: number; shopify: number }>; r: number; bestLag: number; bestR: number } | null
     ```
     Return shape matches what the JSX panel consumes.
   - **The React component:**
     ```typescript
     type Props = {
       reconciliation: ReturnType<typeof buildReconciliation>;   // non-null — parent guards
     };

     export function MetaShopifyReconciliation({ reconciliation }: Props) {
       if (!reconciliation) return null;
       return (
         <section>
           <h3 className="text-sm font-semibold text-text-primary inline-flex items-center gap-1.5 mb-2">
             <TrendingUp size={14} className="text-text-secondary" />
             Meta מול Shopify — מתאם יומי
           </h3>
           {/* COPY VERBATIM from CampaignDrawer.tsx:938-1086 */}
         </section>
       );
     }
     ```
   - Preserve byte-identical: the 3 interpretation Hebrew paragraphs, the lag banner branches, the `<div dir="rtl">` on the tooltip body, the `stroke="#d97706"` + `stroke="#15803d"` Recharts props, the `tick={{ fill: '#64748b' }}` axis tick fill.
2. In `dashboard-web/src/components/CampaignDrawer.tsx`:
   - Add `import { MetaShopifyReconciliation, buildReconciliation } from './MetaShopifyReconciliation';`
   - **Remove** the local `function pearson` (lines 1402-1425) and `function pearsonWithLag` (lines 1427-1443).
   - **Remove** the `reconciliation = (() => { ... })()` IIFE at lines 413-460.
   - Add (in the appropriate scope where `summary`/`mappedIds`/`productsData` exist):
     ```typescript
     const reconciliation = buildReconciliation({
       summary,
       productsData,
       mappedIds,
       storeId,
       /* etc */
     });
     ```
   - In the drawer's panel-render section, mount as:
     ```jsx
     {reconciliation && <MetaShopifyReconciliation reconciliation={reconciliation} />}
     ```
   - This mount goes at panel position #8 (per UI-SPEC §DOM Structural Invariants), AFTER AttributionAnalysisPanel and ProductChannelBreakdown.
3. Verification:
   - `npm run build` exits 0
   - `npm run test` exits 0
   - `wc -l dashboard-web/src/components/MetaShopifyReconciliation.tsx` returns ≤350
   - `grep -c "function pearson" dashboard-web/src/components/CampaignDrawer.tsx` returns 0 (moved out)
   - `grep -c "export function pearson" dashboard-web/src/components/MetaShopifyReconciliation.tsx` returns 1
   - `grep -c "export function pearsonWithLag" dashboard-web/src/components/MetaShopifyReconciliation.tsx` returns 1
   - `grep -F 'Meta מול Shopify' dashboard-web/src/components/MetaShopifyReconciliation.tsx` returns 1
   - `grep -F '#d97706' dashboard-web/src/components/MetaShopifyReconciliation.tsx` returns 1 (Meta line stroke)
   - `grep -F '#15803d' dashboard-web/src/components/MetaShopifyReconciliation.tsx` returns 1+ (Shopify line stroke + possibly r-color)
   - `grep -F 'dir="rtl"' dashboard-web/src/components/MetaShopifyReconciliation.tsx` returns 1+ (chart tooltip — pinned by UI-SPEC)
   - `grep -F 'Math.max(-1, Math.min(1, r))' dashboard-web/src/components/MetaShopifyReconciliation.tsx` returns 1 (Pearson clamp byte-identical)
   - **Manual dep-array / preserve audit:** confirm `effectiveN < 5` guard still present inside `buildReconciliation`.
4. Manual smoke (UI-SPEC §Manual Smoke → After T-E in their numbering, our T-F): open drawer for a Meta campaign with ≥5 days of mapped data. Pearson r section + ComposedChart + day-by-day disclosure must all render.
5. Atomic commit: `refactor(04): extract MetaShopifyReconciliation + pearson exports (T-F)`.
  </action>

  <verify>
    <automated>cd dashboard-web && npm run build && npm run test 2>&1 | tail -10</automated>
    <automated>wc -l dashboard-web/src/components/MetaShopifyReconciliation.tsx</automated>
    <automated>grep -c "^function pearson" dashboard-web/src/components/CampaignDrawer.tsx</automated>
    <automated>grep -c "^export function pearson" dashboard-web/src/components/MetaShopifyReconciliation.tsx</automated>
    <automated>grep -c "^export function pearsonWithLag" dashboard-web/src/components/MetaShopifyReconciliation.tsx</automated>
    <automated>grep -F 'Math.max(-1, Math.min(1, r))' dashboard-web/src/components/MetaShopifyReconciliation.tsx</automated>
    <automated>grep -F '#d97706' dashboard-web/src/components/MetaShopifyReconciliation.tsx</automated>
    <automated>grep -F '#15803d' dashboard-web/src/components/MetaShopifyReconciliation.tsx</automated>
    <automated>grep -F 'dir="rtl"' dashboard-web/src/components/MetaShopifyReconciliation.tsx</automated>
    <automated>! grep -E "useSWR|window\.localStorage|window\.addEventListener" dashboard-web/src/components/MetaShopifyReconciliation.tsx</automated>
  </verify>

  <acceptance_criteria>
- `npm run build` exits 0
- `npm run test` exits 0
- File exists at `dashboard-web/src/components/MetaShopifyReconciliation.tsx`, ≤350 lines
- 3 named exports: `MetaShopifyReconciliation`, `pearson`, `pearsonWithLag` (+ optional `buildReconciliation` if Option A)
- Pearson clamp byte-identical (grep confirms)
- pearsonWithLag verbatim (grep + visual inspection)
- Recharts hex strokes preserved
- `dir="rtl"` on chart tooltip preserved
- Hebrew "Meta מול Shopify — מתאם יומי" preserved
- CampaignDrawer.tsx no longer defines `pearson` (grep returns 0)
- CampaignDrawer.tsx imports + mounts `MetaShopifyReconciliation`
- Manual smoke passes
- Atomic commit per D-07
  </acceptance_criteria>

  <done>
Task F done iff: build green, tests green, sub-component file with byte-identical Pearson + lag + chart, parent removes local defs, hex/RTL preserved, manual smoke passes, atomic commit made.
  </done>
</task>

<task type="auto">
  <name>Task G: Create `ProductChannelBreakdown` sub-component (extract Phase 1 channel breakdown panel)</name>
  <requirement>PH4-CD-G</requirement>

  <read_first>
**Required reading (in order):**
1. `.planning/phases/04-component-decomposition/04-CONTEXT.md` — D-02 (flat components), D-05 (Hebrew verbatim)
2. `.planning/phases/04-component-decomposition/04-UI-SPEC.md` — §Copywriting "ProductChannelBreakdown" (6 Hebrew strings + emoji recommendation chips), §Empty States §CampaignDrawer "ProductChannelBreakdown: section hidden" (triple-gate: platform Meta / mapped products / ≥3 orders — STAYS in parent's memo)
3. `.planning/phases/04-component-decomposition/04-PATTERNS.md` — §"`ProductChannelBreakdown.tsx`" (FULL section — analog at CampaignDrawer.tsx:863-924)
4. `dashboard-web/src/components/CampaignDrawer.tsx` — lines 350-374 (the parent's `productChannelBreakdown` memo with `analyzeProductChannel` call + triple-gate — STAYS in parent), 863-924 (the IIFE block to lift), 875 (the section tooltip with HTML-entity escape), 882 (summary line with `&quot;` entity), 888 (bar breakdown 4-segment line), 907-910 + 915-918 (recommendation chips green ≥60% / amber <30%)

**Critical preservation contract:**
- Hebrew literals byte-identical, including the HTML entities `&quot;` (NOT raw quote characters) and the emojis `💡` (line 907) + `⚠️` (line 915)
- Triple-gate STAYS in parent's memo (executor must NOT duplicate the gate inside the sub-component); the sub-component renders unconditionally given a non-null `breakdown`
- 4-segment bar — blue (facebook) / amber (google) / muted (direct) / subtle (other) — Tailwind classes verbatim
- Type collision: `ProductChannelBreakdown` is BOTH a component name AND a type name (from `@/lib/attributionAnalysis`). Per PATTERNS.md: rename the type import as `ProductChannelBreakdownType` to avoid shadowing
- `'use client';` directive at top
  </read_first>

  <files>
dashboard-web/src/components/ProductChannelBreakdown.tsx (new — ~120-180 lines)
dashboard-web/src/components/CampaignDrawer.tsx (modified — replace IIFE with `<ProductChannelBreakdown breakdown={...} />` mount; parent's `productChannelBreakdown` memo at lines 350-374 STAYS)
  </files>

  <action>
1. Create `dashboard-web/src/components/ProductChannelBreakdown.tsx`:
   - First line: `'use client';`
   - Imports:
     ```typescript
     'use client';

     import { Package } from 'lucide-react';
     import type { ProductChannelBreakdown as ProductChannelBreakdownType } from '@/lib/attributionAnalysis';
     ```
   - Declare props:
     ```typescript
     type Props = {
       breakdown: ProductChannelBreakdownType;
     };
     ```
   - Export `function ProductChannelBreakdown({ breakdown }: Props)`:
     - Inside, derive the counts inline (per PATTERNS.md):
       ```typescript
       const total = breakdown.totalOrders;
       const fb = breakdown.facebookOrders;
       const google = (breakdown.bySource['google-paid']?.orders ?? 0)
                   + (breakdown.bySource['google-organic']?.orders ?? 0);
       const direct = breakdown.bySource['direct']?.orders ?? 0;
       const other = Math.max(0, total - fb - google - direct);
       const fbPct = Math.round(breakdown.facebookShare * 100);
       ```
     - Return `<section>...</section>` — copy CampaignDrawer.tsx:872-923 JSX verbatim, preserving:
       - Section heading `'מכירות לפי ערוץ של המוצרים המשויכים'` (line 876)
       - Heading tooltip (line 875 — quoted string with escaped quotes)
       - Summary line (line 882 — note `&quot;` HTML entity, not raw `"`)
       - Bar breakdown line (line 888)
       - Conditional green chip (line 907 — `💡` emoji) for `fbPct >= 60`
       - Conditional amber chip (line 915 — `⚠️` emoji) for `fbPct < 30 && total >= 5`
2. In `dashboard-web/src/components/CampaignDrawer.tsx`:
   - Add `import { ProductChannelBreakdown } from './ProductChannelBreakdown';`
   - Locate the IIFE at line 863-924 (the `productChannelBreakdown && (() => {...})()` block).
   - Replace with: `{productChannelBreakdown && <ProductChannelBreakdown breakdown={productChannelBreakdown} />}`
   - The `productChannelBreakdown` memo at lines 350-374 (which calls `analyzeProductChannel(...)` + applies the triple-gate) STAYS unchanged in the parent.
   - Panel mount position: #7 (between AttributionAnalysisPanel and MetaShopifyReconciliation per UI-SPEC §DOM Structural Invariants).
3. Verification:
   - `npm run build` exits 0
   - `npm run test` exits 0
   - `wc -l dashboard-web/src/components/ProductChannelBreakdown.tsx` returns ≤180
   - `grep -c "ProductChannelBreakdown" dashboard-web/src/components/CampaignDrawer.tsx` ≥ 2 (import + JSX)
   - `grep -F 'מכירות לפי ערוץ של המוצרים המשויכים' dashboard-web/src/components/ProductChannelBreakdown.tsx` returns 1
   - `grep -F '💡' dashboard-web/src/components/ProductChannelBreakdown.tsx` returns 1+
   - `grep -F '⚠️' dashboard-web/src/components/ProductChannelBreakdown.tsx` returns 1+
   - `grep -F '&quot;' dashboard-web/src/components/ProductChannelBreakdown.tsx` returns 1+ (HTML entity preserved, not raw quote)
4. Manual smoke (UI-SPEC §Manual Smoke → After T-F in their numbering, our T-G): open drawer for a Meta campaign with mapped products + ≥3 orders. Section renders between AttributionAnalysisPanel and MetaShopifyReconciliation. Test conditional chips (≥60% green, <30% amber, in-between no chip).
5. Atomic commit: `refactor(04): extract ProductChannelBreakdown sub-component (T-G)`.
  </action>

  <verify>
    <automated>cd dashboard-web && npm run build && npm run test 2>&1 | tail -10</automated>
    <automated>wc -l dashboard-web/src/components/ProductChannelBreakdown.tsx</automated>
    <automated>grep -c "ProductChannelBreakdown" dashboard-web/src/components/CampaignDrawer.tsx</automated>
    <automated>grep -F 'מכירות לפי ערוץ של המוצרים המשויכים' dashboard-web/src/components/ProductChannelBreakdown.tsx</automated>
    <automated>grep -cF '💡' dashboard-web/src/components/ProductChannelBreakdown.tsx</automated>
    <automated>grep -cF '⚠️' dashboard-web/src/components/ProductChannelBreakdown.tsx</automated>
    <automated>! grep -E "useSWR|window\.localStorage|window\.addEventListener" dashboard-web/src/components/ProductChannelBreakdown.tsx</automated>
  </verify>

  <acceptance_criteria>
- `npm run build` exits 0
- `npm run test` exits 0
- File exists at `dashboard-web/src/components/ProductChannelBreakdown.tsx`, ≤180 lines
- All 6 UI-SPEC anchor strings + 2 emojis preserved (greps)
- `&quot;` HTML entity preserved (not converted to raw `"`)
- Triple-gate stays in parent's memo (NOT inside sub-component) — code review by executor
- CampaignDrawer imports + mounts at position #7 in panel order
- Manual smoke passes (conditional chips render correctly)
- Atomic commit per D-07
  </acceptance_criteria>

  <done>
Task G done iff: build green, tests green, file exists with Hebrew + emoji + HTML-entity preserved, parent mount at correct position, manual smoke passes, atomic commit made.
  </done>
</task>

<task type="auto">
  <name>Task H: Create `AdSetTable` sub-component (extract sortable ad-sets table with per-row trust chips)</name>
  <requirement>PH4-CD-H</requirement>

  <read_first>
**Required reading (in order):**
1. `.planning/phases/04-component-decomposition/04-CONTEXT.md` — D-02 (flat), `<specifics>` 5th bullet (4 behaviors: sort headers + per-row toggle + drill-to-ads + per-ad-set trust chip), `<code_context>` "Integration Points" (the optimization toggle dispatches `roas-campaign-optimized-changed` — wiring preserved)
2. `.planning/phases/04-component-decomposition/04-UI-SPEC.md` — §Components inventory (AdSetTable props), §Copywriting "AdSetTable" (10 Hebrew strings), §Color "Trust chip color mapping" (the second of two 4-level ladders), §Interaction Contracts §CampaignDrawer (toggle stop-prop, drill-to-ads on Meta+id only, Esc drawer-stack contract), §"Critical contract — IN5-01 visual consequence" (AdSetTable MUST grep clean for `analyzeAttributionForAdSet`)
3. `.planning/phases/04-component-decomposition/04-PATTERNS.md` — §"`AdSetTable.tsx`" (FULL section — props shape, AdSetSortKey + AdSetSortDir exports, `AdSetSortHeader` helper stays module-private, 3 callback substitutions, IN5-01 grep verification, `canDrillToAds = !!()` boolean coercion preservation)
4. `dashboard-web/src/components/CampaignDrawer.tsx` — lines 101-102 (`AdSetSortKey` + `AdSetSortDir` type declarations — move to AdSetTable.tsx), 1090-1243 (the `summary.adSets.length > 0 && <section>` block — entire body lifts), 1137 (`canDrillToAds = !!()` byte-identical preservation), 1148-1153 (drill callback substitution point), 1166 (toggle callback substitution point), 1199-1233 (trust chip 4-level ladder — byte-identical), 1217 (trust tooltip header template), 1353-1400 (`AdSetSortHeader` helper — move into AdSetTable.tsx as module-private)
5. `dashboard-web/src/components/DetailTable.tsx` lines 43-97 — table-with-sticky-thead pattern analog

**Critical preservation contract:**
- `!!()` boolean coercion at line 1137 byte-identical (load-bearing — UI-SPEC §"#IN-06 boolean coercion")
- AdSetTable.tsx MUST grep clean for `analyzeAttributionForAdSet` (UI-SPEC IN5-01) — the attribution Map is a PROP, never recomputed inside the row map
- Trust chip 4-level ladder at lines 1211-1215 byte-identical (second of two ladders — first is in CampaignsTableRow.tsx)
- All 10 Hebrew anchor strings preserved (UI-SPEC §Copywriting "AdSetTable" table)
- 3 callback substitutions ONLY: `setAdDrillSet({...})` → `onDrillAds({...})`, `onToggle(markKey)` → `onToggleOptimized(markKey)`, sort header `onClick` → `onSort(key)` 
- `AdSetSortHeader` helper inside AdSetTable.tsx — NO `export` keyword (module-private)
- `'use client';` directive at top
  </read_first>

  <files>
dashboard-web/src/components/AdSetTable.tsx (new — ~300-400 lines incl. AdSetSortHeader helper)
dashboard-web/src/components/CampaignDrawer.tsx (modified — remove AdSetSortKey/AdSetSortDir types, remove AdSetSortHeader helper, replace `<section>` block with `<AdSetTable ... />` mount)
  </files>

  <action>
1. Create `dashboard-web/src/components/AdSetTable.tsx`:
   - First line: `'use client';`
   - Imports per PATTERNS.md §"`AdSetTable.tsx` → Imports pattern" (subset of CampaignDrawer imports):
     ```typescript
     'use client';

     import { Layers, ArrowUp, ArrowDown, ArrowUpDown, CheckCircle2, Circle } from 'lucide-react';
     import { cn, formatCurrency, formatNumber } from '@/lib/utils';
     import { roasLabel } from '@/lib/analytics';
     import type { AttributionAnalysis } from '@/lib/attributionAnalysis';
     ```
   - Export types (moved from CampaignDrawer.tsx:101-102):
     ```typescript
     export type AdSetSortKey = 'name' | 'spend' | 'budget' | 'value' | 'roas' | 'conversions';
     export type AdSetSortDir = 'asc' | 'desc';
     ```
     Match the exact union literals from the source (read CampaignDrawer.tsx:101-102 to confirm — the planner cannot guarantee `conversions` is in the source list; verify).
   - Declare full props type per PATTERNS.md:
     ```typescript
     type AdSetItem = { id: string; name: string; storeId: string; platform: string; campaignId: string; spend: number; value: number; clicks: number; impressions: number; conversions: number; adSetBudgetCad: number | null; roas: number };

     type Props = {
       adSets: AdSetItem[];
       sortKey: AdSetSortKey;
       sortDir: AdSetSortDir;
       onSort: (key: AdSetSortKey) => void;
       attributionByAdSet: Map<string, AttributionAnalysis | null>;
       optimized: Set<string>;
       onToggleOptimized: (key: string) => void;
       onDrillAds: (set: { storeId: string; campaignId: string; adSetId: string; adSetName: string }) => void;
     };
     ```
   - Export `function AdSetTable({ adSets, sortKey, sortDir, onSort, attributionByAdSet, optimized, onToggleOptimized, onDrillAds }: Props)`:
     - Body: copy CampaignDrawer.tsx:1091-1242 VERBATIM
     - Substitute callbacks: `setAdDrillSet({...})` → `onDrillAds({...})`, `onToggle(markKey)` → `onToggleOptimized(markKey)`, sort header onClick wiring → `onSort(col)`
     - The `attributionByAdSet` is now a prop, not a useMemo result — but the lookup pattern `attributionByAdSet.get(key)` stays identical
     - The trust chip 4-level ladder at lines 1211-1215 stays byte-identical
   - Module-private `AdSetSortHeader` helper (no `export`):
     - Copy CampaignDrawer.tsx:1353-1400 VERBATIM
     - Place AFTER the `AdSetTable` export
2. In `dashboard-web/src/components/CampaignDrawer.tsx`:
   - Add `import { AdSetTable, type AdSetSortKey, type AdSetSortDir } from './AdSetTable';`
   - Remove the local `type AdSetSortKey` / `type AdSetSortDir` declarations (lines 101-102).
   - Remove the local `function AdSetSortHeader` (lines 1353-1400).
   - Locate the `summary.adSets.length > 0 && <section>...</section>` block at lines 1090-1243.
   - Replace with:
     ```jsx
     {summary.adSets.length > 0 && (
       <AdSetTable
         adSets={sortedAdSets}
         sortKey={adSetSortKey}
         sortDir={adSetSortDir}
         onSort={(key) => {/* existing sort toggle logic */}}
         attributionByAdSet={attributionByAdSet}
         optimized={optimized}
         onToggleOptimized={toggleOptimized}
         onDrillAds={(set) => setAdDrillSet(set)}
       />
     )}
     ```
     (Match actual variable names in the drawer scope — `sortedAdSets`, `adSetSortKey`, `adSetSortDir`, `setAdDrillSet`, `toggleOptimized` — read source closely.)
   - Panel mount position: #9 (last panel before footer hint per UI-SPEC §DOM Structural Invariants).
3. Verification:
   - `npm run build` exits 0
   - `npm run test` exits 0
   - `wc -l dashboard-web/src/components/AdSetTable.tsx` returns ≤400
   - `grep -c "AdSetTable" dashboard-web/src/components/CampaignDrawer.tsx` ≥ 2 (import + JSX)
   - **CRITICAL grep — UI-SPEC IN5-01:** `grep -c "analyzeAttributionForAdSet" dashboard-web/src/components/AdSetTable.tsx` returns **0** (zero direct calls — must consume via Map prop)
   - `grep -F '!!(' dashboard-web/src/components/AdSetTable.tsx` returns 1+ (canDrillToAds coercion preserved)
   - `grep -F 'אד-סטים' dashboard-web/src/components/AdSetTable.tsx` returns 1+
   - `grep -F 'לחץ לראות את המודעות באד-סט' dashboard-web/src/components/AdSetTable.tsx` returns 1
4. Manual smoke (UI-SPEC §Manual Smoke → After T-G in their numbering, our T-H):
   - Open drawer for a Meta campaign with multiple ad-sets
   - Sort by spend → toggles direction
   - Sort by ROAS → switches key + dir=desc
   - Click ad-set toggle → row marked optimized (and same key reflects in CampaignsTable)
   - Click ad-set row (Meta with id) → AdsDrawer opens NESTED over campaign drawer
   - Press Esc → only AdsDrawer closes, campaign drawer stays open (drawer-stack WR-01 contract)
   - Trust chip color matches `attributionByAdSet.get(key).trust.level` per row
   - **Visual check: no chip flicker on sort** (IN5-01 visual symptom — chips should NOT recompute per render)
5. Atomic commit: `refactor(04): extract AdSetTable sub-component (T-H)`.
  </action>

  <verify>
    <automated>cd dashboard-web && npm run build && npm run test 2>&1 | tail -10</automated>
    <automated>wc -l dashboard-web/src/components/AdSetTable.tsx</automated>
    <automated>grep -c "AdSetTable" dashboard-web/src/components/CampaignDrawer.tsx</automated>
    <automated>grep -c "analyzeAttributionForAdSet" dashboard-web/src/components/AdSetTable.tsx</automated>
    <automated>grep -cF '!!(' dashboard-web/src/components/AdSetTable.tsx</automated>
    <automated>grep -F 'אד-סטים' dashboard-web/src/components/AdSetTable.tsx</automated>
    <automated>! grep -E "useSWR|window\.localStorage|window\.addEventListener" dashboard-web/src/components/AdSetTable.tsx</automated>
  </verify>

  <acceptance_criteria>
- `npm run build` exits 0
- `npm run test` exits 0
- File exists at `dashboard-web/src/components/AdSetTable.tsx`, ≤400 lines
- 3 exports: `AdSetTable`, `AdSetSortKey`, `AdSetSortDir`
- `AdSetSortHeader` is module-private (NOT exported)
- **CRITICAL: grep `analyzeAttributionForAdSet` in AdSetTable.tsx returns 0** (per UI-SPEC IN5-01)
- `!!()` boolean coercion preserved (grep 1+)
- All 10 UI-SPEC Hebrew anchor strings preserved
- Trust chip 4-level ladder at lines 1211-1215 byte-identical (visual code review)
- CampaignDrawer imports + mounts at panel position #9
- Manual smoke passes — especially the no-chip-flicker on sort visual check
- Atomic commit per D-07
  </acceptance_criteria>

  <done>
Task H done iff: build green, tests green, file exists with byte-identical trust ladder, IN5-01 grep clean, parent mount correct, manual smoke (especially chip-flicker check) passes, atomic commit made.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: CampaignDrawer group smoke (after T-E + T-F + T-G + T-H, before T-I)</name>
  <what-built>Four CampaignDrawer extractions complete: AttributionAnalysisPanel (T-E), MetaShopifyReconciliation + pearson exports (T-F), ProductChannelBreakdown (T-G), AdSetTable (T-H). The parent `CampaignDrawer.tsx` is NOT yet ≤500L — Task I after this checkpoint confirms the panel mounts work.</what-built>
  <how-to-verify>
1. `cd dashboard-web && npm run build` — must exit 0
2. `cd dashboard-web && npm run test` — 84 passing tests
3. Run dev server, open Campaigns tab, drill into a campaign.
4. **The 9-position DOM order check (UI-SPEC §DOM Structural Invariants → CampaignDrawer):**
   For a fully-populated Meta campaign (spend > 0, mapped products, ≥5 days of data, click-id orders present), confirm the drawer renders these in ORDER, top-down:
   1. Sticky header (Megaphone + name + meta line + close + Ads-Manager link)
   2. 4-col KPI row (ROAS / הוצאה / ערך המרות / המרות)
   3. 3-col secondary metrics (CTR / CPC / CPA)
   4. Daily trend chart section "הוצאה ↔ ערך המרות לאורך הזמן"
   5. Mapped products section "מוצרי Shopify משויכים"
   6. **AttributionAnalysisPanel** — "ניתוח attribution" with score/breakdown/recommendation
   7. **ProductChannelBreakdown** — "מכירות לפי ערוץ של המוצרים המשויכים"
   8. **MetaShopifyReconciliation** — "Meta מול Shopify — מתאם יומי" with Pearson r + chart
   9. **AdSetTable** — "אד-סטים (N)" table
   10. Footer hint "לחץ Esc או על הרקע לסגירה"
5. Negative renders (per UI-SPEC §Empty States):
   - Google campaign → NO mapped products section, NO AttributionPanel, NO ChannelBreakdown, NO Reconciliation, AdSetTable renders
   - Meta campaign with <5 days data → Reconciliation HIDDEN
   - Meta with mapping but <3 mapped orders → ChannelBreakdown HIDDEN
6. Interactions:
   - Sort ad-sets by spend then by ROAS → no chip flicker (IN5-01 visual check)
   - Click ad-set row → AdsDrawer opens nested
   - Press Esc → only AdsDrawer closes (drawer-stack WR-01)
7. Confirm Pearson r number on a known-attribution campaign matches its pre-refactor value (visually inspect; if you have a screenshot or memory of the number, compare).
  </how-to-verify>
  <resume-signal>Type "approved" if 9-position DOM order is correct, all conditional renders work, and no chip flicker on sort. If anything is broken, describe the symptom and which task to re-run (E / F / G / H).</resume-signal>
  <files>none — human-verify checkpoint pauses execution; no files modified by this task</files>
  <action>This is a human-verification checkpoint. Execute the steps in `<how-to-verify>` above (build, test, dev-server smoke), then wait for the user's `<resume-signal>` reply ("approved" or a re-task description). No code changes made by this task.</action>
  <verify>
    <automated>cd dashboard-web && npm run build 2>&1 | tail -3</automated>
    <automated>cd dashboard-web && npm run test 2>&1 | tail -3</automated>
  </verify>
  <done>User has typed "approved" (or equivalent) confirming all smoke items in `<how-to-verify>` pass. If "approved" is not given, the executor must re-run the indicated upstream task(s) before proceeding.</done>
</task>

<task type="auto">
  <name>Task I: Shrink `CampaignDrawer.tsx` shell to ≤500 lines (drawer shell + tab routing only)</name>
  <requirement>PH4-CD-I</requirement>

  <read_first>
**Required reading (in order):**
1. `.planning/phases/04-component-decomposition/04-CONTEXT.md` — D-04 (≤500L target with override allowance)
2. `.planning/phases/04-component-decomposition/04-UI-SPEC.md` — §DOM Structural Invariants "CampaignDrawer — Top-down DOM order" (12-item sequence pinned), §Components inventory "CampaignDrawer.tsx (≤500 lines)" row
3. `dashboard-web/src/components/CampaignDrawer.tsx` — current state after Tasks B + E + F + G + H landed.

**Critical preservation contract:**
- 12-item DOM order pinned by UI-SPEC §DOM Structural Invariants — header → KPI row → secondary metrics → daily chart → mapped products → AttributionAnalysisPanel → ProductChannelBreakdown → MetaShopifyReconciliation → AdSetTable → footer hint → ProductPickerModal mount → AdsDrawer mount
- `useDrawerEsc(open, onClose)` call STAYS (drawer-stack WR-01 contract)
- Sticky header, KPI rows, daily trend chart, mapped products section all STAY in the shell (per UI-SPEC §Components "CampaignDrawer.tsx (≤500 lines)" — these are NOT extracted to sub-components in Phase 4)
- All 4 panel mounts must remain in the exact order (#6 → #7 → #8 → #9)
  </read_first>

  <files>
dashboard-web/src/components/CampaignDrawer.tsx (modified — final shrink)
  </files>

  <action>
1. Read current CampaignDrawer.tsx end-to-end.
2. Shrink candidates IN ORDER:
   - Dead imports after T-B / T-E / T-F / T-G / T-H landed (especially `analyzeAttributionForAdSet` should be gone)
   - Removed comments tied to lifted blocks (e.g. the IN5-01 docstring at 296-298 already moved to useCampaignAttribution.ts)
   - Inline helpers no longer called from the shrunk render block
   - Verify `pearson` + `pearsonWithLag` are gone (moved in T-F)
   - Verify `function AdSetSortHeader` is gone (moved in T-H)
3. Run `wc -l` after each shrink. Target ≤500. Stop when reached.
4. If still > 500 after all dead code removed, accept the override per D-04 and document line count in commit message.
5. Verification:
   - `npm run build` exits 0
   - `npm run test` exits 0
   - `wc -l dashboard-web/src/components/CampaignDrawer.tsx` returns ≤500 (preferred) OR ≤600 (override documented)
   - `grep -c "function CampaignDrawer" dashboard-web/src/components/CampaignDrawer.tsx` ≥ 1
   - `grep -c "useDrawerEsc" dashboard-web/src/components/CampaignDrawer.tsx` ≥ 1 (drawer-stack preserved)
   - `grep -c "AttributionAnalysisPanel\|ProductChannelBreakdown\|MetaShopifyReconciliation\|AdSetTable" dashboard-web/src/components/CampaignDrawer.tsx` ≥ 4 (all 4 panel mounts present)
   - `grep -c "function pearson" dashboard-web/src/components/CampaignDrawer.tsx` returns 0
   - `grep -c "function AdSetSortHeader" dashboard-web/src/components/CampaignDrawer.tsx` returns 0
6. Manual smoke (UI-SPEC §Manual Smoke → After every T-*): drawer opens, all 4 panels render in correct order, Esc behavior intact.
7. Atomic commit: `refactor(04): shrink CampaignDrawer shell to NNN lines (T-I)`.
  </action>

  <verify>
    <automated>cd dashboard-web && npm run build && npm run test 2>&1 | tail -10</automated>
    <automated>wc -l dashboard-web/src/components/CampaignDrawer.tsx</automated>
    <automated>grep -c "function CampaignDrawer" dashboard-web/src/components/CampaignDrawer.tsx</automated>
    <automated>grep -c "useDrawerEsc" dashboard-web/src/components/CampaignDrawer.tsx</automated>
    <automated>grep -cE "AttributionAnalysisPanel|ProductChannelBreakdown|MetaShopifyReconciliation|AdSetTable" dashboard-web/src/components/CampaignDrawer.tsx</automated>
    <automated>grep -c "^function pearson" dashboard-web/src/components/CampaignDrawer.tsx</automated>
    <automated>grep -c "^function AdSetSortHeader" dashboard-web/src/components/CampaignDrawer.tsx</automated>
  </verify>

  <acceptance_criteria>
- `npm run build` exits 0
- `npm run test` exits 0
- `wc -l` ≤500 (preferred) OR ≤600 with override in commit
- `function CampaignDrawer` still defined
- `useDrawerEsc` still called (drawer-stack contract preserved)
- All 4 panel mounts present (grep returns 4+)
- `function pearson` removed (returns 0)
- `function AdSetSortHeader` removed (returns 0)
- Manual smoke passes
- Atomic commit with line-count in subject
  </acceptance_criteria>

  <done>
Task I done iff: build green, tests green, file ≤500L (or ≤600 with override), all required wirings preserved (Esc, 4 panel mounts in order), dead defs removed, manual smoke green, atomic commit made.
  </done>
</task>

<!-- ============================================================ -->
<!-- WAVE 3 — BillingSettings group (sequential per D-06)        -->
<!-- ============================================================ -->

<task type="auto">
  <name>Task J: Create `useBillingRecurring` + `useBillingOneTime` hooks (localStorage ↔ state ↔ event)</name>
  <requirement>PH4-BS-J</requirement>

  <read_first>
**Required reading (in order):**
1. `.planning/phases/04-component-decomposition/04-CONTEXT.md` — D-01 (hooks under `lib/hooks/`), `<code_context>` "Integration Points" (must continue reading/writing the SAME `STATE_KEYS` + dispatching the SAME `roas-billing-changed` event)
2. `.planning/phases/04-component-decomposition/04-UI-SPEC.md` — §Interaction Contracts §BillingSettings "Critical contract — Custom event wiring" (both hooks listen to the SAME `roas-billing-changed`, NOT separate `roas-billing-onetime-changed`; the cloudSync.ts:58-66 entry confirms both billing keys → SAME event)
3. `.planning/phases/04-component-decomposition/04-PATTERNS.md` — §"`useBillingRecurring.ts`" (FULL section — analog at AnnotationsPanel.tsx:51-80, hook signature, seed-on-empty stays in shell) + §"`useBillingOneTime.ts`" (sibling — same pattern, no totalMonthly)
4. `dashboard-web/src/components/AnnotationsPanel.tsx` — lines 51-80 (the canonical hook-shape: useState + useEffect mount + listener + write-through)
5. `dashboard-web/src/components/BillingSettings.tsx` — lines 99-196 (the source block — extract state init + persist + change-listener), 101-102 (the two `useState` lines), 133-181 (the `useEffect` mount + listen + cleanup with seed-on-empty branch — note the seed branch depends on `storeNames` prop and STAYS in the shell), 184-187 (`totalMonthly` memo — included in `useBillingRecurring`), 189-196 (`persistRecurring` + `persistOneTime` writers)
6. `dashboard-web/src/lib/billing.ts` — lines 73-85 (`safeWrite` — both `writeRecurring` and `writeOneTime` call it, both dispatch `'roas-billing-changed'`)

**Critical preservation contract:**
- Event name: BOTH hooks listen to `'roas-billing-changed'`. DO NOT invent `'roas-billing-onetime-changed'` (the old plan made this error; UI-SPEC explicitly flags it).
- Seed-on-empty branch (BillingSettings.tsx:143-164) STAYS in the shell (it depends on `storeNames` prop — coupling to a use-site is wrong for a generic hook).
- `writeRecurring` / `writeOneTime` from `@/lib/billing` already do localStorage + event + cloud-push internally via `safeWrite`. The hooks just call them; they do NOT call `pushCloudKey` directly.
- The `totalMonthly` memo for recurring goes IN the recurring hook (only consumed by the trigger pill subtitle in the shell — but exposed by the hook so the shell consumes it). `useBillingOneTime` does NOT have an equivalent.
- No `'use client'` directive on hook files.
  </read_first>

  <files>
dashboard-web/src/lib/hooks/useBillingRecurring.ts (new — ~50-80 lines)
dashboard-web/src/lib/hooks/useBillingOneTime.ts (new — ~30-60 lines)
dashboard-web/src/components/BillingSettings.tsx (modified — replace local state + useEffect + persist functions with 2 hook calls; the seed-on-empty branch stays in the shell; NOT yet shrunk to ≤500L — that's Task L)
  </files>

  <action>
1. Create `dashboard-web/src/lib/hooks/useBillingRecurring.ts`:
   - No `'use client'`
   - Imports:
     ```typescript
     import { useEffect, useMemo, useState } from 'react';
     import { readRecurring, writeRecurring, type RecurringCost } from '@/lib/billing';
     ```
   - Export `function useBillingRecurring(): { recurring: RecurringCost[]; setRecurring: (next: RecurringCost[]) => void; totalMonthly: number }`:
     ```typescript
     export function useBillingRecurring() {
       const [recurring, setRecurring] = useState<RecurringCost[]>([]);

       useEffect(() => {
         setRecurring(readRecurring());
         function onChange() { setRecurring(readRecurring()); }
         window.addEventListener('roas-billing-changed', onChange);
         return () => window.removeEventListener('roas-billing-changed', onChange);
       }, []);

       const totalMonthly = useMemo(
         () => recurring.filter(r => r.active).reduce((s, r) => s + r.monthlyCAD, 0),
         [recurring],
       );

       function persist(next: RecurringCost[]) {
         setRecurring(next);
         writeRecurring(next);   // safeWrite dispatches the event + pushes to cloud
       }

       return { recurring, setRecurring: persist, totalMonthly };
     }
     ```
   - JSDoc at top: explain the localStorage → event → cloud triad, reference AnnotationsPanel as the analog.
2. Create `dashboard-web/src/lib/hooks/useBillingOneTime.ts` (sibling — no totalMonthly):
   - No `'use client'`
   - Imports:
     ```typescript
     import { useEffect, useState } from 'react';
     import { readOneTime, writeOneTime, type OneTimeCost } from '@/lib/billing';
     ```
   - Export `function useBillingOneTime(): { oneTime: OneTimeCost[]; setOneTime: (next: OneTimeCost[]) => void }`:
     ```typescript
     export function useBillingOneTime() {
       const [oneTime, setOneTime] = useState<OneTimeCost[]>([]);

       useEffect(() => {
         setOneTime(readOneTime());
         function onChange() { setOneTime(readOneTime()); }
         window.addEventListener('roas-billing-changed', onChange);   // SAME event as recurring
         return () => window.removeEventListener('roas-billing-changed', onChange);
       }, []);

       function persist(next: OneTimeCost[]) {
         setOneTime(next);
         writeOneTime(next);
       }

       return { oneTime, setOneTime: persist };
     }
     ```
3. In `dashboard-web/src/components/BillingSettings.tsx`:
   - Add imports: `import { useBillingRecurring } from '@/lib/hooks/useBillingRecurring';` and `import { useBillingOneTime } from '@/lib/hooks/useBillingOneTime';`
   - Replace lines 101 + 102 (the two `useState` declarations) with: `const { recurring, setRecurring, totalMonthly } = useBillingRecurring(); const { oneTime, setOneTime } = useBillingOneTime();` — but only the parts that DON'T cover the seed branch.
   - Locate the `useEffect` at lines 133-181:
     - The mount-read + change-listener parts are NOW handled by the hooks. REMOVE these from the shell.
     - The seed-on-empty branch (lines 143-164) — keep it as a separate `useEffect` in the shell, gated on `isHydrated()` AND `recurring.length === 0`:
       ```typescript
       useEffect(() => {
         if (!isHydrated()) return;
         if (recurring.length === 0) {
           seedBillingIfEmpty(storeNames);   // existing helper
         }
       }, [recurring.length, storeNames]);
       ```
   - Remove the local `persistRecurring` (lines 189-192) and `persistOneTime` (lines 193-196) — replaced by the hooks' returned setters.
   - Remove the local `totalMonthly` memo (lines 184-187) — provided by `useBillingRecurring`.
   - Update all call sites that used `persistRecurring(next)` / `persistOneTime(next)` to call `setRecurring(next)` / `setOneTime(next)` (note: the hook-returned setters internally call `writeRecurring`/`writeOneTime` — same behavior).
4. Verification:
   - `npm run build` exits 0
   - `npm run test` exits 0
   - `wc -l dashboard-web/src/lib/hooks/useBillingRecurring.ts` ≤ 80
   - `wc -l dashboard-web/src/lib/hooks/useBillingOneTime.ts` ≤ 60
   - `grep -c "useBillingRecurring\|useBillingOneTime" dashboard-web/src/components/BillingSettings.tsx` ≥ 2
   - **CRITICAL grep — event name correctness:** `grep -c "roas-billing-onetime-changed" dashboard-web/src/lib/hooks/` returns **0** (the wrong event name does not appear anywhere)
   - `grep -c "roas-billing-changed" dashboard-web/src/lib/hooks/useBillingRecurring.ts` = 2 (addEventListener + removeEventListener)
   - `grep -c "roas-billing-changed" dashboard-web/src/lib/hooks/useBillingOneTime.ts` = 2 (same)
   - **Manual wiring smoke (UI-SPEC §Manual Smoke → After T-H+T-I in their numbering, our T-J):** open dashboard, open BillingSettings modal, edit a recurring row, save. Check that another component listening to `roas-billing-changed` (e.g. PnLCard or similar) also re-reads. Confirms event still fires.
5. Atomic commit: `refactor(04): extract useBillingRecurring + useBillingOneTime hooks (T-J)`.
  </action>

  <verify>
    <automated>cd dashboard-web && npm run build && npm run test 2>&1 | tail -10</automated>
    <automated>wc -l dashboard-web/src/lib/hooks/useBillingRecurring.ts dashboard-web/src/lib/hooks/useBillingOneTime.ts</automated>
    <automated>grep -cE "useBillingRecurring|useBillingOneTime" dashboard-web/src/components/BillingSettings.tsx</automated>
    <automated>grep -rc "roas-billing-onetime-changed" dashboard-web/src/lib/hooks/</automated>
    <automated>grep -c "roas-billing-changed" dashboard-web/src/lib/hooks/useBillingRecurring.ts</automated>
    <automated>grep -c "roas-billing-changed" dashboard-web/src/lib/hooks/useBillingOneTime.ts</automated>
  </verify>

  <acceptance_criteria>
- `npm run build` exits 0
- `npm run test` exits 0
- 2 hook files exist, both ≤80L
- BillingSettings imports + uses both hooks (grep returns 2+)
- **CRITICAL: `roas-billing-onetime-changed` does NOT appear anywhere in `dashboard-web/src/lib/hooks/`** (grep returns 0)
- Both hooks listen to `'roas-billing-changed'` (grep returns 2 in each file: addEventListener + removeEventListener)
- Seed-on-empty branch stays in shell (gated on hook returning empty + `isHydrated()`)
- Hook files have NO `'use client'` directive
- Manual wiring smoke passes (recurring edit triggers re-read in other listeners)
- Atomic commit per D-07
  </acceptance_criteria>

  <done>
Task J done iff: build green, tests green, 2 hook files exist with correct event names, BillingSettings consumes via hooks, no `roas-billing-onetime-changed` anywhere, seed branch stays in shell, atomic commit made.
  </done>
</task>

<task type="auto">
  <name>Task K: Create `BillingCsvImport` sub-component (extract ImportTab — easiest extraction in Phase 4)</name>
  <requirement>PH4-BS-K</requirement>

  <read_first>
**Required reading (in order):**
1. `.planning/phases/04-component-decomposition/04-CONTEXT.md` — D-02 (flat), D-05 (Hebrew verbatim), `<specifics>` 6th bullet (preserve parse + preview + per-row override + confirm 4-stage flow)
2. `.planning/phases/04-component-decomposition/04-UI-SPEC.md` — §Components "BillingCsvImport" row, §Copywriting "BillingSettings — Modal shell + tabs" (Tab: import = `'ייבא CSV מ-Shopify'`) + the import-tab Hebrew strings inline in source
3. `.planning/phases/04-component-decomposition/04-PATTERNS.md` — §"`BillingCsvImport.tsx`" (FULL section — already-isolated `ImportTab` function at BillingSettings.tsx:1044-1328 just lifts and renames + the SOURCE_LABEL/SOURCE_COLOR hoist recommendation)
4. `dashboard-web/src/components/BillingSettings.tsx` — lines 1-33 (imports — pick subset that `ImportTab` references), 78-96 (`SOURCE_LABEL` + `SOURCE_COLOR` constants — recommend hoisting to `@/lib/billing.ts` per PATTERNS.md; if too risky for this task, leave in BillingSettings.tsx and import from there), 1044-1328 (the entire `function ImportTab({...})` — copy verbatim, rename to `BillingCsvImport`), 1146-1162 (the Hebrew instructions paragraph — `'איך מוציאים CSV מ-Shopify:'` etc., preserve HTML entities `&quot;`/`&apos;`)

**Critical preservation contract:**
- All Hebrew literals from the source — including the long instructions paragraph at 1146-1162, the preview table column headers, the per-row override labels, the confirm button text — byte-identical
- HTML entities (`&quot;`, `&apos;`) preserved as entities; do NOT swap for raw characters
- Component naming change: source `function ImportTab` → export `function BillingCsvImport` (file name + component name + import in shell all match)
- Conditional mount style preserved: shell mounts `<BillingCsvImport />` ONLY when `tab === 'import'`. NOT `display:none` (UI-SPEC §DOM Structural Invariants §BillingSettings critical contract — preserves React unmount + form state).
- `'use client';` at top
  </read_first>

  <files>
dashboard-web/src/components/BillingCsvImport.tsx (new — ~280-400 lines)
dashboard-web/src/components/BillingSettings.tsx (modified — remove `ImportTab` function, import + mount `BillingCsvImport`; NOT yet shrunk — Task L)
dashboard-web/src/lib/billing.ts (optionally modified — hoist SOURCE_LABEL + SOURCE_COLOR if doing so cleanly; otherwise leave the constants where they are and import them from BillingSettings.tsx)
  </files>

  <action>
1. **SOURCE_LABEL + SOURCE_COLOR decision:**
   - Option (a) — clean: hoist both constants from BillingSettings.tsx:78-96 to `@/lib/billing.ts`. Re-export and re-import where consumed.
   - Option (b) — minimal diff: leave them in BillingSettings.tsx, but `export` them so BillingCsvImport can `import { SOURCE_LABEL, SOURCE_COLOR } from './BillingSettings'`.
   - Recommendation per PATTERNS.md: (a). But if executing (a) creates churn beyond the task scope (e.g. RecurringTab/OneTimeTab also import them), fall back to (b) and document.
2. Create `dashboard-web/src/components/BillingCsvImport.tsx`:
   - First line: `'use client';`
   - Imports per PATTERNS.md §"`BillingCsvImport.tsx` → Imports pattern" (subset of BillingSettings.tsx imports — only what `ImportTab` references):
     ```typescript
     'use client';

     import { useMemo, useRef, useState } from 'react';
     import { Upload, AlertCircle, Check } from 'lucide-react';
     import { cn, formatCurrency } from '@/lib/utils';
     import {
       findMatchingRecurring,
       generateId,
       parseShopifyBillsCsv,
       type CostSource,
       type OneTimeCost,
       type ParsedBillLine,
       type RecurringCost,
     } from '@/lib/billing';
     // If option (a): also import SOURCE_LABEL, SOURCE_COLOR from '@/lib/billing'
     // If option (b): import them from './BillingSettings'
     ```
   - Declare props type:
     ```typescript
     type Props = {
       storeNames: string[];
       currentRecurring: RecurringCost[];
       onImported: (
         newRecurring: RecurringCost[],
         newOneTime: OneTimeCost[],
         destination: 'recurring' | 'onetime',
       ) => void;
     };

     type PreviewRow = ParsedBillLine & {
       type: 'recurring' | 'onetime';
       store: string;
       skip: boolean;
       duplicateOfId?: string;
     };
     ```
   - Export `function BillingCsvImport({ storeNames, currentRecurring, onImported }: Props)`:
     - Body = BillingSettings.tsx:1057-1326 VERBATIM (the body of `function ImportTab`, just renamed at the function boundary)
     - Every Hebrew literal stays byte-identical
     - Every HTML entity (`&quot;`, `&apos;`) stays as entity
3. In `dashboard-web/src/components/BillingSettings.tsx`:
   - Add `import { BillingCsvImport } from './BillingCsvImport';`
   - Remove the entire `function ImportTab({...}) { ... }` block (lines 1044-1328).
   - Locate the call site (around line 296-307 per PATTERNS.md): `{tab === 'import' && <ImportTab storeNames={storeNames} currentRecurring={recurring} onImported={handleImported} />}` — change `<ImportTab` to `<BillingCsvImport` (props identical).
   - Confirm the conditional mount style (`{tab === 'import' && <BillingCsvImport ... />}`) — NOT `display: none`.
4. Verification:
   - `npm run build` exits 0
   - `npm run test` exits 0
   - `wc -l dashboard-web/src/components/BillingCsvImport.tsx` ≤ 400
   - `grep -c "BillingCsvImport" dashboard-web/src/components/BillingSettings.tsx` ≥ 2 (import + JSX)
   - `grep -c "function ImportTab" dashboard-web/src/components/BillingSettings.tsx` returns 0
   - `grep -F 'ייבא CSV מ-Shopify' dashboard-web/src/components/BillingSettings.tsx` returns 1 (the tab label STAYS in the shell)
   - `grep -F 'איך מוציאים CSV מ-Shopify' dashboard-web/src/components/BillingCsvImport.tsx` returns 1 (instructions paragraph moved with content)
   - `grep -F '&quot;' dashboard-web/src/components/BillingCsvImport.tsx` returns 1+ (HTML entity preserved)
5. Manual smoke (UI-SPEC §Manual Smoke → After T-J in their numbering, our T-K): open BillingSettings → switch to "ייבא CSV מ-Shopify" tab → paste a sample Shopify Bills CSV → preview renders with per-row override → click "Confirm" → rows merge + modal switches to destination tab automatically.
6. Atomic commit: `refactor(04): extract BillingCsvImport sub-component (T-K)`.
  </action>

  <verify>
    <automated>cd dashboard-web && npm run build && npm run test 2>&1 | tail -10</automated>
    <automated>wc -l dashboard-web/src/components/BillingCsvImport.tsx</automated>
    <automated>grep -c "BillingCsvImport" dashboard-web/src/components/BillingSettings.tsx</automated>
    <automated>grep -c "function ImportTab" dashboard-web/src/components/BillingSettings.tsx</automated>
    <automated>grep -F 'איך מוציאים CSV מ-Shopify' dashboard-web/src/components/BillingCsvImport.tsx</automated>
    <automated>grep -cF '&quot;' dashboard-web/src/components/BillingCsvImport.tsx</automated>
    <automated>! grep -E "useSWR|window\.localStorage|window\.addEventListener" dashboard-web/src/components/BillingCsvImport.tsx</automated>
  </verify>

  <acceptance_criteria>
- `npm run build` exits 0
- `npm run test` exits 0
- File exists at `dashboard-web/src/components/BillingCsvImport.tsx`, ≤400 lines
- `function ImportTab` removed from BillingSettings.tsx (grep returns 0)
- BillingSettings imports + mounts `BillingCsvImport` (grep returns 2+)
- HTML entities preserved (`&quot;` returns 1+)
- Conditional mount style preserved (NOT `display: none`)
- Tab label `'ייבא CSV מ-Shopify'` stays in shell's `<nav>` block
- Manual smoke passes (CSV import flow end-to-end)
- Atomic commit per D-07
  </acceptance_criteria>

  <done>
Task K done iff: build green, tests green, BillingCsvImport file exists with Hebrew + HTML entities preserved, parent removes ImportTab + imports new component, mount style correct, manual smoke (CSV import) passes, atomic commit made.
  </done>
</task>

<task type="auto">
  <name>Task L: Shrink `BillingSettings.tsx` shell to ≤500 lines (modal shell + tab routing only)</name>
  <requirement>PH4-BS-L</requirement>

  <read_first>
**Required reading (in order):**
1. `.planning/phases/04-component-decomposition/04-CONTEXT.md` — D-04 (≤500L target with override allowance)
2. `.planning/phases/04-component-decomposition/04-UI-SPEC.md` — §DOM Structural Invariants "BillingSettings — Modal DOM order" (5-item sequence pinned: trigger pill → backdrop+shell → header → tab nav → body), §Copywriting "BillingSettings — Modal shell + tabs" (Hebrew strings in the shell), §"Critical contract: 3 tabs render in EXACTLY this order: recurring → onetime → import"
3. `dashboard-web/src/components/BillingSettings.tsx` — current state after T-J + T-K landed.

**Critical preservation contract:**
- 3-tab order pinned: recurring → onetime → import (UI-SPEC critical contract on the `<nav>` mapping)
- Tab mount style conditional (`{tab === 'recurring' && <RecurringTab .../>}` etc.) — NOT `display:none`
- Trigger pill + trigger tooltip + trigger subtitle stay verbatim
- Modal header `'עלויות חודשיות'` + subtitle `'Shopify plan, אפליקציות, שירותים — מתעדכנים ב-P&L האמיתי'` stay verbatim
- Close button `aria-label='סגור'` stays
- Per UI-SPEC §"BillingSettings — Auto-detect plan callout (stays in RecurringTab; not in this phase's split)": the auto-detect SWR fetch + the plan-error/plan-detect callouts STAY where they currently live (likely inside `RecurringTab` which is NOT extracted in Phase 4; if `RecurringTab` itself is a function inside BillingSettings.tsx, it stays — Phase 4 only extracts the 3 things listed in ROADMAP).
- `RecurringTab` + `OneTimeTab` (functions inside BillingSettings.tsx) STAY — they are not in ROADMAP Phase 4 scope (only `BillingCsvImport` is).
  </read_first>

  <files>
dashboard-web/src/components/BillingSettings.tsx (modified — final shrink)
  </files>

  <action>
1. Read current BillingSettings.tsx end-to-end after T-J + T-K.
2. Shrink candidates IN ORDER:
   - Dead imports after T-J (state helpers no longer used directly — `readRecurring`/`writeRecurring` etc. are now hidden in the hooks; the shell may still need `RecurringCost`/`OneTimeCost` types — keep those)
   - Dead imports after T-K (`Upload`/`AlertCircle`/`Check` if they were ONLY used in `ImportTab` — confirm via grep before deleting)
   - Dead helpers/sub-functions no longer called (e.g. `findMatchingRecurring` if it was only used by `ImportTab`; `generateId` if not used in shell — verify)
   - The `RecurringTab` and `OneTimeTab` function declarations STAY (Phase 4 ROADMAP doesn't ask for them to be extracted; D-04b explicitly mentions Phase 4 may leave them in the shell)
   - Inline helpers + comments tied to lifted code (e.g. comments about ImportTab structure that no longer apply) can be removed
3. Run `wc -l` after each shrink. Target ≤500.
4. If still > 500 after all safely-removable content is gone, accept override per D-04 and document line count in commit.
   - PATTERNS.md notes: "if the shell still exceeds 500 lines, `RecurringTab` may move to `BillingRecurringTab.tsx`" — but this is OUT OF SCOPE for Phase 4 ROADMAP (only the 3 listed items extract). Do NOT extract RecurringTab/OneTimeTab in this task. Accept the override.
5. Verification:
   - `npm run build` exits 0
   - `npm run test` exits 0
   - `wc -l dashboard-web/src/components/BillingSettings.tsx` returns ≤500 (preferred) OR ≤600 (override documented)
   - `grep -c "function BillingSettings" dashboard-web/src/components/BillingSettings.tsx` ≥ 1
   - `grep -c "BillingCsvImport" dashboard-web/src/components/BillingSettings.tsx` ≥ 2 (import + JSX mount)
   - `grep -cE "useBillingRecurring|useBillingOneTime" dashboard-web/src/components/BillingSettings.tsx` ≥ 2 (both hooks consumed)
   - `grep -F 'חודשי קבוע' dashboard-web/src/components/BillingSettings.tsx` returns 1 (recurring tab label)
   - `grep -F 'חד-פעמיים' dashboard-web/src/components/BillingSettings.tsx` returns 1 (onetime tab label)
   - `grep -F 'ייבא CSV מ-Shopify' dashboard-web/src/components/BillingSettings.tsx` returns 1 (import tab label)
6. Manual smoke (UI-SPEC §Manual Smoke → After T-J + T-K → final): open BillingSettings, all 3 tabs in correct order, switch between tabs, edit recurring → save → reload page → data persists.
7. Atomic commit: `refactor(04): shrink BillingSettings shell to NNN lines (T-L)`.
  </action>

  <verify>
    <automated>cd dashboard-web && npm run build && npm run test 2>&1 | tail -10</automated>
    <automated>wc -l dashboard-web/src/components/BillingSettings.tsx</automated>
    <automated>grep -c "function BillingSettings" dashboard-web/src/components/BillingSettings.tsx</automated>
    <automated>grep -c "BillingCsvImport" dashboard-web/src/components/BillingSettings.tsx</automated>
    <automated>grep -cE "useBillingRecurring|useBillingOneTime" dashboard-web/src/components/BillingSettings.tsx</automated>
    <automated>grep -F 'חודשי קבוע' dashboard-web/src/components/BillingSettings.tsx</automated>
    <automated>grep -F 'חד-פעמיים' dashboard-web/src/components/BillingSettings.tsx</automated>
  </verify>

  <acceptance_criteria>
- `npm run build` exits 0
- `npm run test` exits 0
- `wc -l` ≤500 (preferred) OR ≤600 with override in commit message
- `function BillingSettings` still defined
- `BillingCsvImport` import + mount present (grep 2+)
- Both billing hooks consumed (grep 2+)
- 3 tab labels present in correct order (manual code review of `<nav>` block)
- Manual smoke (UI-SPEC §Manual Smoke → final BillingSettings checks) passes
- Atomic commit with line-count in subject
  </acceptance_criteria>

  <done>
Task L done iff: build green, tests green, file ≤500L (or override documented), 3-tab order preserved, all required wirings (hooks, BillingCsvImport mount) present, manual smoke green, atomic commit made.
  </done>
</task>

<!-- ============================================================ -->
<!-- WAVE 4 — Phase-wide final smoke + verification              -->
<!-- ============================================================ -->

<task type="checkpoint:human-verify" gate="blocking">
  <name>Phase-wide final smoke (UI-SPEC §Manual Smoke Checklist → "Phase-wide final smoke")</name>
  <what-built>All 12 tasks complete. 10 new files (4 hooks + 6 sub-components) + 3 parent shells shrunk. 12 atomic commits across the phase. This is the final human verification before the verifier signs off.</what-built>
  <how-to-verify>
Run through the UI-SPEC §Manual Smoke Checklist "Phase-wide final smoke" section line by line:

1. `cd dashboard-web && npm run build` — green
2. `cd dashboard-web && npm run test` — 84 passing
3. `wc -l dashboard-web/src/components/CampaignsTable.tsx dashboard-web/src/components/CampaignDrawer.tsx dashboard-web/src/components/BillingSettings.tsx` — all 3 ≤500 (or ≤600 with override documented in commit log)
4. Run dev server. Open dashboard fresh — Campaigns tab renders without errors
5. Pick a Meta campaign with: spend > 0, mapping present, click-id data present → CampaignDrawer shows ALL 4 sub-panels in order:
   - #6 AttributionAnalysisPanel — "ניתוח attribution"
   - #7 ProductChannelBreakdown — "מכירות לפי ערוץ של המוצרים המשויכים"
   - #8 MetaShopifyReconciliation — "Meta מול Shopify — מתאם יומי"
   - #9 AdSetTable — "אד-סטים (N)"
6. Pick a Google campaign → drawer shows: KPI / chart / NO mapped products / NO Attribution / NO ChannelBreakdown / NO Reconciliation / AdSetTable only
7. Pick a Meta campaign with < 5 days of data → Reconciliation HIDDEN (effectiveN gate)
8. Pick a Meta campaign with mapping but < 3 mapped orders → ChannelBreakdown HIDDEN (threshold gate)
9. Open BillingSettings → 3 tabs render in order (recurring → onetime → import) → CSV import flow works → cloud sync still works (edit a row, open in another browser, see it appear within 30s)
10. Switch date range from 30d to 7d → all chips + numbers + chart re-compute (memo deps still complete — proves the lifted memos' dep arrays still cover the right inputs)
11. Drill into an ad-set, then press Esc — only the AdsDrawer closes; the CampaignDrawer stays open (WR-01 drawer-stack contract)
12. **Trust chip preservation:** open Campaigns table, pick 4 different campaigns where the data spans `high` / `medium` / `low` / `unknown` levels. Each chip color matches UI-SPEC §Color "ROAS-semantic chips" mapping. Then open one of them in the drawer → AdSetTable per-row chip should match the same logic.
13. **No chip flicker:** in CampaignDrawer's AdSetTable, sort by spend, then by ROAS, then by spend again. Chips remain stable, do not recompute (IN5-01 visual contract).

If any item fails, report which one and which task to re-run.
  </how-to-verify>
  <resume-signal>Type "approved" if all 13 phase-wide smoke items pass. Phase 4 is then complete and ready for the verifier (`/gsd-verify-phase 04`). If anything fails, describe the symptom and re-task.</resume-signal>
  <files>none — human-verify checkpoint pauses execution; no files modified by this task</files>
  <action>This is a human-verification checkpoint. Execute the steps in `<how-to-verify>` above (build, test, dev-server smoke), then wait for the user's `<resume-signal>` reply ("approved" or a re-task description). No code changes made by this task.</action>
  <verify>
    <automated>cd dashboard-web && npm run build 2>&1 | tail -3</automated>
    <automated>cd dashboard-web && npm run test 2>&1 | tail -3</automated>
  </verify>
  <done>User has typed "approved" (or equivalent) confirming all smoke items in `<how-to-verify>` pass. If "approved" is not given, the executor must re-run the indicated upstream task(s) before proceeding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries (Phase 4 Scope)

| Boundary | Description | Phase 4 Impact |
|----------|-------------|----------------|
| Client ↔ localStorage | localStorage is per-origin trusted | UNCHANGED — hooks just relocate the read/write code from inline blocks. Same `STATE_KEYS`, same prefix `roas-dashboard:`. |
| Client ↔ cloud-sync (`/api/dashboard-state`) | Server-side `isAllowedStateKey` allowlist gates the POST | UNCHANGED — Phase 4 does not introduce new state keys (CONTEXT `<code_context>` "Integration Points" final bullet); the allowlist is not touched. |
| Client renderer ↔ Hebrew JSX literals | `<html dir="rtl">` global RTL + Heebo font | UNCHANGED — D-05 mandates byte-identical Hebrew literal moves. |
| Per-component re-render boundary | `useMemo` dep arrays decide cache identity | **CRITICAL TO PRESERVE** — Phase 4 lifts 3 memos with byte-identical dep arrays. Drift = invalidation drift = stale or thrashing renders. |
| Event-system boundary | `window.dispatchEvent('roas-billing-changed')` etc. | **CRITICAL TO PRESERVE** — both `useBillingRecurring` and `useBillingOneTime` MUST listen to the SAME `'roas-billing-changed'` event; cloudSync.ts:58-66 is the single source of truth. |

## STRIDE Threat Register (Phase 4 — Mechanical Refactor)

This is a code-organization refactor. No new endpoints, no new persistent state, no new authentication surface, no new user-input handling, no new external IO. The threat register documents the few transitive risks the refactor introduces and confirms they are mitigated by the byte-identical-lift contract.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | `useMemo` dep arrays in 3 lifted memos (T-A, T-B, T-F) | mitigate | **Dep arrays MUST stay byte-identical** when lifted. Drift triggers either stale cached values (security via correctness — wrong revenue numbers shown to operator) OR over-invalidation (perf). PATTERNS.md + CONTEXT `<code_context>` document the regression contract. Acceptance criteria require explicit grep on dep array contents. |
| T-04-02 | Information Disclosure | Hebrew JSX literals moving from parent → sub-component (10 files, ~150 strings total) | mitigate | D-05 + UI-SPEC §Copywriting tables anchor the inventory. Acceptance criteria grep for canonical anchors per UI-SPEC. No translation, no normalization. Risk: silent dropping of a verdict string (e.g. trust chip label) could mislead operator about campaign trust level. Spot-check greps per task cover the top-N highest-leverage strings. |
| T-04-03 | Spoofing | Cross-origin event firing `roas-billing-changed` | accept | The `window` event system is per-origin; only same-origin scripts can dispatch. localStorage + custom events are the existing trust model — Phase 4 does not change it. |
| T-04-04 | Denial of Service | Per-row re-computation of `analyzeAttributionForAdSet` (UI-SPEC IN5-01 visual symptom) | mitigate | AdSetTable.tsx MUST grep clean for `analyzeAttributionForAdSet` (T-H acceptance criterion). Direct calls inside the row map create O(N×rendersPerSecond) work; the Map prop pattern is O(1) per cell. Acceptance criterion is automated. |
| T-04-05 | Elevation of Privilege | `<div dir="rtl">` on chart tooltip — XSS via bidi reshuffling | accept | The `dir="rtl"` is a presentation attribute; it does NOT execute. Recharts tooltips render React-controlled content; no `dangerouslySetInnerHTML`. UI-SPEC §Typography critical contract just preserves the tooltip's bidi correctness — not a security concern. |
| T-04-06 | Tampering | HTML entity preservation (`&quot;`, `&apos;` in ProductChannelBreakdown summary line + BillingCsvImport instructions) | mitigate | Acceptance criteria grep for `&quot;` in target files. If swapped for raw quotes, JSX still escapes them at render — but the source contract (D-05 byte-identical) is violated. Low severity; correctness over security. |
| T-04-07 | Spoofing | Wrong event name `'roas-billing-onetime-changed'` (old plan error) | mitigate | Acceptance criterion: `grep -rc "roas-billing-onetime-changed" dashboard-web/src/lib/hooks/` returns 0. Using the wrong event name would silently break cloud-sync wakeups for the one-time list — not a security threat per se, but a correctness failure with security adjacent implications (P&L drift). |
| T-04-08 | Information Disclosure | Recharts SVG hex strokes (`#dc2626`, `#15803d`, `#d97706`, `#64748b`) moving with JSX | accept | These are presentation hex literals, not credentials or PII. UI-SPEC §Color critical contract preserves them; replacement with Tailwind classes is impossible (SVG attr namespace). No security threat. |
| T-04-09 | Repudiation | Per-task atomic commits (D-07) | mitigate | Each of 12 tasks gets its own commit with a clear `refactor(04): ... (T-X)` subject. If a regression appears later, `git bisect` identifies the offending task in 4 hops. Acceptance criteria require atomic commits. |
| T-04-10 | Denial of Service | Shell oversize causing IDE / Next.js dev-server perf degradation | mitigate | The phase's primary goal IS this mitigation — 3 files dropping from 4511 to ≤1500-1800 lines total. Verification: `wc -l` checks per shell-shrink task. |

**No high-severity threats.** Phase 4 is a pure mechanical refactor; no new endpoints, no new state keys, no new external dependencies (RESEARCH.md security domain is N/A for this phase). The CRITICAL preservation contracts (dep arrays, Hebrew literals, event names) are correctness-as-security guardrails — wrong numbers shown to operator = wrong business decisions; the byte-identical mandate plus automated greps catch drift before merge.
</threat_model>

<verification>
After all 12 tasks land, the phase is verified by:

1. **Automated checks (per-task + phase-wide):**
   - `cd dashboard-web && npm run build` exits 0 — TypeScript catches missing exports, broken imports, type drift
   - `cd dashboard-web && npm run test` exits 0 — 84 Phase 2 tests catch logic regressions in `lib/` (the only place the lifted hooks/components consume engine helpers)
   - `wc -l` on 3 shells confirms ≤500L target (or ≤600 with documented overrides per D-04)
   - Grep checks per task acceptance criteria — including the IN5-01 critical contract (`grep "analyzeAttributionForAdSet" dashboard-web/src/components/AdSetTable.tsx` returns 0) and the wrong-event-name guard (`grep "roas-billing-onetime-changed" dashboard-web/src/lib/hooks/` returns 0)

2. **Manual smoke per UI-SPEC §Manual Smoke Checklist:**
   - After T-A / T-B / T-C → CampaignsTable group checkpoint (smoke 4 items)
   - After T-E / T-F / T-G / T-H → CampaignDrawer group checkpoint (smoke 7 items + 9-position DOM order)
   - After T-J / T-K → BillingSettings group covered by Task L final smoke
   - Phase-wide final smoke (13 items) at the last checkpoint

3. **Atomic-commit audit (D-07):**
   - `git log --oneline 04-component-decomposition` shows exactly 12 `refactor(04): ... (T-{A..L})` commits (plus optionally hotfix commits if a task is re-run)
   - Each commit touches the minimal file set per the task's `files_modified`

4. **DOM-order audit per UI-SPEC §DOM Structural Invariants:**
   - CampaignsTable 10-item top-down order preserved
   - CampaignDrawer 12-item top-down order preserved (especially panels #6 → #7 → #8 → #9)
   - BillingSettings 5-item modal order + 3-tab order (recurring → onetime → import) preserved

5. **Goal-backward must_haves audit:**
   - All 13 truths verifiable via greps + smokes documented above
   - All 13 artifacts exist with correct exports
   - All 12 key_links verified by grep
</verification>

<success_criteria>
This phase is complete when (per ROADMAP §Phase 4 Success Criteria + must_haves above):

- [ ] All 3 original components ≤500 lines after refactor (with up-to-≤600 line override allowance per D-04 documented in commit log)
- [ ] `cd dashboard-web && npm run build` passes with zero TypeScript errors
- [ ] `cd dashboard-web && npm run test` passes (all 84 tests green — no behavioral regression caught by Phase 2 lib-level tests)
- [ ] Manual smoke: open dashboard → all 3 tabs (Campaigns / Drawer / Billing) render + drill-down works → BillingSettings → all 3 tabs (recurring / onetime / import) work
- [ ] Trust chip in CampaignsTable still renders correctly with all 4 levels + fallback (UI-SPEC §Color critical contract)
- [ ] CampaignDrawer's 4 panels (Attribution / ProductChannelBreakdown / MetaShopifyReconciliation / AdSetTable) all render in correct order (panel positions #6 → #7 → #8 → #9 per UI-SPEC §DOM Structural Invariants)
- [ ] 12 atomic commits in git log with `refactor(04): ... (T-{A..L})` subjects per D-07
- [ ] No `roas-billing-onetime-changed` event name appears anywhere in the codebase (UI-SPEC §Interaction Contracts critical contract)
- [ ] AdSetTable.tsx greps clean for `analyzeAttributionForAdSet` (UI-SPEC IN5-01 critical contract)
- [ ] 3 byte-identical logic chunks preserved: `trueRevenueByKey` memo + dep array, `attributionByAdSet`+`dailyMetaByAdSet` memos + dep arrays, Pearson r + lag detection (with clamp `Math.max(-1, Math.min(1, r))`)
- [ ] `dashboard-web/src/lib/hooks/` directory exists with 4 hook files (D-01 convention established)
- [ ] All 6 sub-components in `dashboard-web/src/components/` flat (D-02 convention preserved)
</success_criteria>

<output>
After phase completion, create `.planning/phases/04-component-decomposition/04-SUMMARY.md` with:

- Final line counts of all 3 shells (CampaignsTable / CampaignDrawer / BillingSettings) with documented overrides per D-04 if any exceeded 500L
- Inventory of 10 new files created (4 hooks + 6 sub-components) with their final line counts
- Confirmation that the 3 byte-identical logic chunks (`trueRevenueByKey`, `attributionByAdSet`, `pearson`/`pearsonWithLag`) were preserved
- Confirmation of grep-verified contracts: `roas-billing-onetime-changed` absent, `analyzeAttributionForAdSet` absent from AdSetTable.tsx, dep arrays byte-identical
- 12 atomic commit SHAs (T-A through T-L) with one-line subjects
- Manual smoke results — pass/fail per UI-SPEC §Manual Smoke Checklist final 13 items
- Lessons learned + any deferred-to-Phase-7 hook-test candidates (per D-03b)
- Notes for next phase: Phase 5 (Scalability) can now hook into the named exports (`pearson`, `pearsonWithLag`, `buildReconciliation`) from MetaShopifyReconciliation.tsx; the `lib/hooks/` directory is the established home for new hooks Phase 5/6/7 will add.
</output>
</content>
</invoke>