# productCentricView.ts post-12.1.3 re-read (Phase 12.2.3, CONTEXT D-08)

**Date:** 2026-05-24
**Investigator:** GSD executor (Phase 12.2.3, Task 3.2)

## ALG-02 site (line ~272: `if (!member?.agg) continue`)

### Status in current file: **STILL EXISTS**

Evidence (grep output):
```bash
$ grep -n "if (!member?.agg) continue" dashboard-web/src/lib/productCentricView.ts
272:        if (!member?.agg) continue;
```

Line 272 sits inside the allocator-branch loop that aggregates `allocByCampaign.entries()` to `platformAllocatedRevenue` totals (block at lines 263-278). The bug shape from raw-returns evidence pack (`lib_algorithm_productCentricView.json` finding ALG-02) is intact — when a campaignKey from the productMap has no aggregated row (stale-mapped paused campaign), its allocator-assigned revenue is silently dropped from the platform total.

## ALG-03 site (line ~311: `.filter(r => r.agg !== undefined)`)

### Status in current file: **STILL EXISTS**

Evidence (grep output):
```bash
$ grep -n "filter(r => r.agg" dashboard-web/src/lib/productCentricView.ts
311:      .filter(r => r.agg !== undefined)
```

Line 311 sits at the head of the `raw.filter(...).map(...)` chain that builds the final `members: ProductCohortMember[]` array. The bug shape — code drops dormant members despite the JSDoc at lines 178-181 promising they are kept with zero metrics — is intact.

## 12.1.3 cascade analysis

**12.1.3 scope (per `productCentricViewSumConservation.test.ts` and the inline AUDIT ALG-01 comment block at lines 288-307):**
- Added `platformMemberCount` map (lines 300-307).
- Added `intraRevShare` separation (lines 319-328) — separates UI-spend-share from revenue-allocation-share so the zero-spend cohort scenario distributes platformRev/N across members instead of `platformRev * 0 = 0`.
- Inline audit-fix comment block at lines 288-299 documents the ALG-01 fix scope.

**Did 12.1.3 cascade-fix ALG-02 or ALG-03? NO — sites untouched.**

Reasoning:
- 12.1.3's `intraRevShare` fix lives inside the `.map(r => { ... })` callback at lines 312-352. That callback ONLY runs AFTER the `.filter(r => r.agg !== undefined)` at line 311 has already dropped dormant members (ALG-03 site).
- 12.1.3's fix presumes `r.agg` is non-undefined (it uses `r.agg!` at line 313); it does not address the upstream filter that excluded dormant members.
- 12.1.3 did not modify the `platformAllocatedRevenue` accumulator at lines 263-278 — the `if (!member?.agg) continue;` at line 272 is unchanged from pre-12.1.3.

**Confirmation cross-check (sum-conservation test):**
The 12.1.3 test `productCentricViewSumConservation.test.ts` lines 180-244 ("stale-mapped cohort: allocator branch still produces sum-conservation among ACTIVE members") explicitly documents:
> "Per ALG-02 (DEFERRED to Phase 12.2 per CONTEXT.md D-05), the stale member is currently dropped from members[]. For this 12.1 scope we assert only the sum-conservation invariant across the SURVIVING (post-filter) members + the platform totals stay consistent with each other — NOT a fix for ALG-02 itself."

The test asserts internal consistency post-filter — it does not assert the missing 300 revenue is preserved (which is the actual ALG-02 bug).

## Verdict

- **ALG-02: PROCEED with fix** (line 272 `if (!member?.agg) continue;` still drops stale-mapped revenue)
- **ALG-03: PROCEED with fix** (line 311 `.filter(r => r.agg !== undefined)` still drops dormant members in violation of JSDoc lines 178-181)

Both fixes will be applied together in **Task 3.3** as a composite commit (same file, same root cause = dormant/stale-mapped semantic, same regression test) per CONTEXT D-04.

**Option choice:** Option A (remove both filters + emit dormant members with zero metrics) per evidence pack preference:
> "Option (a) is preferable — the docstring already claims this behaviour but the code does the opposite."

Option A also fixes ALG-02 as a side effect (the dropped revenue is preserved when the dormant member is emitted with its allocator-assigned `allocatedRevenueEstimate`).
