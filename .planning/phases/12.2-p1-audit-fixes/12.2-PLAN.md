---
phase: 12.2-p1-audit-fixes
plan: 0
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: true
requirements:
  - ALG-01-aiReport
  - ALG-02-aiReport
  - ALG-07-aiReport
  - INN-07
  - INN-14
  - ALG-01-attributionAnalysis
  - ALG-01-insights
  - ALG-01-ordersAttribution
  - ALG-02-productCentricView
  - ALG-03-productCentricView
  - STA-46
  - STA-47

must_haves:
  truths:
    - "All 11 P1 audit findings from AUDIT.md §Phase 12.2 are fixed in source at the exact cited line ranges (post-12.1.3 state)"
    - "Each fix lands with a regression test that fails when the source patch is reverted via git stash"
    - "`cd dashboard-web && npm test` reports 0 failures and total test count >= 987 (up from 971 baseline)"
    - "`cd dashboard-web && npx tsc --noEmit` reports 0 errors"
    - "Commit log shows per-fix atomic commits (composite OK only when 2+ fixes share root cause + file + test, with rationale in body)"
    - "No P0 surface (12.1 fixes) is regressed; no P2 surface (12.3 fixes — API-10/18/23/26/32/37/38) is touched"
    - "Out-of-scope backlog items NOT touched: ALG-08 (aiReport type cast), MMC-WARN-02, ProductCentricView ALG-04..09, ALG-03 aiReport (last-write-wins — Phase 12.3 cascade)"
  artifacts:
    - path: "dashboard-web/src/lib/aiReport.ts"
      provides: "ALG-01 TikTok status via TIKTOK_ACTIVE_ENOUGH + ALG-02 ttSpend in totalSpendAll + ALG-07 raw coverage (no clamp)"
      contains: "TIKTOK_ACTIVE_ENOUGH"
    - path: "dashboard-web/src/inngest/functions/cronLive.ts"
      provides: "INN-07 decoupled Shopify-coupled gating (spend-only persist path)"
      contains: "spendOnly"
    - path: "dashboard-web/src/inngest/functions/eventBackfill.ts"
      provides: "INN-14 DST-safe dateRange via Asia/Jerusalem Intl arithmetic"
      contains: "Asia/Jerusalem"
    - path: "dashboard-web/src/lib/attributionAnalysis.ts"
      provides: "ALG-01 platformLabel parameterization (no hardcoded 'Meta' literal in operator copy)"
      contains: "platformLabel"
    - path: "dashboard-web/src/lib/insights.ts"
      provides: "ALG-01 projectedNet preserves MTD COGS; only remaining days use last-7 rate"
      contains: "mtdCogs"
    - path: "dashboard-web/src/lib/ordersAttribution.ts"
      provides: "ALG-01 open-ended sheet pagination (no row-100000 cap)"
    - path: "dashboard-web/src/lib/productCentricView.ts"
      provides: "ALG-02 stale-mapped revenue retained in cohort + ALG-03 dormant member retained with zero metrics"
    - path: "dashboard-web/src/lib/products.ts"
      provides: "STA-46 calendar-validated parseDate (via dateValidation.isDate) + STA-47 filter symmetry with Postgres reader"
      contains: "isDate"
    - path: "dashboard-web/src/lib/__tests__/aiReportTikTokStatus.test.ts"
      provides: "ALG-01 TikTok status taxonomy regression test (HP-05)"
    - path: "dashboard-web/src/lib/__tests__/aiReportTikTokBudget.test.ts"
      provides: "ALG-02 budget allocation regression test"
    - path: "dashboard-web/src/lib/__tests__/aiReportCoverageHalo.test.ts"
      provides: "ALG-07 coverage raw-ratio regression test"
    - path: "dashboard-web/src/inngest/functions/__tests__/cronLiveShopifyDecoupled.test.ts"
      provides: "INN-07 Shopify-failed + ad-platform-success regression test (HP-03)"
    - path: "dashboard-web/src/inngest/functions/__tests__/eventBackfillDstRange.test.ts"
      provides: "INN-14 DST regression test (MP-18 / HP-24)"
    - path: "dashboard-web/src/lib/__tests__/attributionAnalysisTikTokCopy.test.ts"
      provides: "attributionAnalysis ALG-01 platform-agnostic copy regression test"
    - path: "dashboard-web/src/lib/__tests__/insightsProjectedNetMtd.test.ts"
      provides: "insights ALG-01 MTD-aware projection regression test (MP-14)"
    - path: "dashboard-web/src/lib/__tests__/ordersAttributionPagingCap.test.ts"
      provides: "ordersAttribution ALG-01 open-pagination regression test"
    - path: "dashboard-web/src/lib/__tests__/productCentricViewStaleCohort.test.ts"
      provides: "ProductCentricView ALG-02 + ALG-03 regression test (HP-21)"
    - path: "dashboard-web/src/lib/__tests__/productsStaSheetsParity.test.ts"
      provides: "STA-46 + STA-47 regression test"
  key_links:
    - from: "dashboard-web/src/lib/aiReport.ts"
      to: "dashboard-web/src/lib/platformConfig.ts"
      via: "import { TIKTOK_ACTIVE_ENOUGH }"
      pattern: "import.*TIKTOK_ACTIVE_ENOUGH.*from.*platformConfig"
    - from: "dashboard-web/src/lib/products.ts"
      to: "dashboard-web/src/lib/dateValidation.ts"
      via: "import { isDate }"
      pattern: "import.*isDate.*from.*dateValidation"
    - from: "dashboard-web/src/lib/products.ts"
      to: "dashboard-web/src/lib/postgresReaders.ts"
      via: "filter symmetry: `units <= 0 && revenue <= 0 && (netRevenue ?? 0) <= 0`"
      pattern: "units.*<=.*0.*&&.*revenue.*<=.*0.*&&.*netRevenue"
    - from: "dashboard-web/src/inngest/functions/cronLive.ts"
      to: "Inngest spendOverride persist path"
      via: "spendOnly branch when shopifyOk === false but spendOverride present"
      pattern: "spendOnly|shopifyOk.*false.*spendOverride"
---

<objective>
Phase 12.2 P0/master orchestrator. Pre-flight: confirm CONTEXT committed, capture baseline (tsc + vitest), record baseline test count = 971. Post-sweep: verify target test count + tsc + scope. The three sub-plans (12.2-01, 12.2-02, 12.2-03) ship the 11 P1 fixes as atomic commits with regression tests.

Purpose: Anchor the sub-plans' wave/dependency structure + provide a single goal-backward verification target the verifier can pin against.
Output: Pre-flight baseline + post-execution verification record. No source files modified by this plan directly.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/AUDIT.md
@.planning/phases/12.2-p1-audit-fixes/12.2-CONTEXT.md
@.planning/phases/12.1-p0-audit-fixes/12.1-VERIFICATION.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md

# Same-phase sub-plans (executed by 12.2-01/02/03 in Wave 1, parallel-safe)
@.planning/phases/12.2-p1-audit-fixes/12.2-01-PLAN.md
@.planning/phases/12.2-p1-audit-fixes/12.2-02-PLAN.md
@.planning/phases/12.2-p1-audit-fixes/12.2-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 0.1: Pre-flight — confirm CONTEXT committed + baseline gates green</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/12.2-p1-audit-fixes/12.2-CONTEXT.md (operator-locked decisions D-01..D-08)
    - .planning/phases/12.1-p0-audit-fixes/12.1-VERIFICATION.md (delta-awareness: 971 baseline confirmed)
  </read_first>
  <action>
    Run the pre-flight sanity checks per CONTEXT D-06 (verification gate):

    1. Confirm CONTEXT committed:
       `git log --oneline .planning/phases/12.2-p1-audit-fixes/12.2-CONTEXT.md | head -1`
       Must return a real commit SHA (not empty).

    2. Capture baseline at HEAD (before any 12.2 work):
       ```bash
       cd dashboard-web
       npx tsc --noEmit 2>&1 | tee /tmp/12.2-tsc-baseline.log
       npm test 2>&1 | tee /tmp/12.2-vitest-baseline.log
       ```

    3. Assert baseline test count = 971 (per 12.1-VERIFICATION.md):
       `grep -E "Tests +[0-9]+ passed" /tmp/12.2-vitest-baseline.log` must report `971 passed`.

    4. Record the baseline count in a phase-local note (write to `.planning/phases/12.2-p1-audit-fixes/12.2-BASELINE.md` with a single line: `baseline: 971/971 tests, tsc clean, HEAD=<sha>` — use Write tool, NO heredoc).

    NO source files modified. NO commits. This task is a guard rail that fails the entire phase if HEAD is dirty / not green.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/12.2-p1-audit-fixes/12.2-BASELINE.md &amp;&amp;
      grep -q "baseline: 971" .planning/phases/12.2-p1-audit-fixes/12.2-BASELINE.md
    </automated>
  </verify>
  <acceptance_criteria>
    1. `git log .planning/phases/12.2-p1-audit-fixes/12.2-CONTEXT.md` shows at least 1 commit (not empty).
    2. `npx tsc --noEmit` exits 0 (no TS errors at HEAD).
    3. `npm test` reports exactly `971 passed` (per VERIFICATION report).
    4. `.planning/phases/12.2-p1-audit-fixes/12.2-BASELINE.md` exists with baseline count line.
  </acceptance_criteria>
  <done>Phase 12.2 pre-flight passed. Sub-plans 12.2-01/02/03 cleared to run in Wave 1.</done>
</task>

<task type="auto">
  <name>Task 0.2: Post-sweep verification — target gates + scope audit</name>
  <files>(none — verification only; produces a summary file)</files>
  <read_first>
    - .planning/phases/12.2-p1-audit-fixes/12.2-BASELINE.md (count to compare against)
    - .planning/phases/12.2-p1-audit-fixes/12.2-01-SUMMARY.md (Wave 1 sub-plan output)
    - .planning/phases/12.2-p1-audit-fixes/12.2-02-SUMMARY.md (Wave 1 sub-plan output)
    - .planning/phases/12.2-p1-audit-fixes/12.2-03-SUMMARY.md (Wave 1 sub-plan output)
    - .planning/AUDIT.md §Phase 12.2 table (11 P1 findings)
  </read_first>
  <action>
    AFTER all three sub-plans (12.2-01, 12.2-02, 12.2-03) report COMPLETE, run the verification gate per CONTEXT D-06:

    1. Re-run full gates:
       ```bash
       cd dashboard-web
       npx tsc --noEmit 2>&1 | tee /tmp/12.2-tsc-final.log
       npm test 2>&1 | tee /tmp/12.2-vitest-final.log
       ```

    2. Assert target test count >= 987 (971 baseline + 16 new tests minimum, 11 fixes × ~1.5 tests each):
       Extract pass count from log, assert >= 987.

    3. Scope audit (per CONTEXT D-05 — no scope creep):
       ```bash
       git diff --name-only HEAD~12..HEAD -- dashboard-web/src/ | sort -u
       ```
       Whitelist (P1 surfaces only):
         - dashboard-web/src/lib/aiReport.ts
         - dashboard-web/src/inngest/functions/cronLive.ts
         - dashboard-web/src/inngest/functions/eventBackfill.ts
         - dashboard-web/src/lib/insights.ts
         - dashboard-web/src/lib/ordersAttribution.ts
         - dashboard-web/src/lib/attributionAnalysis.ts
         - dashboard-web/src/lib/productCentricView.ts
         - dashboard-web/src/lib/products.ts
         - dashboard-web/src/lib/__tests__/*.test.ts (new test files only)
         - dashboard-web/src/inngest/functions/__tests__/*.test.ts (new test files only)

       Any file OUTSIDE this whitelist in the diff is scope creep — fail loud and surface to operator.

    4. Independent revert-sanity sample (per CONTEXT D-03 + 12.1 D-06 step 3):
       Pick 2 of the 11 fixes randomly. For each:
       ```bash
       git show <fix-commit-sha> -- <source-file> | git apply -R
       cd dashboard-web && npx vitest run <regression-test-file>
       ```
       Assert the test goes RED (>= 1 failure). Restore via `git checkout -- <source-file>`. Assert the test goes GREEN again.

    5. Per-finding presence check — assert all 11 P1 finding IDs appear in commit messages:
       ```bash
       for fid in ALG-01-aiReport ALG-02-aiReport ALG-07-aiReport INN-07 INN-14 \
                  ALG-01-attributionAnalysis ALG-01-insights ALG-01-ordersAttribution \
                  ALG-02-productCentricView ALG-03-productCentricView STA-46 STA-47; do
         git log HEAD~12..HEAD --grep="${fid%-*}" --oneline | head -1
       done
       ```
       Each finding must appear in at least one commit subject line (composite commits OK per D-04).

    6. Write `.planning/phases/12.2-p1-audit-fixes/12.2-VERIFICATION.md` summarizing:
       - Baseline (971) → Final test count
       - tsc result
       - 11 findings × commit SHA × regression test file
       - Whitelist scope-audit result
       - Independent revert-sanity result (2 samples)
       - Verdict: PASS / FAIL with rationale

    NO source files modified by this task.
  </action>
  <verify>
    <automated>
      test -f .planning/phases/12.2-p1-audit-fixes/12.2-VERIFICATION.md &amp;&amp;
      grep -qE "Verdict.*PASS" .planning/phases/12.2-p1-audit-fixes/12.2-VERIFICATION.md
    </automated>
  </verify>
  <acceptance_criteria>
    1. `npx tsc --noEmit` exits 0.
    2. `npm test` reports >= 987 passing tests (971 + 16 minimum).
    3. `git diff --name-only HEAD~12..HEAD -- dashboard-web/src/` produces 0 files outside the P1 whitelist.
    4. 2 independent revert-sanity samples confirm RED-on-revert / GREEN-on-restore.
    5. All 11 P1 finding IDs appear in commit messages.
    6. `.planning/phases/12.2-p1-audit-fixes/12.2-VERIFICATION.md` exists with `Verdict: PASS`.
  </acceptance_criteria>
  <done>Phase 12.2 closed. 11/11 P1 fixes shipped + regression tests pinning each. VERIFICATION = PASS recorded. Operator notified Phase 12.3 (8 P2 fixes) is the next candidate.</done>
</task>

</tasks>

<verification>
- Sub-plans 12.2-01, 12.2-02, 12.2-03 complete (each has its own SUMMARY.md).
- `cd dashboard-web && npx tsc --noEmit` exits 0.
- `cd dashboard-web && npm test` reports >= 987 passing tests.
- `git log HEAD~12..HEAD` shows 11+ atomic commits, each citing a P1 finding ID + `(Phase 12.2.X)` suffix per CONTEXT D-04.
- `git diff --name-only HEAD~12..HEAD -- dashboard-web/src/` contains 0 files outside P1 whitelist (no P0/P2 scope creep).
- 2 independent revert-sanity samples pass (RED → GREEN).
- `.planning/phases/12.2-p1-audit-fixes/12.2-VERIFICATION.md` written with `Verdict: PASS`.
</verification>

<success_criteria>
All 11 P1 audit findings from AUDIT.md §Phase 12.2 ship with regression tests. tsc clean. vitest green at >= 987 tests. No scope creep into 12.1 (P0) or 12.3 (P2) surface. Each P1 finding ID grep-traceable in commit log. Independent revert-sanity sampling pins TDD discipline (RED on revert proven).
</success_criteria>

<output>
After completion, write `.planning/phases/12.2-p1-audit-fixes/12.2-SUMMARY.md` mirroring the 12.1-SUMMARY format (one section per sub-plan + post-sweep verification record).
</output>
