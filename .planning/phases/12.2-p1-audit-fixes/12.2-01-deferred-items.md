# Phase 12.2-01 — Deferred / Out-of-Scope Items

**Scope:** 12.2-01 owns `dashboard-web/src/lib/aiReport.ts` only (per CONTEXT D-05).

## Pre-existing concurrent-agent WIP observed (NOT acted on)

### TSC error: `eventBackfillDstRange.test.ts`

- **File:** `dashboard-web/src/inngest/functions/__tests__/eventBackfillDstRange.test.ts`
- **Error:** `error TS2459: Module '"../eventBackfill"' declares 'dateRange' locally, but it is not exported.`
- **Owner:** Sub-plan **12.2-02** (Inngest hardening + Algorithm cleanup — INN-07 / INN-14)
- **Status:** Untracked file added by a parallel Wave 1 agent. The test imports `dateRange` from `../eventBackfill`, but that helper is not exported yet — presumably the 12.2-02 executor wrote the RED test first and will export `dateRange` (or rename) as part of the INN-14 GREEN step.
- **Decision:** Do NOT touch. Out of 12.2-01's file scope (D-05). This belongs to 12.2-02's TDD cycle.

### Stale `productCentricView.ts` + `productCentricView.test.ts` mods

- **Files:** `dashboard-web/src/lib/productCentricView.ts` (M), `dashboard-web/src/lib/__tests__/productCentricView.test.ts` (M), `dashboard-web/src/lib/__tests__/productCentricViewStaleCohort.test.ts` (??)
- **Owner:** Sub-plan **12.2-03** (PCV + Sheets — ProductCentricView ALG-02/03 + STA-46/47)
- **Status:** Modified by a parallel Wave 1 agent.
- **Decision:** Do NOT touch. Out of 12.2-01's file scope (D-05).

### TSC error: `attributionAnalysis.ts:801`

- **File:** `dashboard-web/src/lib/attributionAnalysis.ts`
- **Error:** `error TS2353: Object literal may only specify known properties, and 'platform' does not exist in type '{ label: "ad-set" | "campaign" | "ad"; name: string; metaClaim: number; ... }'`
- **Owner:** Sub-plan **12.2-02** (Algorithm cleanup — attributionAnalysis ALG-01 "Meta" literal → platform interpolation)
- **Status:** File is modified (`M`) by a concurrent 12.2-02 agent. The fix presumably needs to widen the analyzer-result type to include `platform` before the call site can pass it. Probably mid-edit.
- **Decision:** Do NOT touch. Out of 12.2-01's file scope (D-05).

### Vitest failures (3): `attributionAnalysisTikTokCopy.test.ts`

- **File:** `dashboard-web/src/lib/__tests__/attributionAnalysisTikTokCopy.test.ts`
- **Tests failing:** 3 tests under "attributionAnalysis platform-agnostic copy (ALG-01 — TikTok widening)".
- **Owner:** Sub-plan **12.2-02** (same as above — RED tests for the "Meta" literal fix).
- **Status:** RED-state TDD tests authored by concurrent 12.2-02 agent; expected to flip GREEN once the agent's attributionAnalysis.ts patch lands.
- **Decision:** Do NOT touch.

## Verification scope note

Because `eventBackfillDstRange.test.ts` is uncommitted WIP from a concurrent agent, `npx tsc --noEmit` over the full `dashboard-web/` tree currently returns a single non-aiReport error. The aiReport changes themselves pass tsc cleanly when isolated (verified by `git stash` of aiReport.ts → tsc clean → restore).

The 12.2 verifier should re-run full-tree tsc AFTER all 3 Wave 1 sub-plans have landed; that's when the cross-file gate becomes meaningful.
