---
phase: 12.2-p1-audit-fixes
plan: 3
subsystem: product-centric-view + state-products
tags: [audit-fix, P1, productCentricView, products, sheets-reader, regression-test]
requires:
  - lib/campaignProductMap.ts (allocator + ProductMap)
  - lib/campaignsAggregator.ts (Aggregated type)
  - lib/dateValidation.ts (isDate calendar validator)
  - lib/postgresReaders.ts (canonical filter symmetry source)
provides:
  - lib/productCentricView.ts: stale-mapped revenue retention + dormant member emission
  - lib/products.ts: calendar-validated date parsing + Postgres-symmetric filter
  - regression test files for both surfaces
affects:
  - components/ProductCentricView.tsx (consumes buildProductCentricView — may now render dormant members)
  - 7 type-only consumers of ProductRow (unaffected, type contract unchanged)
tech-stack:
  added: []
  patterns: [TDD red-green-revert-sanity, composite per-group commits per CONTEXT D-04, consumer-check pre-investigation per CONTEXT D-07, post-12.1.3 re-read per CONTEXT D-08]
key-files:
  created:
    - dashboard-web/src/lib/__tests__/productCentricViewStaleCohort.test.ts
    - dashboard-web/src/lib/__tests__/productsStaSheetsParity.test.ts
    - .planning/phases/12.2-p1-audit-fixes/12.2-03-CONSUMER-CHECK.md
    - .planning/phases/12.2-p1-audit-fixes/12.2-03-PCV-REREAD.md
  modified:
    - dashboard-web/src/lib/productCentricView.ts (ALG-02 + ALG-03 + minor refactor)
    - dashboard-web/src/lib/products.ts (STA-46 + STA-47)
    - dashboard-web/src/lib/__tests__/productCentricView.test.ts (1 test updated per Option A)
decisions:
  - "Option A chosen for ALG-02 + ALG-03: remove both filters, emit dormant members with zero metrics. Aligns with JSDoc lines 178-181 and fixes ALG-02 as a side effect."
  - "Composite commits per CONTEXT D-04: PCV ALG-02+ALG-03 in one commit (same file, same root cause, same test); products STA-46+STA-47 in one commit (same file, same root cause, same test)."
  - "fix-path chosen for STA-46/47 per planner's pre-confirmation: even though 0 runtime consumers exist today (fetchProductsData is dead code post Phase 05.7), 7 type-only consumers of ProductRow exist + the fix is defensive against future re-enable + provides Postgres parity."
  - "Dormant members in simplified-split branch get allocatedRevenueEstimate=0 (not platformRev/N) — active members already absorb the full platform share via intraRevShare; double-counting would break sum-conservation."
metrics:
  duration_minutes: 17
  completed_date: 2026-05-24
  tasks_completed: 4
  files_changed: 5
  tests_added: 13
  tests_baseline: 971
  tests_final: 1012
  tsc_status: clean
---

# Phase 12.2 Plan 3: ProductCentricView + Sheets Summary

**One-liner:** Eliminated revenue leak in product-centric view's stale-mapped cohort + emitted dormant cohort members per the JSDoc contract (Option A); brought lib/products.ts Sheets reader into parity with the post-Phase-11 Postgres reader (WR-04-validated parseDate + 3-way revenue surface filter).

## Tasks completed

### Task 3.1 — STA-46/47 consumer check (CONTEXT D-07)

**Outcome:** `fix-path` (proceed to Tasks 3.2 + 3.3 + 3.4)
**Doc:** `.planning/phases/12.2-p1-audit-fixes/12.2-03-CONSUMER-CHECK.md`
**Commit:** `a989cd1`

`git grep` found 7 consumers of `lib/products.ts` — all type-only `ProductRow` imports across 4 components, 3 lib modules. ZERO runtime consumers of `fetchProductsData` (the Sheets-tier reader is dead code post Phase 05.7 Postgres rollout). Technically maps to CONTEXT D-07's "type-only carve-out" branch, but per planner's `<done>` clause + operator pre-confirmation, the in-place fix is correct: the fixes are small, the type contract is unchanged, and defensive against future re-enable.

### Task 3.2 — PCV post-12.1.3 re-read (CONTEXT D-08)

**Outcome:** Both ALG-02 + ALG-03 sites STILL EXIST in current code; 12.1.3's `intraRevShare` fix did NOT cascade.
**Doc:** `.planning/phases/12.2-p1-audit-fixes/12.2-03-PCV-REREAD.md`
**Commit:** `626d211`

Grep confirmed:
- ALG-02 site (`if (!member?.agg) continue;`) at line 272 — intact.
- ALG-03 site (`.filter(r => r.agg !== undefined)`) at line 311 — intact.

12.1.3's fix lives inside the post-filter `.map()` callback at lines 312-352 (uses `r.agg!`), so it never touched the upstream filters. Confirmed by the existing `productCentricViewSumConservation.test.ts` line 180-244 comment which explicitly documents ALG-02 as DEFERRED to 12.2.

### Task 3.3 — PCV ALG-02 + ALG-03 fix (TDD composite, Option A)

**Commit:** `2a5dabd` — `fix(product-centric-view): ALG-02 + ALG-03 stale + dormant cohort members (Phase 12.2.3)`

**TDD verification:**
- **RED:** 3 of 4 new tests in `productCentricViewStaleCohort.test.ts` fail on pre-fix code (sum mismatch 1200 vs 1500, dormant members dropped 1 vs 2 expected). 4th test (ALG-01 regression guard) passes on pre-fix.
- **GREEN:** 4 of 4 new tests + 20 of 20 existing PCV tests + 5 of 5 sum-conservation tests pass.
- **Revert-sanity:** `git stash` of source only → 3 tests flip RED. Restore → all 29 GREEN.

**Implementation (Option A per evidence pack preference):**
1. `RawMember` carries `platformFromKey`, `storeIdFromKey`, `campaignIdFromKey` parsed from the campaignKey (`${storeId}::${platform}::${campaignId}`) so dormant members have display metadata.
2. `platformGroups` includes dormant-only platforms via the `r.agg?.platform ?? r.platformFromKey` fallback.
3. `platformAllocatedRevenue` accumulator uses the same fallback so stale-mapped allocator output is credited to the correct platform.
4. `members` array no longer filters dormant members; emits them with zero metrics + `allocatedRevenueEstimate` from `allocByCampaign.get(key)` (works for both branches).
5. `platformMemberCount` kept active-only to preserve the 12.1.3 `intraRevShare = 1/N` semantic (dormant members don't dilute the active members' share).
6. Existing test at line 88-103 updated per planner's "Update the line 88-103 test accordingly" instruction.

**Auto-fix (Rule 2 scope):** Removed pre-existing unused parameter `raws` in `byPlatform.map((platform, raws) => …)` callback — same file scope, harmless cleanup while editing.

### Task 3.4 — STA-46 + STA-47 fix (TDD composite)

**Commit:** `296ba36` — `fix(products): STA-46 + STA-47 calendar validation + filter symmetry (Phase 12.2.3)`

**TDD verification:**
- **RED:** 3 of 9 new tests in `productsStaSheetsParity.test.ts` fail on pre-fix code (31/02/2026 silently converted to 2026-02-31; 2026-13-01 silently accepted; refund-only row dropped). 6 regression-guard tests pass on pre-fix.
- **GREEN:** 9 of 9 tests pass on patched code.
- **Revert-sanity:** `git stash` of `products.ts` → 3 flip RED. Restore → all 9 pass.

**Implementation:**
1. **STA-46:** Imported `isDate` from `./dateValidation`. Applied round-trip calendar validation to all 3 date format branches (number, ISO, DMY). Invalid dates return `null` → row skipped.
2. **STA-47:** Filter expression aligned with `postgresReaders.ts:529`: `if (units <= 0 && revenue <= 0 && (netRevenue ?? 0) <= 0) continue;` — refund-correction rows now retained.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 — Same-file cleanup] Removed unused `raws` parameter in productCentricView.ts byPlatform map**
- **Found during:** Task 3.3 (the IDE diagnostic flagged it after my `platformGroups` edit; pre-existing dead code now visible)
- **Issue:** `Array.from(platformGroups.entries()).map(([platform, raws]) => …)` — `raws` never used inside the callback.
- **Fix:** Changed to `Array.from(platformGroups.keys()).map(platform => …)` — same semantic, no dead parameter.
- **Files modified:** `dashboard-web/src/lib/productCentricView.ts`
- **Commit:** `2a5dabd` (composite with ALG-02 + ALG-03)

### Authentication gates

None — both fixes are pure-function refactors with no external services. Tests use vi.mock for googleapis (products.ts).

### Parallel-agent coordination

Wave 1 had 3 plans running in parallel:
- **12.2-01 (aiReport):** Plan 12.2-01 agent staged files for `dashboard-web/src/lib/aiReport.ts` + 3 test files in the git index during my session. I ran `git reset HEAD` to unstage them so they didn't accidentally get into my commit. Their commit `db1212a` landed independently.
- **12.2-02 (Inngest + Algorithm):** Plan 12.2-02 modified `dashboard-web/src/lib/attributionAnalysis.ts`. I left it alone per scope discipline (CONTEXT D-05). Their commits `99c30e2`, `d1f5c31`, `a45a302` landed independently.
- **12.2-03 (this plan):** PCV + products. Disjoint file scope held — no merge conflicts.

Final test count went from 971 → 1012 (41 net new tests across all 3 wave 1 plans).

## Verification

| Item | Result |
|---|---|
| Task 3.1 consumer-check doc | `.planning/phases/12.2-p1-audit-fixes/12.2-03-CONSUMER-CHECK.md` exists, decides fix-path |
| Task 3.2 PCV-REREAD doc | `.planning/phases/12.2-p1-audit-fixes/12.2-03-PCV-REREAD.md` exists, confirms sites still apply |
| Task 3.3 new test file | `productCentricViewStaleCohort.test.ts` exists, 4 cases, all GREEN |
| Task 3.4 new test file | `productsStaSheetsParity.test.ts` exists, 9 cases, all GREEN |
| `npx tsc --noEmit` | exit 0 (clean) |
| `npm test` (full suite) | 1012/1012 passing |
| ALG-03 filter literal removed | `grep -v '^//' productCentricView.ts \| grep -c "filter(r => r.agg !== undefined)"` returns 0 |
| STA-46 + STA-47 grep | `grep -v '^//' products.ts \| grep -cE "isDate\(\|netRevenue.*<=.*0"` returns 5 (>= 2) |
| Per-task commits | 4 commits (2 docs + 2 composite fixes) |

## Commits

| Commit | Subject |
|---|---|
| `a989cd1` | `docs(12.2.3): STA-46/47 consumer check per CONTEXT D-07` |
| `626d211` | `docs(12.2.3): productCentricView post-12.1.3 re-read per CONTEXT D-08` |
| `2a5dabd` | `fix(product-centric-view): ALG-02 + ALG-03 stale + dormant cohort members (Phase 12.2.3)` |
| `296ba36` | `fix(products): STA-46 + STA-47 calendar validation + filter symmetry (Phase 12.2.3)` |

## Success criteria status

4 of 11 P1 fixes from Phase 12.2 shipped in this plan (PCV ALG-02, PCV ALG-03, STA-46, STA-47). All 4 covered by regression tests (13 net new tests). Files modified (productCentricView.ts + products.ts) are DISJOINT from 12.2-01 (aiReport.ts) and 12.2-02 (cronLive/eventBackfill/insights/attributionAnalysis/ordersAttribution) → Wave 1 parallel-safe (confirmed by clean merge with other agents' commits). CONTEXT D-07 + D-08 honored via consumer-check + post-12.1.3 re-read.

## Self-Check: PASSED

All claims verified:
- `dashboard-web/src/lib/__tests__/productCentricViewStaleCohort.test.ts`: FOUND
- `dashboard-web/src/lib/__tests__/productsStaSheetsParity.test.ts`: FOUND
- `.planning/phases/12.2-p1-audit-fixes/12.2-03-CONSUMER-CHECK.md`: FOUND
- `.planning/phases/12.2-p1-audit-fixes/12.2-03-PCV-REREAD.md`: FOUND
- Commit `a989cd1`: FOUND in git log
- Commit `626d211`: FOUND in git log
- Commit `2a5dabd`: FOUND in git log
- Commit `296ba36`: FOUND in git log
