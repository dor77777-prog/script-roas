---
phase: 05.6-ts-port-inngest-operator-console
plan: 17
type: execute
wave: 6
depends_on: [02]
files_modified:
  - dashboard-web/src/lib/postgresReaders.ts
  - dashboard-web/src/lib/__tests__/postgresReaders.test.ts
autonomous: true
requirements: [POSTGRES-READERS]
decisions_addressed: [D-E1, D-E2, D-E3]
tags: [postgres-readers, dormant, sheets-shape-parity, dual-source]

must_haves:
  truths:
    - "postgresReaders.ts exports parallel functions matching sheets.ts: fetchDailyDataFromPostgres, fetchStoreMetaFromPostgres, fetchDashboardStateFromPostgres, fetchProductsFromPostgres, fetchCampaignsFromPostgres, fetchAdsFromPostgres, fetchOrdersAttributionFromPostgres, fetchProductCatalogFromPostgres"
    - "Each function uses getSupabase() (anon — Phase 05.5 grants SELECT to anon)"
    - "Return shapes EXACTLY match sheets.ts equivalents (DailyRow, StoreMetaRow, etc.) so consumer code is unchanged"
    - "Dormant in 05.6 — no consumer wires call these yet (plan 19 adds 1-line branch in /api/* routes)"
    - "tsc passes; vitest passes"
  artifacts:
    - path: "dashboard-web/src/lib/postgresReaders.ts"
      provides: "Parallel Supabase readers mirroring sheets.ts shape"
      contains: "fetchDailyDataFromPostgres"
      min_lines: 200
    - path: "dashboard-web/src/lib/__tests__/postgresReaders.test.ts"
      provides: "Shape-parity tests + WHERE-clause coverage"
      contains: "describe.*postgresReaders"
  key_links:
    - from: "postgresReaders.ts"
      to: "@/lib/supabase"
      via: "import { getSupabase }"
      pattern: "getSupabase\\(\\)"
    - from: "postgresReaders.ts"
      to: "@/lib/types"
      via: "DailyRow, StoreMetaRow, etc."
      pattern: "import type"
    - from: "Plan 19 (/api/* route branching)"
      to: "postgresReaders.ts"
      via: "1-line conditional"
      pattern: "fetchDailyDataFromPostgres\\|fetchStoreMetaFromPostgres"
---

<objective>
Build the Postgres-reading half of the dual-source data plane. Each function in `dashboard-web/src/lib/postgresReaders.ts` returns the SAME shape as its `sheets.ts` counterpart so consumer code in `/api/*` routes can branch without changes downstream.

Purpose: Phase 05.6 builds these but DOES NOT WIRE THEM YET (D-E1 dormant). Plan 19 adds the 1-line branch in each `/api/*` route. The cut-over flip is Phase 05.7's job.

Output: 1 reader module + 1 test file. Mirrors approximately 8 fetchers from sheets.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.6-ts-port-inngest-operator-console/05.6-CONTEXT.md
@.planning/phases/05.6-ts-port-inngest-operator-console/05.6-RESEARCH.md
@dashboard-web/src/lib/sheets.ts
@dashboard-web/src/lib/types.ts
@dashboard-web/src/lib/supabase.ts
@supabase/migrations/20260521063112_initial_schema.sql
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test scaffold for postgresReaders shape parity</name>
  <files>dashboard-web/src/lib/__tests__/postgresReaders.test.ts</files>
  <behavior>
    - Test 1: fetchDailyDataFromPostgres returns DailyRow[] with all required fields (date, storeId, storeName, fbSpend, gaSpend, totalSpend, revenue, roas, grossProfit, cogs, netProfit, hasCogs)
    - Test 2: range filter (gte/lte on date) is applied
    - Test 3: derived fields (roas = revenue/totalSpend, netProfit = revenue - totalSpend - cogs) computed when DB values are null
    - Test 4: fetchStoreMetaFromPostgres returns StoreMetaRow[] (stores table → store-meta tab equivalent)
    - Test 5: fetchDashboardStateFromPostgres returns {kv, updatedAtByKey, lastUpdated} (dashboard_state → dashboard-state)
    - Test 6: Supabase error → throws with reader-name in message
  </behavior>
  <read_first>
    - `dashboard-web/src/lib/sheets.ts` — fetcher return shapes (DailyRow, StoreMetaRow, dashboard_state)
    - `dashboard-web/src/lib/types.ts` — type definitions
    - `.planning/phases/05.6-ts-port-inngest-operator-console/05.6-RESEARCH.md` §Pattern 6.5 lines 843-916
  </read_first>
  <action>
    Create test file with 6 cases. Mock supabase client per:

    ```typescript
    vi.mock('@/lib/supabase', () => ({
      getSupabase: () => ({
        from: vi.fn(() => ({
          select: vi.fn(() => ({
            gte: vi.fn(() => ({
              lte: vi.fn(() => Promise.resolve({ data: [/* rows */], error: null })),
            })),
            // Optionally chain order(), single(), etc.
          })),
        })),
      }),
    }));
    ```

    Test cases assert the return shape is identical to what `sheets.ts:fetchDailyData` returns for the same row data.

    File MUST fail (RED).
  </action>
  <verify>
    <automated>cd dashboard-web && [ -f src/lib/__tests__/postgresReaders.test.ts ] && grep -q "describe.*postgresReaders" src/lib/__tests__/postgresReaders.test.ts && grep -q "DailyRow\|fetchDailyDataFromPostgres" src/lib/__tests__/postgresReaders.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 6 it(...) blocks
    - Currently fails (RED)
  </acceptance_criteria>
  <done>RED test scaffold ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement postgresReaders.ts</name>
  <files>dashboard-web/src/lib/postgresReaders.ts</files>
  <behavior>
    - 8 exported functions mirroring sheets.ts API surface
    - Each uses getSupabase() (anon — Phase 05.5 grants SELECT)
    - Each returns the SAME shape as its sheets.ts counterpart
    - Shape parity is the cardinal invariant — Phase 05.7 flip works only if shapes match
  </behavior>
  <read_first>
    - `.planning/phases/05.6-ts-port-inngest-operator-console/05.6-RESEARCH.md` §Pattern 6.5 lines 843-916 — verbatim fetchDailyData example
    - `dashboard-web/src/lib/sheets.ts` — all exported functions (use Grep to enumerate: `grep "^export" dashboard-web/src/lib/sheets.ts`)
    - `dashboard-web/src/lib/types.ts` — DailyRow + StoreMetaRow + etc.
  </read_first>
  <action>
    Per RESEARCH §Pattern 6.5 line 847-898 as the canonical skeleton (full fetchDailyData implementation). Extend with the other 7 functions following the same pattern:

    1. `fetchDailyDataFromPostgres(opts?: {range?: DateRange}): Promise<DailyRow[]>` — verbatim from RESEARCH §Pattern 6.5
    2. `fetchStoreMetaFromPostgres(): Promise<StoreMetaRow[]>` — SELECT * FROM stores
    3. `fetchDashboardStateFromPostgres(): Promise<{kv, updatedAtByKey, lastUpdated}>` — SELECT * FROM dashboard_state; reshape into kv map
    4. `upsertDashboardStateKeyPostgres(key, value): Promise<void>` — INSERT/UPDATE into dashboard_state (uses service-role per anon SELECT-only grant — NOTE: actually this writer should use supabaseAdmin; signal as "future" since 05.6 only branches the READ path)
    5. `fetchProductsFromPostgres(opts?): Promise<ProductRow[]>` — SELECT FROM products_daily
    6. `fetchCampaignsFromPostgres(opts?): Promise<CampaignRow[]>` — SELECT FROM campaigns_daily
    7. `fetchAdsFromPostgres(opts?): Promise<AdRow[]>` — SELECT FROM ads_daily
    8. `fetchOrdersAttributionFromPostgres(opts?): Promise<OrderAttrRow[]>` — SELECT FROM orders_attribution
    9. `fetchProductCatalogFromPostgres(): Promise<ProductCatalogRow[]>` — SELECT FROM product_catalog

    Note: `upsertDashboardStateKeyPostgres` is a WRITE — should use `getSupabaseAdmin()`. But since the cut-over in 05.7 will need this, defer to a follow-up plan in 05.7 if anon SELECT doesn't suffice. For 05.6, the read functions are the priority.

    **Implementation strategy**: copy RESEARCH §Pattern 6.5 lines 847-898 for `fetchDailyDataFromPostgres` verbatim. For the other 7 functions, follow the same template:
    ```typescript
    export async function fetchXFromPostgres(opts?): Promise<XRow[]> {
      let q = getSupabase().from('table_name').select('col1, col2, ...');
      if (opts?.range) q = q.gte('date', opts.range.from).lte('date', opts.range.to);
      const { data, error } = await q;
      if (error) throw new Error(`postgresReaders.fetchX: ${error.message}`);
      return (data ?? []).map(r => ({
        // Map snake_case DB columns to camelCase shape matching sheets.ts equivalent
      }));
    }
    ```

    For `fetchDashboardStateFromPostgres`: read rows of `(key, value, updated_at)`, build `kv = { [key]: value }` and `updatedAtByKey = { [key]: updated_at }`. Match the existing `sheets.ts:fetchDashboardState` shape exactly.

    For shape parity verification, read the actual return type of each `sheets.ts` function and replicate that — `tsc` will fail at compile time if shapes drift.
  </action>
  <verify>
    <automated>cd dashboard-web && [ -f src/lib/postgresReaders.ts ] && grep -q "fetchDailyDataFromPostgres" src/lib/postgresReaders.ts && grep -q "fetchStoreMetaFromPostgres" src/lib/postgresReaders.ts && grep -q "fetchDashboardStateFromPostgres" src/lib/postgresReaders.ts && grep -q "getSupabase()" src/lib/postgresReaders.ts && [ "$(grep -c "fetchDailyData\|fetchStoreMeta\|fetchDashboardState\|fetchProducts\|fetchCampaigns\|fetchAds\|fetchOrdersAttribution\|fetchProductCatalog" src/lib/postgresReaders.ts)" -ge "8" ] && npx tsc --noEmit && npx vitest run src/lib/__tests__/postgresReaders.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 8 reader functions exported
    - Uses getSupabase() (anon)
    - Shape parity (tsc passes against existing types)
    - All 6 tests pass
  </acceptance_criteria>
  <done>Postgres readers ready — dormant until plan 19 wires the branches.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.6-17-T1 | Tampering | Shape drift between sheets.ts and postgresReaders.ts | mitigate | TypeScript enforces return-type parity. Phase 05.7's cut-over harness compares per-row outputs as the final gate. |
| T-05.6-17-I2 | Info Disclosure | Anon role exposed via REST | accept | Phase 05.5-03 documented this — URL-obscurity (D-D2). |
</threat_model>

<verification>
- `cd dashboard-web && npx tsc --noEmit` exits 0
- `cd dashboard-web && npx vitest run src/lib/__tests__/postgresReaders.test.ts` exits 0
- `grep -c "^export" dashboard-web/src/lib/postgresReaders.ts` >= 8 (at least 8 exports)
</verification>

<success_criteria>
Postgres readers exist and are shape-compatible with sheets.ts. Plan 19 wires them into `/api/*` routes. Phase 05.7 flips the default.
</success_criteria>

<output>
After completion, create `.planning/phases/05.6-ts-port-inngest-operator-console/05.6-17-SUMMARY.md` recording:
- 8 reader functions implemented
- Shape parity confirmed via tsc + 6 vitest tests
- Note for plan 19: branch pattern is `readFrom() === 'postgres' ? await readerPostgres(args) : await readerSheets(args)`
- Note for plan 22: Postgres readers exist but are dormant; cut-over is 05.7
</output>
