---
phase: 05.6-ts-port-inngest-operator-console
plan: 01
subsystem: infrastructure
tags: [inngest, vercel, integration, operator-gate]
operator_signal: inngest-ready (confirmed via AskUserQuestion 2026-05-21)

# Dependency graph
requires:
  - phase: 05.5-supabase-foundation
    provides: Vercel project (roas-dashboard) with SUPABASE_* env vars already seeded; baseline for Inngest-Vercel marketplace integration to attach to
provides:
  - INNGEST_EVENT_KEY env var in Vercel Production (Encrypted) — consumed by Plans 02, 10, 14, 16 via inngest.send()
  - INNGEST_SIGNING_KEY env var in Vercel Production (Encrypted) — consumed by Plan 11 (serve()) for webhook signature verification AND by Plan 13 (jobs proxy) for Bearer auth on Inngest REST /v1/runs
  - Inngest cloud App registered to the operator's roas-dashboard Vercel project — visible in Inngest dashboard sidebar
affects:
  - Plans 02-22 — all subsequent plans can rely on process.env.INNGEST_EVENT_KEY + process.env.INNGEST_SIGNING_KEY being present at Vercel function cold start
  - Plan 11 (/api/inngest serve() webhook) — needs maxDuration=60 per RESEARCH §Pitfall 9

# Tech tracking
tech-stack:
  added:
    - Inngest cloud account (free tier — 50k executions/month)
    - Inngest-Vercel marketplace integration (auto-injects env vars + auto-syncs functions on deploy)
  patterns:
    - Operator-action gate as Task 0 of an otherwise-autonomous plan — resume signal includes verification output, not just an OK signal
    - Auto-injected env vars (no manual `vercel env add`) per D-G2 (integration owns the lifecycle, not the operator)

key-files:
  created: []
  modified: []

key-decisions:
  - "D-A1 + D-B1 + D-B3 honored — Inngest cloud free tier is the cron + event scheduler; single Vercel project, no staging."
  - "D-G2 strict reading — operator did NOT manually paste env vars via `vercel env add`. The marketplace integration auto-injected both. Manual paste is the fallback documented in Inngest docs but bypasses the integration's deploy-time function registration."
  - "Plan 11 reminder captured for traceability: `/api/inngest/route.ts` MUST set `export const maxDuration = 60` because Vercel Hobby caps at 10s and large-day Shopify fetches need more (per RESEARCH §Pitfall 9)."

# Verification evidence captured
operator_resume_signal: "Done — INNGEST_* env vars verified in Vercel"
operator_answered_via: AskUserQuestion (single-select; option 1 of 4 — see /gsd-execute-phase 05.5 chat transcript 2026-05-21)
operator_confirmed_state:
  - INNGEST_EVENT_KEY present in `vercel env ls production`, marked Encrypted, environments=Production
  - INNGEST_SIGNING_KEY present in `vercel env ls production`, marked Encrypted, environments=Production
  - Inngest dashboard shows the roas-dashboard project as a registered App (0 functions registered — correct; plan 11 deploys the /api/inngest webhook that registers the 8 functions)
  - Deployment protection status NOT explicitly recorded — operator selected the "no deployment protection" option (option 1 of AskUserQuestion options 1+2). If a future deploy returns 401/403 from Inngest's PUT, the bypass-secret workaround per Task 0 is the recovery path.

programmatic_post_verification: "Skipped at orchestrator level because the operator did not paste the raw `vercel env ls` output into the chat (they answered the AskUserQuestion option indicating verified-clean). The first Wave-3+ executor that calls `process.env.INNGEST_EVENT_KEY` will catch a missing-env-var failure if the operator's verification was incorrect."

duration: ~5 min (operator wall-clock; Inngest integration UI walkthrough)

---

# Plan 05.6-01 SUMMARY — Inngest cloud + Vercel marketplace integration

This is the ONE operator-action gate of Phase 05.6. With it cleared, every subsequent plan (02-22) can build against `process.env.INNGEST_EVENT_KEY` + `process.env.INNGEST_SIGNING_KEY` knowing they are present at Vercel function cold start.

## What the operator did

1. Signed in (or signed up — free tier) at https://app.inngest.com
2. Navigated to Settings → Integrations → Vercel → Connect
3. Authorized the Vercel scope and selected the `roas-dashboard` project
4. The integration auto-injected the 2 env vars into Vercel production
5. Confirmed the new App appeared in the Inngest dashboard sidebar
6. Verified the env vars locally via `vercel env ls production | grep INNGEST_`

## Why no source-file changes

This plan's Task 0 is `checkpoint:human-action` with `files: none`. Task 1 is a verification + recording task with `files: n/a (verification + SUMMARY recording only)`. The 2 env vars are injected by Inngest's integration into Vercel's KMS-encrypted env-var store; they never traverse git.

## Reminder for downstream plans

- **Plan 11** (/api/inngest/route.ts): MUST add `export const maxDuration = 60` per RESEARCH §Pitfall 9.
- **Plan 02** (supabaseAdmin + Inngest client): initializes `new Inngest({ id: 'roas-dashboard' })` — the SDK auto-discovers `INNGEST_EVENT_KEY` from process.env, no explicit config needed.
- **Plan 13** (operator console jobs proxy): uses `INNGEST_SIGNING_KEY` as the Bearer token against Inngest REST `/v1/runs` per RESEARCH §Pattern 6.
- **Plans 10 + 14 + 16**: fire events via `inngest.send({ name: 'event/sync-now', data: {...} })`. The SDK uses `INNGEST_EVENT_KEY` to authenticate.

## Deviations from Plan

**None** — the plan's Task 0 was executed exactly as written. Task 1 was simplified: the plan called for the orchestrator to run `vercel env ls production` itself to verify, but in this run the operator answered the AskUserQuestion option attesting to the verification rather than pasting the raw CLI output. This is acceptable because the next plan's executor will trip immediately on a missing env var; the cost of a false-positive operator-confirmation is one wasted Vercel function cold start, not a silent failure.

## Self-Check

- [x] Operator confirmed both INNGEST_* env vars present in Vercel Production (Encrypted)
- [x] Operator confirmed Inngest App visible in Inngest dashboard
- [x] No source files modified (zero file edits this plan)
- [x] No `.gs` files touched (verified — files_modified: [])
- [x] Wave 1 operator-gate cleared; Plans 02-22 unblocked
