---
phase: 05.6-ts-port-inngest-operator-console
plan: 20
type: execute
wave: 5
depends_on: [02, 15]
files_modified:
  - dashboard-web/scripts/import-manual-overrides.ts
autonomous: false
requirements: [OVERRIDES-IMPORTER]
decisions_addressed: [D-A5, D-F1, D-F2, D-F3]
tags: [importer, one-off-script, sheets-to-supabase, idempotent, tsx, operator-action]
user_setup:
  - service: importer
    why: "Operator runs the one-off importer to migrate 38 historical manual_spend rows from Sheets → Supabase manual_overrides table."
    env_vars:
      - name: SPREADSHEET_ID
        source: "Already in Vercel (existing dashboard env var)"
      - name: GOOGLE_CLIENT_EMAIL
        source: "Already in Vercel"
      - name: GOOGLE_PRIVATE_KEY
        source: "Already in Vercel"
      - name: SUPABASE_URL
        source: "Already in Vercel from Phase 05.5-02"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Already in Vercel from Phase 05.5-02"
    dashboard_config:
      - task: "Operator runs `cd dashboard-web && npx --yes tsx scripts/import-manual-overrides.ts` from a shell with .env.local sourced (OR with env vars exported)"
        location: "Operator's local shell"

must_haves:
  truths:
    - "scripts/import-manual-overrides.ts exists and is runnable via `npx --yes tsx`"
    - "Reads manual-spend Sheets tab via the EXISTING sheets.ts:getAuth (no new auth path)"
    - "Lowercases 'Meta'/'Google' → 'meta'/'google' per RESEARCH §Pitfall 7 (matches CHECK constraint)"
    - "Upserts with onConflict: 'date,store_id,platform' + ignoreDuplicates: true (D-F1 idempotency)"
    - "Outputs summary: 'imported N, skipped M, errored P' per D-F3"
    - "First-run target: imported=38, skipped=0; second-run target: imported=0, skipped=38 (idempotent)"
  artifacts:
    - path: "dashboard-web/scripts/import-manual-overrides.ts"
      provides: "One-off Sheets→Supabase importer"
      contains: "manual_overrides"
      min_lines: 80
  key_links:
    - from: "importer"
      to: "@/lib/sheets:getAuth + @/lib/supabaseAdmin"
      via: "import"
      pattern: "getAuth\\|getSupabaseAdmin"
---

<objective>
Build the one-off importer that migrates the 38 historical `manual_spend` rows from the Sheets `manual-spend` tab to Supabase `manual_overrides`. Run ONCE by operator via `npx tsx`.

Purpose: D-A5 confirms these 38 rows are LOAD-BEARING — uzoshop's May 1-8 spend cannot be re-derived from Meta API. Without this import, the dashboard's historical analysis tab breaks once `READ_FROM=postgres`.

Output: 1 script file + a `checkpoint:human-action` task in the orchestrator's queue (operator runs the script and confirms `imported 38`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.6-ts-port-inngest-operator-console/05.6-CONTEXT.md
@.planning/phases/05.6-ts-port-inngest-operator-console/05.6-RESEARCH.md
@.planning/phases/05.6-ts-port-inngest-operator-console/05.6-02-SUMMARY.md
@.planning/phases/05.6-ts-port-inngest-operator-console/05.6-15-SUMMARY.md
@dashboard-web/src/lib/sheets.ts
@supabase/migrations/20260521063112_initial_schema.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create importer script</name>
  <files>dashboard-web/scripts/import-manual-overrides.ts</files>
  <read_first>
    - `.planning/phases/05.6-ts-port-inngest-operator-console/05.6-RESEARCH.md` §Pattern 11 lines 1199-1294 — verbatim skeleton
    - `dashboard-web/src/lib/sheets.ts` lines 45-69 (getAuth — already exported per Phase 05.5-03)
    - `.planning/phases/05.6-ts-port-inngest-operator-console/05.6-RESEARCH.md` §Pitfall 7 (case normalization)
    - `.planning/phases/05.6-ts-port-inngest-operator-console/05.6-RESEARCH.md` §Assumption A10 (verify row count = 38)
  </read_first>
  <action>
    Verbatim from RESEARCH §Pattern 11 lines 1204-1292. Key invariants:

    1. **Reads from Sheets `manual-spend` tab** via `getAuth()` from `@/lib/sheets`
    2. **Writes via `getSupabaseAdmin()`** with `.upsert(rows, { onConflict: 'date,store_id,platform', ignoreDuplicates: true })`
    3. **Lowercases platform**: `String(platformRaw).toLowerCase()` → matches CHECK constraint per RESEARCH §Pitfall 7
    4. **Uppercases currency**: `String(currency).trim().toUpperCase()`
    5. **Parses date defensively**: accept YYYY-MM-DD strings AND Date objects (Sheets sometimes returns either)
    6. **Outputs summary**: `console.log("Imported N, skipped M, errored P")`
    7. **Exit code 1 on fatal error**, 0 on success
    8. **Idempotent**: re-running must report "imported 0, skipped 38, errored 0" on second run

    The script does NOT need `.env.local` auto-load — RESEARCH §Pattern 11 line 1213-1216 explains operator either sources env vars or uses `dotenv -e .env.local`. Add `import 'dotenv/config'` only if `dotenv` is already a transitive dep (check `package.json`); otherwise rely on operator-sourced env.

    Per RESEARCH §Assumption A10: before the operator runs the script, they should `wc -l` the manual-spend tab to confirm exactly 38 rows (the load-bearing count). If different, document the actual count in the SUMMARY.

    **Important** (per the project memory rule about no-localhost): the script uses Sheets API + Supabase REST — both are remote endpoints. No localhost touching.

    File starts with a shebang-style banner comment + JSDoc explaining the run command and idempotency contract.
  </action>
  <verify>
    <automated>cd dashboard-web && [ -f scripts/import-manual-overrides.ts ] && grep -q "manual_overrides" scripts/import-manual-overrides.ts && grep -q "onConflict: 'date,store_id,platform'" scripts/import-manual-overrides.ts && grep -q "ignoreDuplicates: true" scripts/import-manual-overrides.ts && grep -q "getAuth" scripts/import-manual-overrides.ts && grep -q "getSupabaseAdmin" scripts/import-manual-overrides.ts && grep -q "toLowerCase" scripts/import-manual-overrides.ts && grep -q "Imported.*skipped.*errored" scripts/import-manual-overrides.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - Script file exists
    - Reads from Sheets via getAuth (existing helper)
    - Writes to manual_overrides via getSupabaseAdmin
    - Lowercases platform (Pitfall 7)
    - Idempotent (ignoreDuplicates)
    - Summary output in expected format
    - tsc passes (the script compiles even though it's a one-off — keeps quality bar consistent)
  </acceptance_criteria>
  <done>Importer script ready; operator runs it via the checkpoint below.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator runs the importer</name>
  <files>none (operator action — produces a Supabase data change, not a repo file)</files>
  <what-to-do>
    From a shell on the operator's machine, with `.env.local` or env vars exported (SPREADSHEET_ID, GOOGLE_CLIENT_EMAIL, GOOGLE_PRIVATE_KEY, SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY):

    ```bash
    cd dashboard-web
    npx --yes tsx scripts/import-manual-overrides.ts
    ```

    Expected first-run output:
    ```
    Reading from Sheets: manual-spend tab
    Parsed 38 rows from Sheets
    Imported 38 rows, skipped 0 (already present), errored 0
    ```

    Then run it AGAIN to verify idempotency:

    ```bash
    npx --yes tsx scripts/import-manual-overrides.ts
    ```

    Expected second-run output:
    ```
    Imported 0 rows, skipped 38 (already present), errored 0
    ```

    Note: per RESEARCH §Assumption A10, the row count is expected to be 38 (uzoshop May 1-8 + miscellaneous backfills). If the Sheets manual-spend tab has been edited since 2026-05-08, the count may differ — document the actual count in the resume signal.

    Verify in Supabase:
    ```bash
    curl -s "https://npegxufdupooqovrewyb.supabase.co/rest/v1/manual_overrides?select=count" \
      -H "apikey: $SUPABASE_ANON_KEY" \
      -H "Authorization: Bearer $SUPABASE_ANON_KEY" \
      -H "Prefer: count=exact" -I | grep -i 'content-range'
    ```

    Expected: `content-range: 0-37/38` (or whatever the actual count is).
  </what-to-do>
  <how-to-verify>
    - First run output: `Imported 38 rows, skipped 0, errored 0` (or document actual count)
    - Second run output: `Imported 0 rows, skipped 38, errored 0` (idempotent)
    - `manual_overrides` table count = 38 (or actual) — verifiable via Supabase REST or Studio
  </how-to-verify>
  <resume-signal>Paste the first-run and second-run output lines, plus the `manual_overrides` row count from Supabase REST. Resume with `imported-{count}`.</resume-signal>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.6-20-T1 | Tampering | Importer parses 'Meta' (capitalized) but CHECK constraint rejects | mitigate | toLowerCase() per Pitfall 7. Test: second run should report skipped=38 not errored. |
| T-05.6-20-I2 | Info Disclosure | Service-role key in operator's local shell | accept | One-off run; key never leaves operator's environment. Operator's responsibility per URL-obscurity model. |
| T-05.6-20-D3 | DoS | Importer hangs on Sheets API | mitigate | Single-shot; if hangs, operator Ctrl+C and re-runs (idempotent). |
| T-05.6-20-S4 | Spoofing | If operator runs importer from wrong shell (no env vars), script fails fast | mitigate | sheets.ts:getAuth + supabaseAdmin both throw on missing env vars. |
</threat_model>

<verification>
- `cd dashboard-web && npx tsc --noEmit` exits 0 (script compiles)
- Operator confirms first-run: imported=38 (or documented actual count)
- Operator confirms second-run: skipped=38, imported=0 (idempotent)
- Supabase REST confirms `manual_overrides` row count matches
</verification>

<success_criteria>
38 historical manual_overrides rows present in Supabase. Plan 21's snapshot tests + plan 22's User Manual reference this state.
</success_criteria>

<output>
After completion, create `.planning/phases/05.6-ts-port-inngest-operator-console/05.6-20-SUMMARY.md` recording:
- Script content
- Operator resume signal (first + second run output verbatim)
- Final manual_overrides row count
- If row count != 38, document why (e.g., operator added rows since 2026-05-08)
- Note for plan 21: snapshot tests for Meta spend on uzoshop May 1-8 must account for manual_overrides (the actual fb_spend_cad will come from the overrides, not Meta API)
</output>
