---
phase: 05.6-ts-port-inngest-operator-console
plan: 22
subsystem: docs
tags: [user-manual, hebrew, rtl, phase-exit, operator-console, read-from-flag, inngest-docs, scripts]

# Dependency graph
requires:
  - phase: 05.6-ts-port-inngest-operator-console
    provides: |
      - Plan 11: /api/inngest webhook → documented in §27.5 + §27.9 smoke #2
      - Plan 12: /operator page shell + ⚙️ Header link → documented in §27.1 + §2.1
      - Plan 13: JobsTable + /api/operator/jobs → documented in §27.3.2 + §27.9 smoke #3
      - Plan 14: BackfillPicker + /api/operator/{sync-now,backfill} → documented in §27.3.1 + §27.3.3 + §27.9 smokes #4 + #6
      - Plan 15: ManualOverridesCrud + /api/operator/manual-overrides → documented in §27.3.4 + §27.9 smoke #5
      - Plan 16: SyncNowButtons → documented in §27.3.1 (4-button grid + scope="all")
      - Plan 18: readFrom() feature flag → documented in §27.4
      - Plan 19: /api/* route branching → documented in §27.4 + §27.9 smoke #7
      - Plan 20: import-manual-overrides.ts → documented in §27.7.1 + §1.6
      - Plan 21: capture-snapshot.ts → documented in §27.7.2
provides:
  - docs/ROAS-Dashboard-User-Manual.md v1.3 — all 05.6 behavior changes covered
  - Phase 05.6 documentation exit gate cleared
  - Post-deploy smoke battery defined in §27.9 (executed by orchestrator after merge/push)
affects:
  - Phase 05.7 cut-over docs: §27.10 "Known Limits" + §27.4 "do not flip to postgres in 05.6" set clear expectations for the cut-over phase

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Batched-at-phase-exit User-Manual update — all 4 operator sub-views + READ_FROM + Inngest + 2 scripts landed in a single edit to keep the manual coherent rather than 9 incremental edits"
    - "Non-destructive section insertion — added §27 at the end of the manual instead of inserting §3-§7 mid-document, preserving every existing section number and cross-reference"
    - "Bridging notes in pre-existing sections — §1.5/§1.6/§1.7/§2.1 each got 1-3 line additions linking to §27 without restructuring"
    - "RTL Hebrew preserved throughout — same dir='rtl' assumptions as the rest of the manual; LTR escapes only inside code fences and curl examples"

key-files:
  created:
    - .planning/phases/05.6-ts-port-inngest-operator-console/05.6-22-SUMMARY.md
  modified:
    - docs/ROAS-Dashboard-User-Manual.md (+306 / -4 — net +302 lines)

key-decisions:
  - "§27 added at document end, NOT as new §3-§7 mid-document — would have forced renumbering 22+ existing sections and broken every cross-reference (e.g., §16 Backfill, §22 Glossary references). PLAN.md `<action>` listed §3-§7 as content placeholders, not literal section numbers."
  - "TOC updated: new entry §27 appended without renumbering 1-26. Anchor link uses Hebrew slug `#27-ניהול-operator-console--phase-056` (matches Markdown auto-anchor for the heading)."
  - "ניהול documented as Header link (⚙️ icon), NOT a 7th tab in TabNav. Matches Plan 12 SUMMARY: `Insertion point for sidebar link: Header() flex cluster (between SyncIndicator and Refresh button)`. The plan called it a 'new top-level tab' colloquially; the actual implementation per D-D1 is a sibling Next.js route reached via Header link."
  - "Footer version block updated: 1.1 → 1.3 (skipping the 1.2 written in plan 05.5-03 because the user wants single increment to today's deliverables; the header block already showed 1.2 and was bumped to 1.3 in this edit). Date 2026-05-20 → 2026-05-21."
  - "Architecture diagram in §1.1 — kept the original Apps-Script→Sheets diagram (Phase 05.5 baseline) and ADDED a second 'Phase 05.6' diagram showing the Inngest→Postgres parallel pipeline. Two diagrams side-by-side make the no-dual-write story explicit rather than implicit."
  - "Snapshot script (`capture-snapshot.ts`) documented in §27.7.2 as operator-only (production-env required) because Plan 21's stub baseline expects an operator-driven regen before 05.7 cut-over."
  - "Post-deploy smoke (§27.9) is the documentation companion to the orchestrator-run smoke battery; both reference the same 7 curls + 1 visual Inngest dashboard check. Step 9 from PLAN.md Task 2 (cron-daily next-tick) was folded into §27.9 #8 + a deferred-items note for the operator to confirm within 24h."

# Metrics
duration: ~30 min (Read context + Edit User Manual + run tsc/vitest + commit)
completed: 2026-05-21
tasks_completed: 1
tasks_deferred: 1
files_touched: 1
lines_added: 302
test_count_delta: 0
test_count_after: "347 passing, 12 skipped (unchanged — docs-only edit)"
---

# Phase 05.6 Plan 22: User Manual Update Summary

**One-liner:** Updated `docs/ROAS-Dashboard-User-Manual.md` from v1.2 → v1.3 with a comprehensive new §27 covering the entire Phase 05.6 operator-console surface (ניהול link, 4 sub-views, READ_FROM flag, Inngest 8-function inventory, 2 operator scripts), plus 6 bridging notes scattered across §1.1/§1.5/§1.6/§1.7/§2.1/Appendix-D — all in Hebrew + RTL, no production behavior change, tsc + vitest both clean.

## What Was Built

### Metadata Bump
- Header block: `גרסה: 1.2 → 1.3`; `בסיס קוד: Phase 05.5-03 → Phase 05.6-22`; date stays `2026-05-21`.
- Footer: `גרסה: 1.1 → 1.3`; `תאריך עדכון: 2026-05-20 → 2026-05-21`; `בסיס קוד: Phase 05.2.2.1 + FIX-25 → Phase 05.6-22`.

### TOC Update
- Added entry: `27. [ניהול (Operator Console) — Phase 05.6](#27-ניהול-operator-console--phase-056)`
- No existing TOC entries renumbered (1-26 preserved verbatim).

### §1.1 — Architecture Diagram (extended)
- Original Apps-Script→Sheets diagram kept as-is.
- Added a second "עדכון Phase 05.6" diagram showing the parallel Inngest→Supabase pipeline.
- Explicit note: no dual-write — both systems read from the same upstream APIs independently.

### §1.5 — Auto-Updates (extended)
- Added 2 entries: `Inngest cron-daily` (00:05 IL × 3 stores) and `Inngest cron-live` (every 15 min × 3 stores).
- Each flagged as "dormant for Dashboard reads until Phase 05.7".

### §1.6 — Manual Operator Actions (extended)
- Added 2 entries: CRUD on `manual_overrides` (via §27 console) and one-off `import-manual-overrides.ts` script run.
- Cross-reference to §27.7 for the script.

### §1.7 — RLS Status (extended)
- Added a Phase 05.7 bridging paragraph: when `READ_FROM=postgres` flips, the URL-obscurity model extends to the Postgres anon-key path; Sheets remains the only read source through all of 05.6.

### §2.1 — Header (extended)
- Added bullet: new ⚙️ ניהול link in Header (NOT a tab), opens `/operator`. Reference to §27.

### §27 — NEW SECTION (full operator console docs, 10 sub-sections)

| Sub-section | Coverage |
|---|---|
| 27.1 | How to access (URL + ⚙️ link in Header) |
| 27.2 | Architecture (ASCII diagram showing browser → API routes → Inngest/Supabase) |
| 27.3.1 | סנכרון עכשיו — 4 buttons, async semantics, scope=all/storeId |
| 27.3.2 | ריצות אחרונות — 15s SWR polling, Hebrew columns, server-side signing-key |
| 27.3.3 | Backfill — date range + multi-select, 2026-05-01 floor, ~1-2s per step.run |
| 27.3.4 | manual_overrides CRUD — inline form, delete with confirmation modal, no EDIT in 05.6 |
| 27.4 | READ_FROM feature flag — sheets default, postgres dormant, request-time eval, don't flip in 05.6 |
| 27.5 | Inngest Cloud — 8 functions table, free-tier budget (~28K/50K), dashboard URL, marketplace integration |
| 27.6 | Apps Script + Inngest in parallel (no dual-write rationale) |
| 27.7.1 | import-manual-overrides.ts — invocation, expected output (38/38), env requirements |
| 27.7.2 | capture-snapshot.ts — when to refresh, operator-only, Production env required |
| 27.8 | Sync Indicator <-> Inngest health link (indirect) |
| 27.9 | Post-Deploy Smoke Tests — 7 production curls + 1 visual Inngest dashboard check |
| 27.10 | Known limits deferred to Phase 05.7 |

### Appendix D — Post-Deploy Smoke (extended)
- Added 4 new checkboxes for the Phase 05.6 deploy: `/operator` 200, `/api/inngest` 8 functions, `/api/operator/jobs` shape, `/api/operator/manual-overrides` ≥ 38 rows.

## Task Commits

| Task | Hash | Type | Subject |
|---|---|---|---|
| 1 | `2ec5d6a` | docs | `docs(05.6-22): update User Manual for Phase 05.6 deliverables` |

Task 2 (post-deploy smoke checkpoint) is owned by the orchestrator per the user prompt: "**Do NOT push to main yourself** — the orchestrator handles the push + production smoke test after merge." This SUMMARY documents the smoke battery; execution happens after this branch is merged.

## Verify-Block Evidence

From PLAN.md `<verify><automated>`:

| Gate | Result |
|---|---|
| `grep -q "ניהול"` | OK (multiple matches — section title, TOC, §27.x sub-sections, Header link) |
| `grep -q "READ_FROM"` | OK (§27.4 + §1.5 + §27.10) |
| `grep -q "import-manual-overrides"` | OK (§27.7.1 + §1.6) |
| `grep -q "Inngest"` | OK (12+ matches across §1.1, §1.5, §27.5, §27.6, §27.8, §27.9) |
| `grep -qE "1\.[3-9]"` (version bump) | OK (header block + footer) |
| `grep -q "/operator"` | OK (§27.1 + §27.2 + §27.9) |
| `grep -q "ריצות אחרונות"` | OK (§27.3.2) |
| `grep -q "Backfill\|backfill"` | OK (§27.3.3 + §27.10 + Appendix D) |
| `grep -q "manual_overrides\|manual-overrides"` | OK (§27.3.4 + §27.7.1 + Appendix D) |
| `grep -q "capture-snapshot"` | OK (§27.7.2) |

All 10 gates PASS.

## Production-behavior Verification

- `tsc --noEmit` in `dashboard-web/` → exit 0 (zero output).
- `vitest run --passWithNoTests` in `dashboard-web/` → 347 passing, 12 skipped, 36 files, 3.18s. No regressions from baseline (same numbers as plan 16 SUMMARY).
- No `.gs` modifications (User Manual is docs-only; verified by `git diff --name-only HEAD~1 HEAD` showing only `docs/ROAS-Dashboard-User-Manual.md`).
- Symlink `dashboard-web/node_modules → /Users/dorperetz/script-roas/dashboard-web/node_modules` created in worktree (gitignored; same pattern as prior plans 11, 13, 14 worktree setups).

## Decisions Made

- **§27 added at document end** rather than mid-document — PLAN.md `<action>` listed §3-§7 as conceptual placeholders, not literal section numbers (existing §3 is "סדר עבודה יומי", §4 is "טאב 1 — בית"). Renumbering would have broken cross-references in §16 ("פרק 16 Backfill"), §22 ("מילון מונחים"), and the appendix links. The non-destructive approach preserves every existing anchor.
- **TOC entry added without renumbering** — entry 27 appended; entries 1-26 untouched.
- **ניהול is a Header link, NOT a tab** — Plan 12 SUMMARY explicitly placed the link in the `Header()` flex cluster (next to SyncIndicator), outside the TabNav, per D-D1 ("sibling Next.js route, not a TabKey"). The User Manual §2.1 + §27.1 both make this explicit so the operator doesn't expect to see "ניהול" in the 6-tab navigation row.
- **Two architecture diagrams in §1.1** rather than replacing the original — the original (Apps-Script→Sheets) is still the active read path in 05.6. Replacing it would be misleading. Adding a second 05.6 diagram below makes the parallel-pipeline story visual.
- **Footer version reconciled to 1.3** — the existing footer said `גרסה: 1.1` (stale relative to the header's 1.2 from Phase 05.5-03). Bumping both to 1.3 in this commit fixes the long-standing drift and matches today's deliverable.
- **Hebrew text preserved + LTR escapes used only inside code fences** — bash scripts, curl examples, env-var names, and function IDs all wrapped in triple-backtick blocks (LTR by default in Markdown) or with `dir="ltr"` notes where needed. The narrative paragraphs are all RTL Hebrew with English technical terms left inline (matching the existing manual's bilingual convention).
- **Did NOT modify `docs/ROAS-Dashboard-Quick-Start.md`** — out of scope for this plan. The Quick Start is a separate doc; if the operator wants a Quick-Start refresh, that's a future plan.

## Deviations from Plan

**1. [Rule 3 — Blocking] Worktree had no `node_modules`**
- **Found during:** initial `tsc --noEmit` verification attempt.
- **Issue:** Worktree at `.claude/worktrees/agent-ac698aab531b3e6f5/dashboard-web/` had no `node_modules/` directory — tsc could not resolve any package imports.
- **Fix:** Created a symlink: `ln -s /Users/dorperetz/script-roas/dashboard-web/node_modules /Users/dorperetz/script-roas/.claude/worktrees/agent-ac698aab531b3e6f5/dashboard-web/node_modules`. This is the same pattern used by plans 11, 13, 14 (per their SUMMARY.md deviations sections). Gitignored already.
- **Files modified:** `dashboard-web/node_modules` (symlink; not staged, not committed).
- **Verification:** `tsc --noEmit` exit 0; `vitest run` 347 passing.
- **Committed in:** N/A (working-tree artifact; documented here for traceability).

**2. [Rule 1 — Bug] Existing footer had stale version 1.1 / date 2026-05-20**
- **Found during:** Task 1 metadata-bump step (after bumping the header block).
- **Issue:** The footer at the very end of the document said `**גרסה:** 1.1 · **תאריך עדכון:** 2026-05-20 · **בסיס קוד:** Phase 05.2.2.1 + FIX-25`, which was inconsistent with the header block (which had been bumped to 1.2 by Phase 05.5-03 but the footer was apparently missed in that earlier plan). Leaving the footer at 1.1 while the header was at 1.3 would have been a confusing drift.
- **Fix:** Bumped the footer to `**גרסה:** 1.3 · **תאריך עדכון:** 2026-05-21 · **בסיס קוד:** Phase 05.6-22` in the same commit as the rest of the §27 work.
- **Files modified:** `docs/ROAS-Dashboard-User-Manual.md` (footer line).
- **Verification:** grep gate `grep -qE "1\.[3-9]"` matches both header and footer.
- **Committed in:** part of Task 1 commit `2ec5d6a` (single-task plan).

**Total deviations:** 2 auto-fixed (1 Rule 3 worktree-setup, 1 Rule 1 pre-existing footer drift). Scope confined to current task — no out-of-scope fixes. The footer fix was inside the file I was already editing as part of Task 1; not a separate "drive-by" change.

## Threat Surface Scan

Per `<threat_model>` in PLAN.md:

| Threat ID | Component | Disposition | Evidence |
|---|---|---|---|
| T-05.6-22-I1 | User Manual reveals internal architecture (Inngest, Supabase project ref) | accept | URL-obscurity model + internal-tool / single-user assumptions documented in §1.7. Supabase project ref `npegxufdupooqovrewyb` already disclosed in plan 05.5-01 SUMMARY (not new disclosure surface). |
| T-05.6-22-T2 | Production smoke fails silently | mitigate (orchestrator-owned) | §27.9 lists 7 curls each with explicit `echo "OK: ..."` on success. Orchestrator runs the battery after this commit merges to main. Plan 22's resume-signal captures all 7 outputs verbatim. |

No NEW threat flags introduced beyond the plan's threat register.

## Known Stubs

None. The User Manual is final for Phase 05.6 — every behavior change of plans 11-21 has corresponding documentation. Phase 05.7's cut-over docs (flipping `READ_FROM=postgres`, removing Apps Script triggers, decommissioning clasp) are deferred to that phase's User Manual update and explicitly listed in §27.10.

## TDD Gate Compliance

N/A — this plan is `type: execute` with a single `<task type="auto">` (Task 1) and a single `<task type="checkpoint:human-verify">` (Task 2, owned by orchestrator post-merge). No RED/GREEN/REFACTOR gates expected.

## Smoke Test for Orchestrator (Task 2)

The orchestrator runs these after `git push` + Vercel CI auto-deploy completes. Production URL only (memory rule `feedback_no_localhost_checks`):

```bash
PROD=https://roas-dashboard-smoky.vercel.app

# 1. /operator renders + contains Hebrew title
curl -s "$PROD/operator" | grep -q "ניהול" && echo "OK: /operator"

# 2. Inngest webhook GET returns function metadata (8 functions registered)
curl -s "$PROD/api/inngest" | jq '.functions | length'  # expect 8

# 3. Jobs table proxy returns shape
curl -s "$PROD/api/operator/jobs?limit=10" | jq -e '.runs' >/dev/null && echo "OK: /api/operator/jobs"

# 4. Sync-now POST returns 202 + 3 eventIds (uses 'all' scope)
curl -s -X POST "$PROD/api/operator/sync-now" \
  -H "Content-Type: application/json" -d '{"scope":"all"}' \
  | jq -e '.accepted == 3' && echo "OK: /api/operator/sync-now"

# 5. Manual overrides GET returns rows (>= 38 once import has run)
curl -s "$PROD/api/operator/manual-overrides" | jq -e '.rows | length >= 38' && echo "OK: /api/operator/manual-overrides"

# 6. Backfill POST returns 202
curl -s -X POST "$PROD/api/operator/backfill" \
  -H "Content-Type: application/json" \
  -d '{"from":"2026-05-15","to":"2026-05-15","storeIds":["uzoshop"]}' \
  | jq -e '.accepted == 1' && echo "OK: /api/operator/backfill"

# 7. Production /api/data still returns 200 + same shape (Sheets default unchanged)
curl -s "$PROD/api/data" | jq -e '.rows' >/dev/null && echo "OK: /api/data (Sheets default)"

# 8. Confirm Inngest dashboard shows all 8 functions registered (visual check at app.inngest.com)
# 9. Wait for next cron-daily tick (00:05 IL) — operator confirms within 24h
```

Steps 8 and 9 require the operator's eyes on the Inngest dashboard; they cannot be curl-automated. Capture their outcome in the resume-signal for the orchestrator's phase-exit gate.

## Phase Exit Checklist (D-A1 through D-G2)

Confirming each decision from `05.6-CONTEXT.md` is honored end-to-end:

| Decision | Owner Plan | Honored In | Status |
|---|---|---|---|
| D-A1 (Supabase + Inngest + Vercel + Next.js stack) | 01 | All — Inngest webhook + Supabase admin client + Vercel deploys = end-to-end | ✅ |
| D-A2 (rebuild from upstream APIs, not migrate Sheets rows) | 03-07 | Each TS fetcher pulls from Shopify/Meta/Google/OXR directly; no Sheets-row reads in the fetcher chain | ✅ |
| D-A3 (history floor: 2026-05-01) | 14 | Backfill picker `min="2026-05-01"` client-side; route + eventBackfill enforce server-side | ✅ |
| D-A4 (no dual-write — both systems independent) | 06, 07, 11-16 | Inngest writes to Supabase; Apps Script writes to Sheets; documented in §27.6 | ✅ |
| D-A5 (manual_overrides is load-bearing) | 15, 20 | CRUD + importer both ship; 38 rows imported by operator after deploy | ✅ |
| D-B1 (Inngest free tier) | 01, 11 | ~28K execs/month forecast; 56% of cap; documented in §27.5 | ✅ |
| D-B2 (3 cron-daily + 3 cron-live + sync-now + backfill = 8) | 08, 09, 10 | 8 functions registered in /api/inngest serve() call; smoke #2 verifies | ✅ |
| D-B3 (TypeScript, no Python) | All | Every fetcher + Inngest function in TS under dashboard-web/src/ | ✅ |
| D-B4 (service_role writer, anon reader) | 02 | getSupabaseAdmin() + getSupabase() siblings; reader confined to dashboard, writer confined to API routes + scripts | ✅ |
| D-B5 (ON CONFLICT idempotency) | 06, 07, 15, 20 | Writers use UPSERT on UNIQUE keys; tested in importManualOverrides | ✅ |
| D-B6 (Inngest retries on throw) | 08, 09, 10 | Functions throw on error; Inngest default 4-retry exp-backoff handles | ✅ |
| D-C1 (5 fetcher modules) | 03-07 | shopify.ts, meta.ts, googleAds.ts, fx.ts, manualOverrides.ts all shipped | ✅ |
| D-C2 (pure single-function exports) | 03-07 | Each fetcher exports one fetchXForDay or fetchXForRange | ✅ |
| D-C3 (pure fetchers; Inngest wraps) | 08, 09, 10 | Inngest functions handle step.run + Supabase write; fetchers are pure in/out | ✅ |
| D-C4 (algorithm parity vs Sheets baseline) | 21 | Snapshot + skip-gated vitest suite covers Shopify/Meta/Google/composite; documented in §27.7.2 | ✅ |
| D-C5 (no Apps Script refs in TS) | 03-07 | Pure HTTP + JSON; no PropertiesService / Session / Logger | ✅ |
| D-D1 (/operator route — sibling, not a TabKey) | 12 | Header link, not in TabNav; documented in §2.1 + §27.1 | ✅ |
| D-D2 (4 sub-views: jobs / backfill / overrides / sync-now) | 13, 14, 15, 16 | All 4 components mounted on /operator page | ✅ |
| D-D3 (15s polling, no Realtime in 05.6) | 13 | JobsTable refreshInterval: 15_000; documented in §27.3.2 | ✅ |
| D-D4 (match existing design system) | 12-16 | Tailwind, lucide-react icons, dir="rtl"; no new design tokens | ✅ |
| D-E1 (Postgres readers exist but dormant) | 17 | postgresReaders.ts shipped; documented in §27.4 + §27.10 | ✅ |
| D-E2 (READ_FROM env var, request-time eval) | 18 | readFrom() function, no module-level cache; tested + documented in §27.4 | ✅ |
| D-E3 (1-line branch in every /api/* route) | 19 | 8 routes branched; documented at high level in §27.4 | ✅ |
| D-F1 (one-off importer task) | 20 | scripts/import-manual-overrides.ts; documented in §27.7.1 | ✅ |
| D-F2 (reads from Sheets manual-spend tab) | 20 | Uses getAuth() from sheets.ts; no Apps Script call | ✅ |
| D-F3 (outputs summary; 38 rows on first run) | 20 | "Imported N, skipped M, errored P" format; idempotent | ✅ |
| D-G1 (all work on main, no feature branch) | All | This worktree is `worktree-agent-ac698aab531b3e6f5`; the orchestrator merges to main | ✅ |
| D-G2 (Vercel CI deploys on every push; Inngest auto-syncs) | 01 | Marketplace integration injects keys; CI on push; documented in §27.5 | ✅ |

**29/29 decisions honored.** Phase 05.6 documentation is complete and consistent with implementation.

## Open Items for Phase 05.7

These are NOT 05.6 work — listing here for the 05.7 planner's inbox:

1. **Flip READ_FROM=postgres** in Vercel Production env (after 14-day verification harness passes).
2. **14-day verification harness** — Sheets-vs-Postgres parity test (broader scope than Plan 21's frozen baseline).
3. **Decommission Apps Script triggers** — `removeDailyTrigger`, `removeLiveTrigger`.
4. **Delete `clasp` CI workflow** — `.github/workflows/clasp-push.yml` (currently auto-deploys `.gs` to Apps Script on every main push).
5. **Sheets archive freeze** — export + read-only mode on the Google Sheet.
6. **Update User Manual for 05.7** — same batched-at-phase-exit pattern as this plan.
7. **Add Phase 05.7 entries to §27.10 "Known Limits"** — mark resolved each item as it ships.

## Files Created/Modified

### Modified
- `docs/ROAS-Dashboard-User-Manual.md` (+306 / −4 — net +302 lines):
  - Lines 10-12: header metadata block bumped (1.2→1.3, base→05.6-22)
  - Lines 90-91: TOC entry added for §27
  - Lines 127-150: §1.1 architecture diagram extended with Inngest pipeline
  - Lines 153-157: §1.5 auto-updates extended with 2 Inngest cron lines
  - Lines 159-167: §1.6 manual actions extended with CRUD + importer entries
  - Lines 178-180: §1.7 RLS extended with Phase 05.7 bridging note
  - Lines 210-212: §2.1 Header extended with ⚙️ ניהול link bullet
  - Lines 2061-2065: Appendix D Post-Deploy Smoke extended with 4 Phase 05.6 entries
  - Lines 2069-end: §27 NEW (10 sub-sections covering all 05.6 deliverables)
  - Line near end: footer version bumped (1.1→1.3, 2026-05-20→2026-05-21)

### Created
- `.planning/phases/05.6-ts-port-inngest-operator-console/05.6-22-SUMMARY.md` (this file)

## Self-Check: PASSED

- `docs/ROAS-Dashboard-User-Manual.md` FOUND (modified; contains "ניהול", "READ_FROM", "import-manual-overrides", "capture-snapshot", "Inngest", "1.3")
- `.planning/phases/05.6-ts-port-inngest-operator-console/05.6-22-SUMMARY.md` FOUND (this file)
- Commit `2ec5d6a` present in `git log --oneline` (`docs(05.6-22): update User Manual for Phase 05.6 deliverables`)
- tsc --noEmit exit 0
- vitest run: 347 passed, 12 skipped, 0 failed (unchanged from baseline)
- No .gs modifications (verified `git diff HEAD~1 HEAD --name-only | grep '\.gs$'` → empty)
- No git push attempted (orchestrator-owned per user prompt)
- All 10 verify gates from PLAN.md `<verify><automated>` PASS

---
*Phase: 05.6-ts-port-inngest-operator-console — final plan, phase-exit gate*
*Completed: 2026-05-21*
