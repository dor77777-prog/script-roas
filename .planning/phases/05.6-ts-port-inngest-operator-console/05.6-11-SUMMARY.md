---
phase: 05.6-ts-port-inngest-operator-console
plan: 11
subsystem: infra
tags: [inngest, webhook, nextjs-route, serve, vercel-max-duration, app-router]

# Dependency graph
requires:
  - phase: 05.6-ts-port-inngest-operator-console
    provides: |
      - Plan 01: INNGEST_SIGNING_KEY auto-injected to Vercel Production env (Encrypted) via the Inngest-Vercel marketplace integration. Auto-read by serve() at request time.
      - Plan 02: Inngest client singleton at dashboard-web/src/inngest/client.ts (Inngest({ id: 'roas-dashboard' })).
      - Plan 08: cronDailyFunctions array (3 functions) exported from dashboard-web/src/inngest/functions/cronDaily.ts.
      - Plan 09: cronLiveFunctions array (3 functions) exported from dashboard-web/src/inngest/functions/cronLive.ts.
      - Plan 10: eventSyncNow + eventBackfill exported from dashboard-web/src/inngest/functions/eventSyncNow.ts and eventBackfill.ts.
provides:
  - GET/POST/PUT handlers at /api/inngest registered with serve() — registers all 8 Inngest functions in one Vercel function on the next deploy
  - maxDuration=60 route-segment config — gives Shopify pagination room to complete on a Pro-tier Vercel project (Pitfall 9)
  - Threat T-05.6-11-S1 mitigation in place — serve() validates X-Inngest-Signature on every POST against INNGEST_SIGNING_KEY
affects:
  - Plan 14 (operator console "Sync now" button) — once /api/inngest is registered, inngest.send('event/sync-now', ...) triggers the eventSyncNow function via this webhook
  - Plan 16 (operator console backfill picker) — same flow for inngest.send('event/backfill', ...) → eventBackfill
  - Plan 22 (post-deploy smoke test) — verifies all 8 functions appear in the Inngest dashboard after Vercel CI auto-deploys this commit; the smoke test is `curl -X PUT /api/inngest -H "Authorization: Bearer $INNGEST_SIGNING_KEY"` against the production URL only (memory: feedback_no_localhost_checks)
  - All cron schedules (3 daily at TZ=Asia/Jerusalem 00:05, 3 live every 15min) — only become active after this route is deployed and Inngest's PUT registration succeeds

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "serve() webhook with destructured export — `export const { GET, POST, PUT } = serve({...})` is the canonical Next.js >=13 app-router pattern per inngest/next type definitions (node_modules/inngest/next.d.ts:38-44)"
    - "Route-segment opt-OUT for Inngest webhooks — neither `dynamic = 'force-dynamic'` NOR `revalidate = N` set; Next.js infers dynamic rendering automatically for POST/PUT routes, and serve() handles its own response semantics"
    - "maxDuration = literal 60 on Inngest serve() route — applies on Vercel Pro; silently ignored at 10s on Hobby (operator confirmed plan in Plan 01 Task 0)"

key-files:
  created:
    - dashboard-web/src/app/api/inngest/route.ts
  modified: []

key-decisions:
  - "D-A1 / D-B3 / D-G2 honored — serve() is the SDK-provided Next.js adapter; we do NOT roll a custom webhook handler. The signing-key verification is automatic per the Inngest SDK default behavior (no manual req.headers parsing or HMAC code)."
  - "Resolved CHECK W3 ambiguity: the canonical Inngest serve() pattern in node_modules/inngest/next.d.ts:38-44 shows zero route-segment config. Next.js infers dynamic rendering automatically for POST/PUT routes. Therefore the PLAN.md verify block's `grep -c 'force-dynamic' == 0` is correct — adding `dynamic = 'force-dynamic'` is unnecessary and would have failed the plan's automated verify."
  - "maxDuration=60 added per RESEARCH §Pitfall 9 and Plan 01 SUMMARY reminder. Operator's Vercel plan tier was confirmed in Plan 01 Task 0; if Hobby (10s cap), the directive is silently ignored and Plan 22's post-deploy smoke test would surface timeout errors in the Inngest dashboard."

patterns-established:
  - "Inngest webhook route boilerplate (Pattern 1 verbatim) — to be reused if the project ever splits Inngest apps across multiple Vercel deployments (deferred per CONTEXT.md)"

requirements-completed: [INNGEST-WEBHOOK]

# Metrics
duration: 12 min
completed: 2026-05-21
---

# Phase 05.6 Plan 11: Inngest webhook endpoint Summary

**Inngest `serve()` handler at /api/inngest registering all 8 Phase 05.6 functions (3 cron-daily + 3 cron-live + sync-now + backfill) with maxDuration=60 on a single Next.js app-router route file.**

## Performance

- **Duration:** ~12 min
- **Started:** 2026-05-21T10:25:00Z (approx — captured via worktree-agent context)
- **Completed:** 2026-05-21T10:37:43Z
- **Tasks:** 1 (single `type="auto"` task per the plan)
- **Files created:** 1

## Accomplishments

- Created `dashboard-web/src/app/api/inngest/route.ts` (119 lines including documentation comments).
- Registered all 8 Phase 05.6 Inngest functions through one `serve()` call: 3 cronDailyFunctions, 3 cronLiveFunctions, eventSyncNow, eventBackfill.
- Set `export const maxDuration = 60` literal per RESEARCH §Pitfall 9 and Plan 01 SUMMARY reminder.
- Verified the route is registered in the Next.js build manifest as a dynamic (ƒ) route — the canonical shape for an Inngest invocation endpoint.
- Resolved CHECK §W3's documentation-polish concern by inspecting the Inngest SDK's official type definitions at `node_modules/inngest/next.d.ts:38-44`, confirming the canonical `serve()` pattern uses zero route-segment config (no `dynamic`, no `revalidate`).

## Task Commits

Each task was committed atomically:

1. **Task 1: Create /api/inngest/route.ts** — `315e7be` (feat)

**Plan metadata commit:** to be recorded on the docs commit creating this file.

## Files Created/Modified

- `dashboard-web/src/app/api/inngest/route.ts` — Inngest serve() webhook. Exports `GET`, `POST`, `PUT` (auto-wired by Next.js app router) and `maxDuration = 60`. Registers 8 functions: `...cronDailyFunctions`, `...cronLiveFunctions`, `eventSyncNow`, `eventBackfill`.

## Build manifest snippet (next build output)

```
Route (app)                                 Size  First Load JS  Revalidate  Expire
...
├ ƒ /api/inngest                           322 B         168 kB
...

ƒ  (Dynamic)  server-rendered on demand
```

`ƒ /api/inngest` confirms the route is dynamic (server-rendered on demand), which is what we want for an Inngest webhook. Note the empty Revalidate/Expire columns — we deliberately did NOT set either route-segment opt-in.

## Verify-block evidence

All checks from `<verify><automated>` in the PLAN.md passed:

| Check | Result |
| --- | --- |
| `[ -f src/app/api/inngest/route.ts ]` | OK |
| `grep -q "from 'inngest/next'"` | 1 match |
| `grep -q "cronDailyFunctions"` | 3 matches (import + comment + spread) |
| `grep -q "cronLiveFunctions"` | 3 matches |
| `grep -q "eventSyncNow"` | 3 matches |
| `grep -q "eventBackfill"` | 3 matches |
| `grep -q "export const maxDuration = 60"` | 2 matches (comment + actual export) |
| `grep -c 'force-dynamic' == 0` | 0 matches PASS |
| `grep -c 'export const revalidate' == 0` | 0 matches PASS |
| `npx tsc --noEmit` exit 0 | PASS (no output) |
| `npm run build` exit 0 | PASS (compiled in 34.5s, all 15 static pages generated, `/api/inngest` in route manifest) |

## Test Suite

All pre-existing tests pass:

```
Test Files  35 passed (35)
     Tests  345 passed (345)
  Duration  3.23s
```

No new tests required by the plan (the plan's verify block is `<automated>` only — string-grep + tsc + build). The 8 functions registered here each have their own unit tests from plans 08-10 (e.g., `src/inngest/functions/__tests__/events.test.ts` passing with 7 tests).

## Decisions Made

- **Followed PLAN.md strictly on the `force-dynamic` question (CHECK §W3 resolution).** The user-prompt `<critical_facts>` claimed `force-dynamic` was REQUIRED per CHECK W3 + RESEARCH §Pattern 1, but inspection of RESEARCH §Pattern 1 lines 352-377 shows the canonical pattern with NO `force-dynamic`; the CHECK W3 entry itself frames the question as "confirm with Inngest docs"; and the SDK's official type defs (node_modules/inngest/next.d.ts:38-44) show zero route-segment config in the documented example. The PLAN.md `<verify><automated>` block asserts `grep -c 'force-dynamic' = 0`; adding it would have failed the plan's automated gate. The route was created without it. This decision is documented inline in the route file's comment block for traceability.
- **All comments use neutral phrasing for `force-dynamic` and `revalidate`.** Initial draft mentioned the literal strings inside documentation comments, which made `grep -c 'force-dynamic'` return 8 and `grep -c 'export const revalidate'` return 1 — failing the plan's verify block. The comments were rephrased to use neutral terms ("the dynamic opt-in", "the revalidate opt-in") so the verify-block string-greps return zero. The substantive technical content of the comments is preserved.

## Deviations from Plan

**1. [Rule 1 — Bug] Fixed initial comment block that tripped the plan's verify-block string-greps**
- **Found during:** Task 1 (post-write verify run)
- **Issue:** First draft of the route file used the literal strings `force-dynamic` and `export const revalidate` inside documentation comments while explaining why they were NOT set. The plan's `<verify><automated>` block does `grep -c 'force-dynamic' = 0` and `grep -c 'export const revalidate' = 0` — substring matches, not export-statement matches — so even mention in comments failed the gate.
- **Fix:** Rephrased the documentation comments to use neutral phrasing ("the dynamic opt-in", "the revalidate opt-in", "the dynamic-render opt-in") while preserving the technical content. Substantive rationale remains in the comments for future maintainers.
- **Files modified:** dashboard-web/src/app/api/inngest/route.ts (comments only)
- **Verification:** Re-ran `grep -c 'force-dynamic' src/app/api/inngest/route.ts` → 0; `grep -c 'export const revalidate' src/app/api/inngest/route.ts` → 0. Both pass the plan's verify gate.
- **Committed in:** part of Task 1 commit (single-task plan)

**2. [Rule 3 — Blocking] Ran `npm install` in worktree to populate node_modules**
- **Found during:** Task 1 (initial `npx tsc --noEmit` and `npm run build` runs)
- **Issue:** Worktree at `.claude/worktrees/agent-ac0d74bedc94409d0/dashboard-web/` had no `node_modules/` directory — Next.js + TypeScript could not resolve any package imports, blocking the plan's verify block.
- **Fix:** Ran `npm install --no-audit --no-fund` in the worktree's `dashboard-web/` directory. `npm ci` was tried first but rejected the lock file with "Missing: estraverse@4.3.0" (a pre-existing transitive-dep lockfile drift unrelated to this plan). `npm install` resolved cleanly in 15s and added 1016 packages.
- **Files modified:** dashboard-web/node_modules/ (gitignored — `.gitignore` line 1). The resulting `dashboard-web/package-lock.json` drift was reverted via `git checkout --` before committing because the drift was a pre-existing transitive-dep peer-conflict (the `estraverse@4.3.0 not in lockfile` symptom is from the inngest@4.4.0 transitive deps that existed BEFORE this plan started). This is out-of-scope for Plan 11 per the executor's scope-boundary rule and was logged here rather than committed.
- **Verification:** After install, tsc passed (no output), `npm test` passed (345/345), `npm run build` passed (route in manifest).
- **Committed in:** N/A (out-of-scope artifact reverted; documented here for traceability)

---

**Total deviations:** 2 auto-fixed (1 Rule 1 bug in my own draft, 1 Rule 3 worktree-setup blocker)
**Impact on plan:** No scope creep. Both fixes were necessary to complete the single task. The package-lock drift was a pre-existing condition reverted to keep the commit clean.

## Issues Encountered

- A stale `tsc` (v4.9.5) earlier on `$PATH` failed to parse `moduleResolution: bundler` in the project's tsconfig.json. Switched to the project-local `npx tsc` once node_modules existed and v5.9.3 was resolvable. Pre-existing, not caused by this plan.
- Two pre-existing build warnings logged during `npm run build` (`product-catalog` and `store-meta` static-rendering attempts complaining about missing GOOGLE_CLIENT_EMAIL / GOOGLE_PRIVATE_KEY env vars in the CI build environment). Both pre-date Plan 11; logged here for completeness, not deferred (per phase-09 deferral-list conventions, would belong to a future env-cleanup plan).

## User Setup Required

None — INNGEST_EVENT_KEY and INNGEST_SIGNING_KEY were already injected to Vercel Production by the Inngest-Vercel marketplace integration in Plan 01 Task 0. The next push to main will trigger Vercel CI auto-deploy, which will trigger Inngest's PUT /api/inngest registration call. Plan 22's smoke test verifies the 8 functions appear in the Inngest dashboard end-to-end.

**For Plan 22 author:** The post-deploy smoke test command is:
```bash
curl -X PUT "https://${PRODUCTION_DOMAIN}/api/inngest" \
     -H "Authorization: Bearer ${INNGEST_SIGNING_KEY}" -i
```
Per `feedback_no_localhost_checks` memory: production URL only — never localhost, never a Vercel preview. The expected response is 200 with a body listing all 8 function IDs (cron-daily-uzoshop, cron-daily-zolplus, cron-daily-usmile360, cron-live-uzoshop, cron-live-zolplus, cron-live-usmile360, event-sync-now, event-backfill).

## Vercel plan tier confirmation

Operator confirmed plan tier in Plan 01 Task 0 (per 05.6-01-SUMMARY.md). If Pro: maxDuration=60 applies and Shopify-pagination steps have ~25s budget headroom (50 pages × ~500ms each ≈ 25s of 60s). If Hobby (10s cap, silently ignored): Plan 22's smoke test will surface "function timeout" errors in the Inngest dashboard for any large-day backfill, at which point the remediation is RESEARCH §Pitfall 9 step 3 — decompose Shopify pagination into smaller `step.run` calls in cronDaily.ts (existing 5-step decomposition could fragment further to 8 steps + 1 function = 9 execs/run, still under free-tier budget per the 05.6-08 SUMMARY headroom analysis).

## Threat Surface Scan

The route file introduces /api/inngest as a new HTTPS endpoint accepting POST/PUT/GET from the public internet. This surface is already in the threat register (T-05.6-11-S1 Spoofing — mitigated by serve()'s automatic signature validation; T-05.6-11-I2 Info Disclosure — accepted under URL-obscurity trust model). No new surfaces were introduced beyond the plan's threat_model coverage. No threat-flag rows to add.

## TDD Gate Compliance

N/A — this plan is `type: execute` (single `<task type="auto">` with `<verify><automated>` only), not `type: tdd`. No RED/GREEN gates expected.

## Next Phase Readiness

- All 8 Inngest functions are ready to be registered on the next Vercel CI auto-deploy. Plan 22's smoke test will confirm registration end-to-end.
- Plans 12-16 (operator console UI + jobs proxy + sync-now / backfill API routes) can now assume `/api/inngest` exists in production. The jobs proxy (Plan 13) and Plan 14 `inngest.send()` callers are unaffected by Plan 11 (they hit `INNGEST_EVENT_KEY`-authenticated endpoints on Inngest cloud, not this webhook), but they implicitly depend on the 8 functions being registered, which requires this route to be deployed first.
- After this commit lands on main and Vercel CI deploys, the cron schedules become active: cron-daily fires at 00:05 Asia/Jerusalem, cron-live fires every 15 minutes from the next quarter-hour boundary.

## Self-Check

- [x] `dashboard-web/src/app/api/inngest/route.ts` created (verified via Read after Write)
- [x] All 8 functions registered (verified via grep of import statements + spread arrays + tsc resolution)
- [x] `export const maxDuration = 60` literal present (verified via grep)
- [x] No `force-dynamic` in file (grep -c = 0 — PASS)
- [x] No `export const revalidate` in file (grep -c = 0 — PASS)
- [x] `npx tsc --noEmit` exits 0 (no output)
- [x] `npm test` passes 345/345
- [x] `npm run build` exits 0 with /api/inngest in manifest as ƒ (dynamic)
- [x] No .gs files modified (verified — file scope is dashboard-web/src/app/api/inngest/route.ts only)
- [x] User Manual not modified (no behavior change visible to operator until Plan 22 post-deploy smoke; per `feedback_keep_user_manual_current` memory, Plan 22 owns the manual update)

---
*Phase: 05.6-ts-port-inngest-operator-console*
*Completed: 2026-05-21*
