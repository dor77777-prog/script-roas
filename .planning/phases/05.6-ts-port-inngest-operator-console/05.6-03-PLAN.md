---
phase: 05.6-ts-port-inngest-operator-console
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - dashboard-web/src/lib/fetchers/shopify.ts
  - dashboard-web/src/lib/fetchers/__tests__/shopify.test.ts
autonomous: true
requirements: [FETCH-SHOPIFY]
decisions_addressed: [D-C1, D-C2, D-C3, D-C4, D-C5]
tags: [fetcher, shopify, ts-port, refund-algorithm, reuse-pattern-9, load-bearing]

must_haves:
  truths:
    - "shopify.ts exports fetchShopifyDayRows(storeId, dateStr) returning {storeId, date, revenueCad, productRows, customItemRefundCad, storeName}"
    - "shopify.ts REUSES computeRevenueWithCrossDayRefunds from @/lib/shopifyRevenueRefunds — DOES NOT re-derive the refund algorithm"
    - "SHOPIFY_API_VERSION = '2024-10' (matches Apps Script Config.gs:12 exactly per D-C4 algorithm parity)"
    - "fetchOrdersWithRefundsForDay paginates BOTH orders.created_at=D AND orders.updated_at=D windows then merges deduped (per RESEARCH §Pattern 9 lines 1131-1138)"
    - "Pagination follows Link header rel='next' (RESEARCH §Don't Hand-Roll Shopify pagination row)"
    - "Cap pagination at 50 pages + console.warn (per RESEARCH §Pitfall 6)"
    - "The 3 load-bearing invariants stay preserved because we do not touch the algorithm file"
    - "tsc passes; vitest passes — including the EXISTING shopifyRevenueRefunds.test.ts suite (8 tests stay green)"
  artifacts:
    - path: "dashboard-web/src/lib/fetchers/shopify.ts"
      provides: "Pure-TS Shopify Admin REST fetcher that delegates to existing refund algorithm"
      contains: "computeRevenueWithCrossDayRefunds"
      min_lines: 100
    - path: "dashboard-web/src/lib/fetchers/__tests__/shopify.test.ts"
      provides: "Fetcher-level tests beyond the algorithm test (pagination, env vars, Link header parse, algo-call-count spy)"
      contains: "describe.*shopify"
  key_links:
    - from: "shopify.ts"
      to: "dashboard-web/src/lib/shopifyRevenueRefunds.ts"
      via: "import { computeRevenueWithCrossDayRefunds, type ShopifyOrderInput }"
      pattern: "computeRevenueWithCrossDayRefunds"
    - from: "Plans 08-10 (Inngest functions)"
      to: "shopify.ts"
      via: "import { fetchShopifyDayRows } from '@/lib/fetchers/shopify'"
      pattern: "fetchShopifyDayRows"
---

<objective>
Port the HTTP layer of `Shopify.gs` (44KB) to a pure-TS module at `dashboard-web/src/lib/fetchers/shopify.ts`. The fetcher paginates Shopify Admin REST 2024-10 (orders + refunds + line_items), feeds the raw order array into the EXISTING `computeRevenueWithCrossDayRefunds()` algorithm from `dashboard-web/src/lib/shopifyRevenueRefunds.ts:150`, and returns store-net + per-product net revenue in CAD.

Purpose: This is the SECOND-most-important fetcher (after Meta) — drives revenue, ROAS, gross profit. The Phase 05.2.3.0 refund-correction algorithm is load-bearing — RESEARCH §Don't Hand-Roll + §Pattern 9 + §Anti-Patterns explicitly forbid re-deriving it. The new fetcher MUST import and delegate to the existing pure-TS function. The fetcher's responsibility is HTTP (paginate orders+refunds for the day, including the cross-day-refund updated_at window) → call algorithm → return per-store + per-product totals.

Output: 1 fetcher module + 1 test file. The 3 load-bearing invariants from RESEARCH §Pattern 9 (lines 1049-1057) stay preserved because we don't touch the algorithm — only the HTTP layer feeding it.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.6-ts-port-inngest-operator-console/05.6-CONTEXT.md
@.planning/phases/05.6-ts-port-inngest-operator-console/05.6-RESEARCH.md
@dashboard-web/src/lib/shopifyRevenueRefunds.ts
@dashboard-web/src/lib/__tests__/shopifyRevenueRefunds.test.ts
@Shopify.gs

<interfaces>
<!-- EXISTING pure-TS algorithm (DO NOT RE-IMPLEMENT) -->
<!-- From dashboard-web/src/lib/shopifyRevenueRefunds.ts -->
```typescript
export type ShopifyOrderInput = {
  id: string | number;
  created_at: string;
  total_price?: string | number;
  current_total_price: string | number;
  test?: boolean;
  financial_status?: string;
  line_items?: ShopifyLineItem[];
  refunds?: ShopifyRefund[];
};

export type CrossDayRefundResult = {
  storeNetCad: number;
  byProduct: Record<string, { netRevenueCad: number }>;
  customItemRefundCad: number;
};

export function computeRevenueWithCrossDayRefunds(
  orders: ShopifyOrderInput[],
  dateStr: string,
  tz: string = 'Asia/Jerusalem',
): CrossDayRefundResult;
```

<!-- Shopify Admin REST 2024-10 (matches Apps Script Config.gs:12) -->
```
GET https://{domain}/admin/api/2024-10/orders.json
  ?status=any&limit=250
  &created_at_min={iso_start}&created_at_max={iso_end}
  &fields=id,created_at,total_price,current_total_price,test,financial_status,line_items,refunds

Pagination: Link header rel="next" → next page URL
Cap: 50 pages safety (RESEARCH §Pitfall 6)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write shopify fetcher test scaffold</name>
  <files>dashboard-web/src/lib/fetchers/__tests__/shopify.test.ts</files>
  <behavior>
    - Test 1: fetchShopifyDayRows('uzoshop', '2026-05-19') calls Shopify API with correct day-boundary window (Asia/Jerusalem)
    - Test 2: Link header rel="next" triggers a second fetch to that URL
    - Test 3: Pagination cap at 50 pages emits console.warn; does NOT throw
    - Test 4: Fetches BOTH orders.created_at=D AND orders.updated_at=D windows; merges deduped by order id
    - Test 5: Result includes revenueCad, productRows, customItemRefundCad, storeName
    - Test 6: Missing SHOPIFY_{storeId}_DOMAIN or SHOPIFY_{storeId}_TOKEN env vars → throws clear error including storeId
    - Test 7: computeRevenueWithCrossDayRefunds is called EXACTLY ONCE per invocation (vi.spyOn) — proves we delegate, not re-derive
  </behavior>
  <read_first>
    - `dashboard-web/src/lib/__tests__/shopifyRevenueRefunds.test.ts` — vitest patterns + ShopifyOrderInput fixture shapes (8 tests already green, MUST stay green)
    - `.planning/phases/05.6-ts-port-inngest-operator-console/05.6-RESEARCH.md` §Pattern 9 (lines 1119-1153) — the fetcher integration snippet
    - `Shopify.gs` lines 90-187 (`getShopifyRevenue`), 633-732 (`getShopifyRefundsForDay_`)
  </read_first>
  <action>
    Create the test file with 7 cases. Mock `globalThis.fetch` for all Shopify API calls. The algorithm function (`computeRevenueWithCrossDayRefunds`) is imported normally — assert it's called exactly once with the merged orders array.

    Test 1 date-window check: assert the first fetch URL contains `created_at_min=2026-05-19` and the upper bound covers all of day D in Asia/Jerusalem (typically `created_at_max=2026-05-20T00:00:00+02:00` or `+03:00`).

    Test 4 dedup: emit two responses where one order appears in BOTH windows (created on day D, refunded on day D too). Assert the order array passed to `computeRevenueWithCrossDayRefunds` contains the order EXACTLY ONCE (assertion: `mergedArray.filter(o => o.id === '1234').length === 1`).

    Test 7 (spy on algorithm):
    ```typescript
    import * as algo from '@/lib/shopifyRevenueRefunds';
    const spy = vi.spyOn(algo, 'computeRevenueWithCrossDayRefunds');
    await fetchShopifyDayRows('uzoshop', '2026-05-19');
    expect(spy).toHaveBeenCalledTimes(1);  // proves we delegate, not re-derive
    ```

    Env vars: `vi.stubEnv('SHOPIFY_UZOSHOP_DOMAIN', 'uzoshop.myshopify.com')`, `vi.stubEnv('SHOPIFY_UZOSHOP_TOKEN', 'shpat_test')`. These mirror the PROPS-MAP destination convention.

    File MUST fail at this point (RED).
  </action>
  <verify>
    <automated>cd dashboard-web && [ -f src/lib/fetchers/__tests__/shopify.test.ts ] && grep -q "describe.*shopify" src/lib/fetchers/__tests__/shopify.test.ts && grep -q "computeRevenueWithCrossDayRefunds" src/lib/fetchers/__tests__/shopify.test.ts && grep -qE "Link|page_info|paginat" src/lib/fetchers/__tests__/shopify.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - 7 it(...) blocks
    - Spy on `computeRevenueWithCrossDayRefunds` proves delegation
    - Currently fails (RED)
  </acceptance_criteria>
  <done>RED test scaffold ready.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement dashboard-web/src/lib/fetchers/shopify.ts</name>
  <files>dashboard-web/src/lib/fetchers/shopify.ts</files>
  <behavior>
    - Exports `fetchShopifyDayRows(storeId, dateStr): Promise<{storeId, date, revenueCad, productRows, customItemRefundCad, storeName}>`
    - Internal helper `fetchOrdersWithRefundsForDay(domain, token, dateStr): Promise<ShopifyOrderInput[]>` that does the two-window paginated fetch + merge
    - Uses `SHOPIFY_API_VERSION = '2024-10'` (matches Apps Script — D-C4 parity)
    - Imports + delegates to `computeRevenueWithCrossDayRefunds` from `@/lib/shopifyRevenueRefunds`
    - storeName comes from the `stores` table (via getSupabaseAdmin lookup OR a hard-coded map matching Phase 05.5-01 seeds: uzoshop→'uzoshop', zolplus→'Zol Plus', usmile360→'360usmile')
  </behavior>
  <read_first>
    - `.planning/phases/05.6-ts-port-inngest-operator-console/05.6-RESEARCH.md` §Pattern 9 lines 1119-1153 — verbatim fetcher integration
    - `dashboard-web/src/lib/shopifyRevenueRefunds.ts` lines 70-256 — the API surface this fetcher consumes
    - `Shopify.gs` lines 110-187 — Apps Script HTTP layer to mirror (request shape, fields list, pagination loop)
  </read_first>
  <action>
    Implement per RESEARCH §Pattern 9 lines 1119-1153. Key invariants:

    1. **SHOPIFY_API_VERSION = '2024-10'** literal (D-C4 parity). Matches `Config.gs:12`.

    2. **REUSE the algorithm — do NOT re-derive**. Import and call:
       ```typescript
       import { computeRevenueWithCrossDayRefunds, type ShopifyOrderInput }
         from '@/lib/shopifyRevenueRefunds';
       ```

    3. **Two-window fetch + merge** (per RESEARCH §Pattern 9 comment block lines 1131-1138):
       - Window A: `created_at_min/max=dateStr` (catches same-day orders)
       - Window B: `updated_at_min/max=dateStr` (catches cross-day refunds — order created on day < D, refunded on day D)
       - Merge deduped by order id (use a Map<string, ShopifyOrderInput>)

    4. **Day boundary** in Asia/Jerusalem timezone. For dateStr='2026-05-19', the window is `[2026-05-19T00:00:00+02:00, 2026-05-20T00:00:00+02:00)` (or `+03:00` during IDT — date arithmetic should compute the offset correctly). Mirror Apps Script's `Config.gs:78-82` (`nextDayStr_`) for the upper bound.

       Simple implementation: build an ISO string using `Intl.DateTimeFormat` with `timeZone: 'Asia/Jerusalem'` and the standard fields, then add 1 day for the upper bound.

    5. **Pagination via Link header** (matches Shopify.gs:163-165):
       ```typescript
       const link = res.headers.get('Link') ?? '';
       const next = /<([^>]+)>;\s*rel="next"/.exec(link)?.[1];
       ```
       URL contains `page_info=...` token. Cap at 50 pages with `console.warn`.

    6. **Auth header**: `X-Shopify-Access-Token: ${token}` (NOT Bearer).

    7. **Fields list** matches `Shopify.gs:139`:
       `id,created_at,total_price,current_total_price,test,financial_status,line_items,refunds`

    8. **No `Math.max(0, ...)` clamping ANYWHERE** (consistent with D-D3 + RESEARCH §Pattern 9). Negatives are valid.

    9. **storeName** — hard-code the 3-store map (matches Phase 05.5-01 seed):
       ```typescript
       const STORE_NAMES: Record<string, string> = {
         uzoshop: 'uzoshop',
         zolplus: 'Zol Plus',
         usmile360: '360usmile',
       };
       const storeName = STORE_NAMES[storeId] ?? storeId;
       ```
       (Avoids a Supabase round-trip per fetch; storeName is a near-immutable display string. If it ever changes, plan 13's stores-CRUD-UI is the right place — out of scope for 05.6.)

    10. **Result shape** matches RESEARCH §Pattern 9 line 1142-1152:
        ```typescript
        return {
          storeId,
          date: dateStr,
          storeName,
          revenueCad: storeNetCad,
          productRows: Object.entries(byProduct).map(([pid, p]) => ({
            product_id: pid,
            net_revenue_cad: p.netRevenueCad,
          })),
          customItemRefundCad,
        };
        ```

    Verbatim skeleton from RESEARCH §Pattern 9 lines 1119-1153 as starting point; expand with the windowed fetch + Link parser + dedup map.
  </action>
  <verify>
    <automated>cd dashboard-web && [ -f src/lib/fetchers/shopify.ts ] && grep -q "SHOPIFY_API_VERSION.*'2024-10'" src/lib/fetchers/shopify.ts && grep -q "computeRevenueWithCrossDayRefunds" src/lib/fetchers/shopify.ts && grep -q "fetchShopifyDayRows" src/lib/fetchers/shopify.ts && grep -qE "rel=\"next\"|Link" src/lib/fetchers/shopify.ts && [ "$(grep -c 'Math.max(0' src/lib/fetchers/shopify.ts)" = "0" ] && [ "$(grep -c 'current_total_price' src/lib/fetchers/shopify.ts | head)" -ge "0" ] && npx tsc --noEmit && npx vitest run src/lib/fetchers/__tests__/shopify.test.ts src/lib/__tests__/shopifyRevenueRefunds.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - File exports `fetchShopifyDayRows`
    - Imports `computeRevenueWithCrossDayRefunds` from the existing algorithm file
    - SHOPIFY_API_VERSION literal `2024-10`
    - Link header parsing for pagination
    - No `Math.max(0, ...)` (D-D3)
    - All 7 new tests pass (GREEN) AND the existing 8 shopifyRevenueRefunds.test.ts tests stay GREEN
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Shopify fetcher complete; delegates to the load-bearing Phase 05.2.3.0 algorithm without re-derivation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shopify Admin REST 2024-10 → fetcher | Untrusted; per-store tokens are long-lived (shpat_*) |
| Fetcher → algorithm (shopifyRevenueRefunds.ts) | Trusted internal — delegated boundary; algorithm tests guard the contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.6-03-T1 | Tampering | Refund algorithm re-derived (drift from existing Phase 05.2.3.0 fix) | mitigate | Test 7 spies on the imported function; would fail if re-derived. Grep gate `grep -c 'computeRevenueWithCrossDayRefunds' dashboard-web/src/lib/fetchers/shopify.ts >= 1`. |
| T-05.6-03-S2 | Spoofing | Shopify-style URL parameter injection via storeId | mitigate | storeId is from an internal constant set (`STORE_NAMES` map keys); not user-controlled in 05.6. Future plans should sanitize if exposed. |
| T-05.6-03-I3 | Info Disclosure | Shopify token in URL query | n/a | Shopify Admin REST uses `X-Shopify-Access-Token` header (Apps Script convention), not URL param. |
| T-05.6-03-D4 | DoS | Pagination loop runaway | mitigate | 50-page cap + console.warn (RESEARCH §Pitfall 6). |
</threat_model>

<verification>
- `cd dashboard-web && npx tsc --noEmit` exits 0
- `cd dashboard-web && npx vitest run src/lib/fetchers/__tests__/shopify.test.ts src/lib/__tests__/shopifyRevenueRefunds.test.ts` exits 0 (7 new + 8 existing = 15 tests)
- `grep -c 'computeRevenueWithCrossDayRefunds' dashboard-web/src/lib/fetchers/shopify.ts` >= 1 (delegation present)
- `! grep -q "Math.max(0" dashboard-web/src/lib/fetchers/shopify.ts` (no clamping)
- `git diff --quiet dashboard-web/src/lib/shopifyRevenueRefunds.ts` (algorithm file UNCHANGED)
</verification>

<success_criteria>
`fetchShopifyDayRows` delegates to the existing algorithm; the Phase 05.2.3.0 refund-correction stays intact. Plans 08-10 wire this fetcher into the daily/live crons + backfill event.
</success_criteria>

<output>
After completion, create `.planning/phases/05.6-ts-port-inngest-operator-console/05.6-03-SUMMARY.md` recording:
- Confirmation `computeRevenueWithCrossDayRefunds` import is present (line number in shopify.ts)
- Confirmation `shopifyRevenueRefunds.ts` is UNTOUCHED (`git diff --stat`)
- Test results: 7 new + 8 existing = 15 green
- Note for plan 08: revenueCad is in CAD already (Shopify accounts are CAD-denominated per existing Apps Script config)
- Algorithm-parity confirmation: the 3 load-bearing invariants (RESEARCH §Pattern 9) are preserved by virtue of not touching the algorithm
</output>
