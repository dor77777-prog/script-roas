---
phase: 05.6-ts-port-inngest-operator-console
verified: 2026-05-21T14:30:00Z
status: human_needed
score: 22/22 must-haves verified (automated); 2 items need human verification
overrides_applied: 0
human_verification:
  - test: "Operator runs production capture-snapshot.ts (cd dashboard-web && npx --yes tsx scripts/capture-snapshot.ts) with .env.local containing production GOOGLE_CLIENT_EMAIL + GOOGLE_PRIVATE_KEY + SPREADSHEET_ID, replacing the stub snapshot JSON"
    expected: "snapshots/sheets-baseline-2026-05-15-to-2026-05-20.json updated with capturedAt timestamp (not 'STUB'), rowCount=18 (6 days × 3 stores), real DailyRow values. Then run `npx vitest run src/lib/fetchers/__tests__/algorithm-parity.test.ts --testTimeout=120000` and confirm parity holds within ±5% / ±1 CAD band."
    why_human: "Production env vars are not in worktree; only the operator's local shell has access. The stub snapshot is intentional per plan 21 SUMMARY — test scaffold is the real deliverable, baseline regeneration is pre-Phase-05.7-cut-over operator work."
  - test: "Operator observes the next cron-daily firing at 00:05 IL (2026-05-22) and confirms all 3 store functions complete successfully in the Inngest dashboard at app.inngest.com"
    expected: "3 cron-daily-{uzoshop,zolplus,usmile360} runs visible with status=Completed in the Inngest UI within 24h; Postgres data_daily table contains rows for 2026-05-21 for all 3 stores"
    why_human: "Live cron timing — cannot test programmatically without waiting; visual Inngest dashboard inspection is the canonical verification per CONTEXT.md §27.9 step 8-9."
---

# Phase 05.6: TS port + Inngest + operator console — Verification Report

**Phase Goal:** Port the 5 Apps Script fetchers (Shopify / Meta / Google Ads / FX / ManualOverrides) to TS, set up Inngest cloud (daily cron + 15-min cron + on-demand events), build the operator console UI in dashboard. Apps Script continues running in parallel writing to Sheets — the two systems are independent (no dual-write), both reading from the same upstream APIs. Dashboard reads via feature flag (READ_FROM=sheets|postgres, defaults to sheets).

**Verified:** 2026-05-21T14:30:00Z
**Status:** human_needed (22/22 codebase truths VERIFIED; 2 operator-side actions remain by design)
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (Must-Haves from all 22 PLANs)

| # | Truth | Status | Evidence |
|---|---|---|---|
| 1 | 5 TS fetchers exist under `dashboard-web/src/lib/fetchers/` (shopify, meta, googleAds, fx, manualOverrides) | VERIFIED | All 5 files present (shopify.ts 16KB, meta.ts 11KB, googleAds.ts 18KB, fx.ts 3KB, manualOverrides.ts 5.5KB). Sized substantively. |
| 2 | shopify.ts REUSES `computeRevenueWithCrossDayRefunds` from shopifyRevenueRefunds.ts (Phase 05.2.3.0 algorithm preserved) | VERIFIED | Line 58-60 imports from `@/lib/shopifyRevenueRefunds`; line 412 invokes the function. NO re-derivation found. |
| 3 | shopify, meta, googleAds, fx fetchers are pure (no Supabase/Sheets imports) | VERIFIED | `grep -c "supabaseAdmin\|getSupabase"` returns 0 for all 4. manualOverrides intentionally reads Supabase (per D-C1 design — Sheets-tab read is REPLACED by Postgres read). |
| 4 | Inngest client singleton at `dashboard-web/src/inngest/client.ts` | VERIFIED | File present, exports `new Inngest({id: 'roas-dashboard'})`. |
| 5 | supabaseAdmin lazy factory at `dashboard-web/src/lib/supabaseAdmin.ts` (mirrors Phase 05.5-03 BL-01 fix) | VERIFIED | `let cached: SupabaseClient \| null = null` + `getSupabaseAdmin()` function — matches BL-01 lazy-factory pattern. |
| 6 | 4 Inngest function files: cronDaily, cronLive, eventSyncNow, eventBackfill | VERIFIED | All 4 present at `dashboard-web/src/inngest/functions/`. cronDaily.ts 17.8KB, cronLive.ts 17.9KB, eventBackfill.ts 10.9KB, eventSyncNow.ts 5.2KB. |
| 7 | `/api/inngest/route.ts` serve() webhook registers 8 functions (3 cron-daily + 3 cron-live + sync-now + backfill) | VERIFIED | Lines 108-119: `serve({client, functions: [...cronDailyFunctions, ...cronLiveFunctions, eventSyncNow, eventBackfill]})`. cronDailyFunctions = STORES.map (3 funcs), cronLiveFunctions also 3. Total = 8. |
| 8 | Inngest cloud registration verified live (PUT discovery) | VERIFIED (live deploy) | User-confirmed in task brief: "/api/inngest PUT → HTTP 200, Inngest cloud successfully registered all 8 functions". Process-deviation note confirms no force-dynamic was needed (executor correct per Inngest SDK). |
| 9 | Operator console at `/operator`: `page.tsx` + `layout.tsx` | VERIFIED | Both present in `dashboard-web/src/app/operator/`. page.tsx renders 4 sections, layout.tsx sets dir="rtl". |
| 10 | 4 operator components: JobsTable, BackfillPicker, ManualOverridesCrud, SyncNowButtons | VERIFIED | All 4 present in `dashboard-web/src/components/operator/`. Sizes 10KB / 8KB / 15KB / 7KB respectively — substantive implementations, not stubs. |
| 11 | 4 operator API routes: jobs, backfill, sync-now, manual-overrides | VERIFIED | All 4 routes present at `dashboard-web/src/app/api/operator/`. jobs (REST proxy to Inngest /v1/events + /v1/runs), backfill (validate + send event/backfill), sync-now (scope all/store + send event/sync-now), manual-overrides (GET/POST/PATCH/DELETE CRUD). |
| 12 | Operator page wires all 4 components into the 4 sections | VERIFIED | page.tsx lines 22-25 import all 4; lines 41-77 render each within a `<section>`. No placeholder copy remaining (per Plan 16 SUMMARY). |
| 13 | Hebrew sidebar/header link to /operator labeled "ניהול" | VERIFIED | `Dashboard.tsx:548-553`: `<Link href="/operator" aria-label="ניהול">ניהול</Link>` (Header link per D-D1, NOT a TabKey). |
| 14 | `readFrom()` feature flag at `dashboard-web/src/lib/featureFlags.ts`; request-time env read; defaults to 'sheets' | VERIFIED | Function reads `process.env.READ_FROM` per CALL (not module-load); returns 'postgres' only when env var literally === 'postgres'. Tested in `featureFlags.test.ts` (5 passing tests including "reads env at each call (not cached)"). |
| 15 | `postgresReaders.ts` has 8 dormant reader functions mirroring sheets.ts shape | VERIFIED | 8 `export async function` declarations: fetchDailyDataFromPostgres, fetchStoreMetaFromPostgres, fetchDashboardStateFromPostgres, fetchProductsFromPostgres, fetchCampaignsFromPostgres, fetchAdsFromPostgres, fetchOrdersAttributionFromPostgres, fetchProductCatalogFromPostgres. |
| 16 | All 8 data routes have `readFrom()` branch (default sheets) | VERIFIED | `grep -c "readFrom()"` returns 1 for each of: /api/data, /api/products, /api/campaigns, /api/ads, /api/store-meta, /api/orders-attribution, /api/product-catalog, /api/dashboard-state. |
| 17 | Importer at `dashboard-web/scripts/import-manual-overrides.ts` is idempotent (ON CONFLICT) | VERIFIED | Lines 30-35: `onConflict: 'date,store_id,platform'` with `ignoreDuplicates: true`. Lowercases 'Meta'/'Google' (Pitfall 7). |
| 18 | Capture-snapshot script at `dashboard-web/scripts/capture-snapshot.ts` (operator-only) | VERIFIED | File present (3KB); writes to `__tests__/snapshots/sheets-baseline-2026-05-15-to-2026-05-20.json`. Calls `fetchDailyData` from sheets.ts. |
| 19 | Algorithm-parity test at `dashboard-web/src/lib/fetchers/__tests__/algorithm-parity.test.ts` | VERIFIED | 337-line test suite covering Shopify/Meta/Google/composite. All `it.skip`-gated by default (avoids CI quota burn). The 12 "skipped" tests in vitest output match this expectation. |
| 20 | Snapshot file exists (stub — operator regenerates pre-cut-over) | VERIFIED | File present with explicit `_TODO: "STUB SNAPSHOT — regenerate from production Sheets..."` marker. Stub is the intentional plan-21 deliverable (per SUMMARY). |
| 21 | NO `.gs` file modifications during Phase 05.6 batch | VERIFIED | `git diff --name-only 0d90fe0..930d9d6 -- "*.gs"` returns 0 files. All .gs modifications in HEAD~96..HEAD are from Phase 05.2.3.0 (pre-05.6), confirming clean 05.6 boundary. |
| 22 | User Manual updated (v1.3, §27 with 10 sub-sections covering all Phase 05.6 surfaces) | VERIFIED | docs/ROAS-Dashboard-User-Manual.md header line 10: "גרסה: 1.3"; footer line 2364: "גרסה: 1.3 · תאריך עדכון: 2026-05-21 · בסיס קוד: Phase 05.6-22". §27 spans lines 2100-2363 with 10 sub-sections (27.1 through 27.10). |

**Score:** 22/22 codebase truths VERIFIED.

### Required Artifacts

| Artifact | Expected | Status | Details |
|---|---|---|---|
| `dashboard-web/src/lib/fetchers/shopify.ts` | TS port reusing shopifyRevenueRefunds.ts algorithm | VERIFIED | 16.3KB; imports + invokes `computeRevenueWithCrossDayRefunds` (line 412); covers BOTH `created_at` and `updated_at` windows + dedup (per RESEARCH Pattern 9). |
| `dashboard-web/src/lib/fetchers/meta.ts` | TS port of MetaAds.gs | VERIFIED | 11KB; exports `fetchMetaAdSetInsights` + `fetchMetaSpendForDay`; v23 API; purchase priority extraction (omni_purchase → purchase → offsite). |
| `dashboard-web/src/lib/fetchers/googleAds.ts` | TS port of GoogleAds.gs | VERIFIED | 18.3KB; STORES_WITH_GOOGLE_ADS = ['uzoshop'] (matches Config.gs); v24 API; module-level token cache. |
| `dashboard-web/src/lib/fetchers/fx.ts` | Frankfurter port | VERIFIED | 3KB; `getFxRate(from, to, dateStr)`; same-currency short-circuit; weekend/holiday tolerance per RESEARCH Pitfall 13. |
| `dashboard-web/src/lib/fetchers/manualOverrides.ts` | Reads Supabase manual_overrides (not Sheets tab) | VERIFIED | 5.5KB; exports `mergeOverridesFromSupabase`; uses getSupabaseAdmin + getFxRate. Per-platform REPLACE semantics. |
| `dashboard-web/src/inngest/client.ts` | Singleton Inngest client | VERIFIED | `id: 'roas-dashboard'`; auto-reads INNGEST_EVENT_KEY at runtime. |
| `dashboard-web/src/lib/supabaseAdmin.ts` | Lazy service_role factory | VERIFIED | `cached` var + `getSupabaseAdmin()` mirror BL-01 lazy-factory; SECURITY: throws inside function, not at module load. |
| `dashboard-web/src/inngest/functions/cronDaily.ts` | 3 cron-daily-{store} functions @ TZ=Asia/Jerusalem 5 0 * * * | VERIFIED | `cronDailyFunctions = STORES.map(makeCronDaily)` — 3 functions registered. Shared handler `runDailyForStore` exported for reuse by sync-now/backfill. |
| `dashboard-web/src/inngest/functions/cronLive.ts` | 3 cron-live-{store} functions @ */15min | VERIFIED | Rolling 3-day Shopify-only refresh (matches Apps Script live trigger semantics per D-C4). |
| `dashboard-web/src/inngest/functions/eventSyncNow.ts` | event/sync-now wrapper around runDailyForStore | VERIFIED | Validates StoreId; default date = today in Asia/Jerusalem. |
| `dashboard-web/src/inngest/functions/eventBackfill.ts` | event/backfill iterator with W6 prefix shim | VERIFIED | Implements `makePrefixedStep` (W6 mitigation); HISTORY_BOUNDARY='2026-05-01' guard; per-pair try/catch for partial progress. |
| `dashboard-web/src/app/api/inngest/route.ts` | serve() webhook with 8 functions | VERIFIED | `serve({client: inngest, functions: [...]})`; maxDuration=60; no force-dynamic (executor verified correct per Inngest SDK pattern, deploy confirms HTTP 200 on PUT). |
| `dashboard-web/src/app/operator/page.tsx` | 4 sections with components mounted | VERIFIED | 4 `<section>` blocks; each mounts the corresponding component (no placeholder copy remaining). |
| `dashboard-web/src/app/operator/layout.tsx` | RTL wrapper | VERIFIED | `dir="rtl"`; `bg-background`, `text-text-primary` tokens (D-D4). |
| `dashboard-web/src/components/operator/JobsTable.tsx` | SWR 15s polling | VERIFIED | 10KB; SWR with refreshInterval 15000ms; no INNGEST_SIGNING_KEY references on client (verified by inspection). |
| `dashboard-web/src/components/operator/BackfillPicker.tsx` | from/to + multi-store + history boundary | VERIFIED | 8KB; HISTORY_BOUNDARY UX hint; `<input min={HISTORY_BOUNDARY}>`. |
| `dashboard-web/src/components/operator/ManualOverridesCrud.tsx` | ADD + LIST + DELETE (no inline edit in 05.6) | VERIFIED | 15.4KB; confirmation modal on DELETE; SWR mutate after success. |
| `dashboard-web/src/components/operator/SyncNowButtons.tsx` | 4 buttons (global + 3 per-store) | VERIFIED | 7KB; single `pendingKey` state disables all buttons during in-flight POST (T-05.6-16-T1 mitigation). |
| `dashboard-web/src/app/api/operator/jobs/route.ts` | Inngest REST proxy | VERIFIED | Bearer-key auth; N+1 fan-out (events → runs); soft-fails to HTTP 200 with empty runs; 24h lookback; revalidate=5. |
| `dashboard-web/src/app/api/operator/sync-now/route.ts` | POST → inngest.send event/sync-now | VERIFIED | scope='all' fans out to 3 events; scope='store' validates against allowlist; returns 202 + eventIds. force-dynamic. |
| `dashboard-web/src/app/api/operator/backfill/route.ts` | POST → inngest.send event/backfill | VERIFIED | from/to YYYY-MM-DD validation; HISTORY_BOUNDARY guard; storeIds allowlist; single event with iteration inside worker. |
| `dashboard-web/src/app/api/operator/manual-overrides/route.ts` | GET/POST/PATCH/DELETE CRUD | VERIFIED | All 4 verbs use getSupabaseAdmin (service_role); platform lowercase + currency uppercase normalization. |
| `dashboard-web/src/lib/featureFlags.ts` | readFrom() request-time env read | VERIFIED | Single function; reads process.env.READ_FROM per call; defaults to 'sheets'. |
| `dashboard-web/src/lib/postgresReaders.ts` | 8 dormant readers | VERIFIED | 28KB; mirrors sheets.ts shape exactly; TitleCases platform at boundary (lowercase 'meta'/'google' in DB → 'Meta'/'Google' in shape). |
| `dashboard-web/scripts/import-manual-overrides.ts` | 38-row idempotent importer | VERIFIED | 11.8KB; reads via `getAuth` from sheets.ts; lowercases platform; ignoreDuplicates: true. |
| `dashboard-web/scripts/capture-snapshot.ts` | Operator-only Sheets baseline capture | VERIFIED | Imports `fetchDailyData` from src/lib/sheets; writes to `__tests__/snapshots/sheets-baseline-...json`. |
| `dashboard-web/src/lib/fetchers/__tests__/snapshots/sheets-baseline-2026-05-15-to-2026-05-20.json` | Stub baseline (operator regenerates) | VERIFIED | Stub present with `_TODO` marker explaining regeneration; 3 zero-rows; valid DailyRow shape per `_LOAD_BEARING` comment. |
| `dashboard-web/src/lib/fetchers/__tests__/algorithm-parity.test.ts` | Skip-gated parity scaffold | VERIFIED | 337 lines; all `it.skip` (vitest reports 12 skipped); covers Shopify/Meta/Google/composite parity vs baseline. |
| `docs/ROAS-Dashboard-User-Manual.md` | v1.3 with §27 | VERIFIED | Header + footer say "גרסה: 1.3"; §27 lines 2100-2363 with 10 sub-sections; bridging notes in §1.1/§1.5/§1.6/§1.7/§2.1/Appendix-D. |

### Key Link Verification

| From | To | Via | Status | Details |
|---|---|---|---|---|
| `shopify.ts` | `shopifyRevenueRefunds.ts` | `import { computeRevenueWithCrossDayRefunds }` | WIRED | Line 58 import; line 412 invocation. |
| `cronDaily.ts` | All 5 fetchers + `getSupabaseAdmin` | imports | WIRED | Lines 43-50 import all 5 fetchers + admin client. |
| `cronLive.ts` | `fetchShopifyDayRows` + `getSupabaseAdmin` | imports | WIRED | Shopify-only refresh path. |
| `eventSyncNow.ts` | `runDailyForStore` from cronDaily | re-export pattern | WIRED | Line 57 imports `runDailyForStore`; delegates with payload. |
| `eventBackfill.ts` | `runDailyForStore` from cronDaily | with W6 prefix shim | WIRED | Line 93 imports; `makePrefixedStep` wraps before invocation. |
| `/api/inngest/route.ts` | All 4 Inngest function modules | serve() registration | WIRED | Live deploy confirmed all 8 functions registered (PUT → HTTP 200). |
| `/api/operator/sync-now` | `inngest.send('event/sync-now')` | inngest client | WIRED | Line 92: `await inngest.send(events)`. |
| `/api/operator/backfill` | `inngest.send('event/backfill')` | inngest client | WIRED | Lines 131-138: single send. |
| `/api/operator/manual-overrides` | Supabase `manual_overrides` table | getSupabaseAdmin().from() | WIRED | All 4 verbs use admin client. |
| `/api/operator/jobs` | Inngest REST `/v1/events` + `/v1/events/{id}/runs` | Bearer key | WIRED | Live deploy confirms 200 + actual runs array. |
| `/operator/page.tsx` | All 4 operator components | imports | WIRED | Lines 22-25 import; lines 41-77 render. |
| `Dashboard.tsx` | `/operator` route | `<Link href="/operator">ניהול</Link>` | WIRED | Lines 543-553 in Dashboard.tsx. |
| 8 /api/* routes | `readFrom()` from featureFlags | conditional branch | WIRED | `grep -c "readFrom()"` returns 1 for each route. |
| 8 /api/* routes | `postgresReaders.ts` functions (when flag=postgres) | conditional import | WIRED | Each route imports its postgres counterpart and branches based on `readFrom() === 'postgres'`. Currently dormant (default sheets). |
| `manualOverrides.ts` | Supabase `manual_overrides` table | getSupabaseAdmin | WIRED | Per D-C1 design (replaces Sheets-tab read with Postgres read). |
| `import-manual-overrides.ts` | Sheets `manual-spend` tab + Supabase `manual_overrides` | getAuth + getSupabaseAdmin | WIRED | Reads from Sheets, writes to Postgres with `onConflict: 'date,store_id,platform'`. |
| `capture-snapshot.ts` | `fetchDailyData` from sheets.ts | direct import | WIRED | Writes baseline JSON for parity test consumption. |
| `algorithm-parity.test.ts` | `snapshots/sheets-baseline-2026-05-15-to-2026-05-20.json` | readFileSync | WIRED | Test scaffold loads + iterates; currently skip-gated (intentional). |

### Data-Flow Trace (Level 4)

Phase 05.6 has no production behavior change — Postgres readers are dormant by design. The active data flow remains the existing Sheets → /api/data path that Phase 05.5 already verified. Level 4 traces below verify the new flows are present and correctly wired (even though they're not the default read source).

| Artifact | Data Variable | Source | Produces Real Data | Status |
|---|---|---|---|---|
| `/operator` page | Jobs list (SWR) | `/api/operator/jobs` → Inngest REST `/v1/events` + `/v1/events/{id}/runs` | YES (live deploy verified — `{"runs":[{run_id, status:"Running", function_id:..., event_name:"event/backfill"}]}`) | FLOWING |
| `/operator` page | Manual overrides list | `/api/operator/manual-overrides` GET → Supabase `manual_overrides` table | YES (route uses real `getSupabaseAdmin().from('manual_overrides').select(...)`) | FLOWING |
| `/operator` page | Backfill submit | `/api/operator/backfill` POST → `inngest.send('event/backfill')` | YES (live deploy verified — actual `event/backfill` run captured in jobs response) | FLOWING |
| `/operator` page | Sync-now submit | `/api/operator/sync-now` POST → `inngest.send('event/sync-now')` | YES (route returns 202 with eventIds; no mocked fallback) | FLOWING |
| `cronDaily` Inngest fn | Shopify revenue, Meta spend, Google spend, FX rate, overrides → data_daily/products_daily/campaigns_daily | All 5 fetchers + getSupabaseAdmin.upsert | YES (cronDaily.ts:212-343 performs real `.upsert({...}, {onConflict})` for 3 tables) | FLOWING (awaiting first cron tick at 00:05 IL 2026-05-22) |
| `algorithm-parity.test.ts` | Frozen DailyRow[] from snapshot | readFileSync(`snapshots/sheets-baseline-...json`) | NO (stub has 3 zero-rows; operator regenerates) | STATIC (by design — stub is intentional per plan 21) |
| /api/data (active path) | DailyRow[] from sheets.ts | `fetchDailyData` (sheets path, default) | YES (live deploy: returned 6 rows with non-zero revenue/spend for 2026-05-19..20) | FLOWING (unchanged by Phase 05.6) |

### Behavioral Spot-Checks (Production URL — per memory rule `feedback_no_localhost_checks`)

| Behavior | Command | Result | Status |
|---|---|---|---|
| Operator page renders with Hebrew title | `curl -s https://roas-dashboard-smoky.vercel.app/operator \| grep -c "ניהול"` | 1 (HTTP 200) | PASS |
| Inngest webhook accepts auth probe (8 functions registered) | `curl -s -o /dev/null -w "%{http_code}" https://roas-dashboard-smoky.vercel.app/api/inngest` | 401 (Unauthorized — expected; GET requires Inngest signing-key. PUT during deploy returned 200 with 8 functions per user confirmation) | PASS |
| Jobs proxy returns 200 with runs shape | `curl -s https://roas-dashboard-smoky.vercel.app/api/operator/jobs` | `{"runs":[{run_id,run_started_at,...,event_name:"event/backfill"}],"lastUpdated":...}` HTTP 200 | PASS |
| /api/health both ok | `curl -s https://roas-dashboard-smoky.vercel.app/api/health` | `{"sheets":"ok","supabase":"ok","lastChecked":"..."}` HTTP 200 | PASS |
| /api/data preserves Sheets shape (READ_FROM=sheets default) | `curl -s "https://roas-dashboard-smoky.vercel.app/api/data?from=2026-05-19&to=2026-05-20"` | HTTP 200; keys=`['fxIlsToCad','lastUpdated','rows','stores']`; 6 rows returned | PASS |
| TypeScript compiles | `cd dashboard-web && npx tsc --noEmit` | exit 0 (no output) | PASS |
| Test suite passes | `cd dashboard-web && npx vitest run --passWithNoTests` | 347 passed \| 12 skipped (algorithm-parity intentionally skip-gated) | PASS |
| 0 .gs file modifications across Phase 05.6 batch | `git diff --name-only 0d90fe0..930d9d6 -- "*.gs"` | 0 files | PASS |
| User Manual v1.3 + §27 present | `grep -c "ניהול" docs/ROAS-Dashboard-User-Manual.md` and version check | header line 10 = "גרסה: 1.3"; footer line 2364 = "v1.3 · 2026-05-21 · Phase 05.6-22"; §27 spans 2100-2363 | PASS |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
|---|---|---|---|---|
| `dashboard-web/src/lib/fetchers/__tests__/snapshots/sheets-baseline-2026-05-15-to-2026-05-20.json` | 2 | `"_TODO": "STUB SNAPSHOT — regenerate from production Sheets before merging Phase 05.6-21 to main"` | Info | Intentional stub per Plan 21 SUMMARY. The parity test is skip-gated so the stub does not block CI. Operator regenerates pre-Phase-05.7 cut-over (one of the 2 human-verification items). NOT a blocker. |
| `.planning/phases/05.6-ts-port-inngest-operator-console/deferred-items.md` | 1-20 | `dashboard-web/package-lock.json drift (not committed)` | Info | Pre-existing lockfile drift discovered during Plan 14; explicitly deferred to future chore commit. Does NOT affect Phase 05.6 deliverables (tests pass, tsc clean, deploy verified). |

No `TODO`/`FIXME`/`HACK`/`PLACEHOLDER` patterns found in any of the 22-plan deliverable files. The single STUB marker is intentional, documented, and gated by skip-tests + operator workflow.

### Requirements Coverage

This phase does not declare granular REQ-IDs in PLAN frontmatter (`requirements:` fields not used). Coverage is by phase-goal deliverable inventory (above 22 must-haves) and CONTEXT.md Decision Sets A-G (29 decisions). Per Plan 22 SUMMARY "Phase Exit Checklist", all 29 decisions (D-A1 through D-G2) are honored end-to-end with concrete owner plans and evidence. This verification corroborates each via codebase inspection.

| Decision | Owner Plan | Honored In Codebase | Status |
|---|---|---|---|
| D-A1..D-A5 (Stack + rebuild-from-upstream + history floor + no-dual-write + manual_overrides load-bearing) | 01, 03-07, 14, 15, 20 | All artifacts present | SATISFIED |
| D-B1..D-B6 (Inngest free tier + 8 functions + TS + service_role + ON CONFLICT + retries) | 01, 02, 08-11 | 8 functions registered live; getSupabaseAdmin used; all upserts use ON CONFLICT | SATISFIED |
| D-C1..D-C5 (5 fetchers + pure + algorithm parity + no Apps Script refs) | 03-07 | All 5 present; 4 pure; parity test scaffold present | SATISFIED |
| D-D1..D-D4 (/operator route + 4 sub-views + 15s polling + matching design tokens) | 12-16 | page.tsx + 4 components + SWR 15000ms + Tailwind/lucide tokens | SATISFIED |
| D-E1..D-E3 (Postgres readers dormant + READ_FROM request-time + 8-route branching) | 17-19 | postgresReaders.ts (8 funcs) + featureFlags.ts (request-time) + 8 /api/* routes branched | SATISFIED |
| D-F1..D-F3 (One-off importer + Sheets read + idempotent) | 20 | import-manual-overrides.ts uses getAuth+getSupabaseAdmin with ignoreDuplicates | SATISFIED |
| D-G1..D-G2 (main branch only + CI auto-deploy) | All | 101 commits all on main; Vercel auto-deployed (user-confirmed live) | SATISFIED |

### Human Verification Required

#### 1. Operator regenerates production snapshot for algorithm-parity test

**Test:** Operator runs `cd dashboard-web && npx --yes tsx scripts/capture-snapshot.ts` with `.env.local` containing production `GOOGLE_CLIENT_EMAIL` + `GOOGLE_PRIVATE_KEY` + `SPREADSHEET_ID`, replacing the stub snapshot JSON. Then runs `npx vitest run src/lib/fetchers/__tests__/algorithm-parity.test.ts --testTimeout=120000` (with `it.skip` → `it` toggled or `--no-skip` flag).

**Expected:** `snapshots/sheets-baseline-2026-05-15-to-2026-05-20.json` updated with real `capturedAt` ISO timestamp (not "STUB"), `rowCount=18` (6 days × 3 stores), and real DailyRow values. The parity test passes within the ±5% / ±1 CAD band documented in the test file header.

**Why human:** Production env vars (`GOOGLE_CLIENT_EMAIL`, `GOOGLE_PRIVATE_KEY`, `SPREADSHEET_ID`) are not in the worktree and never can be (operator memory rule: production credentials live only in Vercel + operator's local shell). The stub snapshot is intentional per Plan 21 SUMMARY — the test scaffold is the real deliverable; baseline regeneration is pre-Phase-05.7 cut-over operator work.

#### 2. Operator observes next cron-daily tick + confirms Postgres writes

**Test:** Operator opens https://app.inngest.com at ~00:10 IL on 2026-05-22 (after the 00:05 IL cron-daily tick fires). Confirms all 3 `cron-daily-{uzoshop,zolplus,usmile360}` runs show status=Completed. Then runs a Postgres query (Supabase SQL editor) to confirm rows for 2026-05-21 exist in `data_daily`.

**Expected:** 3 successful cron-daily runs visible in Inngest UI; `SELECT * FROM data_daily WHERE date='2026-05-21'` returns 3 rows (one per store) with non-null `revenue_cad`, `total_spend_cad`, etc.

**Why human:** Live cron timing cannot be tested programmatically without waiting for the actual fire time (00:05 IL = ~21:05 UTC). Inngest dashboard inspection is a visual workflow. Per CONTEXT.md §27.9 step 8-9, this is the documented operator-side post-deploy verification gate.

### Gaps Summary

**No automated gaps.** All 22 plan must-haves are verified in the codebase:
- 5 TS fetchers exist + 4 are pure + 1 (manualOverrides) is intentionally Supabase-reading per D-C1
- Shopify reuses the Phase 05.2.3.0 algorithm (no re-derivation)
- 8 Inngest functions registered (3 cron-daily + 3 cron-live + sync-now + backfill); deploy-confirmed live
- 4 operator API routes + 4 operator components + /operator page+layout all wired
- READ_FROM feature flag is request-time-evaluated and 8 /api/* routes have branching (default sheets preserved)
- Importer + capture-snapshot script + parity test scaffold present (snapshot is intentional stub per Plan 21)
- User Manual v1.3 with §27 (10 sub-sections) shipped
- 0 .gs file modifications across the 101-commit Phase 05.6 batch (0d90fe0..930d9d6)
- TypeScript clean; 347 tests pass + 12 intentionally skipped

**Outstanding work — operator only:**
1. Regenerate production snapshot for algorithm-parity baseline (Plan 21 deferred deliverable).
2. Observe first cron-daily tick at 00:05 IL 2026-05-22 and confirm Postgres writes via Inngest dashboard.

Both items are explicitly out-of-scope for code verification (production env access required) and were anticipated in the phase plans — they are NOT gaps in execution but the natural human gate before Phase 05.7's cut-over.

### Process-Deviation Disposition

The user brief notes two process deviations from PLAN frontmatter:

- **Plan 11 (`/api/inngest` webhook) — no `export const dynamic = 'force-dynamic'`**: The executor declined this assertion based on inspection of the Inngest SDK's canonical pattern (`node_modules/inngest/next.d.ts` lines 38-44 show no route-segment config). The live deploy proves this was correct — PUT discovery returned HTTP 200 and registered all 8 functions. CHECK.md W3 raised this as a question; the executor's resolution is correct. NO verification failure.

- **Plan 21 (algorithm-parity snapshot) — STUB content**: The snapshot file's `_TODO` marker explicitly documents that the file is a stub awaiting operator regeneration with production env vars. The test scaffold is the real Plan 21 deliverable (skip-gated; doesn't block CI). NO verification failure — intentional design per Plan 21 SUMMARY.

---

_Verified: 2026-05-21T14:30:00Z_
_Verifier: Claude (gsd-verifier)_
