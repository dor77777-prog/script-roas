---
phase: 05.6-ts-port-inngest-operator-console
plan: 18
subsystem: infra
tags: [feature-flag, env-var, request-time-eval, dormant, READ_FROM, D-E2]

# Dependency graph
requires:
  - phase: 05.5-supabase-foundation
    provides: "Supabase Postgres + dashboard env-var infra (this plan adds a flag that switches between the existing Sheets reader and the future Postgres readers)"
provides:
  - "readFrom(): 'sheets' | 'postgres' — pure function evaluating process.env.READ_FROM at CALL TIME (no module-level cache)"
  - "Safety-defaults to 'sheets' for missing / unknown / wrong-case values"
  - "Five-case vitest coverage including the Pitfall-14 anti-cache guard"
affects:
  - "Plan 19 (api-route READ_FROM branching) — every /api/* route imports readFrom from @/lib/featureFlags"
  - "Plan 22 (operator console doc + user-manual update) — documents READ_FROM env var to operator"
  - "Phase 05.7 (cut-over) — flips Vercel env var to 'postgres' without redeploy"

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Request-time env-var read (Pattern 10 / Pitfall 14): export function — not module-level const — so Vercel env-var flips take effect on the next request without redeploy"

key-files:
  created:
    - "dashboard-web/src/lib/featureFlags.ts"
    - "dashboard-web/src/lib/__tests__/featureFlags.test.ts"
  modified: []

key-decisions:
  - "D-E2: READ_FROM evaluated at request time (function call), not at module load, to enable hot-flip via Vercel env var"
  - "Safety default: any value other than the exact string 'postgres' resolves to 'sheets' (case-sensitive)"
  - "Dormant in 05.6: function exists, has zero callers in 05.6 plan 18; plan 19 wires it into /api/* routes"

patterns-established:
  - "Pattern 10 — Feature flag (Request-time env read): 1-line ternary inside an exported function; no top-level const READ_FROM cache"

requirements-completed: [FEATURE-FLAG]

# Metrics
duration: 1m
completed: 2026-05-21
---

# Phase 05.6 Plan 18: READ_FROM Feature Flag Summary

**Tiny but load-bearing: `readFrom(): 'sheets' | 'postgres'` evaluated at request time — Vercel env-var flips take effect on next request without redeploy (D-E2 + Pitfall 14 guard).**

## Performance

- **Duration:** ~1 min
- **Started:** 2026-05-21T09:19:10Z
- **Completed:** 2026-05-21T09:19:59Z
- **Tasks:** 1 (TDD: RED + GREEN, no refactor required)
- **Files modified:** 2 created

## Accomplishments

- `readFrom()` exported as a plain function that re-reads `process.env.READ_FROM` on every call (NOT a module-level const — Pitfall 14 anti-pattern guard).
- Case-sensitive match against `'postgres'`; everything else (missing env, `'sheets'`, `'Invalid'`, `'POSTGRES'`) resolves to `'sheets'`.
- Five-case vitest coverage: default, explicit-sheets, explicit-postgres, unknown-value fallback, env-mutation-between-calls.
- Grep gate satisfied: zero `^const READ_FROM` / `^let READ_FROM` at module scope (T-05.6-18-T1 mitigation).

## Task Commits

Each task was committed atomically per TDD:

1. **Task 1 (RED): add failing tests for readFrom feature flag** — `c3dc7c8` (test)
2. **Task 1 (GREEN): implement readFrom() READ_FROM feature flag (D-E2)** — `996255f` (feat)

_REFACTOR phase skipped: implementation is already at minimum complexity (1-line ternary inside one exported function). Nothing to clean up without changing behavior._

## Files Created/Modified

### Created

- **`dashboard-web/src/lib/featureFlags.ts`** (19 lines) — `export function readFrom(): 'sheets' | 'postgres'`. Body is `return process.env.READ_FROM === 'postgres' ? 'postgres' : 'sheets'`. Comment block points at RESEARCH §Pitfall 14 and Phase 05.6 D-E2 for the rationale.
- **`dashboard-web/src/lib/__tests__/featureFlags.test.ts`** (45 lines) — Five `it` cases inside `describe('readFrom feature flag (D-E2)', ...)`. `beforeEach` deletes `process.env.READ_FROM`; `afterEach` restores the original. The fifth test mutates the env between calls in a single `it` block to prove there is no module-level cache.

### Full implementation (small enough to inline)

```typescript
// dashboard-web/src/lib/featureFlags.ts
export function readFrom(): 'sheets' | 'postgres' {
  return process.env.READ_FROM === 'postgres' ? 'postgres' : 'sheets';
}
```

### Verification output

```
$ npx tsc --noEmit
(exit 0, no output)

$ npx vitest run src/lib/__tests__/featureFlags.test.ts
 ✓ src/lib/__tests__/featureFlags.test.ts (5 tests) 2ms
 Test Files  1 passed (1)
      Tests  5 passed (5)
```

All three plan-verification block conditions satisfied:
1. `npx tsc --noEmit` exits 0.
2. `npx vitest run src/lib/__tests__/featureFlags.test.ts` exits 0 (5 pass).
3. `! grep -q "^const READ_FROM\|^let READ_FROM" dashboard-web/src/lib/featureFlags.ts` (no module-level cache).

## Decisions Made

None beyond what is already locked in `<decisions>` D-E2 and RESEARCH Pattern 10. Implementation matches the plan's `<action>` block byte-for-byte (modulo comment formatting).

## Deviations from Plan

None - plan executed exactly as written. Both files match the plan's `<action>` block; the verification block in `<verify>` and `<verification>` passed on the first GREEN run.

## Issues Encountered

- The worktree did not have `dashboard-web/node_modules/` populated. Resolved by symlinking the parent repo's installed `node_modules` (`ln -sf /Users/dorperetz/script-roas/dashboard-web/node_modules dashboard-web/node_modules`). `node_modules/` is in `dashboard-web/.gitignore`, so the symlink is not (and cannot be) committed. This is purely an environment-setup detail — not a behavior change.

## Threat Mitigation

| Threat ID | Disposition | Mitigation status |
|-----------|-------------|-------------------|
| T-05.6-18-T1 (Tampering — module-level caching defeats hot-flip semantics) | mitigate | **Implemented.** Test 5 (`reads env at each call (not cached)`) mutates `process.env.READ_FROM` between two `readFrom()` calls and asserts the second call reflects the new value. Grep gate `! grep -q "^const READ_FROM\|^let READ_FROM" dashboard-web/src/lib/featureFlags.ts` passes (0 matches). |

No new threat surface introduced — `readFrom()` does not touch the network, file system, database, or user input. It is a pure read of `process.env`.

## Notes for Downstream Plans

- **Plan 19 (Wave 2 — /api/* route branching):** import path is `import { readFrom } from '@/lib/featureFlags';`. Use inside each route handler, not at module scope: `if (readFrom() === 'postgres') { return readFromPostgres(...); } else { return readFromSheets(...); }`. Both branches must return the same shape (D-E3).
- **Plan 22 (USER-MANUAL-UPDATE) + Phase 05.7 (cut-over):** the Vercel env var `READ_FROM` defaults to absent — `readFrom()` returns `'sheets'`. The operator flips the flag to `'postgres'` via the Vercel UI in Phase 05.7 with no redeploy required (each Vercel function invocation re-reads `process.env`). The user manual update in plan 22 should explain this hot-flip behavior to the operator.

## Next Phase Readiness

- Feature-flag module is ready and dormant. Plan 19 (next wave) can wire `readFrom()` into every `/api/*` route.
- No blockers, no checkpoints, no auth gates.

## Self-Check: PASSED

- `dashboard-web/src/lib/featureFlags.ts` — FOUND.
- `dashboard-web/src/lib/__tests__/featureFlags.test.ts` — FOUND.
- Commit `c3dc7c8` (RED) — FOUND in git log.
- Commit `996255f` (GREEN) — FOUND in git log.

---
*Phase: 05.6-ts-port-inngest-operator-console*
*Plan: 18*
*Completed: 2026-05-21*
