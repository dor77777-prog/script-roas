---
phase: 05.6-ts-port-inngest-operator-console
plan: 12
subsystem: dashboard-web/app/operator
tags: [operator-console, dashboard-ui, rtl, hebrew, sidebar-link, page-shell]

dependency_graph:
  requires:
    - dashboard-web/src/components/Dashboard.tsx (existing header)
    - dashboard-web/src/app/layout.tsx (existing RTL root)
    - 05.6-02 (no direct code dep — declared because the operator console will
      consume Inngest plumbing landed in 02; the shell itself imports nothing
      from 02 yet)
  provides:
    - dashboard-web/src/app/operator/page.tsx (landing page with 4 placeholder sections)
    - dashboard-web/src/app/operator/layout.tsx (RTL wrapper)
    - /operator route registered in Next.js manifest
    - header sidebar link with Hebrew "ניהול" label
  affects:
    - dashboard-web/src/components/Dashboard.tsx (added Cog icon + Link in Header())
    - plans 13-16 (each plan replaces ONE section's body with its component)

tech_stack:
  added: []
  patterns:
    - S-8 RTL + Hebrew layout (dir="rtl", Hebrew labels, lucide-react icons)
    - D-D1 sibling Next.js route (NOT a TabKey)
    - D-D4 match existing dashboard tokens (no new design primitives)

key_files:
  created:
    - dashboard-web/src/app/operator/page.tsx
    - dashboard-web/src/app/operator/layout.tsx
  modified:
    - dashboard-web/src/components/Dashboard.tsx

decisions:
  - "Insertion point for sidebar link: Header() flex cluster (between SyncIndicator and Refresh button), styled identically to the existing white-on-navy header buttons. This is the 'Preferred' insertion point listed in Task 2's <action>."
  - "Used `lucide-react` Cog icon for the link (consistent with existing dashboard icon library; matches S-8 convention)."
  - "Placeholder Hebrew copy 'ממומש בשלב הבא' inside each section — explicit signpost that plans 13-16 fill these in. NOT a UI bug; documented in 'Known Stubs' below."
  - "Did NOT add `Cog` import to TabNav.tsx — the operator link is OUTSIDE the in-page tab nav per D-D1."

metrics:
  duration_seconds: 421
  duration_human: "~7 minutes"
  completed_date: 2026-05-21
  tasks_completed: 2
  files_created: 2
  files_modified: 1
  commits: 2

requirements:
  - OPERATOR-ROUTE (complete)

threat_model:
  flags: []
  notes:
    - "T-05.6-12-I1 (accept): /operator accessible to anyone with the URL. URL-obscurity per project trust model (memory rule project_script_roas_dashboard.md). Same posture as the rest of the dashboard."
    - "T-05.6-12-T2 (mitigate): pre-merge `npm run build` + visual header inspection. Build passed (exit 0); header markup change is a single new <Link> element next to <SyncIndicator />."
---

# Phase 05.6 Plan 12: Operator Console Route Shell Summary

**One-liner:** Created the `/operator` route shell (Hebrew "ניהול") + sidebar link in the dashboard header — landing page with 4 placeholder sections that plans 13-16 will fill in (JobsTable, BackfillPicker, ManualOverridesCrud, SyncNowButtons).

## What landed

| File | Lines | Purpose |
| ---- | ----- | ------- |
| `dashboard-web/src/app/operator/page.tsx` | 66 | Landing page with h1 "ניהול" + 4 placeholder sections |
| `dashboard-web/src/app/operator/layout.tsx` | 29 | RTL wrapper (inherits dashboard tokens) |
| `dashboard-web/src/components/Dashboard.tsx` | +14 | Sidebar link to /operator in Header() |

Total diff: 95 insertions across 2 new files + 14 insertions to the existing Dashboard header.

## Architecture

```
app/
├── layout.tsx                  (existing root — <html dir="rtl" lang="he">)
├── page.tsx                    (existing — renders <Dashboard />)
├── operator/                   ← NEW
│   ├── layout.tsx              ← RTL wrapper, metadata title "ניהול"
│   └── page.tsx                ← 4 <section> placeholders, each ~10 lines
└── api/...                     (unchanged)
```

The operator route is a **sibling** of the main dashboard (per D-D1) — NOT a tab inside the existing in-page TabNav. This isolates the operator console from the consumer-facing dashboard's filter/aggregate state machinery.

## Task 1 details: `app/operator/page.tsx` + `layout.tsx`

**Acceptance criteria — all met:**

| Criterion | Status |
| --- | --- |
| `page.tsx` exists | PASS |
| `layout.tsx` exists | PASS |
| Hebrew title `ניהול` present in `page.tsx` | PASS |
| `dir="rtl"` set in `layout.tsx` | PASS |
| 4 section headers present (סנכרון עכשיו, ריצות אחרונות, Backfill טווח תאריכים, החלפות הוצאה ידניות) | PASS |
| `min_lines: 30` for page.tsx | PASS (66 lines) |
| `npx tsc --noEmit` exits 0 | PASS |
| `npm run build` exits 0 | PASS (`/operator  319 B  168 kB` static route) |

**Section ↔ plan mapping** (commented inline in page.tsx for future plans):

| Section heading | Plan | Component |
| --- | --- | --- |
| סנכרון עכשיו | 16 | `SyncNowButtons` |
| ריצות אחרונות | 13 | `JobsTable` |
| Backfill טווח תאריכים | 14 | `BackfillPicker` |
| החלפות הוצאה ידניות | 15 | `ManualOverridesCrud` |

## Task 2 details: sidebar link in `Dashboard.tsx`

**Acceptance criteria — all met:**

| Criterion | Status |
| --- | --- |
| `href="/operator"` link present in Dashboard.tsx | PASS |
| Link is OUTSIDE the TABS array (not a TabKey) | PASS (`grep -c 'TabKey.*operator' = 0`) |
| `npm run build` exits 0 | PASS |
| Existing routes unchanged | PASS (only `/` size changed: 217 kB → 219 kB from new Link import + Cog icon — expected) |

**Imports added to Dashboard.tsx:**
- `Link` from `next/link`
- `Cog` from `lucide-react`

**Visual style:** matches the existing `<SyncIndicator />` and `Refresh` button in the same header cluster — `bg-white/12 hover:bg-white/20 active:bg-white/25 + ring-1 ring-white/10`. Icon-only on mobile (`hidden sm:inline` on label span), icon + Hebrew label on `>=sm`. `aria-label="ניהול"` for accessibility on the icon-only mobile view.

## Build manifest snippet (post-Task 2)

```
Route (app)                                 Size  First Load JS  Revalidate  Expire
┌ ○ /                                     219 kB         387 kB
├ ○ /_not-found                          1.15 kB         169 kB
├ ƒ /api/ads                               319 B         168 kB
├ ƒ /api/campaigns                         319 B         168 kB
├ ƒ /api/dashboard-state                   319 B         168 kB
├ ƒ /api/data                              319 B         168 kB
├ ○ /api/health                            319 B         168 kB         10s      1y
├ ƒ /api/orders-attribution                320 B         168 kB
├ ○ /api/product-catalog                   318 B         168 kB          1m      1y
├ ƒ /api/products                          319 B         168 kB
├ ○ /api/store-meta                        319 B         168 kB          1h      1y
└ ○ /operator                              319 B         168 kB
```

`/operator` registered as a **static** route (`○`), 319 B page size + 168 kB shared First Load JS — same envelope as the API routes (which use the framework boilerplate). No revalidate config (page is pure-static placeholders until plans 13-16 add client components / data fetches).

## Verification evidence

### Automated (Task 1 + Task 2)

| # | Command | Result |
| --- | --- | --- |
| 1 | `[ -f src/app/operator/page.tsx ]` | PASS |
| 1 | `[ -f src/app/operator/layout.tsx ]` | PASS |
| 1 | `grep -q "ניהול" src/app/operator/page.tsx` | PASS |
| 1 | `grep -q 'dir="rtl"' src/app/operator/layout.tsx` | PASS |
| 1 | `grep -qE "ריצות אחרונות\|Backfill טווח\|החלפות הוצאה" src/app/operator/page.tsx` | PASS |
| 1 | `npx tsc --noEmit` | exit 0 |
| 1 | `npm run build` | exit 0 |
| 2 | `grep -q 'href="/operator"' src/components/Dashboard.tsx` | PASS |
| 2 | `grep -qE "ניהול\|Cog" src/components/Dashboard.tsx` | PASS |
| 2 | `grep -c 'TabKey.*operator' src/components/Dashboard.tsx` | 0 (correct — sibling route, not a tab) |
| 2 | `npx tsc --noEmit` | exit 0 |
| 2 | `npm run build` | exit 0 |
| 2 | `npm run test` | 250/250 PASS (no regression) |

### Production smoke test (deferred to plan 22)

`curl -sI https://roas-dashboard-smoky.vercel.app/operator` is deferred per the plan's `<verification>` block — the production deploy happens after the phase merge. Plan 22 verifies the production HTML contains "ניהול".

## Commits

| # | Hash | Message |
| --- | --- | --- |
| 1 | `9272f0f` | `feat(05.6-12): add /operator route shell with Hebrew placeholders` |
| 2 | `29f068c` | `feat(05.6-12): add /operator sidebar link to dashboard header` |

## Deviations from Plan

None — plan executed exactly as written. The "Preferred" insertion point (alongside SyncIndicator in the header) was chosen over the alternative (separate nav row next to TabNav). All acceptance criteria were met on the first pass; tsc + build + vitest all passed without any auto-fixes.

### Environment note (not a deviation)

The worktree had no `node_modules`, so `npm install` was needed before `tsc`/`build` could run. `npm install` updated `dashboard-web/package-lock.json` (semver-compatible sub-dependency churn from re-resolving versions). This change was **reverted before committing** (`git checkout -- dashboard-web/package-lock.json`) so Plan 12's commits contain only the route shell + sidebar link, not unrelated dependency-lock churn. Re-install in any future worktree will re-trigger the same lockfile diff; it is not a Plan 12 artifact.

## Known Stubs

The 4 placeholder `<section>` bodies are **intentional stubs by design** — this plan creates the page shell only. Plans 13-16 each replace ONE section's body with a real component. The stubs are explicitly signposted to the operator:

| File | Section | Placeholder text | Resolved by |
| --- | --- | --- | --- |
| `app/operator/page.tsx:32-37` | סנכרון עכשיו | "(כפתורי סנכרון יופיעו כאן — ממומש בשלב הבא)" | Plan 16 (SyncNowButtons) |
| `app/operator/page.tsx:40-45` | ריצות אחרונות | "(טבלת ריצות Inngest תופיע כאן — ממומש בשלב הבא)" | Plan 13 (JobsTable) |
| `app/operator/page.tsx:48-53` | Backfill טווח תאריכים | "(בורר טווח תאריכים יופיע כאן — ממומש בשלב הבא)" | Plan 14 (BackfillPicker) |
| `app/operator/page.tsx:56-61` | החלפות הוצאה ידניות | "(CRUD על manual_overrides יופיע כאן — ממומש בשלב הבא)" | Plan 15 (ManualOverridesCrud) |

These do **NOT** block plan acceptance — the plan's `must_haves.truths[1]` explicitly states "Page renders 4 placeholder sections: ...". The User Manual will not document `/operator` until plan 22 (when the page is functional); the placeholder copy is operator-facing acknowledgement that work-in-progress is expected.

## Note for plans 13-16

Each downstream plan should:

1. Create ONE new component file under `dashboard-web/src/components/operator/<Component>.tsx` (matches "Plans 13-16 / from / operator/page.tsx" in must_haves.key_links).
2. Modify ONE section's body in `app/operator/page.tsx` to import + render the new component (replace the placeholder `<p>`).
3. NOT restructure the page or change `app/operator/layout.tsx`.
4. NOT add a TabKey to Dashboard.tsx — the operator console stays a sibling route per D-D1.

The header sidebar link added in this plan (`<Link href="/operator">` with `Cog` icon + "ניהול" label) is the SINGLE entry point — plans 13-16 do not need to add any more nav.

## Self-Check: PASSED

**Files created exist:**
- `dashboard-web/src/app/operator/page.tsx` — FOUND
- `dashboard-web/src/app/operator/layout.tsx` — FOUND

**Files modified exist:**
- `dashboard-web/src/components/Dashboard.tsx` — FOUND (contains `href="/operator"`)

**Commits exist in git log:**
- `9272f0f` — FOUND
- `29f068c` — FOUND
