---
phase: 05.6-ts-port-inngest-operator-console
plan: 12
type: execute
wave: 5
depends_on: [02]
files_modified:
  - dashboard-web/src/app/operator/page.tsx
  - dashboard-web/src/app/operator/layout.tsx
  - dashboard-web/src/components/Dashboard.tsx
autonomous: true
requirements: [OPERATOR-ROUTE]
decisions_addressed: [D-D1, D-D2, D-D4]
tags: [operator-console, dashboard-ui, rtl, hebrew, sidebar-link]

must_haves:
  truths:
    - "Route /operator exists at dashboard-web/src/app/operator/page.tsx (Pattern 5 Option A — sibling Next.js route, NOT a TabKey)"
    - "Page renders 4 placeholder sections: סנכרון עכשיו, ריצות אחרונות, Backfill טווח תאריכים, החלפות הוצאה ידניות"
    - "Page has dir='rtl' and uses Hebrew section headings per D-D1"
    - "Dashboard.tsx has a new sidebar link to /operator labelled 'ניהול' (Hebrew tab name from D-D1)"
    - "Layout file applies dark sidebar styling matching existing dashboard (D-D4)"
    - "next build passes; tsc passes"
  artifacts:
    - path: "dashboard-web/src/app/operator/page.tsx"
      provides: "Operator console landing page with 4 section placeholders"
      contains: "ניהול"
      min_lines: 30
    - path: "dashboard-web/src/app/operator/layout.tsx"
      provides: "RTL layout wrapper for /operator (inherits dashboard design tokens)"
      contains: "dir=\"rtl\""
    - path: "dashboard-web/src/components/Dashboard.tsx"
      provides: "Sidebar link to /operator added to header"
      contains: "/operator"
  key_links:
    - from: "Dashboard.tsx header"
      to: "/operator"
      via: "<Link href=\"/operator\">ניהול</Link>"
      pattern: "href=\"/operator\""
    - from: "Plans 13-16"
      to: "operator/page.tsx"
      via: "Component imports (JobsTable, BackfillPicker, ManualOverridesCrud, SyncNowButtons)"
      pattern: "from '@/components/operator/"
---

<objective>
Create the `/operator` route + sidebar entry, with 4 placeholder sections that plans 13-16 fill in. This is the scaffolding plan — the components themselves come later.

Purpose: Get the route live early so plans 13-16 can be built incrementally; each plan adds ONE component without changing page.tsx.

Output: 3 files — `app/operator/page.tsx`, `app/operator/layout.tsx`, modification to `components/Dashboard.tsx` (adds sidebar link).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05.6-ts-port-inngest-operator-console/05.6-CONTEXT.md
@.planning/phases/05.6-ts-port-inngest-operator-console/05.6-RESEARCH.md
@dashboard-web/src/components/Dashboard.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create operator route + layout + page placeholders</name>
  <files>
    dashboard-web/src/app/operator/page.tsx,
    dashboard-web/src/app/operator/layout.tsx
  </files>
  <read_first>
    - `.planning/phases/05.6-ts-port-inngest-operator-console/05.6-RESEARCH.md` §Pattern 5 (lines 678-737)
  </read_first>
  <action>
    Create `layout.tsx` (RTL wrapper):

    ```typescript
    // dashboard-web/src/app/operator/layout.tsx
    import type { ReactNode } from 'react';

    export const metadata = {
      title: 'ניהול — ROAS Dashboard',
    };

    export default function OperatorLayout({ children }: { children: ReactNode }) {
      return (
        <div dir="rtl" className="min-h-screen bg-background text-foreground">
          {children}
        </div>
      );
    }
    ```

    Create `page.tsx` per RESEARCH §Pattern 5 lines 697-728. For 05.6-12 we use placeholders; plans 13-16 replace each `<section>` body with the real component:

    ```typescript
    // dashboard-web/src/app/operator/page.tsx
    //
    // ניהול (Management) — operator console tab. Per D-D1, this is a sibling Next.js
    // route (NOT a TabKey in the main dashboard's in-page tab nav). The 4 sub-sections
    // are filled in incrementally by plans 13-16:
    //
    //   - Plan 13: JobsTable
    //   - Plan 14: BackfillPicker
    //   - Plan 15: ManualOverridesCrud
    //   - Plan 16: SyncNowButtons (and the global "Sync now" button)
    //
    // Until each component lands, these sections render placeholder paragraphs.

    export const metadata = {
      title: 'ניהול — ROAS Dashboard',
    };

    export default function OperatorPage() {
      return (
        <div className="max-w-7xl mx-auto px-4 py-6 space-y-8">
          <header>
            <h1 className="text-2xl font-bold">ניהול</h1>
            <p className="text-text-secondary text-sm">
              ניהול אוטומציה: ריצות Inngest, backfill, החלפות ידניות, ו-Sync.
            </p>
          </header>

          <section>
            <h2 className="text-lg font-semibold mb-3">סנכרון עכשיו</h2>
            <p className="text-text-secondary text-sm">
              {/* Plan 16: SyncNowButtons */}
              (כפתורי סנכרון יופיעו כאן — ממומש בשלב הבא)
            </p>
          </section>

          <section>
            <h2 className="text-lg font-semibold mb-3">ריצות אחרונות</h2>
            <p className="text-text-secondary text-sm">
              {/* Plan 13: JobsTable */}
              (טבלת ריצות Inngest תופיע כאן — ממומש בשלב הבא)
            </p>
          </section>

          <section>
            <h2 className="text-lg font-semibold mb-3">Backfill טווח תאריכים</h2>
            <p className="text-text-secondary text-sm">
              {/* Plan 14: BackfillPicker */}
              (בורר טווח תאריכים יופיע כאן — ממומש בשלב הבא)
            </p>
          </section>

          <section>
            <h2 className="text-lg font-semibold mb-3">החלפות הוצאה ידניות</h2>
            <p className="text-text-secondary text-sm">
              {/* Plan 15: ManualOverridesCrud */}
              (CRUD על manual_overrides יופיע כאן — ממומש בשלב הבא)
            </p>
          </section>
        </div>
      );
    }
    ```
  </action>
  <verify>
    <automated>cd dashboard-web && [ -f src/app/operator/page.tsx ] && [ -f src/app/operator/layout.tsx ] && grep -q "ניהול" src/app/operator/page.tsx && grep -q 'dir="rtl"' src/app/operator/layout.tsx && grep -qE "ריצות אחרונות|Backfill טווח|החלפות הוצאה" src/app/operator/page.tsx && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Both files exist
    - Hebrew title `ניהול` present in page.tsx
    - RTL direction set in layout.tsx
    - 4 section headers present (placeholder bodies OK)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Operator route exists; plans 13-16 fill the sections.</done>
</task>

<task type="auto">
  <name>Task 2: Add /operator sidebar link to Dashboard.tsx</name>
  <files>dashboard-web/src/components/Dashboard.tsx</files>
  <read_first>
    - `dashboard-web/src/components/Dashboard.tsx` — current header/sidebar structure (the file lists TABS in-page; the operator link is OUTSIDE that)
  </read_first>
  <action>
    Locate the dashboard's existing header/nav region (above or alongside the in-page TabNav). Add a Next.js `<Link>` to `/operator` styled consistently with existing nav elements.

    Acceptable insertion points:
    1. **Preferred:** alongside the SyncIndicator in the header (top-right area), as an icon-link. Use Lucide `Settings` or `Cog` icon + Hebrew label "ניהול".
    2. **Alternative:** as a separate horizontal nav item next to the in-page TabNav.

    Sample insertion (top-right header — adjust to match existing header markup):

    ```typescript
    import Link from 'next/link';
    import { Cog } from 'lucide-react';
    // ... existing imports ...

    // Inside the header element where SyncIndicator renders:
    <Link
      href="/operator"
      className="flex items-center gap-1 text-sm text-text-secondary hover:text-foreground"
      aria-label="ניהול"
    >
      <Cog className="w-4 h-4" />
      <span className="hidden md:inline">ניהול</span>
    </Link>
    ```

    The change should be minimal (single new `<Link>` element). Do NOT touch the TabKey union or TABS array — the operator route is a sibling page, NOT a tab.

    Verify the existing dashboard still renders correctly (no broken layout, no missing imports).
  </action>
  <verify>
    <automated>cd dashboard-web && grep -q 'href="/operator"' src/components/Dashboard.tsx && grep -qE "ניהול|Cog" src/components/Dashboard.tsx && [ "$(grep -c 'TabKey.*operator' src/components/Dashboard.tsx)" = "0" ] && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Dashboard.tsx contains `href="/operator"` link
    - Link is OUTSIDE the TABS array (not added as a TabKey)
    - `npm run build` exits 0
    - Existing routes (e.g., `/api/data`, `/api/store-meta`) unchanged
  </acceptance_criteria>
  <done>Sidebar link to /operator added; main dashboard's in-page tabs unaffected.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.6-12-I1 | Info Disclosure | /operator route accessible to anyone with URL | accept | URL-obscurity (D-D2). Documented in User Manual §1.7. Plan 22 updates manual. |
| T-05.6-12-T2 | Tampering | Sidebar link breaks existing layout | mitigate | Pre-merge `npm run build` + visual inspection of header. |
</threat_model>

<verification>
- `cd dashboard-web && npx tsc --noEmit` exits 0
- `cd dashboard-web && npm run build` exits 0 (page registered as `/operator`)
- `curl -sI https://roas-dashboard-smoky.vercel.app/operator` returns 200 + HTML (post-deploy smoke; verify in plan 22 since deploy happens after merge)
</verification>

<success_criteria>
Operator route is live. Plans 13-16 replace placeholders. Plan 22 verifies production HTML contains "ניהול".
</success_criteria>

<output>
After completion, create `.planning/phases/05.6-ts-port-inngest-operator-console/05.6-12-SUMMARY.md` recording:
- 3 files created/modified
- Confirmation `/operator` is a sibling route (NOT a TabKey)
- Build manifest snippet
- Note for plans 13-16: each plan modifies ONE section's body in page.tsx and creates ONE component file
</output>
