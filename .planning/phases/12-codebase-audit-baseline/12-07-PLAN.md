---
phase: 12-codebase-audit-baseline
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-codebase-audit-baseline/raw-returns/
autonomous: true
requirements: [REQ-1, REQ-2, REQ-3, REQ-4, REQ-5]
tags: [audit, wave-G, components-borderline]

must_haves:
  truths:
    - "Each of the 9 borderline components has a structured JSON return in raw-returns/"
  artifacts:
    - path: ".planning/phases/12-codebase-audit-baseline/raw-returns/cmp_borderline_*.json"
      provides: "Structured per-file audit returns for Wave G"
  key_links:
    - from: "9 parallel reviewers"
      to: "raw-returns/*.json"
      via: "scaffolding template"
      pattern: "parallel-spawn"
---

<objective>
**Wave G — components-borderline (9 reviewers, parallel).** Components SPEC.md flagged as "Definitely IN" beyond the DP-01 grep-flagged set: 4 grep-missed-but-have-derivation-logic + 5 operator-confirmed-IN.

Purpose: Cover the components a naive grep would miss because their derivation logic is subtle (computed-style values, prop transforms, conditional fetch branches).
Output: 9 JSON returns in `raw-returns/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/12-codebase-audit-baseline/12-SPEC.md
@.planning/phases/12-codebase-audit-baseline/12-CONTEXT.md
</context>

<reviewer_scaffolding>
Same scaffolding. These components specifically warrant a "show me the derivation" eye — reviewer should focus on whether any prop transform / inline calculation hides logic that the grep heuristic missed.
</reviewer_scaffolding>

<tasks>

<task type="auto" id="12.G.1">
  <name>Review components/CampaignsTableRow.tsx</name>
  <files>dashboard-web/src/components/CampaignsTableRow.tsx</files>
  <action>Spawn gsd-code-reviewer. Single row render with chips (derivation logic for trust chip, health chip). Output → `raw-returns/cmp_borderline_CampaignsTableRow.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_borderline_CampaignsTableRow.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.G.2">
  <name>Review components/CohortComparisonPanel.tsx</name>
  <files>dashboard-web/src/components/CohortComparisonPanel.tsx</files>
  <action>Spawn gsd-code-reviewer. Cross-ref lib/multiMappingCohort.ts (Wave A) + lib/cannibalizationDetection.ts. Output → `raw-returns/cmp_borderline_CohortComparisonPanel.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_borderline_CohortComparisonPanel.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.G.3">
  <name>Review components/RoasChart.tsx</name>
  <files>dashboard-web/src/components/RoasChart.tsx</files>
  <action>Spawn gsd-code-reviewer. Recharts wrapper with axis/legend derivation. Output → `raw-returns/cmp_borderline_RoasChart.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_borderline_RoasChart.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.G.4">
  <name>Review components/Sparkline.tsx</name>
  <files>dashboard-web/src/components/Sparkline.tsx</files>
  <action>Spawn gsd-code-reviewer. Cross-ref lib/sparklineGeometry.ts. TEST_PATH=`dashboard-web/src/lib/__tests__/sparklineGeometry.test.ts`. Output → `raw-returns/cmp_borderline_Sparkline.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_borderline_Sparkline.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.G.5">
  <name>Review components/AdSetTable.tsx</name>
  <files>dashboard-web/src/components/AdSetTable.tsx</files>
  <action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_borderline_AdSetTable.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_borderline_AdSetTable.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.G.6">
  <name>Review components/DetailTable.tsx</name>
  <files>dashboard-web/src/components/DetailTable.tsx</files>
  <action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_borderline_DetailTable.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_borderline_DetailTable.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.G.7">
  <name>Review components/HealthScorePanel.tsx</name>
  <files>dashboard-web/src/components/HealthScorePanel.tsx</files>
  <action>Spawn gsd-code-reviewer. Cross-ref lib/campaignHealthScore.ts. Output → `raw-returns/cmp_borderline_HealthScorePanel.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_borderline_HealthScorePanel.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.G.8">
  <name>Review components/InsightsPanel.tsx</name>
  <files>dashboard-web/src/components/InsightsPanel.tsx</files>
  <action>Spawn gsd-code-reviewer. Cross-ref lib/insights.ts. Output → `raw-returns/cmp_borderline_InsightsPanel.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_borderline_InsightsPanel.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.G.9">
  <name>Review components/PerStoreCards.tsx</name>
  <files>dashboard-web/src/components/PerStoreCards.tsx</files>
  <action>Spawn gsd-code-reviewer. Per-store aggregation derivation. Output → `raw-returns/cmp_borderline_PerStoreCards.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_borderline_PerStoreCards.json</automated></verify>
  <done>JSON return exists.</done>
</task>

</tasks>

<verification>
All 9 raw-return files exist. Zero source-code modifications.
</verification>

<success_criteria>
Wave G complete: 9 JSON returns. Combined with prior waves: 12 (A) + 16 (B) + 37 (C) + 6 (D) + 19 (E) + 41 (F) + 9 (G) = 140 raw returns covering the 134 in-scope files (a handful are touched by two waves, e.g. productCentricView in A + ProductCentricView component in F — both are legitimate per the per-file ownership).
</success_criteria>

<output>
No SUMMARY.md. After this wave completes, orchestrator moves to the mid-execution checkpoint in 12-PLAN.md.
</output>
</content>
</invoke>