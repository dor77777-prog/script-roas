---
phase: 12-codebase-audit-baseline
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-codebase-audit-baseline/raw-returns/
autonomous: true
requirements: [REQ-1, REQ-2, REQ-3, REQ-4, REQ-5]
tags: [audit, wave-F, components-bizlogic]

must_haves:
  truths:
    - "Each of the 40 business-logic components has a structured JSON return in raw-returns/"
    - "Split into 4 sub-waves of ~10 to respect DP-01 concurrency cap"
  artifacts:
    - path: ".planning/phases/12-codebase-audit-baseline/raw-returns/cmp_*.json"
      provides: "Structured per-file audit returns for Wave F"
  key_links:
    - from: "40 parallel reviewers (4 sub-waves)"
      to: "raw-returns/*.json"
      via: "scaffolding template"
      pattern: "parallel-spawn (sub-waved)"
---

<objective>
**Wave F — components-bizlogic (40 reviewers in 4 sub-waves of ~10).** All UI components grep-flagged as having state/effects/fetches per CONTEXT.md DP-01.

Purpose: Audit per-component derivation logic, fetch wiring, state handling, and operator constraint compliance.
Output: 40 JSON returns in `raw-returns/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/12-codebase-audit-baseline/12-SPEC.md
@.planning/phases/12-codebase-audit-baseline/12-CONTEXT.md
@.planning/codebase/CONVENTIONS.md
</context>

<reviewer_scaffolding>
Same scaffolding. Operator constraints especially load-bearing here:
- **GoalTracker GLOBAL** (ignores filters.store + filters.range — NEVER flag as bug)
- **TodayLive always LIVE** (own SWR fetch — NEVER flag as bug)
- **WhatsApp alerts ONLY to +972524809540** (NEVER flag as bug)
- **Hebrew RTL — start/end Tailwind properties** (NEVER flag `left/right` as missing — they're intentional)
- **Per-store COGS via ${STORE}_COGS_RATE env var** (NEVER flag as hardcoded)

Component tests are sparse (1 spec total per STRUCTURE.md). Most coverage relies on lib/ tests. Reviewer should flag missing component-level tests as test gaps in inline list.

**Sub-wave F1 — Dashboard shell + KPI surfaces + filters (10 components):** Dashboard, CloudSync, SyncIndicator, TabNav, Filters, KpiCards, HeroOverview, FreshnessChip, RollingNumber, CollapsibleSection.

**Sub-wave F2 — Campaigns + Drawer (10 components):** CampaignsTable, CampaignsColumnsMenu, CampaignDrawer, AdsDrawer, AttributionAnalysisPanel (note: it IS in scope per DP-01 even though SPEC §Boundaries OUT-list mentions it — DP-01 reviewer-list is authoritative), MetaShopifyReconciliation, ProductChannelBreakdown, MetricHelp, AiReportButton, AnnotationsPanel.

**NOTE on AttributionAnalysisPanel:** SPEC.md §Boundaries lists it as OUT (pure presentation). DP-01 includes it because operator confirmed it has derivation logic post-U-05 (halo-warning chip). When in doubt, include — reviewer issues a thin "✅ Verified, presentation-only confirmed" verdict if it really has zero logic.

**Sub-wave F3 — Products + Insights + Goals (10 components):** ProductsTable, ProductPickerModal, ProductCentricView, GoalTracker (GLOBAL constraint!), InsightsBoard, WhatsWorking, RefundIndicator, MonthlyTables, TodayLive (LIVE constraint!), PnLBreakdown.

**Sub-wave F4 — Billing + operator/* + misc (10 components):** BillingSettings, BillingCsvImport, CommandPalette, HealthScoreBadge, operator/BackfillPicker, operator/JobsTable, operator/ManualOverridesCrud, operator/ResetData, operator/SyncNowButtons, operator/TokenFailuresTable, operator/WhatsappTestButtons.

That's 41 components (one extra in F4 — operator group has 7). Orchestrator may move one operator/* into F3's slot count or accept the 11-task micro-wave.
</reviewer_scaffolding>

<tasks>

<!-- Sub-wave F1 (10) -->
<task type="auto" id="12.F1.1"><name>Review components/Dashboard.tsx</name><files>dashboard-web/src/components/Dashboard.tsx</files><action>Spawn gsd-code-reviewer. 6-tab shell. TEST_PATH=`dashboard-web/src/components/__tests__/Dashboard.test.tsx` (likely none). Output → `raw-returns/cmp_Dashboard.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_Dashboard.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F1.2"><name>Review components/CloudSync.tsx</name><files>dashboard-web/src/components/CloudSync.tsx</files><action>Spawn gsd-code-reviewer. Mounts hydrate + 30s poll. Output → `raw-returns/cmp_CloudSync.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_CloudSync.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F1.3"><name>Review components/SyncIndicator.tsx</name><files>dashboard-web/src/components/SyncIndicator.tsx</files><action>Spawn gsd-code-reviewer. Status pill. SWR ternary extension from Phase 05.5-03. Output → `raw-returns/cmp_SyncIndicator.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_SyncIndicator.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F1.4"><name>Review components/TabNav.tsx</name><files>dashboard-web/src/components/TabNav.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_TabNav.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_TabNav.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F1.5"><name>Review components/Filters.tsx</name><files>dashboard-web/src/components/Filters.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_Filters.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_Filters.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F1.6"><name>Review components/KpiCards.tsx</name><files>dashboard-web/src/components/KpiCards.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_KpiCards.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_KpiCards.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F1.7"><name>Review components/HeroOverview.tsx</name><files>dashboard-web/src/components/HeroOverview.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_HeroOverview.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_HeroOverview.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F1.8"><name>Review components/FreshnessChip.tsx</name><files>dashboard-web/src/components/FreshnessChip.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_FreshnessChip.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_FreshnessChip.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F1.9"><name>Review components/RollingNumber.tsx</name><files>dashboard-web/src/components/RollingNumber.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_RollingNumber.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_RollingNumber.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F1.10"><name>Review components/CollapsibleSection.tsx</name><files>dashboard-web/src/components/CollapsibleSection.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_CollapsibleSection.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_CollapsibleSection.json</automated></verify><done>JSON return exists.</done></task>

<!-- Sub-wave F2 (10) -->
<task type="auto" id="12.F2.1"><name>Review components/CampaignsTable.tsx</name><files>dashboard-web/src/components/CampaignsTable.tsx</files><action>Spawn gsd-code-reviewer. Largest component (post-Phase-4 shrink to ≤500). Output → `raw-returns/cmp_CampaignsTable.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_CampaignsTable.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F2.2"><name>Review components/CampaignsColumnsMenu.tsx</name><files>dashboard-web/src/components/CampaignsColumnsMenu.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_CampaignsColumnsMenu.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_CampaignsColumnsMenu.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F2.3"><name>Review components/CampaignDrawer.tsx</name><files>dashboard-web/src/components/CampaignDrawer.tsx</files><action>Spawn gsd-code-reviewer. Cross-ref drawerStack.ts (ESC handling). Output → `raw-returns/cmp_CampaignDrawer.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_CampaignDrawer.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F2.4"><name>Review components/AdsDrawer.tsx</name><files>dashboard-web/src/components/AdsDrawer.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_AdsDrawer.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_AdsDrawer.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F2.5"><name>Review components/AttributionAnalysisPanel.tsx</name><files>dashboard-web/src/components/AttributionAnalysisPanel.tsx</files><action>Spawn gsd-code-reviewer. SPEC.md OUT-list says presentation-only, DP-01 includes it. Reviewer issues thin "✅ presentation-only confirmed" verdict if no logic; otherwise full audit. U-05 halo-warning chip lives here (commit b846ae7). Output → `raw-returns/cmp_AttributionAnalysisPanel.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_AttributionAnalysisPanel.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F2.6"><name>Review components/MetaShopifyReconciliation.tsx</name><files>dashboard-web/src/components/MetaShopifyReconciliation.tsx</files><action>Spawn gsd-code-reviewer. 4-series chart (Phase 05.2). Output → `raw-returns/cmp_MetaShopifyReconciliation.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_MetaShopifyReconciliation.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F2.7"><name>Review components/ProductChannelBreakdown.tsx</name><files>dashboard-web/src/components/ProductChannelBreakdown.tsx</files><action>Spawn gsd-code-reviewer. TEST_PATH=`dashboard-web/src/lib/__tests__/productChannelBreakdownExclusiveSources.test.ts` (+ Guard variant). Output → `raw-returns/cmp_ProductChannelBreakdown.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_ProductChannelBreakdown.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F2.8"><name>Review components/MetricHelp.tsx</name><files>dashboard-web/src/components/MetricHelp.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_MetricHelp.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_MetricHelp.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F2.9"><name>Review components/AiReportButton.tsx</name><files>dashboard-web/src/components/AiReportButton.tsx</files><action>Spawn gsd-code-reviewer. Cross-ref lib/aiReport.ts. Output → `raw-returns/cmp_AiReportButton.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_AiReportButton.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F2.10"><name>Review components/AnnotationsPanel.tsx</name><files>dashboard-web/src/components/AnnotationsPanel.tsx</files><action>Spawn gsd-code-reviewer. Cross-ref lib/annotations.ts. Output → `raw-returns/cmp_AnnotationsPanel.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_AnnotationsPanel.json</automated></verify><done>JSON return exists.</done></task>

<!-- Sub-wave F3 (10) -->
<task type="auto" id="12.F3.1"><name>Review components/ProductsTable.tsx</name><files>dashboard-web/src/components/ProductsTable.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_ProductsTable.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_ProductsTable.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F3.2"><name>Review components/ProductPickerModal.tsx</name><files>dashboard-web/src/components/ProductPickerModal.tsx</files><action>Spawn gsd-code-reviewer. TEST_PATH=`dashboard-web/src/lib/__tests__/productPickerModalRtl.test.ts`. Output → `raw-returns/cmp_ProductPickerModal.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_ProductPickerModal.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F3.3"><name>Review components/ProductCentricView.tsx</name><files>dashboard-web/src/components/ProductCentricView.tsx</files><action>Spawn gsd-code-reviewer. Cross-ref lib/productCentricView.ts (also reviewed in Wave A). TEST_PATH=`dashboard-web/src/lib/__tests__/productCentricView.test.ts`. Output → `raw-returns/cmp_ProductCentricView.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_ProductCentricView.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F3.4"><name>Review components/GoalTracker.tsx</name><files>dashboard-web/src/components/GoalTracker.tsx</files><action>**OPERATOR CONSTRAINT: GLOBAL — ignores filters.store + filters.range (one business-wide target). NEVER flag this as a bug.** Spawn gsd-code-reviewer with this constraint emphasized. Cross-ref recent commit `f91d03b` (writeGoal pushes immediately, bypassing 400ms debounce — CC-01). Output → `raw-returns/cmp_GoalTracker.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_GoalTracker.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F3.5"><name>Review components/InsightsBoard.tsx</name><files>dashboard-web/src/components/InsightsBoard.tsx</files><action>Spawn gsd-code-reviewer. Cross-ref lib/insights.ts. Output → `raw-returns/cmp_InsightsBoard.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_InsightsBoard.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F3.6"><name>Review components/WhatsWorking.tsx</name><files>dashboard-web/src/components/WhatsWorking.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_WhatsWorking.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_WhatsWorking.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F3.7"><name>Review components/RefundIndicator.tsx</name><files>dashboard-web/src/components/RefundIndicator.tsx</files><action>Spawn gsd-code-reviewer. TEST_PATH=`dashboard-web/src/lib/__tests__/refundIndicatorBehavior.test.ts`. Output → `raw-returns/cmp_RefundIndicator.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_RefundIndicator.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F3.8"><name>Review components/MonthlyTables.tsx</name><files>dashboard-web/src/components/MonthlyTables.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_MonthlyTables.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_MonthlyTables.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F3.9"><name>Review components/TodayLive.tsx</name><files>dashboard-web/src/components/TodayLive.tsx</files><action>**OPERATOR CONSTRAINT: always LIVE (own SWR fetch, decoupled from main dashboard data). NEVER flag this as a bug.** TEST_PATH=`dashboard-web/src/lib/__tests__/todayLiveDecoupling.test.ts`. Output → `raw-returns/cmp_TodayLive.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_TodayLive.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F3.10"><name>Review components/PnLBreakdown.tsx</name><files>dashboard-web/src/components/PnLBreakdown.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_PnLBreakdown.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_PnLBreakdown.json</automated></verify><done>JSON return exists.</done></task>

<!-- Sub-wave F4 (10-11) -->
<task type="auto" id="12.F4.1"><name>Review components/BillingSettings.tsx</name><files>dashboard-web/src/components/BillingSettings.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_BillingSettings.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_BillingSettings.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F4.2"><name>Review components/BillingCsvImport.tsx</name><files>dashboard-web/src/components/BillingCsvImport.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_BillingCsvImport.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_BillingCsvImport.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F4.3"><name>Review components/CommandPalette.tsx</name><files>dashboard-web/src/components/CommandPalette.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_CommandPalette.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_CommandPalette.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F4.4"><name>Review components/HealthScoreBadge.tsx</name><files>dashboard-web/src/components/HealthScoreBadge.tsx</files><action>Spawn gsd-code-reviewer. Cross-ref lib/campaignHealthScore.ts. Output → `raw-returns/cmp_HealthScoreBadge.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_HealthScoreBadge.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F4.5"><name>Review components/operator/BackfillPicker.tsx</name><files>dashboard-web/src/components/operator/BackfillPicker.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_operator_BackfillPicker.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_operator_BackfillPicker.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F4.6"><name>Review components/operator/JobsTable.tsx</name><files>dashboard-web/src/components/operator/JobsTable.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_operator_JobsTable.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_operator_JobsTable.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F4.7"><name>Review components/operator/ManualOverridesCrud.tsx</name><files>dashboard-web/src/components/operator/ManualOverridesCrud.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_operator_ManualOverridesCrud.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_operator_ManualOverridesCrud.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F4.8"><name>Review components/operator/ResetData.tsx</name><files>dashboard-web/src/components/operator/ResetData.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_operator_ResetData.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_operator_ResetData.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F4.9"><name>Review components/operator/SyncNowButtons.tsx</name><files>dashboard-web/src/components/operator/SyncNowButtons.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_operator_SyncNowButtons.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_operator_SyncNowButtons.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F4.10"><name>Review components/operator/TokenFailuresTable.tsx</name><files>dashboard-web/src/components/operator/TokenFailuresTable.tsx</files><action>Spawn gsd-code-reviewer. Output → `raw-returns/cmp_operator_TokenFailuresTable.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_operator_TokenFailuresTable.json</automated></verify><done>JSON return exists.</done></task>

<task type="auto" id="12.F4.11"><name>Review components/operator/WhatsappTestButtons.tsx</name><files>dashboard-web/src/components/operator/WhatsappTestButtons.tsx</files><action>**OPERATOR CONSTRAINT: alerts ONLY to +972524809540.** Spawn gsd-code-reviewer. Output → `raw-returns/cmp_operator_WhatsappTestButtons.json`.</action><verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/cmp_operator_WhatsappTestButtons.json</automated></verify><done>JSON return exists.</done></task>

</tasks>

<verification>
All 41 raw-return files exist for Wave F (10+10+10+11). Zero source-code modifications.
</verification>

<success_criteria>
Wave F complete: 41 JSON returns.
</success_criteria>

<output>
No SUMMARY.md.
</output>
</content>
</invoke>