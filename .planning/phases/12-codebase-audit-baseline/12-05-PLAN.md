---
phase: 12-codebase-audit-baseline
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-codebase-audit-baseline/raw-returns/
autonomous: true
requirements: [REQ-1, REQ-2, REQ-3, REQ-4, REQ-5]
tags: [audit, wave-E, api-routes]

must_haves:
  truths:
    - "Each of the 19 api route.ts files has a structured JSON return in raw-returns/"
    - "Split into 2 sub-waves of ~10 to respect DP-01 concurrency cap"
  artifacts:
    - path: ".planning/phases/12-codebase-audit-baseline/raw-returns/api_*.json"
      provides: "Structured per-file audit returns for Wave E"
  key_links:
    - from: "19 parallel reviewers (2 sub-waves)"
      to: "raw-returns/*.json"
      via: "scaffolding template"
      pattern: "parallel-spawn (sub-waved)"
---

<objective>
**Wave E — api routes (19 reviewers in 2 sub-waves of ~10).** All Next.js App Router `route.ts` handlers: 9 data/dashboard surfaces + 7 operator surfaces + 1 oauth callback + 1 debug + 1 inngest webhook.

Purpose: Audit the HTTP boundary between client UI/operator console and Inngest/Postgres.
Output: 19 JSON returns in `raw-returns/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/12-codebase-audit-baseline/12-SPEC.md
@.planning/phases/12-codebase-audit-baseline/12-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
</context>

<reviewer_scaffolding>
Same scaffolding. Tests for routes are sparse in this repo (per `__tests__/postgresReaders.test.ts` etc., most coverage lives in lib/, not route handlers). Reviewer should explicitly flag route handlers with zero test coverage as test gaps.

**Sub-wave E1 — data + dashboard surfaces (10 routes):** ads, campaigns, dashboard-state, data, health, inngest, orders-attribution, product-catalog, products, store-meta.

**Sub-wave E2 — operator + oauth + debug (9 routes):** debug/shopify-fetch, oauth/tiktok/callback, operator/backfill, operator/jobs, operator/manual-overrides, operator/notifications/send, operator/reset, operator/sync-now, operator/token-failures.
</reviewer_scaffolding>

<tasks>

<!-- Sub-wave E1 (10 routes) -->
<task type="auto" id="12.E1.1">
  <name>Review app/api/ads/route.ts</name>
  <files>dashboard-web/src/app/api/ads/route.ts</files>
  <action>Spawn gsd-code-reviewer. Output → `raw-returns/api_ads.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/api_ads.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.E1.2">
  <name>Review app/api/campaigns/route.ts</name>
  <files>dashboard-web/src/app/api/campaigns/route.ts</files>
  <action>Spawn gsd-code-reviewer. Output → `raw-returns/api_campaigns.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/api_campaigns.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.E1.3">
  <name>Review app/api/dashboard-state/route.ts</name>
  <files>dashboard-web/src/app/api/dashboard-state/route.ts</files>
  <action>Spawn gsd-code-reviewer. Cross-ref ALLOWED_STATE_KEYS (lib/dashboardStateKeys.ts) — runtime allowlist must match TS types. Output → `raw-returns/api_dashboard-state.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/api_dashboard-state.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.E1.4">
  <name>Review app/api/data/route.ts</name>
  <files>dashboard-web/src/app/api/data/route.ts</files>
  <action>Spawn gsd-code-reviewer. Output → `raw-returns/api_data.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/api_data.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.E1.5">
  <name>Review app/api/health/route.ts</name>
  <files>dashboard-web/src/app/api/health/route.ts</files>
  <action>Spawn gsd-code-reviewer. Promise.allSettled (D-D2 ping per Phase 05.5). Output → `raw-returns/api_health.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/api_health.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.E1.6">
  <name>Review app/api/inngest/route.ts</name>
  <files>dashboard-web/src/app/api/inngest/route.ts</files>
  <action>Spawn gsd-code-reviewer. Inngest serve() webhook — registers all 11 functions in the `functions: [...]` array. Output → `raw-returns/api_inngest.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/api_inngest.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.E1.7">
  <name>Review app/api/orders-attribution/route.ts</name>
  <files>dashboard-web/src/app/api/orders-attribution/route.ts</files>
  <action>Spawn gsd-code-reviewer. Output → `raw-returns/api_orders-attribution.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/api_orders-attribution.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.E1.8">
  <name>Review app/api/product-catalog/route.ts</name>
  <files>dashboard-web/src/app/api/product-catalog/route.ts</files>
  <action>Spawn gsd-code-reviewer. Output → `raw-returns/api_product-catalog.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/api_product-catalog.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.E1.9">
  <name>Review app/api/products/route.ts</name>
  <files>dashboard-web/src/app/api/products/route.ts</files>
  <action>Spawn gsd-code-reviewer. Output → `raw-returns/api_products.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/api_products.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.E1.10">
  <name>Review app/api/store-meta/route.ts</name>
  <files>dashboard-web/src/app/api/store-meta/route.ts</files>
  <action>Spawn gsd-code-reviewer. Output → `raw-returns/api_store-meta.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/api_store-meta.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<!-- Sub-wave E2 (9 routes) -->
<task type="auto" id="12.E2.1">
  <name>Review app/api/debug/shopify-fetch/route.ts</name>
  <files>dashboard-web/src/app/api/debug/shopify-fetch/route.ts</files>
  <action>Spawn gsd-code-reviewer. Debug route — check for production-leaking surface area. Output → `raw-returns/api_debug_shopify-fetch.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/api_debug_shopify-fetch.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.E2.2">
  <name>Review app/api/oauth/tiktok/callback/route.ts</name>
  <files>dashboard-web/src/app/api/oauth/tiktok/callback/route.ts</files>
  <action>Spawn gsd-code-reviewer. OAuth callback — cross-ref fetchers/tiktok.ts and Google OAuth refresh-token expiry context (per memory: token expires ~2026-05-30). Output → `raw-returns/api_oauth_tiktok_callback.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/api_oauth_tiktok_callback.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.E2.3">
  <name>Review app/api/operator/backfill/route.ts</name>
  <files>dashboard-web/src/app/api/operator/backfill/route.ts</files>
  <action>Spawn gsd-code-reviewer. Output → `raw-returns/api_operator_backfill.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/api_operator_backfill.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.E2.4">
  <name>Review app/api/operator/jobs/route.ts</name>
  <files>dashboard-web/src/app/api/operator/jobs/route.ts</files>
  <action>Spawn gsd-code-reviewer. Output → `raw-returns/api_operator_jobs.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/api_operator_jobs.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.E2.5">
  <name>Review app/api/operator/manual-overrides/route.ts</name>
  <files>dashboard-web/src/app/api/operator/manual-overrides/route.ts</files>
  <action>Spawn gsd-code-reviewer. Output → `raw-returns/api_operator_manual-overrides.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/api_operator_manual-overrides.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.E2.6">
  <name>Review app/api/operator/notifications/send/route.ts</name>
  <files>dashboard-web/src/app/api/operator/notifications/send/route.ts</files>
  <action>Spawn gsd-code-reviewer. Operator constraint: ONLY +972524809540. Output → `raw-returns/api_operator_notifications_send.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/api_operator_notifications_send.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.E2.7">
  <name>Review app/api/operator/reset/route.ts</name>
  <files>dashboard-web/src/app/api/operator/reset/route.ts</files>
  <action>Spawn gsd-code-reviewer. Cross-ref lib/operatorReset.ts. Output → `raw-returns/api_operator_reset.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/api_operator_reset.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.E2.8">
  <name>Review app/api/operator/sync-now/route.ts</name>
  <files>dashboard-web/src/app/api/operator/sync-now/route.ts</files>
  <action>Spawn gsd-code-reviewer. Fires inngest.send(event/sync-now). Output → `raw-returns/api_operator_sync-now.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/api_operator_sync-now.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.E2.9">
  <name>Review app/api/operator/token-failures/route.ts</name>
  <files>dashboard-web/src/app/api/operator/token-failures/route.ts</files>
  <action>Spawn gsd-code-reviewer. Cross-ref notifications/tokenFailures.ts + Meta template approval pending context. Output → `raw-returns/api_operator_token-failures.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/api_operator_token-failures.json</automated></verify>
  <done>JSON return exists.</done>
</task>

</tasks>

<verification>
All 19 raw-return files exist. Zero source-code modifications.
</verification>

<success_criteria>
Wave E complete: 19 JSON returns.
</success_criteria>

<output>
No SUMMARY.md.
</output>
</content>
</invoke>