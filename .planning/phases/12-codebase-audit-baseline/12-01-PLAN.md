---
phase: 12-codebase-audit-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-codebase-audit-baseline/raw-returns/
autonomous: true
requirements: [REQ-1, REQ-2, REQ-3, REQ-4, REQ-5]
tags: [audit, wave-A, algorithm, cross-ai]

must_haves:
  truths:
    - "Each of the 12 lib/algorithm files has a structured JSON return in raw-returns/"
    - "Each return contains verdict ∈ {✅, 🔴, ⚠️} + inline channels + inline test gaps"
    - "5 statistical files have an additional Codex cross-AI return (per D-14/D-15) — auto-on"
  artifacts:
    - path: ".planning/phases/12-codebase-audit-baseline/raw-returns/lib_algorithm_*.json"
      provides: "Structured per-file audit returns for Wave A"
  key_links:
    - from: "12 reviewers"
      to: "raw-returns/*.json"
      via: "scaffolding template from CONTEXT.md, customized per file"
      pattern: "parallel-spawn"
---

<objective>
**Wave A — lib/algorithm (12 reviewers, parallel).** Per-file deep audit of the core algorithmic surface of the codebase. 5 files (statistical) get an auto-spawned Codex cross-AI critique pass per D-14/D-15.

Purpose: First-pass evidence for every algorithm module's correctness verdict before checkpoint.
Output: 12 JSON returns + 5 Codex critique returns in `raw-returns/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/12-codebase-audit-baseline/12-SPEC.md
@.planning/phases/12-codebase-audit-baseline/12-CONTEXT.md
@.planning/audit-2026-05-23-v3/AUDIT-phase9-snapshot.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
</context>

<reviewer_scaffolding>
Every Wave A reviewer is a `gsd-code-reviewer` subagent receiving the scaffolding template VERBATIM from CONTEXT.md "Definitely include reviewer prompt scaffolding" section, with the following substitutions per task:
- `{FILE}` — the source file path
- `{TEST_PATH}` — `dashboard-web/src/lib/__tests__/{base}.test.ts` (or note "no test file exists")
- Output path — `.planning/phases/12-codebase-audit-baseline/raw-returns/lib_algorithm_{file-slug}.json`

**Cross-AI auto-on (D-14):** For the 5 statistical files marked `cross_ai: true`, the orchestrator MUST spawn a Codex (GPT-5) critique pass immediately after the Opus reviewer's return lands. Codex receives: Opus's JSON return + the source file + the test file + relevant fixtures. Codex outputs: `VERIFY | VERIFY-WITH-CAVEAT | REJECT | UNCERTAIN` per Opus finding + up to 5 NEW findings Opus missed. Codex return path: `raw-returns/lib_algorithm_{file-slug}.codex.json`.

**Skip condition:** If the FIRST statistical Opus pass returns zero findings AND zero ⚠️, orchestrator may skip Codex for that file (D-14 escape hatch). All 5 still get Opus.
</reviewer_scaffolding>

<tasks>

<task type="auto" id="12.A.1">
  <name>Review lib/analytics.ts</name>
  <files>dashboard-web/src/lib/analytics.ts</files>
  <action>Spawn gsd-code-reviewer with scaffolding template. FILE=`dashboard-web/src/lib/analytics.ts`. TEST_PATH=`dashboard-web/src/lib/__tests__/analytics.test.ts`. Cross-ref AUDIT-phase9-snapshot.md for prior verdicts. Output → `raw-returns/lib_algorithm_analytics.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_algorithm_analytics.json</automated></verify>
  <done>JSON return exists with verdict + findings array + inline channels + inline test gaps + verdict_rationale.</done>
</task>

<task type="auto" id="12.A.2">
  <name>Review lib/insights.ts</name>
  <files>dashboard-web/src/lib/insights.ts</files>
  <action>Spawn gsd-code-reviewer. FILE=`dashboard-web/src/lib/insights.ts`. TEST_PATH=`dashboard-web/src/lib/__tests__/insights.test.ts`. Output → `raw-returns/lib_algorithm_insights.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_algorithm_insights.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.A.3" cross_ai="true">
  <name>Review lib/attributionAnalysis.ts (+Codex cross-AI)</name>
  <files>dashboard-web/src/lib/attributionAnalysis.ts</files>
  <action>**Statistical file — cross-AI auto-on per D-14.** Spawn gsd-code-reviewer (Opus). FILE=`dashboard-web/src/lib/attributionAnalysis.ts`. TEST_PATH=`dashboard-web/src/lib/__tests__/attributionAnalysis.test.ts` (+ siblings). Output → `raw-returns/lib_algorithm_attributionAnalysis.json`. After Opus return lands, orchestrator spawns Codex critique pass per D-15. Codex output → `raw-returns/lib_algorithm_attributionAnalysis.codex.json`. Skip Codex only if Opus returned zero findings AND zero ⚠️.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_algorithm_attributionAnalysis.json</automated></verify>
  <done>Opus JSON exists; if Opus found ≥1 finding or ⚠️, Codex JSON also exists.</done>
</task>

<task type="auto" id="12.A.4" cross_ai="true">
  <name>Review lib/aiReport.ts (+Codex cross-AI)</name>
  <files>dashboard-web/src/lib/aiReport.ts</files>
  <action>**Statistical — cross-AI auto-on.** Spawn gsd-code-reviewer (Opus). FILE=`dashboard-web/src/lib/aiReport.ts`. TEST_PATH=`dashboard-web/src/lib/__tests__/aiReport.test.ts` (note: aiReport.ts was 700 LOC with 0 unit tests until Phase 10 — pay extra attention to test gaps). Output → `raw-returns/lib_algorithm_aiReport.json`. Codex critique → `.codex.json`. Skip-condition same as 12.A.3.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_algorithm_aiReport.json</automated></verify>
  <done>Opus JSON exists; Codex JSON exists unless skip-condition met.</done>
</task>

<task type="auto" id="12.A.5" cross_ai="true">
  <name>Review lib/multiMappingCohort.ts (+Codex cross-AI)</name>
  <files>dashboard-web/src/lib/multiMappingCohort.ts</files>
  <action>**Statistical — cross-AI auto-on.** Bayesian shrinkage; cross-ref recent commit `5b5958e` (orders-axis Bayesian shrinkage). Output → `raw-returns/lib_algorithm_multiMappingCohort.json` + `.codex.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_algorithm_multiMappingCohort.json</automated></verify>
  <done>Opus JSON exists; Codex JSON exists unless skip.</done>
</task>

<task type="auto" id="12.A.6" cross_ai="true">
  <name>Review lib/cannibalizationDetection.ts (+Codex cross-AI)</name>
  <files>dashboard-web/src/lib/cannibalizationDetection.ts</files>
  <action>**Statistical — cross-AI auto-on.** Cross-ref recent commit `74fdf42` (rebalanced mid-range cohort detection). Output → `raw-returns/lib_algorithm_cannibalizationDetection.json` + `.codex.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_algorithm_cannibalizationDetection.json</automated></verify>
  <done>Opus JSON exists; Codex JSON exists unless skip.</done>
</task>

<task type="auto" id="12.A.7">
  <name>Review lib/campaignHealthScore.ts</name>
  <files>dashboard-web/src/lib/campaignHealthScore.ts</files>
  <action>Spawn gsd-code-reviewer. Cohort-aware health-score algorithm. TEST_PATH=`dashboard-web/src/lib/__tests__/campaignHealthScore.test.ts`. Output → `raw-returns/lib_algorithm_campaignHealthScore.json`. NOT marked cross_ai (not in D-14 list).</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_algorithm_campaignHealthScore.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.A.8" cross_ai="true">
  <name>Review lib/cpmRoasAnalysis.ts (+Codex cross-AI)</name>
  <files>dashboard-web/src/lib/cpmRoasAnalysis.ts</files>
  <action>**Statistical — cross-AI auto-on.** Output → `raw-returns/lib_algorithm_cpmRoasAnalysis.json` + `.codex.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_algorithm_cpmRoasAnalysis.json</automated></verify>
  <done>Opus JSON exists; Codex JSON exists unless skip.</done>
</task>

<task type="auto" id="12.A.9">
  <name>Review lib/productCentricView.ts</name>
  <files>dashboard-web/src/lib/productCentricView.ts</files>
  <action>Spawn gsd-code-reviewer. Note: this file is currently modified on working tree (per git status); reviewer audits the on-disk version. TEST_PATH=`dashboard-web/src/lib/__tests__/productCentricView.test.ts`. Output → `raw-returns/lib_algorithm_productCentricView.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_algorithm_productCentricView.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.A.10">
  <name>Review lib/ordersAttribution.ts</name>
  <files>dashboard-web/src/lib/ordersAttribution.ts</files>
  <action>Spawn gsd-code-reviewer. Type module + helpers. TEST_PATH=`dashboard-web/src/lib/__tests__/orderSourceContract.test.ts` (closest spec). Output → `raw-returns/lib_algorithm_ordersAttribution.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_algorithm_ordersAttribution.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.A.11">
  <name>Review lib/campaignProductMap.ts</name>
  <files>dashboard-web/src/lib/campaignProductMap.ts</files>
  <action>Spawn gsd-code-reviewer. Campaign→product mapping logic. TEST_PATH=`dashboard-web/src/lib/__tests__/campaignProductMap.test.ts`. Output → `raw-returns/lib_algorithm_campaignProductMap.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_algorithm_campaignProductMap.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.A.12">
  <name>Review lib/campaignsAggregator.ts</name>
  <files>dashboard-web/src/lib/campaignsAggregator.ts</files>
  <action>Spawn gsd-code-reviewer. Cross-platform aggregation. TEST_PATH=`dashboard-web/src/lib/__tests__/campaignsAggregator.test.ts`. Output → `raw-returns/lib_algorithm_campaignsAggregator.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_algorithm_campaignsAggregator.json</automated></verify>
  <done>JSON return exists.</done>
</task>

</tasks>

<verification>
All 12 raw-return files exist; 5 statistical files have `.codex.json` siblings (unless skip-condition met). Zero source-code modifications.
</verification>

<success_criteria>
Wave A complete: 12 Opus + up-to-5 Codex JSON returns in raw-returns/.
</success_criteria>

<output>
No SUMMARY.md for wave sub-plans — Wave A returns feed directly into the orchestrator's mid-execution checkpoint and the final synthesis (12-PLAN.md task 12.X.2).
</output>
</content>
</invoke>