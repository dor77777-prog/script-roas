{"task_id":"12.F.17","file":"dashboard-web/src/components/HeroOverview.tsx","test_path":"NONE","verdict":"minor","findings":[{"id":"CMP-17","severity":"MINOR","file_line":"HeroOverview.tsx:139-194","failing_input":"Renders aggregate(cur) and aggregate(prev) without passing the request range — fixedCosts are prorated over the data-derived min/max date, not the operator's selected window","expected":"aggregate(cur, filters.range) and aggregate(prev, prevRange) — matches Dashboard.tsx:163-169 which Audit fix d/CR-02 explicitly enforced","actual":"Both aggregate calls omit the range argument. Result: HeroOverview's netProfit + spend deltas use the older non-range-aware fixed-cost proration; can disagree with KpiCards which DO use Dashboard.tsx's pre-aggregated curAgg/prevAgg. Operator sees a different 'רווח נטו' on the hero vs the KPI strip below for the same scope when fixed costs are non-zero","fix_sketch":"aggregate(cur, filters.range) and aggregate(prev, prevRange) — same pattern Dashboard.tsx already uses. Verify by re-running goalTrackerScope.test.ts and dashboard tests","regression_test_idea":"unit: HeroOverview's kpis.curAgg.netProfit should equal Dashboard.tsx's filtered.curAgg.netProfit for the same data + range"}],"uncertainty_notes":"Annotations chart-overlay logic correctly groups by date (c/HI-03 fix). c/CR-03 enumerateDateRange fills gap days with null roas (Recharts connectNulls={false}). Audit FIND-04 fix prevents Math.min(...[]) = Infinity display. Range-keyed SWR for both /api/campaigns calls. CPM agg correctly walks campaignsData with from/to/store guards. The inverseDelta flag correctly inverts the color treatment for spend + CPM (lower = better). Hebrew RTL + dir='ltr' overrides on chart sections.","test_coverage_gaps":["No test for the aggregate(cur, ...) range-passing regression (CMP-17)","No test for the annotation date-grouping (one ReferenceLine per date)","No test for the Infinity-min guard"],"channels":{"imports_from":["@/lib/annotations","@/lib/types","@/app/api/campaigns/route","@/lib/analytics","@/lib/dateRange","@/lib/presets","@/lib/utils","@/lib/format"],"exports_to":["Dashboard (HomeTab)"],"events_emitted":[],"events_consumed":["roas-annotations-changed"],"swr_keys":["/api/campaigns?from=&to=","/api/campaigns?from=&to= (prev)"],"inngest_triggers":[],"supabase_writes":[],"supabase_reads":[],"api_calls":["/api/campaigns"]}}