---
phase: 12-codebase-audit-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-codebase-audit-baseline/raw-returns/
autonomous: true
requirements: [REQ-1, REQ-2, REQ-3, REQ-4, REQ-5]
tags: [audit, wave-B, services, fetchers, hooks, notifications]

must_haves:
  truths:
    - "Each of 16 lib/services files (7 fetchers + 4 hooks + 5 notifications) has a structured JSON return in raw-returns/"
  artifacts:
    - path: ".planning/phases/12-codebase-audit-baseline/raw-returns/lib_services_*.json"
      provides: "Structured per-file audit returns for Wave B"
  key_links:
    - from: "16 parallel reviewers"
      to: "raw-returns/*.json"
      via: "scaffolding template"
      pattern: "parallel-spawn"
---

<objective>
**Wave B — lib/services (16 reviewers, parallel).** External-API fetchers, React hooks split from large components, and notification pipeline.

Purpose: Audit the I/O + glue layer between the algorithm core and the Inngest/UI surfaces.
Output: 16 JSON returns in `raw-returns/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/12-codebase-audit-baseline/12-SPEC.md
@.planning/phases/12-codebase-audit-baseline/12-CONTEXT.md
@.planning/codebase/INTEGRATIONS.md
@.planning/codebase/STACK.md
</context>

<reviewer_scaffolding>
Same as Wave A — VERBATIM CONTEXT.md scaffolding, customized per file. NOT cross-AI (none in D-14 list).
**Concurrency:** 16 in flight may exceed the soft cap of ~10 (DP-01). Orchestrator may split into two micro-waves of 8 if needed; both are still wave 1 from a dependency-graph perspective.
</reviewer_scaffolding>

<tasks>

<task type="auto" id="12.B.1">
  <name>Review lib/fetchers/fx.ts</name>
  <files>dashboard-web/src/lib/fetchers/fx.ts</files>
  <action>Spawn gsd-code-reviewer. Frankfurter currency conversion. TEST_PATH=`dashboard-web/src/lib/__tests__/fx.test.ts`. Output → `raw-returns/lib_services_fx.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_services_fx.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.B.2">
  <name>Review lib/fetchers/googleAds.ts</name>
  <files>dashboard-web/src/lib/fetchers/googleAds.ts</files>
  <action>Spawn gsd-code-reviewer. GAQL queries. TEST_PATH=`dashboard-web/src/lib/__tests__/googleAds.test.ts`. Output → `raw-returns/lib_services_googleAds.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_services_googleAds.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.B.3">
  <name>Review lib/fetchers/manualOverrides.ts</name>
  <files>dashboard-web/src/lib/fetchers/manualOverrides.ts</files>
  <action>Spawn gsd-code-reviewer. Supabase manual_overrides merger. TEST_PATH=`dashboard-web/src/lib/__tests__/manualOverrides.test.ts`. Output → `raw-returns/lib_services_manualOverrides.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_services_manualOverrides.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.B.4">
  <name>Review lib/fetchers/meta.ts</name>
  <files>dashboard-web/src/lib/fetchers/meta.ts</files>
  <action>Spawn gsd-code-reviewer. Meta Marketing Insights. TEST_PATH=`dashboard-web/src/lib/__tests__/meta.test.ts`. Output → `raw-returns/lib_services_meta.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_services_meta.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.B.5">
  <name>Review lib/fetchers/shopify.ts</name>
  <files>dashboard-web/src/lib/fetchers/shopify.ts</files>
  <action>Spawn gsd-code-reviewer. Admin REST orders + products. Cross-ref `shopifyRevenueRefunds.ts` (canonical refund algorithm). TEST_PATH=`dashboard-web/src/lib/__tests__/shopify.test.ts`. Output → `raw-returns/lib_services_shopify.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_services_shopify.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.B.6">
  <name>Review lib/fetchers/shopifyAuth.ts</name>
  <files>dashboard-web/src/lib/fetchers/shopifyAuth.ts</files>
  <action>Spawn gsd-code-reviewer. Multi-store credential router. TEST_PATH=`dashboard-web/src/lib/__tests__/shopifyAuth.test.ts`. Output → `raw-returns/lib_services_shopifyAuth.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_services_shopifyAuth.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.B.7">
  <name>Review lib/fetchers/tiktok.ts</name>
  <files>dashboard-web/src/lib/fetchers/tiktok.ts</files>
  <action>Spawn gsd-code-reviewer. TikTok Business API. Cross-ref `platformConfig.ts` TIKTOK_ACTIVE_ENOUGH. TEST_PATH=`dashboard-web/src/lib/__tests__/tiktok.test.ts`. Output → `raw-returns/lib_services_tiktok.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_services_tiktok.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.B.8">
  <name>Review lib/hooks/useBillingOneTime.ts</name>
  <files>dashboard-web/src/lib/hooks/useBillingOneTime.ts</files>
  <action>Spawn gsd-code-reviewer. One-time billing CRUD. TEST_PATH=`dashboard-web/src/lib/__tests__/useBillingOneTime.test.ts`. Output → `raw-returns/lib_services_useBillingOneTime.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_services_useBillingOneTime.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.B.9">
  <name>Review lib/hooks/useBillingRecurring.ts</name>
  <files>dashboard-web/src/lib/hooks/useBillingRecurring.ts</files>
  <action>Spawn gsd-code-reviewer. Recurring billing CRUD. TEST_PATH=`dashboard-web/src/lib/__tests__/useBillingRecurring.test.ts`. Output → `raw-returns/lib_services_useBillingRecurring.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_services_useBillingRecurring.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.B.10">
  <name>Review lib/hooks/useCampaignAttribution.ts</name>
  <files>dashboard-web/src/lib/hooks/useCampaignAttribution.ts</files>
  <action>Spawn gsd-code-reviewer. analyzeAttribution memo wrapper. TEST_PATH=`dashboard-web/src/lib/__tests__/useCampaignAttribution.test.ts` (+ Gate variant). Output → `raw-returns/lib_services_useCampaignAttribution.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_services_useCampaignAttribution.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.B.11">
  <name>Review lib/hooks/useCampaignTrueRevenue.ts</name>
  <files>dashboard-web/src/lib/hooks/useCampaignTrueRevenue.ts</files>
  <action>Spawn gsd-code-reviewer. Proportional revenue allocation + 4-channel reconciliation builder. TEST_PATH=`dashboard-web/src/lib/__tests__/useCampaignTrueRevenue.test.ts`. Output → `raw-returns/lib_services_useCampaignTrueRevenue.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_services_useCampaignTrueRevenue.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.B.12">
  <name>Review lib/notifications/sendDailySummary.ts</name>
  <files>dashboard-web/src/lib/notifications/sendDailySummary.ts</files>
  <action>Spawn gsd-code-reviewer. Orchestrator for the 3 cron flows. TEST_PATH=`dashboard-web/src/lib/__tests__/sendDailySummary.test.ts`. Cross-ref recent commit `764cec0` (throw on ANY recipient failure). Output → `raw-returns/lib_services_sendDailySummary.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_services_sendDailySummary.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.B.13">
  <name>Review lib/notifications/summary.ts</name>
  <files>dashboard-web/src/lib/notifications/summary.ts</files>
  <action>Spawn gsd-code-reviewer. buildStoreSummary. TEST_PATH=`dashboard-web/src/lib/__tests__/summary.test.ts`. Output → `raw-returns/lib_services_summary.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_services_summary.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.B.14">
  <name>Review lib/notifications/templateParams.ts</name>
  <files>dashboard-web/src/lib/notifications/templateParams.ts</files>
  <action>Spawn gsd-code-reviewer. 5-slot template parameter builder. TEST_PATH=`dashboard-web/src/lib/__tests__/templateParams.test.ts`. Output → `raw-returns/lib_services_templateParams.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_services_templateParams.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.B.15">
  <name>Review lib/notifications/tokenFailures.ts</name>
  <files>dashboard-web/src/lib/notifications/tokenFailures.ts</files>
  <action>Spawn gsd-code-reviewer. notifyTokenFailure + throttle. TEST_PATH=`dashboard-web/src/lib/__tests__/tokenFailures.test.ts`. Note operator constraint: alerts ONLY to +972524809540. Output → `raw-returns/lib_services_tokenFailures.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_services_tokenFailures.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.B.16">
  <name>Review lib/notifications/whatsapp.ts</name>
  <files>dashboard-web/src/lib/notifications/whatsapp.ts</files>
  <action>Spawn gsd-code-reviewer. sendWhatsAppTemplate + config loader. Operator constraint: ONLY +972524809540. TEST_PATH=`dashboard-web/src/lib/__tests__/whatsapp.test.ts`. Output → `raw-returns/lib_services_whatsapp.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_services_whatsapp.json</automated></verify>
  <done>JSON return exists.</done>
</task>

</tasks>

<verification>
All 16 raw-return files exist. Zero source-code modifications.
</verification>

<success_criteria>
Wave B complete: 16 JSON returns in raw-returns/.
</success_criteria>

<output>
No SUMMARY.md — returns feed orchestrator synthesis.
</output>
</content>
</invoke>