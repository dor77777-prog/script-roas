---
phase: 12-codebase-audit-baseline
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-codebase-audit-baseline/raw-returns/
autonomous: true
requirements: [REQ-1, REQ-2, REQ-3, REQ-4, REQ-5]
tags: [audit, wave-D, inngest, crons]

must_haves:
  truths:
    - "Each of the 6 inngest files has a structured JSON return in raw-returns/"
  artifacts:
    - path: ".planning/phases/12-codebase-audit-baseline/raw-returns/inngest_*.json"
      provides: "Structured per-file audit returns for Wave D"
  key_links:
    - from: "6 parallel reviewers"
      to: "raw-returns/*.json"
      via: "scaffolding template"
      pattern: "parallel-spawn"
---

<objective>
**Wave D — inngest (6 reviewers, parallel).** Cron + event functions: client singleton, cronDaily (3 functions), cronLive (3 functions), cronWhatsapp (3+1 functions), eventBackfill, eventSyncNow.

Purpose: Audit the data-plane writer tier — the Inngest jobs that pull from external APIs and write to Supabase.
Output: 6 JSON returns in `raw-returns/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/12-codebase-audit-baseline/12-SPEC.md
@.planning/phases/12-codebase-audit-baseline/12-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md
</context>

<reviewer_scaffolding>
Same scaffolding template. Tests live in `dashboard-web/src/inngest/functions/__tests__/`. Cross-ref recent fix `f91d03b` (notifications/sendDailySummary chain wired through cronWhatsapp).
</reviewer_scaffolding>

<tasks>

<task type="auto" id="12.D.1">
  <name>Review inngest/client.ts</name>
  <files>dashboard-web/src/inngest/client.ts</files>
  <action>Spawn gsd-code-reviewer. Inngest singleton ({id: 'roas-dashboard'}). Output → `raw-returns/inngest_client.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/inngest_client.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.D.2">
  <name>Review inngest/functions/cronDaily.ts</name>
  <files>dashboard-web/src/inngest/functions/cronDaily.ts</files>
  <action>Spawn gsd-code-reviewer. 3 cron-daily-{store} functions @ 00:05 IL. Shared `runDailyForStore` handler (~5K LOC). Cross-ref Phase 10 CRIT-5 (cronDaily FX) — already fixed. TEST_PATH=`dashboard-web/src/inngest/functions/__tests__/cronDaily.test.ts`. Output → `raw-returns/inngest_cronDaily.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/inngest_cronDaily.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.D.3">
  <name>Review inngest/functions/cronLive.ts</name>
  <files>dashboard-web/src/inngest/functions/cronLive.ts</files>
  <action>Spawn gsd-code-reviewer. 3 cron-live-{store} functions @ */10 IL. 3-day rolling window. Cross-ref Phase 10 HIGH-NEW-4 (Supabase {error} swallow) — already fixed. TEST_PATH=`dashboard-web/src/inngest/functions/__tests__/cronLive.test.ts`. Output → `raw-returns/inngest_cronLive.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/inngest_cronLive.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.D.4">
  <name>Review inngest/functions/cronWhatsapp.ts</name>
  <files>dashboard-web/src/inngest/functions/cronWhatsapp.ts</files>
  <action>Spawn gsd-code-reviewer. 3 whatsapp-{noon|evening|eod} + 1 event. Operator constraint: ONLY +972524809540. Cross-ref recent commit `764cec0` (throw on ANY recipient failure). TEST_PATH=`dashboard-web/src/inngest/functions/__tests__/cronWhatsapp.test.ts`. Output → `raw-returns/inngest_cronWhatsapp.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/inngest_cronWhatsapp.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.D.5">
  <name>Review inngest/functions/eventBackfill.ts</name>
  <files>dashboard-web/src/inngest/functions/eventBackfill.ts</files>
  <action>Spawn gsd-code-reviewer. event/backfill — operator backfill picker. TEST_PATH=`dashboard-web/src/inngest/functions/__tests__/eventBackfill.test.ts`. Output → `raw-returns/inngest_eventBackfill.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/inngest_eventBackfill.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.D.6">
  <name>Review inngest/functions/eventSyncNow.ts</name>
  <files>dashboard-web/src/inngest/functions/eventSyncNow.ts</files>
  <action>Spawn gsd-code-reviewer. event/sync-now — operator sync button. TEST_PATH=`dashboard-web/src/inngest/functions/__tests__/eventSyncNow.test.ts`. Output → `raw-returns/inngest_eventSyncNow.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/inngest_eventSyncNow.json</automated></verify>
  <done>JSON return exists.</done>
</task>

</tasks>

<verification>
All 6 raw-return files exist. Zero source-code modifications.
</verification>

<success_criteria>
Wave D complete: 6 JSON returns.
</success_criteria>

<output>
No SUMMARY.md.
</output>
</content>
</invoke>