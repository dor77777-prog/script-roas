---
phase: 12-codebase-audit-baseline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/12-codebase-audit-baseline/raw-returns/
autonomous: true
requirements: [REQ-1, REQ-2, REQ-3, REQ-4, REQ-5]
tags: [audit, wave-C, state, infra, types, formatters]

must_haves:
  truths:
    - "Each of 35+ lib/state+infra files has a structured JSON return in raw-returns/"
    - "Reviewers split across 3 sub-waves of ~10-12 to respect DP-01 concurrency cap"
  artifacts:
    - path: ".planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_*.json"
      provides: "Structured per-file audit returns for Wave C"
  key_links:
    - from: "~36 parallel reviewers in 3 sub-waves"
      to: "raw-returns/*.json"
      via: "scaffolding template"
      pattern: "parallel-spawn (sub-waved)"
---

<objective>
**Wave C — lib/state+infra (~36 reviewers in 3 sub-waves of ~12).** State management (cloudSync, drawerStack, useDashboardRefresh), database (postgresReaders, supabase, supabaseAdmin), config (presets, dashboardStateKeys, platformConfig, platformsByStore, cacheConfig, sessionKeys, constants), types/formatters (types, utils, format, dateRange, dateValidation, rangeClamp, urlState, drillFilter, apiErrors, sparklineGeometry, chartColors), and domain data (shopifyRevenueRefunds, lineItems, productCatalog, ads, billing, campaigns, campaignOptimized, campaignsColumnPrefs, campaignsLinks, products, costs, operatorReset, annotations).

Purpose: Audit the rest of lib/ — everything that isn't core algorithm (Wave A) or external service (Wave B).
Output: ~36 JSON returns in `raw-returns/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/12-codebase-audit-baseline/12-SPEC.md
@.planning/phases/12-codebase-audit-baseline/12-CONTEXT.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
</context>

<reviewer_scaffolding>
Same scaffolding as Waves A/B. NOT cross-AI. Orchestrator runs in 3 sub-waves to respect DP-01.

**Sub-wave C1 — state/db/config (12 files):** cloudSync, drawerStack, useDashboardRefresh, postgresReaders, supabase, supabaseAdmin, presets, dashboardStateKeys, platformConfig, platformsByStore, cacheConfig, sessionKeys.

**Sub-wave C2 — types/formatters (12 files):** types, utils, format, dateRange, dateValidation, rangeClamp, urlState, drillFilter, apiErrors, sparklineGeometry, chartColors, constants.

**Sub-wave C3 — domain data (12 files):** shopifyRevenueRefunds, lineItems, productCatalog, ads, billing, campaigns, campaignOptimized, campaignsColumnPrefs, campaignsLinks, products, costs, operatorReset (+ annotations as 13th if reviewer-budget allows in same sub-wave).
</reviewer_scaffolding>

<tasks>

<!-- Sub-wave C1 (12 files) -->
<task type="auto" id="12.C1.1">
  <name>Review lib/cloudSync.ts</name>
  <files>dashboard-web/src/lib/cloudSync.ts</files>
  <action>Spawn gsd-code-reviewer. localStorage ↔ /api/dashboard-state mirror. Cross-ref recent commit `f91d03b` (writeGoal pushes immediately, bypassing 400ms debounce — CC-01). TEST_PATH=`dashboard-web/src/lib/__tests__/cloudSync.test.ts`. Output → `raw-returns/lib_state_cloudSync.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_cloudSync.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C1.2">
  <name>Review lib/drawerStack.ts</name>
  <files>dashboard-web/src/lib/drawerStack.ts</files>
  <action>Spawn gsd-code-reviewer. Nested drawer ESC handling. Cross-ref recent commit `0708bbb` (useDrawerEsc registers once per open-cycle — CC-02). TEST_PATH=`dashboard-web/src/lib/__tests__/drawerStack.test.ts`. Output → `raw-returns/lib_state_drawerStack.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_drawerStack.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C1.3">
  <name>Review lib/useDashboardRefresh.ts</name>
  <files>dashboard-web/src/lib/useDashboardRefresh.ts</files>
  <action>Spawn gsd-code-reviewer. Global SWR mutate hook. TEST_PATH=`dashboard-web/src/lib/__tests__/useDashboardRefresh.test.ts`. Output → `raw-returns/lib_state_useDashboardRefresh.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_useDashboardRefresh.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C1.4">
  <name>Review lib/postgresReaders.ts</name>
  <files>dashboard-web/src/lib/postgresReaders.ts</files>
  <action>Spawn gsd-code-reviewer. All 8 fetch*FromPostgres + 1 writer. Critical path. TEST_PATH=`dashboard-web/src/lib/__tests__/postgresReaders.test.ts` (+ NewestRowDedup variant). Output → `raw-returns/lib_state_postgresReaders.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_postgresReaders.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C1.5">
  <name>Review lib/supabase.ts</name>
  <files>dashboard-web/src/lib/supabase.ts</files>
  <action>Spawn gsd-code-reviewer. Anon client (read-only SELECT). TEST_PATH=`dashboard-web/src/lib/__tests__/supabase.test.ts` (note: may not exist). Output → `raw-returns/lib_state_supabase.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_supabase.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C1.6">
  <name>Review lib/supabaseAdmin.ts</name>
  <files>dashboard-web/src/lib/supabaseAdmin.ts</files>
  <action>Spawn gsd-code-reviewer. service_role client (Inngest writes). TEST_PATH=`dashboard-web/src/lib/__tests__/supabaseAdmin.test.ts` (note: may not exist). Output → `raw-returns/lib_state_supabaseAdmin.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_supabaseAdmin.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C1.7">
  <name>Review lib/presets.ts</name>
  <files>dashboard-web/src/lib/presets.ts</files>
  <action>Spawn gsd-code-reviewer. Date-range presets. TEST_PATH=`dashboard-web/src/lib/__tests__/presetsDst.test.ts`. Output → `raw-returns/lib_state_presets.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_presets.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C1.8">
  <name>Review lib/dashboardStateKeys.ts</name>
  <files>dashboard-web/src/lib/dashboardStateKeys.ts</files>
  <action>Spawn gsd-code-reviewer. ALLOWED_STATE_KEYS (Phase 11 home). TEST_PATH=`dashboard-web/src/lib/__tests__/dashboardStateKeys.test.ts`. Output → `raw-returns/lib_state_dashboardStateKeys.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_dashboardStateKeys.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C1.9">
  <name>Review lib/platformConfig.ts</name>
  <files>dashboard-web/src/lib/platformConfig.ts</files>
  <action>Spawn gsd-code-reviewer. TIKTOK_ACTIVE_ENOUGH writer↔reader symmetric sets (v3 U-01 historical bug context). TEST_PATH=`dashboard-web/src/lib/__tests__/platformConfig.test.ts`. Output → `raw-returns/lib_state_platformConfig.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_platformConfig.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C1.10">
  <name>Review lib/platformsByStore.ts</name>
  <files>dashboard-web/src/lib/platformsByStore.ts</files>
  <action>Spawn gsd-code-reviewer. Store → active platforms map. TEST_PATH=`dashboard-web/src/lib/__tests__/platformsByStore.test.ts`. Output → `raw-returns/lib_state_platformsByStore.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_platformsByStore.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C1.11">
  <name>Review lib/cacheConfig.ts</name>
  <files>dashboard-web/src/lib/cacheConfig.ts</files>
  <action>Spawn gsd-code-reviewer. Cache-Control config per route. TEST_PATH=`dashboard-web/src/lib/__tests__/cacheConfig.test.ts`. Output → `raw-returns/lib_state_cacheConfig.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_cacheConfig.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C1.12">
  <name>Review lib/sessionKeys.ts</name>
  <files>dashboard-web/src/lib/sessionKeys.ts</files>
  <action>Spawn gsd-code-reviewer. Output → `raw-returns/lib_state_sessionKeys.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_sessionKeys.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<!-- Sub-wave C2 (12 files) -->
<task type="auto" id="12.C2.1">
  <name>Review lib/types.ts</name>
  <files>dashboard-web/src/lib/types.ts</files>
  <action>Spawn gsd-code-reviewer. DailyRow, DashboardData, Filters. Type module — verdict often ✅ if no logic. Output → `raw-returns/lib_state_types.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_types.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C2.2">
  <name>Review lib/utils.ts</name>
  <files>dashboard-web/src/lib/utils.ts</files>
  <action>Spawn gsd-code-reviewer. safeDecode + misc helpers. TEST_PATH=`dashboard-web/src/lib/__tests__/utils.test.ts`. Output → `raw-returns/lib_state_utils.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_utils.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C2.3">
  <name>Review lib/format.ts</name>
  <files>dashboard-web/src/lib/format.ts</files>
  <action>Spawn gsd-code-reviewer. number/currency/percent formatters. TEST_PATH=`dashboard-web/src/lib/__tests__/format.test.ts`. Output → `raw-returns/lib_state_format.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_format.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C2.4">
  <name>Review lib/dateRange.ts</name>
  <files>dashboard-web/src/lib/dateRange.ts</files>
  <action>Spawn gsd-code-reviewer. parseRangeParams, isInRange. Operator constraint: Asia/Jerusalem TZ canonical. TEST_PATH=`dashboard-web/src/lib/__tests__/dateRange.test.ts`. Output → `raw-returns/lib_state_dateRange.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_dateRange.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C2.5">
  <name>Review lib/dateValidation.ts</name>
  <files>dashboard-web/src/lib/dateValidation.ts</files>
  <action>Spawn gsd-code-reviewer. isDate helper. TEST_PATH=`dashboard-web/src/lib/__tests__/dateValidation.test.ts`. Output → `raw-returns/lib_state_dateValidation.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_dateValidation.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C2.6">
  <name>Review lib/rangeClamp.ts</name>
  <files>dashboard-web/src/lib/rangeClamp.ts</files>
  <action>Spawn gsd-code-reviewer. Clamp ranges to data availability. TEST_PATH=`dashboard-web/src/lib/__tests__/rangeClamp.test.ts`. Output → `raw-returns/lib_state_rangeClamp.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_rangeClamp.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C2.7">
  <name>Review lib/urlState.ts</name>
  <files>dashboard-web/src/lib/urlState.ts</files>
  <action>Spawn gsd-code-reviewer. readDashboardState, syncUrl. TEST_PATH=`dashboard-web/src/lib/__tests__/urlState.test.ts`. Output → `raw-returns/lib_state_urlState.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_urlState.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C2.8">
  <name>Review lib/drillFilter.ts</name>
  <files>dashboard-web/src/lib/drillFilter.ts</files>
  <action>Spawn gsd-code-reviewer. TEST_PATH=`dashboard-web/src/lib/__tests__/drillFilter.test.ts`. Output → `raw-returns/lib_state_drillFilter.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_drillFilter.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C2.9">
  <name>Review lib/apiErrors.ts</name>
  <files>dashboard-web/src/lib/apiErrors.ts</files>
  <action>Spawn gsd-code-reviewer. userFacingError sanitiser. TEST_PATH=`dashboard-web/src/lib/__tests__/apiErrors.test.ts`. Output → `raw-returns/lib_state_apiErrors.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_apiErrors.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C2.10">
  <name>Review lib/sparklineGeometry.ts</name>
  <files>dashboard-web/src/lib/sparklineGeometry.ts</files>
  <action>Spawn gsd-code-reviewer. TEST_PATH=`dashboard-web/src/lib/__tests__/sparklineGeometry.test.ts`. Output → `raw-returns/lib_state_sparklineGeometry.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_sparklineGeometry.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C2.11">
  <name>Review lib/chartColors.ts</name>
  <files>dashboard-web/src/lib/chartColors.ts</files>
  <action>Spawn gsd-code-reviewer. Recharts palette. TEST_PATH=`dashboard-web/src/lib/__tests__/chartColors.test.ts` (likely none). Output → `raw-returns/lib_state_chartColors.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_chartColors.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C2.12">
  <name>Review lib/constants.ts</name>
  <files>dashboard-web/src/lib/constants.ts</files>
  <action>Spawn gsd-code-reviewer. Output → `raw-returns/lib_state_constants.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_constants.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<!-- Sub-wave C3 (13 files) -->
<task type="auto" id="12.C3.1">
  <name>Review lib/shopifyRevenueRefunds.ts</name>
  <files>dashboard-web/src/lib/shopifyRevenueRefunds.ts</files>
  <action>Spawn gsd-code-reviewer. Canonical refund-correction algorithm (Phase 05.2.3.0 outcome). KEEP-IN-SYNC invariants in docstring. TEST_PATH=`dashboard-web/src/lib/__tests__/shopifyRevenueRefunds.test.ts` (707 LOC, 6 invariants). Output → `raw-returns/lib_state_shopifyRevenueRefunds.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_shopifyRevenueRefunds.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C3.2">
  <name>Review lib/lineItems.ts</name>
  <files>dashboard-web/src/lib/lineItems.ts</files>
  <action>Spawn gsd-code-reviewer. Shopify line-item helpers. TEST_PATH=`dashboard-web/src/lib/__tests__/lineItems.test.ts`. Output → `raw-returns/lib_state_lineItems.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_lineItems.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C3.3">
  <name>Review lib/productCatalog.ts</name>
  <files>dashboard-web/src/lib/productCatalog.ts</files>
  <action>Spawn gsd-code-reviewer. CatalogProduct type + helpers. TEST_PATH=`dashboard-web/src/lib/__tests__/productCatalog.test.ts`. Output → `raw-returns/lib_state_productCatalog.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_productCatalog.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C3.4">
  <name>Review lib/ads.ts</name>
  <files>dashboard-web/src/lib/ads.ts</files>
  <action>Spawn gsd-code-reviewer. TEST_PATH=`dashboard-web/src/lib/__tests__/ads.test.ts`. Output → `raw-returns/lib_state_ads.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_ads.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C3.5">
  <name>Review lib/billing.ts</name>
  <files>dashboard-web/src/lib/billing.ts</files>
  <action>Spawn gsd-code-reviewer. Recurring + one-time cost model. TEST_PATH=`dashboard-web/src/lib/__tests__/billing.test.ts`. Output → `raw-returns/lib_state_billing.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_billing.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C3.6">
  <name>Review lib/campaigns.ts</name>
  <files>dashboard-web/src/lib/campaigns.ts</files>
  <action>Spawn gsd-code-reviewer. CampaignRow type. Output → `raw-returns/lib_state_campaigns.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_campaigns.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C3.7">
  <name>Review lib/campaignOptimized.ts</name>
  <files>dashboard-web/src/lib/campaignOptimized.ts</files>
  <action>Spawn gsd-code-reviewer. Operator-typed "optimized" flag. TEST_PATH=`dashboard-web/src/lib/__tests__/campaignOptimized.test.ts`. Output → `raw-returns/lib_state_campaignOptimized.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_campaignOptimized.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C3.8">
  <name>Review lib/campaignsColumnPrefs.ts</name>
  <files>dashboard-web/src/lib/campaignsColumnPrefs.ts</files>
  <action>Spawn gsd-code-reviewer. TEST_PATH=`dashboard-web/src/lib/__tests__/campaignsColumnPrefs.test.ts`. Output → `raw-returns/lib_state_campaignsColumnPrefs.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_campaignsColumnPrefs.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C3.9">
  <name>Review lib/campaignsLinks.ts</name>
  <files>dashboard-web/src/lib/campaignsLinks.ts</files>
  <action>Spawn gsd-code-reviewer. TEST_PATH=`dashboard-web/src/lib/__tests__/campaignsLinks.test.ts`. Output → `raw-returns/lib_state_campaignsLinks.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_campaignsLinks.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C3.10">
  <name>Review lib/products.ts</name>
  <files>dashboard-web/src/lib/products.ts</files>
  <action>Spawn gsd-code-reviewer. ProductRow type. Output → `raw-returns/lib_state_products.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_products.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C3.11">
  <name>Review lib/costs.ts</name>
  <files>dashboard-web/src/lib/costs.ts</files>
  <action>Spawn gsd-code-reviewer. Per-store COGS via ${STORE_UPPERCASE}_COGS_RATE env var (operator constraint). TEST_PATH=`dashboard-web/src/lib/__tests__/costs.test.ts`. Output → `raw-returns/lib_state_costs.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_costs.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C3.12">
  <name>Review lib/operatorReset.ts</name>
  <files>dashboard-web/src/lib/operatorReset.ts</files>
  <action>Spawn gsd-code-reviewer. Reset payload validator. TEST_PATH=`dashboard-web/src/lib/__tests__/operatorReset.test.ts`. Output → `raw-returns/lib_state_operatorReset.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_operatorReset.json</automated></verify>
  <done>JSON return exists.</done>
</task>

<task type="auto" id="12.C3.13">
  <name>Review lib/annotations.ts</name>
  <files>dashboard-web/src/lib/annotations.ts</files>
  <action>Spawn gsd-code-reviewer. TEST_PATH=`dashboard-web/src/lib/__tests__/annotations.test.ts`. Output → `raw-returns/lib_state_annotations.json`.</action>
  <verify><automated>test -f .planning/phases/12-codebase-audit-baseline/raw-returns/lib_state_annotations.json</automated></verify>
  <done>JSON return exists.</done>
</task>

</tasks>

<verification>
All 37 raw-return files exist for Wave C (12 + 12 + 13). Zero source-code modifications.
</verification>

<success_criteria>
Wave C complete: 37 JSON returns in raw-returns/.
</success_criteria>

<output>
No SUMMARY.md — returns feed orchestrator synthesis.
</output>
</content>
</invoke>