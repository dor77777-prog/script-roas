---
phase: 12-codebase-audit-baseline
plan: 10
type: execute
wave: 2
depends_on: ["12-PLAN.md::12.X.1"]
files_modified:
  - .planning/phases/12-codebase-audit-baseline/12-CHANNELS.md
autonomous: true
requirements: [REQ-3]
tags: [audit, plan-agent, cross-cutting, channels, graphify]

must_haves:
  truths:
    - "12-CHANNELS.md exists with ≥5 channel-type sections"
    - "Each section enumerates concrete edges with source→target"
    - "Per-reviewer inline channels are deduped and grouped into bird's-eye view"
    - "Knowledge graph at .planning/graphs/graph.json was queried (D-17 reuse)"
  artifacts:
    - path: ".planning/phases/12-codebase-audit-baseline/12-CHANNELS.md"
      provides: "Inter-component channel map"
  key_links:
    - from: "raw-returns/*.json inline channels + graphify graph.json"
      to: "12-CHANNELS.md (deduped + grouped by channel type)"
      via: "Plan-agent cross-cutting sweep"
      pattern: "post-reviewer-consolidation"
---

<objective>
**Wave H3 — CHANNELS.md cross-cutting sweep (1 Plan-agent task).** Per D-17 — aggregate every channel each per-file reviewer surfaced (imports, props, custom events, SWR keys, Inngest triggers, Supabase reads/writes, API calls) into ONE bird's-eye view, grouped by channel type. Query the knowledge graph first to leverage prior work.

Purpose: Make inter-component bugs (e.g. v3 U-01 TIKTOK_ACTIVE_ENOUGH writer↔reader asymmetry) systematically detectable.
Output: `12-CHANNELS.md` — ≥5 sections, every concrete edge source→target.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/12-codebase-audit-baseline/12-SPEC.md
@.planning/phases/12-codebase-audit-baseline/12-CONTEXT.md
@.planning/graphs/graph.json
@.planning/graphs/GRAPH_REPORT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/INTEGRATIONS.md
</context>

<tasks>

<task type="auto" id="12.H3.1">
  <name>Build 12-CHANNELS.md from raw-returns + graphify</name>
  <files>.planning/phases/12-codebase-audit-baseline/12-CHANNELS.md</files>
  <action>
    **Plan-agent task.** Procedure:

    1. **Query the knowledge graph FIRST (D-17 reuse — do not grep-from-scratch):**
       Build domain-keyed maps via graphify queries — one per channel domain:
       ```bash
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" graphify status
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" graphify query "import" --budget 2000
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" graphify query "inngest" --budget 2000
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" graphify query "supabase" --budget 2000
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" graphify query "swr" --budget 1000
       node "$HOME/.claude/get-shit-done/bin/gsd-tools.cjs" graphify query "api route" --budget 1000
       ```
       Collect the returned edges as the seed set.

    2. **Aggregate inline channels from all raw-return JSONs:**
       Iterate over `.planning/phases/12-codebase-audit-baseline/raw-returns/*.json`. Each return has an inline field (per scaffolding template): `channels: {imports: [...], props: [...], custom_events: [...], swr_keys: [...], inngest_triggers: [...], supabase_reads: [...], supabase_writes: [...], api_calls: [...]}`. Extract every entry.

    3. **Merge graph + reviewer findings, then dedupe:**
       For each channel type, take the union of graph edges and reviewer-flagged edges. Dedupe by `(source, target, channel_type)` triple. Reviewer-flagged edges WIN on conflict (richer context, prose annotation).

    4. **Group by channel type — produce at least 5 sections** (REQ-3 minimum). Recommended 7:

       | Section | What it lists |
       |---------|---------------|
       | `## Imports` | Inter-module TypeScript imports across `lib/`, `components/`, `inngest/`, `app/`. Flag asymmetric writer↔reader sets (the v3 U-01 pattern). |
       | `## Component Props` | Concrete prop edges between parent → child components. |
       | `## Custom Events / Hooks Bus` | useDashboardRefresh, drawerStack ESC, mutate() callbacks. |
       | `## SWR Keys` | Every distinct SWR key string + which component(s) consume it + which API route serves it. |
       | `## Inngest Events / Triggers` | `event/sync-now`, `event/backfill`, `cron-daily-{store}`, `cron-live-{store}`, `whatsapp-{noon|evening|eod}` — who emits, who handles. |
       | `## Supabase Reads/Writes` | Every table touched by lib/postgresReaders.ts (reads, anon client) + lib/supabaseAdmin.ts (writes, service_role). Flag write-only or read-only tables (often a bug indicator). |
       | `## API Calls` | Every `fetch('/api/...')` from a UI component + which route serves it + Cache-Control config. |

    5. **For each edge, the format is:**
       ```
       - `{source-file}:{line}` → `{target-file or external system}` (channel-detail: e.g. event name / SWR key / table name / prop name) [{source: "reviewer" | "graph"}]
       ```

    6. **Cross-reference AUDIT.md (forward-reference):**
       Findings in AUDIT.md may reference channel entries by ID (`CHN-NN`). Assign IDs sequentially across all sections (CHN-001, CHN-002, ...).

    7. **Write `12-CHANNELS.md`:**
       ```markdown
       # Phase 12: Inter-Component Channel Map

       **Generated:** {ISO-8601 UTC}
       **Sections:** 7
       **Total edges:** {N}
       **Sources:** graphify graph.json + 134 reviewer returns

       ## Imports ({N} edges)
       - CHN-001: `dashboard-web/src/lib/platformConfig.ts:12` → `dashboard-web/src/inngest/functions/cronLive.ts:88` (TIKTOK_ACTIVE_ENOUGH writer→reader) [reviewer]
       - CHN-002: ...

       ## Component Props ({N} edges)
       ...

       ## Custom Events / Hooks Bus ({N} edges)
       ...

       ## SWR Keys ({N} edges)
       ...

       ## Inngest Events / Triggers ({N} edges)
       ...

       ## Supabase Reads/Writes ({N} edges)
       ...

       ## API Calls ({N} edges)
       ...
       ```

    8. Write using the Write tool — NEVER `cat << 'EOF'`.

    **Minimum gates (SPEC.md REQ-3):**
    - ≥5 sections (we ship 7)
    - Each section enumerates concrete edges with source→target
    - Every edge has a CHN-NN ID

    **If graphify is unavailable or graph.json is stale (status says stale=true), proceed anyway** — the reviewer-flagged channels are the canonical source per REQ-3. Note the graph staleness inline in the doc header.
  </action>
  <verify>
    <automated>test -f .planning/phases/12-codebase-audit-baseline/12-CHANNELS.md &amp;&amp; test "$(grep -cE '^## ' .planning/phases/12-codebase-audit-baseline/12-CHANNELS.md)" -ge 5 &amp;&amp; grep -qE 'CHN-[0-9]+' .planning/phases/12-codebase-audit-baseline/12-CHANNELS.md</automated>
  </verify>
  <done>12-CHANNELS.md exists with ≥5 sections, every edge has a CHN-NN ID, graph + reviewer sources both feed the final list.</done>
</task>

</tasks>

<verification>
12-CHANNELS.md exists with ≥5 sections, all edges have IDs. Zero source-code modifications.
</verification>

<success_criteria>
Wave H3 complete: channel map deliverable produced.
</success_criteria>

<output>
No SUMMARY.md. After this wave + H1 + H2 complete, orchestrator runs the final synthesis task in 12-PLAN.md (12.X.2).
</output>
</content>
</invoke>