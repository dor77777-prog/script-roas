---
phase: 05.2-multi-channel-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard-web/src/components/MetaShopifyReconciliation.tsx
  - dashboard-web/src/components/CampaignDrawer.tsx
  - dashboard-web/src/lib/__tests__/buildReconciliation.test.ts
autonomous: true
requirements: [P5.2-MULTI-CHANNEL]

must_haves:
  truths:
    - "buildReconciliation מחזיר series של 4 ערכים לכל יום: { date, meta, google, organic, shopify }"
    - "Google series מחושב כ-Σ conversionValue של campaigns שעוברים את ה-mapping של ה-מוצרים והם platform='Google' (אותו storeId)"
    - "Organic series מחושב כ-Σ revenue של orders שיש בהם productId מ-mappedIds AND order.source ∈ {meta-organic, direct, organic-search-engine, other, אם קיים}"
    - "buildReconciliation לא חוזר null עוד אם summary.platform === 'Google' — מחזיר 4 series תקין בכל מקרה. הסיכום שמופיע בכותרת לעולם חוזר Pearson עבור Meta+Google+Organic ביחס ל-Shopify."
    - "Stores ללא Google Ads (zolplus, usmile360): Google series כל ימים = 0, לא שגיאה"
    - "אם Σ(meta+google+organic) / shopify_actual < 0.8 במצטבר על כל הטווח → רנדור chip 'dark traffic' בכותרת עם הפער באחוזים"
    - "מבחנים חדשים ב-buildReconciliation.test.ts מאמתים: (1) 4 series populating, (2) Google שווה 0 כשאין Google campaigns, (3) Pearson פר ערוץ נכון"
  artifacts:
    - path: "dashboard-web/src/components/MetaShopifyReconciliation.tsx"
      provides: "buildReconciliation מעודכן עם 4 series + 4 Pearson values, ChartConfig חדש עם 4 colors, חוזר אם הקריאה למוצר מקושר ל-Google גם"
      contains: "google:"
    - path: "dashboard-web/src/components/CampaignDrawer.tsx"
      provides: "קריאה ל-buildReconciliation חיה גם כשה-summary.platform === 'Google' (פיתחנו את ה-guard המקורי)"
      contains: "buildReconciliation"
    - path: "dashboard-web/src/lib/__tests__/buildReconciliation.test.ts"
      provides: "מבחנים ל-4 ערוצים + edge case של 0 Google + coverage gap detection"
      contains: "describe('buildReconciliation'"
  key_links:
    - from: "MetaShopifyReconciliation chart"
      to: "Recharts ComposedChart with 4 Line series"
      via: "dataKey='meta'/'google'/'organic'/'shopify'"
      pattern: "dataKey=\"google\""
    - from: "CampaignDrawer reconciliation render"
      to: "MetaShopifyReconciliation"
      via: "reconciliation prop"
      pattern: "MetaShopifyReconciliation"
---

<objective>
להרחיב את ה-`MetaShopifyReconciliation` panel ב-CampaignDrawer מ-2 series (Meta vs Shopify) ל-4 series (Meta vs Google vs Organic vs Shopify), כדי שהמשתמש יראה את כל הערוצים בגרף אחד.

Purpose: גילוי במהלך session UAT של Phase 5.1 — המשתמש שאל "האם אפשר להציג גם מכירות Google ואורגניות בגרף ה-reconciliation?". הסתבר שכל הנתונים כבר נאספים אבל לא מוצגים. הסיכון אם לא נוסיף: המשתמש חסר חצי מהתמונה ועלול להאשים את Meta בלקיחת קרדיט שמגיע לערוץ אחר.

Output: 1 קובץ הרחבה + 1 קובץ component edit + 1 test file חדש.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@dashboard-web/src/components/MetaShopifyReconciliation.tsx
@dashboard-web/src/components/CampaignDrawer.tsx
@dashboard-web/src/lib/campaigns.ts
@dashboard-web/src/lib/campaignProductMap.ts
@dashboard-web/src/lib/ordersAttribution.ts
@dashboard-web/src/lib/attributionAnalysis.ts

<interfaces>
<!-- חוזים קיימים שמשתמשים בהם — לא משנים, רק קוראים -->

From dashboard-web/src/lib/campaigns.ts:
```typescript
export type CampaignRow = {
  date: string;          // 'YYYY-MM-DD'
  platform: 'Meta' | 'Google' | string;
  storeId: string;
  campaignId: string;
  campaignName: string;
  spendCad: number;
  conversionValue: number;   // ← זה ה-Google/Meta "claimed revenue"
  // ...
};
```

From dashboard-web/src/lib/ordersAttribution.ts:
```typescript
export type OrderSource =
  | 'meta-paid' | 'google-paid' | 'meta-organic' | 'meta-other'
  | 'organic-search-engine' | 'direct' | 'other';
export type OrderAttributionRow = {
  date: string;
  storeId: string;
  orderId: string;
  source: OrderSource;
  netRevenueCad: number;
  fbclidPresent: boolean;
  lineItems: OrderLineItem[];  // { productId, units, revenueCad }
};
```

From dashboard-web/src/lib/campaignProductMap.ts:
```typescript
export type ProductMap = Record<string, string[]>;
export function campaignsForProduct(storeId, productId, map): string[];
```

From `MetaShopifyReconciliation.tsx` (existing, will be EXTENDED):
```typescript
export function buildReconciliation(opts: {
  summary: { campaignId, platform, dailyArr: Array<{ date, conversionValue }> };
  productsData: { rows: ProductRow[] } | null | undefined;
  mappedIds: string[];
  storeId: string;
}): {
  series: Array<{ date: string; meta: number; shopify: number }>;  // ← changes to 4 keys
  r: number;
  bestLag: number;
  bestR: number;
} | null;
```

The current behavior:
- Returns null if mappedIds is empty
- Returns null if summary.platform !== 'Meta'  ← we relax this
- Builds {date → shopify revenue} from productsData (mapped products only)
- Builds {date → meta revenue} from summary.dailyArr.conversionValue
- Pearson(meta, shopify) + best lag check
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: הרחב את buildReconciliation עם Google + Organic series</name>
  <files>dashboard-web/src/components/MetaShopifyReconciliation.tsx</files>
  <behavior>
    - חתימת ה-opts מקבלת **בנוסף**:
      - `campaignsData: { rows: CampaignRow[] } | null` — כל ה-campaigns בטווח (כדי לחפש Google campaigns שמיפו לאותם מוצרים)
      - `ordersData: { rows: OrderAttributionRow[] } | null` — כל ה-orders בטווח עם line_items=true (כדי לזהות organic+direct revenue)
      - `productMap: ProductMap` — ה-mapping product↔campaign של המשתמש
    - מחזיר series של `{ date, meta, google, organic, shopify }` (4 כפולות) במקום 2
    - מחזיר 4 Pearson values במקום אחד: `r` (Meta-vs-Shopify, נשאר), `rGoogle`, `rOrganic`, `rCombined`
    - bestLag נשאר Meta-only (Google/Organic לא צריכים lag detection — Meta אוהבת לדחוף attribution גלובלי קדימה)
    - אם summary.platform === 'Google': כן מחזיר reconciliation תקין; ה-"meta" series של אותו campaign = 0 כל ימים (כי הקמפיין הוא Google, אין לו Meta data), אבל ה-"google" series מקבל את ה-conversionValue שלו
    - מוסיף `darkTrafficPercent: number` ל-output: אחוז הפער בין Σ(meta+google+organic) ל-Σ(shopify) אם > 20%, אחרת 0
  </behavior>
  <action>
    1. הרחב את חתימת `buildReconciliation` ב-`MetaShopifyReconciliation.tsx`. הוסף 3 פרמטרים ל-opts:
       - `campaignsData: { rows: CampaignRow[] } | null | undefined`
       - `ordersData: { rows: OrderAttributionRow[] } | null | undefined`
       - `productMap: import('@/lib/campaignProductMap').ProductMap`

    2. הסר את הקטע `if (summary.platform !== 'Meta') return null;` — עכשיו תומך גם ב-Google campaigns.

    3. בנה 4 maps של date → revenue:
       ```typescript
       // Existing — keep
       const metaByDate = new Map<string, number>();
       const shopifyByDate = new Map<string, number>();

       // NEW
       const googleByDate = new Map<string, number>();
       const organicByDate = new Map<string, number>();
       ```

    4. **metaByDate** (existing logic): from `summary.dailyArr` if platform === 'Meta', else 0 per day.
       **shopifyByDate** (existing logic): from productsData rows filtered by mappedIds.
       **googleByDate** (NEW): walk `campaignsData.rows`, for each row:
         - if `row.platform !== 'Google'` continue
         - if `row.storeId !== storeId` continue
         - if `row.campaignId === summary.campaignId` (the CURRENT campaign) — include directly
         - else: only include if the row's campaignId is mapped to ANY of `mappedIds` via `productMap` (i.e., other Google campaigns that promote the same products)
         - For Phase 5.2 simplicity: include ALL Google campaigns whose campaignKey is in `Object.keys(productMap)` AND share at least one product with `mappedIds`. This means the Google series represents "all Google revenue claimed across all Google campaigns that promote the same products as this campaign."
         - Accumulate `row.conversionValue` into `googleByDate.get(row.date)`.
       **organicByDate** (NEW): walk `ordersData.rows`, for each order:
         - if `order.storeId !== storeId` continue
         - if `!order.lineItems || order.lineItems.length === 0` continue
         - check if ANY line item has `productId ∈ mappedIds` — if not, continue
         - check `order.source`: include if source ∈ `['meta-organic', 'organic-search-engine', 'direct', 'meta-other', 'other']` (i.e., NOT 'meta-paid' or 'google-paid')
         - Accumulate `order.netRevenueCad` (or sum of mapped line items' revenueCad — see note below) into `organicByDate.get(order.date)`.
         - **Note for line-item attribution**: when a single order contains BOTH mapped and unmapped products, only count the mapped-product portion of revenue. Use the same partial-order logic as `analyzeProductChannel` (sum of li.revenueCad for li where li.productId ∈ wantedIds).

    5. הרכב את ה-series בדיוק על אותו ציר של תאריכים שכבר משתמש בו (`summary.dailyArr.map(d => d.date)`), אבל עם 4 ערכים לכל יום:
       ```typescript
       const series = summary.dailyArr.map(d => ({
         date: d.date,
         meta: metaByDate.get(d.date) ?? 0,
         google: googleByDate.get(d.date) ?? 0,
         organic: organicByDate.get(d.date) ?? 0,
         shopify: shopifyByDate.get(d.date) ?? 0,
       }));
       ```

    6. חישוב 4 Pearson values (השתמש ב-`pearson` הקיים):
       ```typescript
       const r = pearson(series.map(s => s.meta), series.map(s => s.shopify));
       const rGoogle = pearson(series.map(s => s.google), series.map(s => s.shopify));
       const rOrganic = pearson(series.map(s => s.organic), series.map(s => s.shopify));
       const rCombined = pearson(
         series.map(s => s.meta + s.google + s.organic),
         series.map(s => s.shopify)
       );
       ```

    7. חישוב `darkTrafficPercent`:
       ```typescript
       const sumChannels = series.reduce((acc, s) => acc + s.meta + s.google + s.organic, 0);
       const sumShopify = series.reduce((acc, s) => acc + s.shopify, 0);
       const darkTrafficPercent =
         sumShopify > 0 && sumChannels / sumShopify < 0.8
           ? Math.round((1 - sumChannels / sumShopify) * 100)
           : 0;
       ```

    8. החזר את ה-object החדש:
       ```typescript
       return { series, r, rGoogle, rOrganic, rCombined, bestLag, bestR, darkTrafficPercent };
       ```

    9. עדכן את ה-render של `MetaShopifyReconciliation` component:
       - הוסף 2 `<Line>` חדשים אחרי הקיים: `<Line dataKey="google" stroke="#2563eb" strokeWidth={2} dot={false} />` ו-`<Line dataKey="organic" stroke="#16a34a" strokeWidth={2} dot={false} />`. גוונים: Meta=#d97706 (orange, קיים), Google=#2563eb (blue), Organic=#16a34a (green), Shopify=existing color.
       - הוסף Legend עם 4 entries
       - מעל הגרף, אם `reconciliation.darkTrafficPercent > 0`: רנדור chip באמבר: `⚠ פער "Dark traffic" {N}%: סכום Meta+Google+Organic נמוך מ-Shopify בפועל. ייתכן channel attribution חסר (UTMs לא מוגדרים נכון, סוגי orders שלא מתויגים).`
       - בשורת ה-Pearson הקיימת, הצג גם את 3 ה-correlations החדשים: `r(Meta)={r.toFixed(2)}, r(Google)={rGoogle.toFixed(2)}, r(Organic)={rOrganic.toFixed(2)}, r(Combined)={rCombined.toFixed(2)}` — או stacked vertically בפורמט קריא.

    10. אל תיגע ב-bestLag detection (זה Meta-only, נשאר כפי שהוא).
  </action>
  <verify>
    <automated>cd dashboard-web && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -v "^$" | head -20 && grep -c "dataKey=\"google\"\|dataKey=\"organic\"" src/components/MetaShopifyReconciliation.tsx</automated>
  </verify>
  <done>
    - buildReconciliation מחזיר series של 4 ערכים + 4 Pearson values + darkTrafficPercent
    - הקומפוננטה רינדרת 4 Line series + legend + dark-traffic chip (כשרלוונטי)
    - tsc --noEmit עובר
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: עדכן את CampaignDrawer להעביר את ה-3 פרמטרים החדשים</name>
  <files>dashboard-web/src/components/CampaignDrawer.tsx</files>
  <behavior>
    - הקריאה ל-buildReconciliation בתוך CampaignDrawer מקבלת עכשיו campaignsData, ordersData, ו-productMap
    - ה-`campaignsData` כבר זמין ב-CampaignDrawer (useSWR ל-/api/campaigns)
    - ה-`ordersData` כבר זמין (useSWR ל-/api/orders-attribution?lineItems=true — מ-Phase 5-03)
    - ה-`productMap` קוראים מ-`readProductMap()` של campaignProductMap.ts (זה כבר כנראה משמש למיפוי הקיים)
    - הסר את ה-guard ב-CampaignDrawer שמסתיר את ה-panel כשה-summary.platform !== 'Meta', אם קיים
  </behavior>
  <action>
    1. מצא את הקריאה הקיימת ל-`buildReconciliation` ב-CampaignDrawer.tsx (סביב שורה 270 לפי ה-grep הקודם).

    2. הוסף ל-opts:
       ```typescript
       const reconciliation = buildReconciliation({
         summary,
         productsData,
         mappedIds,
         storeId,
         // NEW:
         campaignsData,  // כבר זמין כ-useSWR result
         ordersData,     // כבר זמין כ-useSWR?lineItems=true
         productMap,     // קרא ב-useMemo: const productMap = useMemo(() => readProductMap(), [reload]);
       });
       ```

    3. אם יש guard כמו `{summary.platform === 'Meta' && reconciliation && <MetaShopifyReconciliation ... />}` — הסר את ה-`summary.platform === 'Meta'` (עכשיו תומך גם ב-Google) אבל השאר את ה-`reconciliation` truthy check.

    4. אל תשנה את שאר הקריאות ל-SWR או את ה-state של ה-CampaignDrawer.
  </action>
  <verify>
    <automated>cd dashboard-web && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -v "^$" | head -20 && grep -c "campaignsData\|ordersData\|productMap" src/components/CampaignDrawer.tsx</automated>
  </verify>
  <done>
    - CampaignDrawer מעביר 3 פרמטרים חדשים ל-buildReconciliation
    - הפאנל מוצג גם עבור Google campaigns (לא רק Meta)
    - tsc עובר
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: מבחנים לbuildReconciliation</name>
  <files>dashboard-web/src/lib/__tests__/buildReconciliation.test.ts</files>
  <behavior>
    - מבחן 1: campaign Meta אחד + 2 מוצרים מקושרים + 5 ימים נתונים → series של 5 עם 4 ערכים, Pearson values בכל הערוצים
    - מבחן 2: store ללא Google campaigns (zolplus stub) → google series כל ימים = 0, rGoogle = 0 (אין שונות)
    - מבחן 3: order עם מוצר מקושר ו-source='direct' → organicByDate מקבל את ה-revenue של line items המקושרים בלבד (לא revenue של order כולו אם יש unmapped products)
    - מבחן 4: dark traffic detection — אם Meta+Google+Organic = 60, Shopify = 100 → darkTrafficPercent = 40
    - מבחן 5: backwards-compat — קריאה ישנה ללא campaignsData/ordersData/productMap (כל 3 = null/empty) → google=0, organic=0, meta+shopify יחזרו כפי שהיו לפני
  </behavior>
  <action>
    צור קובץ חדש `dashboard-web/src/lib/__tests__/buildReconciliation.test.ts` עם 5 בלוקי `it(...)` שבודקים את המקרים לעיל. ה-import:
    ```typescript
    import { describe, it, expect } from 'vitest';
    import { buildReconciliation } from '@/components/MetaShopifyReconciliation';
    // אם buildReconciliation לא exported מ-component file כרגע, expose אותו export בקובץ של ה-component
    ```
    
    אם הפונקציה לא exported, הוסף `export` בפני `function buildReconciliation` ב-MetaShopifyReconciliation.tsx (כבר exported לפי ה-grep). 
    
    כל מבחן בונה fixtures מינימליים (3-5 days × 1-2 stores) ומאמת את הערכים המספריים בדיוק (`toBe` או `toBeCloseTo` ל-floats).

    אם הקובץ הקיים `MetaShopifyReconciliation.tsx` מציב את `buildReconciliation` כ-helper פנימי (לא exported), אזי ה-task הזה יוצא קטן מדי — ממש exposes the existing פונקציה ובנה את ה-tests.
  </action>
  <verify>
    <automated>cd dashboard-web && npm test -- buildReconciliation 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 5 מבחנים עוברים
    - buildReconciliation exported מ-MetaShopifyReconciliation.tsx (אם לא היה)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| productMap (localStorage) → reconciliation | פנימי — המשתמש כתב אותו ידנית; אין input חיצוני |
| External APIs (Meta/Google/Shopify) → conversionValue | כבר אומת ב-Phase 5 (סינון בצד שרת + degraded fallback) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.2-01 | Tampering | productMap manipulation | accept | localStorage hack על המכשיר של המשתמש = רק שמשנה את התצוגה שלו. אין סיכון cross-user (single-user product). |
| T-05.2-02 | Information disclosure | Google conversionValue | accept | אותו gate שכבר מגן על Meta — service-account read-only. אין חשיפה נוספת. |
| T-05.2-03 | DoS | extra Google data fetch per render | mitigate | ה-Google data כבר ב-`campaignsData` של ה-Drawer (useSWR קיים). אין fetch נוסף. רק חישוב מקומי במ memo. |
</threat_model>

<verification>
- Compile: `npx tsc --noEmit` עובר ב-Task 1, 2, 3
- Build: `npm run build` עובר אחרי Task 2
- Tests: `npm test -- buildReconciliation` — כל 5 המבחנים החדשים עוברים
- **Post-deploy ידני (פרודקשן)**: 
  - פתח את הדשבורד ב-https://roas-dashboard-smoky.vercel.app
  - בחר טווח של 30 ימים אחורה
  - פתח קמפיין Meta כלשהו שמיפית מוצרים ב-CampaignDrawer
  - גלול ל-MetaShopifyReconciliation panel
  - **ודא:** 4 lines ב-Recharts, legend עם 4 צבעים, Pearson values של 4 ערוצים
  - **בונוס:** אם יש לך קמפיין uzoshop שמיפית עם Google campaigns של אותם מוצרים, ה-google line לא יהיה flat 0
  - **dark traffic chip:** אם הופיע — בדוק שהמספר סביר (לא negative, לא > 100%)
</verification>

<success_criteria>
- buildReconciliation מחזיר 4 series + 4 Pearson values + darkTrafficPercent
- הגרף בדשבורד מציג 4 lines עם legend ברור
- כל המבחנים החדשים עוברים
- אין רגרסיה ב-`npm test` הכולל (84+ tests)
- `npm run build` עובר
- Eyeball בפרודקשן: 4 lines נראים
</success_criteria>

<output>
After completion, create `.planning/phases/05.2-multi-channel-reconciliation/05.2-01-SUMMARY.md`
</output>
