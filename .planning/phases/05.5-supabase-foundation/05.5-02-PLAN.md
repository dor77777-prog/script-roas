---
phase: 05.5-supabase-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 05.5-01
files_modified:
  - docs/PROPS-MAP.md
  - .env
autonomous: false
requirements: []
decisions_addressed:
  - D-C1   # Single markdown table at docs/PROPS-MAP.md, 5 cols
  - D-C2   # Preserve lowercase.with.dots key names; UPPER_SNAKE in Destination
  - D-C3   # 3-value taxonomy: SECRET / CONFIG / DATA
  - D-C4   # Rotation playbook DEFERRED (no SECRET-ROTATION.md in 05.5)
  - D-C5   # Add 3 new Supabase keys to .env + PROPS-MAP

user_setup:
  - service: vercel
    why: "Seed 3 new Supabase env vars in production environment"
    env_vars:
      - name: SUPABASE_URL
        source: "Supabase Dashboard → Project Settings → API → Project URL (set in plan 01 Task 0)"
      - name: SUPABASE_ANON_KEY
        source: "Supabase Dashboard → Project Settings → API → Project API keys → anon public"
      - name: SUPABASE_SERVICE_ROLE_KEY
        source: "Supabase Dashboard → Project Settings → API → Project API keys → service_role secret"
    dashboard_config:
      - task: "Vercel CLI must be logged in (vercel login) and project linked"
        location: "operator's local shell — run `vercel link` once if not already linked to roas-dashboard project"

must_haves:
  truths:
    - "docs/PROPS-MAP.md exists with 43 rows (40 existing + 3 Supabase per D-C5)"
    - "Every row has a Type value of SECRET, CONFIG, DATA, or `n/a` (only row 2 is n/a per A8)"
    - "Every row preserves the lowercase.with.dots key in column 1 (D-C2)"
    - "All 3 new Supabase env vars appear in `vercel env ls production` marked sensitive"
    - "All 3 new Supabase keys appended to .env Section 5 (gitignored)"
    - "No actual secret tokens appear in docs/PROPS-MAP.md (truncated previews only — first 8 chars + `...`)"
  artifacts:
    - path: "docs/PROPS-MAP.md"
      provides: "43-row classification table — operator checklist gating 05.7 cut-over"
      min_lines: 60
    - path: ".env"
      provides: ".env Section 5 with 3 new Supabase keys (gitignored)"
      contains: "supabase.url"
  key_links:
    - from: ".env Section 5 keys (lowercase.with.dots)"
      to: "Vercel env vars (UPPER_SNAKE)"
      via: "Destination column in docs/PROPS-MAP.md"
      pattern: "VERCEL_ENV: SUPABASE_(URL|ANON_KEY|SERVICE_ROLE_KEY)"
    - from: "Vercel production env vars"
      to: "Plan 03 /api/health route (consumed at runtime)"
      via: "process.env.SUPABASE_URL / SUPABASE_ANON_KEY"
      pattern: "process\\.env\\.SUPABASE_"
---

<objective>
Produce the 43-row PROPS-MAP operator checklist that classifies every Apps Script PropertiesService key from `.env` into SECRET / CONFIG / DATA (D-C1, D-C3), preserves the original lowercase.with.dots key names (D-C2), and adds the 3 new Supabase keys (D-C5). Seed the 3 Supabase env vars into Vercel's production environment so plan 03's `/api/health` route can resolve them at runtime.

Purpose: This is the artifact that gates Phase 05.7's cut-over. Every property must be classified + destination-mapped before we flip the read flag from Sheets to Postgres. Today's deliverable is the classification table itself; the operator will tick `[ ]` → `[x]` as each property gets seeded over phases 05.5 → 05.6 → 05.7.

Output:
- `docs/PROPS-MAP.md` with 43 rows in 5-column markdown table format (committed to git — safe because secrets are abbreviated)
- `.env` Section 5 appended with `supabase.url`, `supabase.anon.key`, `supabase.service.role.key` (stays gitignored)
- Vercel production env vars: `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` — verified via `vercel env ls production`
</objective>

<execution_context>
@/Users/dorperetz/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dorperetz/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/dorperetz/script-roas/.planning/phases/05.5-supabase-foundation/05.5-CONTEXT.md
@/Users/dorperetz/script-roas/.planning/phases/05.5-supabase-foundation/05.5-RESEARCH.md
@/Users/dorperetz/script-roas/.planning/phases/05.5-supabase-foundation/05.5-PATTERNS.md
@/Users/dorperetz/script-roas/.env
@/Users/dorperetz/script-roas/.planning/phases/05.5-supabase-foundation/05.5-01-SUMMARY.md

<interfaces>
<!-- Existing PROPS-MAP analog (heading + table style): docs/ROAS-Dashboard-Quick-Start.md -->
<!-- Long-form intro pattern: docs/ROAS-Dashboard-User-Manual.md:1-29 -->
<!-- .env Sections 1-4 already follow `service.subkey = value` format — append Section 5 in-place. -->
<!-- The full 43-row table content is inlined in Task 1 below (verbatim from RESEARCH.md §Pattern 8). -->

<!-- Vercel CLI behavior (per RESEARCH.md Pitfall 5): -->
<!--   - `vercel env add <NAME> production` defaults to "sensitive" (cannot be viewed in dashboard after) -->
<!--   - Reading from stdin: `echo "value" | vercel env add NAME production` works -->
<!--   - `--force` flag overwrites existing entries (safe for idempotent re-seeding) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author docs/PROPS-MAP.md (43-row classification table)</name>
  <files>docs/PROPS-MAP.md</files>
  <read_first>
    - .planning/phases/05.5-supabase-foundation/05.5-RESEARCH.md §Architecture Patterns Pattern 8 (lines 653-713) — full 43-row layout
    - .planning/phases/05.5-supabase-foundation/05.5-CONTEXT.md §decisions D-C1..D-C5
    - .planning/phases/05.5-supabase-foundation/05.5-PATTERNS.md §docs/PROPS-MAP.md (analog: docs/ROAS-Dashboard-Quick-Start.md heading style)
    - .env (full file — to extract actual abbreviated source values for each row)
    - docs/ROAS-Dashboard-Quick-Start.md lines 1-30 (heading + intro style analog)
  </read_first>
  <action>
    Create `docs/PROPS-MAP.md` with the EXACT content below (verbatim from RESEARCH.md §Pattern 8, with actual abbreviated `.env` values substituted in column 4). The file is committed to git — abbreviated values are safe (no full secrets exposed). The file MUST contain all 43 rows (1 n/a + 42 actionable).

    ```markdown
    # PROPS-MAP — v2.0 migration property classification

    > Operator checklist gating Phase 05.7 cut-over. Each row is one Apps Script
    > PropertiesService key from `.env`. Tick `[x]` in "Seeded?" only after
    > verifying the destination (`vercel env ls production` OR
    > `SELECT * FROM notification_config`).
    >
    > **Type values (per D-C3):** `SECRET` = Vercel env var, sensitive (tokens, keys);
    > `CONFIG` = Vercel env var, low-sensitivity (domains, IDs); `DATA` = Supabase row
    > (operator-editable via UI in Phase 05.6); `n/a` = legacy duplicate, do not seed.
    >
    > **Sorting:** by `.env` section (matches mental model "work one service at a
    > time"), NOT by type. See §`Sorting rationale` in RESEARCH.md §Pattern 8.
    >
    > Total: **43 rows** (40 from `.env` + 3 new Supabase keys per D-C5).

    | # | Property key | Type | Destination | Source value | Seeded? |
    |---|--------------|------|-------------|--------------|---------|
    | **§1 — Google Sheets data plane (3 props)** | | | | | |
    | 1 | spreadsheet.id | CONFIG | VERCEL_ENV: SPREADSHEET_ID | (sheet ID from `.env`) | [ ] |
    | 2 | spreadsheet.canonical-id | n/a | — (legacy duplicate of #1; do not seed) | — | n/a |
    | 3 | archive.spreadsheet.id | CONFIG | VERCEL_ENV: ARCHIVE_SPREADSHEET_ID | (archive sheet ID from `.env`) | [ ] |
    | **§2.1 — Notification dispatch (6 props)** | | | | | |
    | 4 | notify.provider | DATA | supabase: notification_config.provider | metacloud | [x] |
    | 5 | notify.dashboardUrl | DATA | supabase: notification_config.dashboard_url | https://roas-dashboard-smoky.vercel.app | [x] |
    | 6 | notification.email | DATA | supabase: notification_config.notification_email | dor77777@gmail.com | [x] |
    | 7 | notify.phone1 | DATA | supabase: notification_config.phone1 | +972524809540 | [x] |
    | 8 | notify.phone2 | DATA | supabase: notification_config.phone2 | +972546100067 | [x] |
    | 9 | test-notify.phone1 | DATA | supabase: notification_config.test_phone1 (or omit if test-only) | whatsapp:+972524809540 | [ ] |
    | **§2.2 — Meta Cloud (active provider, 4 props)** | | | | | |
    | 10 | metacloud.accessToken | SECRET | VERCEL_ENV: METACLOUD_ACCESS_TOKEN | EAAVHQVc... | [ ] |
    | 11 | metacloud.phoneNumberId | CONFIG | VERCEL_ENV: METACLOUD_PHONE_NUMBER_ID | 1091010644104167 | [ ] |
    | 12 | metacloud.templateName | DATA | supabase: notification_config.template_name | roas_daily_summary | [x] |
    | 13 | metacloud.templateLang | DATA | supabase: notification_config.template_lang | he | [x] |
    | **§2.3 — Twilio (legacy fallback, 3 props)** | | | | | |
    | 14 | twilio.accountSid | SECRET | VERCEL_ENV: TWILIO_ACCOUNT_SID | ACc90784... | [ ] |
    | 15 | twilio.authToken | SECRET | VERCEL_ENV: TWILIO_AUTH_TOKEN | 1d8ed5c8... | [ ] |
    | 16 | twilio.whatsappFrom | CONFIG | VERCEL_ENV: TWILIO_WHATSAPP_FROM | whatsapp:+14155238886 | [ ] |
    | **§3 — Google Ads (5 props)** | | | | | |
    | 17 | googleads.clientId | CONFIG | VERCEL_ENV: GOOGLEADS_CLIENT_ID | 99469225... | [ ] |
    | 18 | googleads.clientSecret | SECRET | VERCEL_ENV: GOOGLEADS_CLIENT_SECRET | GOCSPX-_... | [ ] |
    | 19 | googleads.developerToken | SECRET | VERCEL_ENV: GOOGLEADS_DEVELOPER_TOKEN | IrOq9uap... | [ ] |
    | 20 | googleads.loginCustomerId | CONFIG | VERCEL_ENV: GOOGLEADS_LOGIN_CUSTOMER_ID | 1774599931 | [ ] |
    | 21 | googleads.refreshToken | SECRET | VERCEL_ENV: GOOGLEADS_REFRESH_TOKEN | 1//04jv7F... | [ ] |
    | **§4.1 — uzoshop (7 props)** | | | | | |
    | 22 | uzoshop.shopify.domain | CONFIG | VERCEL_ENV: UZOSHOP_SHOPIFY_DOMAIN | uzo-d-s-2.myshopify.com | [ ] |
    | 23 | uzoshop.shopify.clientId | CONFIG | VERCEL_ENV: UZOSHOP_SHOPIFY_CLIENT_ID | f44d1af6... | [ ] |
    | 24 | uzoshop.shopify.clientSecret | SECRET | VERCEL_ENV: UZOSHOP_SHOPIFY_CLIENT_SECRET | shpss_da... | [ ] |
    | 25 | uzoshop.shopify.token | SECRET | VERCEL_ENV: UZOSHOP_SHOPIFY_TOKEN | shpat_01... | [ ] |
    | 26 | uzoshop.meta.accessToken | SECRET | VERCEL_ENV: UZOSHOP_META_ACCESS_TOKEN | EABAHWeV... | [ ] |
    | 27 | uzoshop.meta.adAccountId | CONFIG | VERCEL_ENV: UZOSHOP_META_AD_ACCOUNT_ID | 26442930835313109 | [ ] |
    | 28 | uzoshop.googleads.customerId | CONFIG | VERCEL_ENV: UZOSHOP_GOOGLEADS_CUSTOMER_ID | 4014537400 | [ ] |
    | **§4.2 — zolplus (6 props)** | | | | | |
    | 29 | zolplus.shopify.domain | CONFIG | VERCEL_ENV: ZOLPLUS_SHOPIFY_DOMAIN | (from .env) | [ ] |
    | 30 | zolplus.shopify.clientId | CONFIG | VERCEL_ENV: ZOLPLUS_SHOPIFY_CLIENT_ID | (from .env) | [ ] |
    | 31 | zolplus.shopify.clientSecret | SECRET | VERCEL_ENV: ZOLPLUS_SHOPIFY_CLIENT_SECRET | shpss_... | [ ] |
    | 32 | zolplus.shopify.token | SECRET | VERCEL_ENV: ZOLPLUS_SHOPIFY_TOKEN | shpat_... | [ ] |
    | 33 | zolplus.meta.accessToken | SECRET | VERCEL_ENV: ZOLPLUS_META_ACCESS_TOKEN | EAB... | [ ] |
    | 34 | zolplus.meta.adAccountId | CONFIG | VERCEL_ENV: ZOLPLUS_META_AD_ACCOUNT_ID | 800776975668368 | [ ] |
    | **§4.3 — usmile360 (6 props)** | | | | | |
    | 35 | usmile360.shopify.domain | CONFIG | VERCEL_ENV: USMILE360_SHOPIFY_DOMAIN | (from .env) | [ ] |
    | 36 | usmile360.shopify.clientId | CONFIG | VERCEL_ENV: USMILE360_SHOPIFY_CLIENT_ID | (from .env) | [ ] |
    | 37 | usmile360.shopify.clientSecret | SECRET | VERCEL_ENV: USMILE360_SHOPIFY_CLIENT_SECRET | shpss_... | [ ] |
    | 38 | usmile360.shopify.token | SECRET | VERCEL_ENV: USMILE360_SHOPIFY_TOKEN | shpat_... | [ ] |
    | 39 | usmile360.meta.accessToken | SECRET | VERCEL_ENV: USMILE360_META_ACCESS_TOKEN | EAB... | [ ] |
    | 40 | usmile360.meta.adAccountId | CONFIG | VERCEL_ENV: USMILE360_META_AD_ACCOUNT_ID | 981695850074160 | [ ] |
    | **§5 — Supabase (NEW, 3 props per D-C5)** | | | | | |
    | 41 | supabase.url | CONFIG | VERCEL_ENV: SUPABASE_URL | https://<ref>.supabase.co | [ ] |
    | 42 | supabase.anon.key | SECRET | VERCEL_ENV: SUPABASE_ANON_KEY | eyJhbGc... | [ ] |
    | 43 | supabase.service.role.key | SECRET | VERCEL_ENV: SUPABASE_SERVICE_ROLE_KEY | eyJhbGc... | [ ] |

    ## Notes

    - Row 2 (`spreadsheet.canonical-id`) is `n/a` — legacy duplicate of #1 per `.env` Section 1
      comment and CONTEXT.md `<code_context>`. Migrate only #1 to Vercel; ignore the duplicate.
    - The 6 DATA rows in §2.1 + §2.2 (rows 4-8, 12, 13) are already pre-ticked `[x]` because
      they are seeded by the `notification_config` INSERT in `supabase/migrations/{ts}_seed_stores.sql`
      (plan 01 Task 3). Operator verifies in Supabase Studio Table Editor.
    - Row 9 (`test-notify.phone1`) is a test-environment value — operator decides at 05.6 whether
      to add a `test_phone1` column to `notification_config` or omit entirely.
    - Source values for rows 23, 24, 26, 31, 32, 33, 37, 38, 39 (Shopify + Meta tokens for all 3
      stores) are abbreviated to the first 8 chars + `...` — this file is git-tracked and must
      not leak full tokens. Operator reads the full values from `.env` (gitignored) when seeding.
    - Rotation playbook deferred to a future phase per D-C4 (no `docs/SECRET-ROTATION.md` in 05.5).
    ```

    Note: The actual `.env` Section 4.2 / 4.3 values for `zolplus` and `usmile360` should be
    extracted from `.env` and substituted into rows 29-30 and 35-36 (the placeholder
    `(from .env)` should be replaced with the actual abbreviated domain / clientId values).
    For SECRET rows in those sections (31-33, 37-39), use first 8 chars + `...` (matches uzoshop pattern).
  </action>
  <verify>
    <automated>
      test -f docs/PROPS-MAP.md &&
      grep -c '^| [0-9]' docs/PROPS-MAP.md | grep -qE '^4[3-9]$|^[5-9][0-9]$' &&
      grep -q '^| 41 | supabase.url' docs/PROPS-MAP.md &&
      grep -q '^| 42 | supabase.anon.key' docs/PROPS-MAP.md &&
      grep -q '^| 43 | supabase.service.role.key' docs/PROPS-MAP.md &&
      grep -q 'PROPS-MAP' docs/PROPS-MAP.md &&
      grep -q 'D-C3' docs/PROPS-MAP.md &&
      grep -c 'SECRET\|CONFIG\|DATA\|n/a' docs/PROPS-MAP.md | grep -qE '^[3-9][0-9]+$' &&
      ! grep -qE 'shpat_[a-zA-Z0-9]{15,}' docs/PROPS-MAP.md &&
      ! grep -qE 'EAAVHQVc[a-zA-Z0-9]{15,}' docs/PROPS-MAP.md &&
      grep -q 'spreadsheet.canonical-id.*n/a' docs/PROPS-MAP.md
    </automated>
  </verify>
  <acceptance_criteria>
    - File exists at `docs/PROPS-MAP.md`
    - Row count: `grep -c '^| [0-9]' docs/PROPS-MAP.md` returns a number between 43 and 99 (43 rows minimum; some grouping headers are non-numeric)
    - Contains literal strings `supabase.url`, `supabase.anon.key`, `supabase.service.role.key` (D-C5)
    - Row 2 (`spreadsheet.canonical-id`) marked `n/a` (A8 — legacy duplicate)
    - No full secret tokens: `grep -E 'shpat_[a-zA-Z0-9]{15,}'` and `grep -E 'EAAVHQVc[a-zA-Z0-9]{15,}'` both return empty
    - At least 30 occurrences of `SECRET|CONFIG|DATA|n/a` across rows (3-value taxonomy)
    - Heading + intro structure matches docs/ROAS-Dashboard-Quick-Start.md pattern (top-level `# Title` + `> blockquote intro`)
  </acceptance_criteria>
  <done>43-row PROPS-MAP committed to git, safe to expose (no full secrets).</done>
</task>

<task type="auto">
  <name>Task 2: Append .env Section 5 with 3 new Supabase keys (gitignored)</name>
  <files>.env</files>
  <read_first>
    - .env (current file — extract format from Sections 1-4 so Section 5 is consistent)
    - .planning/phases/05.5-supabase-foundation/05.5-PATTERNS.md §`.env` (append Section 5)
    - .planning/phases/05.5-supabase-foundation/05.5-CONTEXT.md §decisions D-C5
    - .planning/phases/05.5-supabase-foundation/05.5-RESEARCH.md §Code Examples Example 2 (key naming)
    - .planning/phases/05.5-supabase-foundation/05.5-01-SUMMARY.md (for project ref + actual keys from Supabase Dashboard)
  </read_first>
  <action>
    Append a new Section 5 to `/Users/dorperetz/script-roas/.env`. Use the existing `service.subkey = value` format (per Sections 1-4 in `.env`). The actual key values come from the operator's Supabase Dashboard (Project Settings → API), which the operator already copied during plan 01 Task 0. The plan 01 SUMMARY records them.

    The Section 5 block to append (substitute the actual cloud values where indicated):

    ```
    # ─────────────────────────────────────────────────────────────────
    # Section 5 — Supabase (v2.0 stack — Phase 05.5)
    # ─────────────────────────────────────────────────────────────────
    # Added 2026-05-21 per D-C5. PROPS-MAP rows 41-43 (docs/PROPS-MAP.md).
    # Vercel destination: SUPABASE_URL / SUPABASE_ANON_KEY / SUPABASE_SERVICE_ROLE_KEY.
    # Plan 03's /api/health route reads these at runtime via process.env.* (server-side).

    supabase.url = https://<PROJECT_REF>.supabase.co
    supabase.anon.key = <ANON_JWT_FROM_DASHBOARD>
    supabase.service.role.key = <SERVICE_ROLE_JWT_FROM_DASHBOARD>
    ```

    Replace `<PROJECT_REF>` with the value from plan 01 Task 0 resume-signal (e.g., `abcdefghijklmnop` → `https://abcdefghijklmnop.supabase.co`). Replace the two JWT placeholders with the actual `anon` and `service_role` keys from Supabase Dashboard → Project Settings → API.

    **Critical:** `.env` is in the repo `.gitignore` (committed `5521978`). After editing, run:

    ```bash
    git status -- .env
    ```

    Expected output: `.env` does NOT appear (because it's gitignored). If git shows it as modified, abort and verify `.gitignore` lists `.env`.

    Do NOT commit `.env`. Do NOT print the new key values in console output beyond the redacted form (e.g., `eyJhbGc...`).
  </action>
  <verify>
    <automated>
      test -f .env &&
      grep -q '^supabase.url ' .env &&
      grep -q '^supabase.anon.key ' .env &&
      grep -q '^supabase.service.role.key ' .env &&
      grep -q '# Section 5 — Supabase' .env &&
      ! git ls-files --error-unmatch .env 2>/dev/null
    </automated>
  </verify>
  <acceptance_criteria>
    - `.env` contains 3 new lines starting with `supabase.url`, `supabase.anon.key`, `supabase.service.role.key`
    - `.env` contains a section header `# Section 5 — Supabase`
    - `git ls-files .env` exits non-zero (file remains gitignored — `git ls-files --error-unmatch` returns error)
    - `git status` does NOT list `.env` as modified or untracked (because gitignore covers it)
  </acceptance_criteria>
  <done>3 Supabase keys recorded in `.env` Section 5 for local reference; file remains gitignored.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Seed 3 Supabase env vars into Vercel production</name>
  <files>docs/PROPS-MAP.md (rows 41-43 — Seeded? column flipped to [x]); no other files modified by this task; `vercel env add` mutates the remote Vercel project env-var store only</files>
  <action>Operator runs `vercel env add SUPABASE_URL production --force`, `vercel env add SUPABASE_ANON_KEY production --force`, `vercel env add SUPABASE_SERVICE_ROLE_KEY production --force` (pasting the values from `.env` Section 5 when prompted — Method A in `<how-to-verify>` keeps values out of shell history). After all 3 are seeded, trigger a redeploy (`vercel --prod` or push to main). Finally, mark PROPS-MAP rows 41-43 `[x]`. See `<how-to-verify>` for the exact CLI sequence.</action>
  <verify><automated>vercel env ls production 2>&amp;1 | grep -c "^SUPABASE_" | grep -qE "^[[:space:]]*[3-9][[:space:]]*$"</automated></verify>
  <done>All 3 Supabase env vars present in Vercel production marked Encrypted; PROPS-MAP rows 41-43 ticked; fresh production deployment is live.</done>
  <what-built>
    The 3 Supabase values are recorded in `.env` (local). They must be propagated to Vercel's production environment so plan 03's `/api/health` route can read `process.env.SUPABASE_URL` etc. when the dashboard runs in production. This task uses `vercel env add` with stdin to seed each value as a sensitive production env var.
  </what-built>
  <read_first>
    - .planning/phases/05.5-supabase-foundation/05.5-RESEARCH.md §Code Examples Example 2 (vercel env add from stdin)
    - .planning/phases/05.5-supabase-foundation/05.5-RESEARCH.md §Common Pitfalls Pitfall 5 (production default = sensitive)
    - .env (Section 5 — source of values to pipe into vercel CLI)
  </read_first>
  <how-to-verify>
    Run from the repo root. The operator pipes each value into `vercel env add` so values stay out of shell history flags. Replace `<URL>`, `<ANON>`, `<SRVRL>` with the actual values from `.env` Section 5 (the operator reads them locally — DO NOT copy/paste through any other system):

    ```bash
    # Verify Vercel CLI logged in + project linked
    vercel whoami           # should print operator's username
    vercel env ls production # baseline — note which vars already exist

    # Seed the 3 new vars (per RESEARCH.md Example 2; --force overwrites if previously seeded)
    # Use `< file` or here-string to avoid leaving the value in history.
    # Method A (preferred — no shell history leak):
    vercel env add SUPABASE_URL production --force                # prompt asks "What's the value?" — paste the URL
    vercel env add SUPABASE_ANON_KEY production --force           # paste the anon JWT
    vercel env add SUPABASE_SERVICE_ROLE_KEY production --force   # paste the service_role JWT

    # Method B (scripted, less secure — values land in shell history):
    # echo "https://<PROJECT_REF>.supabase.co" | vercel env add SUPABASE_URL production --force
    # echo "<ANON_JWT>"                         | vercel env add SUPABASE_ANON_KEY production --force
    # echo "<SERVICE_ROLE_JWT>"                 | vercel env add SUPABASE_SERVICE_ROLE_KEY production --force

    # Verify all 3 land in production with sensitive marker
    vercel env ls production | grep '^SUPABASE_'
    # Expected: 3 rows, each labeled "Encrypted" or "Sensitive" in the Environment column.
    # The exact Vercel UI label for production secrets is "Encrypted" (per Pitfall 5).

    # Trigger a redeploy so the new env vars reach the running dashboard:
    vercel --prod    # OR push any noop commit to trigger CI-driven deploy
    ```

    **After redeploy completes** (`vercel --prod` finishes and Vercel shows the new deployment as Production):

    ```bash
    # Optional sanity check (the dashboard's existing /api/store-meta should still respond — proves the redeploy didn't break anything)
    curl -s https://roas-dashboard-smoky.vercel.app/api/store-meta | head -c 200
    # Expected: JSON with stores. Confirms the deploy is green.
    ```

    Once `vercel env ls production` shows all 3 SUPABASE_* vars + a fresh production deployment is live, mark the corresponding PROPS-MAP rows (41-43) as `[x]` Seeded:

    ```bash
    # Update docs/PROPS-MAP.md rows 41-43 (Seeded? column: [ ] → [x])
    sed -i.bak 's/| 41 |.*| \[ \] |$/&/; s/^\(| 41 .*\)| \[ \] |$/\1| [x] |/' docs/PROPS-MAP.md
    # ...similar for 42, 43; OR edit by hand. Confirm with grep.
    ```
  </how-to-verify>
  <resume-signal>Type "seeded" when `vercel env ls production | grep -c '^SUPABASE_'` returns 3 AND a fresh production deployment is green AND rows 41-43 in `docs/PROPS-MAP.md` are ticked `[x]`. If a step fails, type "blocked: <reason>" with the relevant CLI output.</resume-signal>
  <acceptance_criteria>
    - `vercel env ls production | grep -c '^SUPABASE_'` returns 3
    - All 3 env vars (`SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`) appear in the Vercel dashboard marked as Encrypted/Sensitive for the Production environment
    - A production deployment has been triggered (and ideally completed green) post-seeding
    - `docs/PROPS-MAP.md` rows 41, 42, 43 show `[x]` in the Seeded? column (operator confirms via `grep '^| 4[123] |' docs/PROPS-MAP.md | grep -c '\\[x\\]'` returns 3)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Operator's local shell → Vercel | `vercel env add` HTTPS — protected by operator's `vercel login` token. Seeded values stored encrypted in Vercel's KMS. |
| `.env` (local) → Vercel production | Manual operator copy via CLI; values never traverse git. |
| `docs/PROPS-MAP.md` → public git repo | Committed; abbreviated previews (first 8 chars + `...`) keep full secrets out. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05.5-02-S1 | Information Disclosure | `.env` accidentally committed | mitigate | `.env` already in `.gitignore` (commit `5521978`); Task 2 verify includes `git ls-files --error-unmatch .env` check that exits non-zero (file gitignored). HIGH risk if violated — service_role bypasses RLS. |
| T-05.5-02-02 | Information Disclosure | Full token in `docs/PROPS-MAP.md` | mitigate | Task 1 acceptance criteria includes `grep -E 'shpat_[a-zA-Z0-9]{15,}'` returns empty (rejects full Shopify tokens) AND `grep -E 'EAAVHQVc[a-zA-Z0-9]{15,}'` returns empty (rejects full Meta tokens). Truncate to first 8 chars + `...` per Pattern 8. MEDIUM risk (file is public). |
| T-05.5-02-03 | Information Disclosure | Echo-piped values leak to shell history | mitigate | Task 3 default uses interactive `vercel env add` prompt (Method A), NOT `echo | vercel env add` (Method B). Pitfall 5 acknowledged. Single-user URL-obscurity (local shell history is operator-private). LOW risk for this trust model. |
| T-05.5-02-S3 | Information Disclosure | `NEXT_PUBLIC_SUPABASE_*` aliases exposing key to browser bundle | mitigate | Plan-level discipline: the only env-var names allowed are `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY` (server-only). Per RESEARCH.md Anti-Patterns: "Adding `NEXT_PUBLIC_` prefix to Supabase env vars promotes the value to the browser bundle." Plan 03 enforces this in code review. MEDIUM risk if added. |
| T-05.5-02-T4 | Tampering | Vercel env var overwritten silently by `--force` | accept | `--force` is the documented happy-path flag (Vercel CLI docs). Single-user environment; rollback = re-seed. LOW. |
</threat_model>

<verification>
After this plan completes:
- `docs/PROPS-MAP.md` exists with 43 rows + heading + intro paragraph (committed)
- `.env` Section 5 contains the 3 Supabase keys (gitignored, never committed)
- `vercel env ls production` shows `SUPABASE_URL`, `SUPABASE_ANON_KEY`, `SUPABASE_SERVICE_ROLE_KEY`, each marked Encrypted/Sensitive
- A fresh production deployment is live (so plan 03 can `curl /api/health` against it)
- PROPS-MAP rows 41-43 are ticked `[x]`
</verification>

<success_criteria>
- 43 rows in PROPS-MAP, every row has a Type (SECRET/CONFIG/DATA/n/a)
- 3 new env vars present in Vercel production (verified by CLI list)
- No full tokens in committed files (grep gates pass)
- `.env` still gitignored after Task 2
</success_criteria>

<output>
After completion, create `.planning/phases/05.5-supabase-foundation/05.5-02-SUMMARY.md` recording:
- Count of seeded env vars in Vercel production (should be 3)
- Confirmation that `.env` remains gitignored after Section 5 append
- Which PROPS-MAP rows are still `[ ]` Unseeded (the 37 SECRET/CONFIG rows from §1, §2.1-§2.3, §3, §4.1-§4.3 — those get ticked progressively in plans 05.6 + 05.7, NOT in 05.5)
- The production deployment URL + commit SHA that received the new env vars (for plan 03's smoke-test curl)
</output>
</content>
</invoke>