---
phase: 05.2.1.1-algorithm-correctness-fixes-codex-via-gsd-cross-ai
plan: 02
subsystem: algorithms
tags: [shopify, apps-script, react, attribution, outlier, mad, lockservice, productmap, bayesian]
requires:
  - phase: 05.2.1.1-01
    provides: P0/P1 critical fixes (TZ, organic predicate, LockService, symmetric Meta sum, etc.)
provides:
  - 8 P2 secondary correctness fixes (ID text format + migration, consistent date range, robust outlier detection, productMap chip, canonical revenue basis, ad-set docstring honesty, platform-namespaced campaignKey + migration, Bessel-corrected Bayesian CI)
affects: [phase-06-security-and-cloud-sync, phase-07-observability]
tech-stack:
  added: []
  patterns: ["median + MAD robust outlier detection", "platform-namespaced campaignKey (storeId::platform::campaignId)", "localStorage migration on first read", "session-dismissible info chip pattern"]
key-files:
  created: []
  modified:
    - SheetBuilder.gs
    - DailyUpdate.gs
    - dashboard-web/src/lib/attributionAnalysis.ts
    - dashboard-web/src/lib/hooks/useCampaignAttribution.ts
    - dashboard-web/src/components/CampaignDrawer.tsx
    - dashboard-web/src/components/MetaShopifyReconciliation.tsx
    - dashboard-web/src/lib/campaignProductMap.ts
    - dashboard-web/src/components/ProductChannelBreakdown.tsx
    - dashboard-web/src/lib/hooks/useCampaignTrueRevenue.ts
    - dashboard-web/src/lib/dateRange.ts
    - dashboard-web/src/components/CampaignsTable.tsx
    - dashboard-web/src/components/CampaignsTableRow.tsx
key-decisions:
  - "Use orders-attribution lineItems[].revenueCad as the canonical Shopify actual basis for reconciliation; no products-daily fallback."
  - "Migrate old productMap keys by inferring platform from loaded campaign rows, merging with existing new keys when needed, and retaining unmigratable old keys with a warning."
  - "Apply the Bessel-corrected interval math to campaign, ad-set, and ad analyzers because they share the same displayed interval contract."
patterns-established:
  - "Fixed 7-day trailing median + MAD outlier baseline with MAD=0 fallback."
  - "Platform-namespaced campaign keys for product mapping and true-revenue allocation."
  - "Session-dismissible current-state productMap disclosure chip shared across mapping-dependent panels."
requirements-completed: [FIX-08, FIX-09, FIX-10, FIX-11, FIX-12, FIX-13, FIX-14, FIX-15]
duration: 32min
completed: 2026-05-19
---

# Phase 05.2.1.1 Plan 02: Secondary Correctness Fixes

**Shipped the eight P2 secondary correctness fixes across Apps Script ID handling, drawer date semantics, robust stats, product mapping disclosure, reconciliation revenue basis, platform-namespaced mapping keys, and CI uncertainty math.**

## Performance
- **Duration:** 32min
- **Tasks:** 8
- **Files modified:** 15

## Accomplishments
- **FIX-08:** Formatted campaign/ad ID columns as text before writes and added `migrateIdColumnsToText()` for existing Sheets rows.
- **FIX-09:** Aligned ad-set attribution, campaign attribution, reconciliation, and nested ad drawer panels to the user-selected `rangeFrom`/`rangeTo`; reconciliation now enumerates the full selected date range.
- **FIX-10:** Replaced mean/stddev outlier detection with trailing 7-day median + MAD and updated tests for the new MAD=0 fallback.
- **FIX-11:** Added a session-dismissible productMap freshness chip to both mapping-dependent panels.
- **FIX-12:** Switched Shopify actual reconciliation to `orders-attribution` line-item revenue so Shopify and Organic use the same revenue basis.
- **FIX-13:** Corrected `analyzeAttributionForAdSet` documentation to state the real `utm_term === adSetId` behavior with no campaign fallback.
- **FIX-14:** Changed campaign product-map keys to `storeId::platform::campaignId`, updated all consumers, and added localStorage migration.
- **FIX-15:** Changed ROAS interval variance to sample variance (`N - 1`) with an honest CI docstring and updated assertions.

## Task Commits
1. **FIX-08 ID columns formatted as text + migration helper** — `a14be04` (fix)
2. **FIX-09 consistent user-selected date range across drawer panels** — `f0ea792` (fix)
3. **FIX-10 detectOutlierDays median+MAD** — `abedf45` (fix)
4. **FIX-11 productMap freshness disclosure chip** — `7cc1a45` (fix)
5. **FIX-12 canonical revenue basis for reconciliation** — `0d626e9` (fix)
6. **FIX-13 analyzeAttributionForAdSet docstring honesty** — `d063b42` (fix)
7. **FIX-14 campaignKey platform namespace + migration** — `9a22418` (fix)
8. **FIX-15 Bayesian CI Bessel correction + docstring** — `a1026e9` (fix)

## Files Created/Modified
- `SheetBuilder.gs`
- `DailyUpdate.gs`
- `dashboard-web/src/lib/attributionAnalysis.ts`
- `dashboard-web/src/lib/hooks/useCampaignAttribution.ts`
- `dashboard-web/src/components/CampaignDrawer.tsx`
- `dashboard-web/src/components/MetaShopifyReconciliation.tsx`
- `dashboard-web/src/lib/campaignProductMap.ts`
- `dashboard-web/src/components/ProductChannelBreakdown.tsx`
- `dashboard-web/src/lib/hooks/useCampaignTrueRevenue.ts`
- `dashboard-web/src/lib/dateRange.ts`
- `dashboard-web/src/components/CampaignsTable.tsx`
- `dashboard-web/src/components/CampaignsTableRow.tsx`
- `dashboard-web/src/lib/__tests__/analyzeAttribution.test.ts`
- `dashboard-web/src/lib/__tests__/buildReconciliation.test.ts`
- `dashboard-web/src/lib/__tests__/detectOutlierDays.test.ts`

## Decisions Made
- Chose no `products-daily` fallback for reconciliation Shopify actuals, to keep one canonical line-item revenue basis.
- Kept productMap migration idempotent and non-destructive: ambiguous legacy keys remain in localStorage and log a warning.
- Updated ad/ad-set CI math alongside campaign CI math so the shared interval display stays internally consistent.

## Deviations from Plan
None - plan executed exactly as written, with necessary call-site and test updates.

## Issues Encountered
- `npm run build` passed but emitted existing warnings about multiple lockfiles, Sentry global-error setup, and missing Google service-account env vars during static API prerender attempts.

## Flagged for Follow-up
None

## User Setup Required

**Apps Script editor — one-time migration required after next deploy:**
1. Open script.google.com → ROAS project
2. Open `DailyUpdate.gs`
3. Run `migrateIdColumnsToText()` from the function dropdown
4. Verify the log shows `Reformatted N rows across M tabs.`

This is idempotent — re-running is a no-op.

**UI chip behavior:**
A small disclosure chip will appear on `CampaignDrawer` panels noting that the product↔campaign mapping is current-state (not date-versioned). Dismissible per session via X button.

## Verification Status
- [x] `tsc --noEmit` passes
- [x] `npm run build` passes
- [x] `npm test` passes (test count: 89; assertions updated for FIX-10, FIX-12, FIX-14, FIX-15)
- [x] Apps Script syntax clean (`node -c <(cat ...)`)
- [ ] Production smoke checks (manual, post-deploy)

## Next Phase Readiness
All P2 secondary fixes shipped. Wave 3 (Plan 03) can now add regression tests against the corrected algorithms.
