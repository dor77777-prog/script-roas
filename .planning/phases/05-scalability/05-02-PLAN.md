---
phase: 05-scalability
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard-web/src/lib/dateRange.ts
  - dashboard-web/src/lib/sheets.ts
  - dashboard-web/src/lib/campaigns.ts
  - dashboard-web/src/lib/products.ts
  - dashboard-web/src/lib/ordersAttribution.ts
  - dashboard-web/src/app/api/data/route.ts
  - dashboard-web/src/app/api/campaigns/route.ts
  - dashboard-web/src/app/api/products/route.ts
  - dashboard-web/src/app/api/orders-attribution/route.ts
  - dashboard-web/src/components/Dashboard.tsx
  - dashboard-web/src/components/CampaignsTable.tsx
  - dashboard-web/src/components/CampaignDrawer.tsx
  - dashboard-web/src/components/ProductsTable.tsx
autonomous: true
requirements: [P5-API-PAGINATION, P5-SWR-KEYS]

must_haves:
  truths:
    - "GET /api/data?from=2026-01-01&to=2026-04-30 מחזיר שורות data-daily בטווח, ולא יותר"
    - "GET /api/data ללא פרמטרים מחזיר ברירת מחדל של 90 ימים אחרונים"
    - "אותו דבר עבור /api/campaigns, /api/products, /api/orders-attribution"
    - "Dashboard SWR keys כוללים from+to: שינוי range ב-Filters מפעיל fetch חדש (לא cache hit)"
    - "CampaignDrawer ו-CampaignsTable מקבלים את range מ-props ושולחים אותו ב-SWR key"
    - "תאריך שמועבר ב-query string תקף לפורמט YYYY-MM-DD; אחרת 400"
  artifacts:
    - path: "dashboard-web/src/lib/dateRange.ts"
      provides: "parseRangeParams(searchParams) + DEFAULT_RANGE_DAYS + buildDateRangeKey helper"
      contains: "export function parseRangeParams"
    - path: "dashboard-web/src/lib/sheets.ts"
      provides: "fetchDailyData(opts?: {from, to}) — מסונן בצד server"
      contains: "from?: string"
    - path: "dashboard-web/src/lib/campaigns.ts"
      provides: "fetchCampaignsData(opts?: {from, to})"
      contains: "from?: string"
    - path: "dashboard-web/src/lib/products.ts"
      provides: "fetchProductsData(opts?: {from, to})"
      contains: "from?: string"
    - path: "dashboard-web/src/lib/ordersAttribution.ts"
      provides: "fetchOrdersAttribution(opts?: {from, to})"
      contains: "from?: string"
  key_links:
    - from: "Dashboard.tsx useSWR keys"
      to: "/api/data?from=&to="
      via: "buildDateRangeKey(filters.range)"
      pattern: "/api/data\\?from="
    - from: "CampaignsTable.tsx useSWR keys"
      to: "/api/campaigns?from=&to= + /api/products?from=&to= + /api/orders-attribution?from=&to="
      via: "props.range מועבר ל-buildDateRangeKey"
      pattern: "/api/campaigns\\?from="
    - from: "CampaignDrawer.tsx useSWR keys"
      to: "/api/products?from=&to= + /api/orders-attribution?from=&to="
      via: "props.range"
      pattern: "/api/(products|orders-attribution)\\?from="
    - from: "ProductsTable.tsx useSWR key"
      to: "/api/products?from=&to="
      via: "buildDateRangeKey(localRange) — localRange משתנה ע"י המשתמש"
      pattern: "/api/products\\?from="
---

<objective>
להעביר את כל 4 ה-routes ל-pagination מבוססת `?from=&to=` עם ברירת מחדל של 90 ימים, ולוודא ש-SWR cache keys כוללים את הטווח כך שהחלפת range מפעילה fetch חדש (לא cache hit שמראה נתונים מסוננים בקליינט).

Purpose: מסיר את הצוואר בקבוק המרכזי שמתועד ב-CONCERNS.md — היום ה-payload גדל לינארית עם ההיסטוריה, ובתוך שנה צפויים 5MB+ payloads. סינון בצד שרת חוסך גם רוחב פס וגם CPU של פירסור בקליינט.

Output: helper משותף `parseRangeParams` + `buildDateRangeKey`, 4 lib functions שמקבלות `{from, to}`, 4 routes שמפרסרים `searchParams`, ו-4 components שמעבירים את ה-range דרך SWR key.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@dashboard-web/src/lib/sheets.ts
@dashboard-web/src/lib/campaigns.ts
@dashboard-web/src/lib/products.ts
@dashboard-web/src/lib/ordersAttribution.ts
@dashboard-web/src/app/api/data/route.ts
@dashboard-web/src/app/api/campaigns/route.ts
@dashboard-web/src/app/api/products/route.ts
@dashboard-web/src/app/api/orders-attribution/route.ts
@dashboard-web/src/components/Dashboard.tsx
@dashboard-web/src/components/CampaignsTable.tsx
@dashboard-web/src/components/CampaignDrawer.tsx
@dashboard-web/src/components/ProductsTable.tsx

<interfaces>
<!-- חוזים קיימים — לעטוף אותם, לא לכתוב מחדש -->

From dashboard-web/src/lib/types.ts (assumed pattern):
```typescript
export type DateRange = { from: string; to: string };  // 'YYYY-MM-DD'
```

Existing exported functions (זהירות — חתימה משתנה):
```typescript
// dashboard-web/src/lib/sheets.ts
export async function fetchDailyData(): Promise<DailyRow[]>;       // → fetchDailyData(opts?: RangeOpts)
// dashboard-web/src/lib/campaigns.ts
export async function fetchCampaignsData(): Promise<CampaignRow[]>; // → ↑
// dashboard-web/src/lib/products.ts
export async function fetchProductsData(): Promise<ProductRow[]>;   // → ↑
// dashboard-web/src/lib/ordersAttribution.ts
export async function fetchOrdersAttribution(): Promise<OrderAttributionRow[]>; // → ↑
```

Existing components props (ל-Dashboard.tsx state):
```typescript
// Dashboard.tsx — already has:
//   const [filters, setFilters] = useState<F>({...range: {from, to}});
// CampaignsTable, CampaignDrawer, ProductsTable receive `range: DateRange` via props
```

SWR keys today (פני שינוי):
- Dashboard.tsx: `'/api/data'`
- CampaignsTable.tsx: `'/api/campaigns'`, `'/api/products'`, `'/api/orders-attribution'`
- CampaignDrawer.tsx: `open ? '/api/products' : null`, `open ? '/api/orders-attribution' : null`
- ProductsTable.tsx: `'/api/products'`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: helper משותף ל-parseRangeParams + buildDateRangeKey + lib filtering</name>
  <files>dashboard-web/src/lib/dateRange.ts, dashboard-web/src/lib/sheets.ts, dashboard-web/src/lib/campaigns.ts, dashboard-web/src/lib/products.ts, dashboard-web/src/lib/ordersAttribution.ts</files>
  <behavior>
    - parseRangeParams(searchParams) מחזיר `{from, to}` תקפים (YYYY-MM-DD) או זורק שגיאת validation שמופיעה כ-400
    - אם searchParams ריק → ברירת מחדל: היום ועד 90 ימים אחורה
    - DEFAULT_RANGE_DAYS = 90
    - buildDateRangeKey({from, to}, basePath) מחזיר `${basePath}?from=${from}&to=${to}` — שמיש כ-SWR key
    - 4 fetch functions ב-lib מקבלים `{from, to}` אופציונלי. כשהפרמטרים סופקו, הפילטר מתבצע **לאחר** קריאת ה-Sheets (לא נחיל range read מתוחכם — שורות מסוננות בצד הענן ב-JavaScript). הסיבה: ה-tabs אינם בהכרח sorted חזק לפי תאריך (idempotent writes יכולים להשאיר שורות בכל מיקום), אז סינון client-side על מערך גולמי שהושלף הוא הדרך הוודאית להחזיר תוצאה נכונה. תועלת ה-pagination נשמרת ב-payload החוזר ל-browser (Vercel cache + network), שזו המטרה העיקרית.
    - אם `from > to`, זורקים `Error('Invalid range: from must be <= to')` עם status 400.
  </behavior>
  <action>
    1. **צור קובץ חדש** `dashboard-web/src/lib/dateRange.ts`:

    ```typescript
    /**
     * Shared date-range parsing for paginated API routes (Phase 5).
     *
     * All four telemetry routes (/api/data, /api/campaigns, /api/products,
     * /api/orders-attribution) accept ?from=YYYY-MM-DD&to=YYYY-MM-DD. When
     * absent, we default to the LAST 90 DAYS (DEFAULT_RANGE_DAYS) — covers
     * the 95% case where the dashboard renders its default range without
     * forcing the full multi-year history through the payload.
     *
     * Server-side filtering is performed in-memory by each lib's `fetch*`
     * function. We do NOT attempt to push the range filter down to a
     * Sheets API range-read because the underlying tabs are NOT strictly
     * sorted by date (idempotent writes leave rows in arbitrary positions).
     * The benefit of pagination is still realized at the HTTP layer —
     * the server pays the Sheets read once and returns only the slice the
     * client asked for.
     */
    export type DateRange = { from: string; to: string };

    export const DEFAULT_RANGE_DAYS = 90;

    const ISO_DATE = /^\d{4}-\d{2}-\d{2}$/;

    /**
     * Parses ?from=&to= out of a Next.js Request URL. Validation:
     *   - Both keys absent  → returns the default last-90-days range
     *   - Exactly one key absent  → 400-able error (caller throws)
     *   - Either key malformed (not YYYY-MM-DD) → 400-able error
     *   - from > to → 400-able error
     */
    export function parseRangeParams(searchParams: URLSearchParams): DateRange {
      const from = searchParams.get('from');
      const to = searchParams.get('to');

      if (!from && !to) return defaultRange();

      if (!from || !to) {
        throw new RangeParamError('Both ?from and ?to are required when either is provided.');
      }
      if (!ISO_DATE.test(from) || !ISO_DATE.test(to)) {
        throw new RangeParamError('from/to must be in YYYY-MM-DD format.');
      }
      if (from > to) {
        throw new RangeParamError('Invalid range: from must be <= to.');
      }
      return { from, to };
    }

    /**
     * Custom error so route handlers can map to HTTP 400 without catching
     * all errors (which include Sheets API errors that should be 500).
     */
    export class RangeParamError extends Error {
      constructor(message: string) {
        super(message);
        this.name = 'RangeParamError';
      }
    }

    /**
     * Returns the default last-90-days range, anchored on "today" in UTC
     * so server and client agree on the boundary (no timezone surprises).
     */
    export function defaultRange(): DateRange {
      const today = new Date();
      const to = today.toISOString().slice(0, 10);
      const fromDate = new Date(today.getTime() - DEFAULT_RANGE_DAYS * 86400 * 1000);
      const from = fromDate.toISOString().slice(0, 10);
      return { from, to };
    }

    /**
     * Builds the SWR cache key for a paginated route. SWR keys are strings,
     * so changing `range` (e.g., user picks a wider range in Filters) yields
     * a NEW key → SWR fires a fresh fetch (no stale-cache shadow).
     *
     * Used by Dashboard.tsx, CampaignsTable.tsx, CampaignDrawer.tsx,
     * ProductsTable.tsx. The shape must match how the route parses params.
     */
    export function buildDateRangeKey(basePath: string, range: DateRange | null | undefined): string | null {
      if (!range || !range.from || !range.to) return null;
      return `${basePath}?from=${range.from}&to=${range.to}`;
    }

    /** Helper for lib-side filtering. Returns true if `date` ∈ [from, to]. */
    export function isInRange(date: string, range: DateRange): boolean {
      return date >= range.from && date <= range.to;
    }
    ```

    2. **עדכן** `dashboard-web/src/lib/sheets.ts` — `fetchDailyData`:

    החלף את חתימת הפונקציה ואת ה-loop כך שהפילטר על range יחול לפני ה-push ל-rows. שמירה על כל הלוגיקה הקיימת (parseDate, parseNumber, COGS computation וכו'). דוגמה:

    ```typescript
    import { type DateRange, isInRange } from './dateRange';

    export async function fetchDailyData(opts?: { range?: DateRange }): Promise<DailyRow[]> {
      // ... כל ה-auth/sheets boilerplate הקיים נשאר ...
      const values = res.data.values ?? [];
      const rows: DailyRow[] = [];

      for (const row of values) {
        const dateStr = parseDate(row[0]);
        if (!dateStr) continue;
        if (opts?.range && !isInRange(dateStr, opts.range)) continue;  // ← השורה החדשה
        const storeId = String(row[1] ?? '').trim();
        // ... שאר הלוגיקה הקיימת ללא שינוי ...
      }
      return rows;
    }
    ```

    בדוק שאתה לא משנה את `fetchStoreMeta`, `fetchDashboardState`, `upsertDashboardStateKey`, או `ALLOWED_STATE_KEYS` — הם נשארים כפי שהם.

    3. **עדכן** `dashboard-web/src/lib/campaigns.ts` — `fetchCampaignsData`:

    בצורה זהה: הוסף `opts?: { range?: DateRange }`, ייבוא `DateRange/isInRange`, וסנן בלולאה הראשית מיד אחרי `parseDate(row[0])`.

    4. **עדכן** `dashboard-web/src/lib/products.ts` — `fetchProductsData`: זהה.

    5. **עדכן** `dashboard-web/src/lib/ordersAttribution.ts` — `fetchOrdersAttribution`: זהה. שים לב שהפונקציה הזו עושה batchGet על 3 tabs ב-loop פנימי — הוסף את ה-`if (opts?.range && !isInRange(date, opts.range)) continue;` בתוך ה-`for (const row of values)` הפנימי, מיד אחרי `parseDate(row[0])`.

    אל תשנה את שיטת ה-sheets read (טווח A:N נשאר כמו שהוא — הפילטר ב-JavaScript). זה החלטה מודעת כי הטאבים לא ממוינים חזק לפי תאריך.
  </action>
  <verify>
    <automated>cd dashboard-web && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -v "^$" | head -20</automated>
  </verify>
  <done>
    - קובץ `dashboard-web/src/lib/dateRange.ts` קיים עם 5 exports: `DateRange`, `DEFAULT_RANGE_DAYS`, `parseRangeParams`, `RangeParamError`, `defaultRange`, `buildDateRangeKey`, `isInRange`
    - 4 lib functions מקבלים `opts?: { range?: DateRange }` ומסננים בצד שרת
    - `npx tsc --noEmit` עובר ללא שגיאות
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 4 routes פירסור searchParams + העברה ל-lib functions</name>
  <files>dashboard-web/src/app/api/data/route.ts, dashboard-web/src/app/api/campaigns/route.ts, dashboard-web/src/app/api/products/route.ts, dashboard-web/src/app/api/orders-attribution/route.ts</files>
  <behavior>
    - כל route משנה את חתימת GET ל-`GET(req: Request)` ומפרסר `new URL(req.url).searchParams`
    - אם RangeParamError נזרק → status 400 עם הודעת השגיאה ב-body (לא נחשף ל-CDN cache — `Cache-Control: no-store` על 400 בלבד)
    - אם הצליח, עוברים את ה-range ל-lib function
    - ה-headers הקיימים (`Cache-Control`, `revalidate`, `dynamic`) לא משתנים — ה-CDN ימטמן לפי URL ב-query string, אז כל range מקבל cache entry משלו
    - `/api/orders-attribution` שומר על ה-degraded path הקיים (`status: 200` + `error` ב-body) — תשובת 400 רק אם RangeParamError; שגיאות sheets נשארות 200 עם empty rows
  </behavior>
  <action>
    1. **עדכן** `dashboard-web/src/app/api/data/route.ts`:

    ```typescript
    import { NextResponse } from 'next/server';
    import { fetchDailyData } from '@/lib/sheets';
    import { parseRangeParams, RangeParamError } from '@/lib/dateRange';
    import type { DashboardData } from '@/lib/types';

    export const revalidate = 60;
    export const dynamic = 'force-dynamic';

    async function fetchTodayFx(): Promise<number | null> {
      try {
        const r = await fetch('https://api.frankfurter.dev/v1/latest?base=ILS&symbols=CAD', {
          next: { revalidate: 3600 },
        });
        if (!r.ok) return null;
        const j = (await r.json()) as { rates?: { CAD?: number } };
        return j.rates?.CAD ?? null;
      } catch {
        return null;
      }
    }

    export async function GET(req: Request) {
      let range;
      try {
        range = parseRangeParams(new URL(req.url).searchParams);
      } catch (e) {
        if (e instanceof RangeParamError) {
          return NextResponse.json({ error: e.message }, {
            status: 400,
            headers: { 'Cache-Control': 'no-store' },
          });
        }
        throw e;
      }
      try {
        const [rows, fxIlsToCad] = await Promise.all([
          fetchDailyData({ range }),
          fetchTodayFx(),
        ]);
        const stores = Array.from(new Set(rows.map(r => r.storeName))).sort();
        const data: DashboardData = {
          rows,
          stores,
          lastUpdated: new Date().toISOString(),
          fxIlsToCad,
        };
        return NextResponse.json(data, {
          headers: { 'Cache-Control': 'public, s-maxage=60, stale-while-revalidate=120' },
        });
      } catch (err) {
        const message = err instanceof Error ? err.message : String(err);
        console.error('Sheets fetch failed:', message);
        return NextResponse.json({ error: message }, { status: 500 });
      }
    }
    ```

    2. **עדכן** `dashboard-web/src/app/api/campaigns/route.ts` — אותו pattern: `GET(req)` → `parseRangeParams` → `fetchCampaignsData({ range })` → אותו response body.

    3. **עדכן** `dashboard-web/src/app/api/products/route.ts` — אותו pattern עם `fetchProductsData({ range })`.

    4. **עדכן** `dashboard-web/src/app/api/orders-attribution/route.ts` — אותו pattern עם `fetchOrdersAttribution({ range })`, **אבל**: אל תשנה את ה-degraded fallback (catch של sheets errors מחזיר `{ rows: [], lastUpdated, error }` עם status 200). רק RangeParamError חוזר עם 400.

    שים לב: בכל ה-routes, מאחר ש-`dynamic = 'force-dynamic'` (וב-orders-attribution אין `dynamic` אז זו ברירת מחדל של static), URLs שונים (כל range = URL שונה) ייצרו cache entries שונים בצד CDN של Vercel באופן אוטומטי. זו ההתנהגות הרצויה.
  </action>
  <verify>
    <automated>cd dashboard-web && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -v "^$" | head -20 && npm run build --silent 2>&1 | tail -20</automated>
  </verify>
  <done>
    - 4 routes מפרסרים `?from=&to=` ומחזירים 400 ל-RangeParamError
    - 4 routes עוברים `range` ל-lib functions
    - `npm run build` עובר
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 4 components — SWR keys כוללים from+to</name>
  <files>dashboard-web/src/components/Dashboard.tsx, dashboard-web/src/components/CampaignsTable.tsx, dashboard-web/src/components/CampaignDrawer.tsx, dashboard-web/src/components/ProductsTable.tsx</files>
  <behavior>
    - Dashboard.tsx: SWR key `/api/data` הופך ל-`buildDateRangeKey('/api/data', filters.range)`. כאשר המשתמש משנה את ה-Filters, SWR מבחין במפתח חדש ויוצר fetch חדש.
    - CampaignsTable.tsx: 3 ה-SWR keys (`/api/campaigns`, `/api/products`, `/api/orders-attribution`) הופכים לכלולי range. ה-`range` כבר זמין כ-prop.
    - CampaignDrawer.tsx: 2 ה-SWR keys (`/api/products`, `/api/orders-attribution`) מותנים ב-`open && range` ועוברים דרך `buildDateRangeKey`. ה-`range` כבר זמין כ-prop.
    - ProductsTable.tsx: ה-SWR key הופך ל-`buildDateRangeKey('/api/products', localRange)` — `localRange` הוא ה-state המקומי שכבר קיים (משתמשים בו לסינון per-section).
    - אין שינוי בכל הלוגיקה הפנימית של הקומפוננטות — רק ב-SWR keys.
  </behavior>
  <action>
    1. **עדכן** `dashboard-web/src/components/Dashboard.tsx`:

    - הוסף import: `import { buildDateRangeKey } from '@/lib/dateRange';`
    - מצא את `useSWR<DashboardData>('/api/data', fetcher, ...)` והחלף את המפתח ב-`buildDateRangeKey('/api/data', filters.range)`.
    - שאר הלוגיקה (initialPreset, syncUrl, filters state management) נשאר ללא שינוי.

    דוגמה:
    ```typescript
    const { data, error, isLoading, mutate, isValidating } = useSWR<DashboardData>(
      buildDateRangeKey('/api/data', filters.range),
      fetcher,
      { /* existing options */ }
    );
    ```

    2. **עדכן** `dashboard-web/src/components/CampaignsTable.tsx`:

    - הוסף import: `import { buildDateRangeKey } from '@/lib/dateRange';`
    - שלוש useSWR keys משתנות:
      - `'/api/campaigns'` → `buildDateRangeKey('/api/campaigns', range)`
      - `'/api/products'` → `buildDateRangeKey('/api/products', range)`
      - `'/api/orders-attribution'` → `buildDateRangeKey('/api/orders-attribution', range)`
    - `range` כבר prop של הקומפוננטה (`{ range, store: globalStore, stores, dailyRows }`).
    - אל תיגע ב-store-meta key (אין pagination שם).

    3. **עדכן** `dashboard-web/src/components/CampaignDrawer.tsx`:

    - הוסף import.
    - שתי useSWR keys משתנות:
      - `open ? '/api/products' : null` → `open ? buildDateRangeKey('/api/products', range) : null`
      - `open ? '/api/orders-attribution' : null` → `open ? buildDateRangeKey('/api/orders-attribution', range) : null`
    - הקומפוננטה מקבלת `range` כ-prop (לפי הבדיקה ב-CONCERNS.md שזה כבר עובר ל-reconciliation panel).
    - אם `range` לא קיים כ-prop — הוסף אותו לחתימת Props ולמעבר מ-CampaignsTable. בדוק את ה-props של CampaignDrawer לפני שעורכים.

    4. **עדכן** `dashboard-web/src/components/ProductsTable.tsx`:

    - הוסף import.
    - `useSWR<ProductsResponse>('/api/products', fetcher, ...)` → `useSWR(buildDateRangeKey('/api/products', localRange), fetcher, ...)`
    - שים לב ש-`localRange` (לא `range`) — זה ה-state ש-ProductsTable מנהל פנימית כדי לאפשר drill-down per-section.

    אל תשנה גם את הלוגיקה הפנימית של הקומפוננטות (filterRows, useMemo וכו') — סינון בקליינט עדיין מתבצע אבל על data set קטן יותר שמגיע מ-API.

    הערה ל-CampaignDrawer: בדוק שה-prop `range` כבר עובר אליו מ-CampaignsTable. אם לא — צריך להוסיף אותו לחתימת ה-Props ולעדכן את הקריאה.
  </action>
  <verify>
    <automated>cd dashboard-web && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -v "^$" | head -20 && grep -c "buildDateRangeKey" src/components/Dashboard.tsx src/components/CampaignsTable.tsx src/components/CampaignDrawer.tsx src/components/ProductsTable.tsx</automated>
  </verify>
  <done>
    - כל אחד מ-4 הקומפוננטים מייבא ומשתמש ב-`buildDateRangeKey`
    - `npx tsc --noEmit` עובר
    - `npm run build` עובר
    - ידנית (אופציונלי — לא חלק מ-verify האוטומטי): ב-dev mode, פתח DevTools Network, החלף range ב-Filters — צריך לראות fetch חדש לכל אחד מ-4 ה-routes עם `?from=...&to=...` בכתובת.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser query string → /api/* GET handlers | input חיצוני — חייב validation |
| /api/* → fetch* lib functions → Google Sheets | פנימי — כבר תחת service-account scope readonly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering (injection) | parseRangeParams | mitigate | regex `^\d{4}-\d{2}-\d{2}$` קשיח. אין הברחת SQL/sheet formula כי הערך לא מוזרק לטקסט חיצוני, רק בשוואת string < / >=. |
| T-05-02-02 | DoS via huge range | fetchDailyData ו-co. | accept | ה-server עדיין קורא את ה-tab במלואו (Sheets read) ואז מסנן. אין אקסקלרציה אם המשתמש שולח range של 10 שנים — payload פשוט יחזור באותו גודל כמו היום (בלי pagination). למעשה pagination רק מקטין את ה-payload, לא מגדיל. |
| T-05-02-03 | Information disclosure | error messages | mitigate | RangeParamError מחזיר Hebrew-safe English message; שגיאות Sheets ממשיכות להיות מטופלות ע"י `userFacingError` הקיים. |
</threat_model>

<verification>
- כל 4 ה-routes נבדקים ב-curl:
  - `curl -i 'http://localhost:3000/api/data'` → 200 (ברירת מחדל 90 ימים)
  - `curl -i 'http://localhost:3000/api/data?from=2026-01-01&to=2026-04-30'` → 200, payload מסונן
  - `curl -i 'http://localhost:3000/api/data?from=2026-05-01&to=2026-01-01'` → 400
  - `curl -i 'http://localhost:3000/api/data?from=bad'` → 400
- בדפדפן: החלפת range ב-Filters → 4 קריאות חדשות ב-Network tab עם `?from=&to=` שונים
</verification>

<success_criteria>
- 4 ה-routes מחזירים 400 ל-input פגום
- ברירת מחדל = 90 ימים אחרונים
- SWR keys כוללים `from+to` — שינוי טווח גורם fetch
- `npm run build` עובר
- `npm run test` (אם קיים) עובר ללא רגרסיה
</success_criteria>

<output>
After completion, create `.planning/phases/05-scalability/05-02-SUMMARY.md`
</output>
