---
phase: 05-scalability
plan: 04
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - DailyUpdate.gs
  - Main.gs
  - dashboard-web/src/lib/sheets.ts
  - dashboard-web/src/app/api/data/route.ts
autonomous: false
requirements: [P5-ARCHIVE-SCRIPT, P5-ARCHIVE-FALLBACK]

user_setup:
  - service: "Google Sheets — archive spreadsheet"
    why: "ארכיון לשורות ישנות מ-data-daily / products-daily / orders-attribution / campaigns / ads"
    env_vars:
      - name: "(N/A — נקבע ב-Script Property באפס Script, לא ב-Vercel)"
        source: ""
    dashboard_config:
      - task: "צור spreadsheet ריק חדש ב-Google Drive בשם 'ROAS Tracker — Archive'"
        location: "https://drive.google.com → New → Google Sheets"
      - task: "שתף את ה-spreadsheet החדש עם ה-service-account (אותו service-account של ה-warm spreadsheet: roas-dashboard-reader@...) ב-Editor (write) — נדרש כדי שה-Apps Script יוכל לכתוב, וה-/api/data של הדשבורד יוכל לקרוא"
        location: "Share → Add the service-account email → Editor"
      - task: "העתק את ה-Spreadsheet ID מהכתובת (`/d/<ID>/edit`) והגדר אותו ב-Apps Script Properties → `archive.spreadsheet.id`"
        location: "Apps Script editor → Project Settings → Script Properties"
      - task: "[Optional] להגדיר גם ב-Vercel environment variable `ARCHIVE_SPREADSHEET_ID` (אם הדשבורד צריך לקרוא ממנו ישירות — ראה PLAN.md)"
        location: "Vercel → Project Settings → Environment Variables"

must_haves:
  truths:
    - "archiveOlderThan(months, opts) קיים ב-DailyUpdate.gs ומקבל dryRun כברירת מחדל"
    - "ב-dryRun mode הפונקציה מדפיסה את כל ה-(tab, rowCount, dateRange) שיועברו אבל לא כותבת ולא מוחקת כלום"
    - "בריצה רגילה (dryRun=false) הפונקציה: (1) קוראת שורות עם date < cutoff מ-5 tabs, (2) מוסיפה אותן ל-tab תואם ב-archive spreadsheet, (3) מוחקת אותן מה-warm spreadsheet"
    - "ה-cutoff מחושב כ-`monthsAgoStr_(months)` — YYYY-MM-DD לפני months חודשים מהיום"
    - "אם `archive.spreadsheet.id` לא מוגדר, הפונקציה זורקת שגיאה ברורה ולא מוחקת כלום"
    - "Dashboard /api/data?from=&to= בודק האם cutoff < 18 חודשים אחורה; אם כן, fetches גם מהארכיון"
    - "ARCHIVE_SPREADSHEET_ID ב-env מאפשר את ה-fallback ב-dashboard. אם לא מוגדר, ה-fallback מושבת בשקט (warm only)"
  artifacts:
    - path: "DailyUpdate.gs"
      provides: "archiveOlderThan + helpers (monthsAgoStr_, archiveTabRows_, ARCHIVE_TAB_SPECS)"
      contains: "function archiveOlderThan"
    - path: "Main.gs"
      provides: "menu items: dry-run + production archive"
      contains: "archiveOlderThan"
    - path: "dashboard-web/src/lib/sheets.ts"
      provides: "fetchDailyData(opts) קורא גם מארכיון כשהטווח דורש"
      contains: "ARCHIVE_SPREADSHEET_ID"
    - path: "dashboard-web/src/app/api/data/route.ts"
      provides: "כבר תומך ב-from/to (PLAN 02) — מקבל את לוגיקת ה-fallback אוטומטית דרך fetchDailyData"
      contains: "(no signature change — only delegates)"
  key_links:
    - from: "archiveOlderThan"
      to: "5 archive tabs ב-archive spreadsheet"
      via: "archiveTabRows_(spec) שעובר על ARCHIVE_TAB_SPECS"
      pattern: "archive.spreadsheet.id"
    - from: "fetchDailyData (warm + archive)"
      to: "/api/data?from=&to="
      via: "אם ה-from ישן מ-18 חודש, איחוד warm + archive"
      pattern: "ARCHIVE_SPREADSHEET_ID"
---

<objective>
ליצור מנגנון retention שמעביר שורות ישנות (מעל גיל קונפיגרבילי, ברירת מחדל 18 חודש) מ-5 ה-tabs הגדולים (`data-daily`, `products-daily`, `{store}-campaigns`, `{store}-ads`, `{store}-orders-attribution`) ל-archive spreadsheet נפרד. הדשבורד יודע לקרוא מהארכיון רק כש-`from` שהמשתמש בחר ישן מ-18 חודשים.

Purpose: מונע מ-warm spreadsheet להגיע ל-10M cell cap ושומר על ה-payloads קלים (יעד הפאזה — ראה CONCERNS.md "Single spreadsheet for all 3 stores", current usage ~6M cells, על קצב לגדול ל-8M).

Output: פונקציה אחת ב-Apps Script עם dry-run מובנה, 2 menu items ל-onOpen, ועדכון `fetchDailyData` לפצל קריאות בין warm ל-archive בעת הצורך.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/codebase/CONCERNS.md
@.planning/phases/05-scalability/05-01-PLAN.md
@.planning/phases/05-scalability/05-01-SUMMARY.md
@.planning/phases/05-scalability/05-02-PLAN.md
@.planning/phases/05-scalability/05-02-SUMMARY.md
@DailyUpdate.gs
@Main.gs
@Config.gs
@dashboard-web/src/lib/sheets.ts
@dashboard-web/src/app/api/data/route.ts

<interfaces>
<!-- חוזים שכבר קיימים / נוצרים ב-Phase 5 -->

From Config.gs:
```javascript
const TZ = 'Asia/Jerusalem';
const DAILY_FLAT_TAB = 'data-daily';
const PRODUCTS_DAILY_TAB = 'products-daily';
const STORES = [{ id: 'uzoshop', ... }, ...];
function campaignTabName_(storeId): string;       // '{storeId}-campaigns'
function adsTabName_(storeId): string;            // '{storeId}-ads'
function ordersAttributionTabName_(storeId): string;  // '{storeId}-orders-attribution'
function getProp(key, defaultValue?): string;
function ensureSpreadsheet(): Spreadsheet;
```

From SheetBuilder.gs (no change here, just consuming):
- 5 ה-tabs באים עם header rows קיימים; ה-archive חייב להכיל את אותו schema.
- Date נמצא תמיד בעמודה A.

From dashboard-web/src/lib/dateRange.ts (PLAN 02):
```typescript
export type DateRange = { from: string; to: string };
export function isInRange(date: string, range: DateRange): boolean;
```

From dashboard-web/src/lib/sheets.ts (PLAN 02 updated):
```typescript
export async function fetchDailyData(opts?: { range?: DateRange }): Promise<DailyRow[]>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: archiveOlderThan + helpers ב-DailyUpdate.gs (Apps Script side)</name>
  <files>DailyUpdate.gs, Main.gs</files>
  <behavior>
    - archiveOlderThan(months, opts?) — opts = `{dryRun: true}` כברירת מחדל. כדי לבצע פעולה הרסנית, הקורא חייב להעביר `{dryRun: false}` במפורש.
    - הפונקציה עוברת על ARCHIVE_TAB_SPECS (5 specs: data-daily, products-daily, ועוד 3 × אחת לכל חנות מתוך {store}-campaigns/{store}-ads/{store}-orders-attribution → סה"כ 11 archive tabs ב-archive spreadsheet)
    - לכל spec: קורא את כל השורות של ה-tab ב-warm, מסנן את אלה עם `date < cutoff`, מוסיף אותן (append) ל-tab ב-archive, ואז מוחק אותן (clearContent + setValues של נשארות) ב-warm.
    - הכל מתוך לוג מפורט: "[<tabName>] would move N rows; dateRange: oldest=X newest=Y".
    - אם `archive.spreadsheet.id` לא מוגדר → throws.
    - אם ה-archive tab לא קיים — נוצר על המקום עם header copy מה-warm tab (קריאה ל-getRange(1,1,1,lastCol).getValues()).
  </behavior>
  <action>
    1. **הוסף** בסוף `DailyUpdate.gs` (אחרי `notifyError_`):

    ```javascript
    // ============================================================================
    // Archive / retention (Phase 5).
    //
    // Moves rows older than `months` from 5 high-growth tabs into a separate
    // ARCHIVE spreadsheet. Reduces the cell-count pressure on the warm
    // spreadsheet (warm is currently ~6M of the 10M cell cap; without
    // retention we'll cross 8M within a year).
    //
    // Safety:
    //   - DRY-RUN by default — opts.dryRun=false must be passed explicitly
    //   - Verbose logging of every (tab, rowCount, dateRange) BEFORE any write
    //   - archive.spreadsheet.id MUST be set in Script Properties — throws
    //     otherwise (no implicit creation, no "phantom archive" risk)
    //   - Append-then-delete order: rows land in archive BEFORE removal from
    //     warm, so a crash mid-run leaves duplicates (recoverable) not gaps
    //     (unrecoverable). Same defensive pattern as ensureSpreadsheet.
    // ============================================================================

    /**
     * Returns YYYY-MM-DD that is `months` months before today (in TZ).
     */
    function monthsAgoStr_(months) {
      const now = new Date();
      const tzStr = Utilities.formatDate(now, TZ, 'yyyy-MM-dd');
      const [y, m, d] = tzStr.split('-').map(Number);
      // JavaScript Date handles negative month offset correctly (e.g. month=-1 → Dec previous year).
      const dt = new Date(y, m - 1 - months, d);
      return Utilities.formatDate(dt, TZ, 'yyyy-MM-dd');
    }

    /**
     * Tab specs the archiver knows how to move. Each entry resolves at runtime
     * to a list of (warmTabName, archiveTabName) — the latter mirrors the
     * former. Per-store tabs expand at runtime via STORES.
     */
    function ARCHIVE_TAB_SPECS_() {
      const specs = [
        { warm: DAILY_FLAT_TAB,      archive: DAILY_FLAT_TAB,      label: 'data-daily' },
        { warm: PRODUCTS_DAILY_TAB,  archive: PRODUCTS_DAILY_TAB,  label: 'products-daily' },
      ];
      for (const store of STORES) {
        specs.push({ warm: campaignTabName_(store.id),           archive: campaignTabName_(store.id),           label: `${store.id}-campaigns` });
        specs.push({ warm: adsTabName_(store.id),                archive: adsTabName_(store.id),                label: `${store.id}-ads` });
        specs.push({ warm: ordersAttributionTabName_(store.id),  archive: ordersAttributionTabName_(store.id),  label: `${store.id}-orders-attribution` });
      }
      return specs;
    }

    /**
     * Public entry. Default is DRY-RUN — logs what *would* move without writing.
     * Pass {dryRun: false} (and ONLY after dry-running first) to actually move.
     *
     * @param {number} months   Rows with date < (today - months) are archived.
     *                          Recommend >= 18 (matches the dashboard fallback).
     * @param {object} [opts]
     * @param {boolean} [opts.dryRun=true]  If true, logs intended moves only.
     */
    function archiveOlderThan(months, opts) {
      const dryRun = !opts || opts.dryRun !== false;
      const archiveId = getProp('archive.spreadsheet.id');
      if (!archiveId) {
        throw new Error(
          'Set Script Property "archive.spreadsheet.id" to the ID of an empty ' +
          'Google Sheets spreadsheet shared with the service-account. See ' +
          '05-04-PLAN.md user_setup section for the full procedure.'
        );
      }
      if (!Number.isFinite(months) || months <= 0) {
        throw new Error(`archiveOlderThan: invalid months=${months}, expected positive number`);
      }

      const warm = ensureSpreadsheet();
      const archive = SpreadsheetApp.openById(archiveId);
      const cutoff = monthsAgoStr_(months);
      Logger.log(`========== archive ${dryRun ? '[DRY-RUN]' : '[PRODUCTION]'} ==========`);
      Logger.log(`Cutoff: rows with date < ${cutoff} will be moved`);
      Logger.log(`Source: ${warm.getUrl()}`);
      Logger.log(`Target: ${archive.getUrl()}`);

      const specs = ARCHIVE_TAB_SPECS_();
      const summary = [];
      for (const spec of specs) {
        try {
          const moved = archiveTabRows_(warm, archive, spec, cutoff, dryRun);
          summary.push(`[${spec.label}] ${moved.count} rows (${moved.oldest || '—'} → ${moved.newest || '—'})`);
        } catch (e) {
          const msg = e && e.message ? e.message : String(e);
          Logger.log(`[${spec.label}] ERROR: ${msg}`);
          summary.push(`[${spec.label}] ERROR: ${msg}`);
        }
      }
      Logger.log(`========== summary (${dryRun ? 'DRY-RUN' : 'PRODUCTION'}) ==========`);
      for (const line of summary) Logger.log(line);
      Logger.log(`Done. To execute for real after dry-run, call archiveOlderThan(${months}, {dryRun: false}).`);
    }

    /**
     * Moves rows with col-A date < cutoff from a warm tab to its archive
     * counterpart. Returns {count, oldest, newest} so the orchestrator can
     * print a summary. When dryRun=true, performs no writes and no deletes.
     */
    function archiveTabRows_(warm, archive, spec, cutoff, dryRun) {
      const warmSheet = warm.getSheetByName(spec.warm);
      if (!warmSheet) {
        Logger.log(`[${spec.label}] warm tab missing — skipping`);
        return { count: 0, oldest: null, newest: null };
      }
      const lastRow = warmSheet.getLastRow();
      const lastCol = warmSheet.getLastColumn();
      if (lastRow <= 1 || lastCol === 0) {
        Logger.log(`[${spec.label}] empty — skipping`);
        return { count: 0, oldest: null, newest: null };
      }

      const header = warmSheet.getRange(1, 1, 1, lastCol).getValues();
      const allRows = warmSheet.getRange(2, 1, lastRow - 1, lastCol).getValues();

      const toMove = [];
      const toKeep = [];
      let oldest = null, newest = null;
      for (const r of allRows) {
        const v = r[0];
        let key = null;
        if (v instanceof Date && !isNaN(v.getTime())) {
          key = Utilities.formatDate(v, TZ, 'yyyy-MM-dd');
        } else if (typeof v === 'string' && /^\d{4}-\d{2}-\d{2}/.test(v)) {
          key = v.slice(0, 10);
        }
        // Preserve rows with unparseable dates — mirror the pattern used in
        // writeOrdersAttributionForDay etc. (WR5-02). Better to keep noise
        // than silently destroy a mid-format row.
        if (key && key < cutoff) {
          toMove.push(r);
          if (!oldest || key < oldest) oldest = key;
          if (!newest || key > newest) newest = key;
        } else {
          toKeep.push(r);
        }
      }

      Logger.log(`[${spec.label}] would move ${toMove.length} rows (oldest=${oldest || '—'} newest=${newest || '—'}); ${toKeep.length} stay`);

      if (dryRun || toMove.length === 0) {
        return { count: toMove.length, oldest, newest };
      }

      // 1) Ensure archive tab exists with same header.
      let archSheet = archive.getSheetByName(spec.archive);
      if (!archSheet) {
        archSheet = archive.insertSheet(spec.archive);
        archSheet.getRange(1, 1, 1, lastCol).setValues(header);
        archSheet.setFrozenRows(1);
      }
      // 2) Append moved rows to archive FIRST. Crash here = nothing lost.
      archSheet.getRange(archSheet.getLastRow() + 1, 1, toMove.length, lastCol).setValues(toMove);
      // Preserve date format on col A so archive reads parse correctly.
      archSheet.getRange(2, 1, archSheet.getLastRow() - 1, 1).setNumberFormat('yyyy-mm-dd');
      Logger.log(`[${spec.label}] appended ${toMove.length} rows to archive`);

      // 3) Rewrite warm with only the kept rows.
      warmSheet.getRange(2, 1, lastRow - 1, lastCol).clearContent();
      if (toKeep.length > 0) {
        warmSheet.getRange(2, 1, toKeep.length, lastCol).setValues(toKeep);
        warmSheet.getRange(2, 1, toKeep.length, 1).setNumberFormat('yyyy-mm-dd');
      }
      Logger.log(`[${spec.label}] warm now has ${toKeep.length} data rows (was ${allRows.length})`);

      return { count: toMove.length, oldest, newest };
    }

    /** Menu helpers — dry-run + production for the standard 18-month retention. */
    function archive18MonthsDryRun()    { archiveOlderThan(18, {dryRun: true});  }
    function archive18MonthsProduction(){ archiveOlderThan(18, {dryRun: false}); }
    ```

    2. **עדכן** את ה-`onOpen` ב-`Main.gs` להוסיף 2 פריטים בתפריט (לפני `addSeparator` הסופי):

    ```javascript
    .addItem('ארכוב יבש: dry-run 18 חודש (Phase 5)', 'archive18MonthsDryRun')
    .addItem('ארכוב production 18 חודש (אחרי dry-run!)', 'archive18MonthsProduction')
    .addSeparator()
    ```

    הוסף את שני הפריטים האלה בתוך הבלוק של `SpreadsheetApp.getUi().createMenu('ROAS')`, אחרי הפריט `'הצג טאבים עזריים (debug)'` — בקבוצה לוגית של פעולות אדמין.

    אל תיגע בשאר הפריטים בתפריט.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('DailyUpdate.gs','utf8');['archiveOlderThan','archiveTabRows_','ARCHIVE_TAB_SPECS_','monthsAgoStr_','archive18MonthsDryRun','archive18MonthsProduction'].forEach(fn=>{if(!new RegExp('function '+fn+'\\\\b').test(s))throw new Error('missing '+fn)});if(!/archive\\.spreadsheet\\.id/.test(s))throw new Error('Script Property name missing');if(!/dryRun/.test(s))throw new Error('dryRun guard missing');console.log('OK')" && node -e "const fs=require('fs');const s=fs.readFileSync('Main.gs','utf8');if(!/archive18MonthsDryRun/.test(s))throw new Error('menu missing dry-run');if(!/archive18MonthsProduction/.test(s))throw new Error('menu missing prod');console.log('OK')"</automated>
  </verify>
  <done>
    - 6 פונקציות חדשות קיימות ב-DailyUpdate.gs (archiveOlderThan + 5 helpers)
    - 2 פריטי תפריט קיימים ב-Main.gs.onOpen
    - dryRun ברירת מחדל
    - הפונקציה זורקת שגיאה אם `archive.spreadsheet.id` לא מוגדר
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    archiveOlderThan + dry-run + production helpers. **לא הורץ עדיין** — דורש הגדרה ידנית של archive spreadsheet ב-Google Drive + הגדרת `archive.spreadsheet.id` ב-Script Properties.
  </what-built>
  <how-to-verify>
    1. **צור archive spreadsheet ב-Google Drive**: New → Google Sheets → תקרא לזה "ROAS Tracker — Archive". העתק את ה-Spreadsheet ID מה-URL.

    2. **שתף** את ה-spreadsheet עם ה-service-account (`roas-dashboard-reader@roas-tracker-ga.iam.gserviceaccount.com`) ב-Editor — נדרש כדי שגם Apps Script וגם Vercel יוכלו לקרוא/לכתוב.

    3. **הגדר ב-Apps Script**: Project Settings → Script Properties → Add property:
       - Property: `archive.spreadsheet.id`
       - Value: ה-ID שהעתקת.

    4. **upload** את הגרסה החדשה של `DailyUpdate.gs` ו-`Main.gs` (Phase 5 PLAN 04 changes).

    5. **dry-run**: הרץ `archive18MonthsDryRun` מהעורך. בדוק ב-Logs:
       - "[DRY-RUN]" prefix מופיע
       - summary של 11 ה-tabs מציג כמה שורות *היו* עוברות לכל אחת (oldest/newest dates)
       - אין שינוי בפועל באף spreadsheet (warm או archive)

    6. **בדוק את ה-output** של ה-dry-run. שאל את עצמך:
       - האם המספרים סבירים? (לדוגמה, data-daily אמורה להראות שורות מ-2024-לתחילת 2025 — לפני 18 חודש מ-היום)
       - האם יש tabs שעוד לא קיימים בפועל? (אלה ידלגו בשקט עם "warm tab missing — skipping")
       - האם יש מצבי edge חשודים? (oldest=null בעוד שיש newest, או להפך — סימן ל-bug)

    7. **רק אם** ה-dry-run נראה בריא, הרץ `archive18MonthsProduction`. בדוק ב-Logs:
       - "[PRODUCTION]" prefix
       - לכל tab: "appended N rows to archive" + "warm now has M data rows (was M+N)"

    8. **ודא ב-warm**: data-daily אמור עכשיו להציג רק את 18 החודשים האחרונים (פתח את ה-tab, גלול לתאריך הראשון).

    9. **ודא ב-archive**: ה-archive spreadsheet אמור להכיל data-daily + products-daily + 9 ה-store-specific tabs, עם header rows + השורות הישנות.

    **אם משהו השתבש**: אל תרוץ שוב; השורות הישנות עדיין נמצאות גם בארכיון (נשמרו לפני המחיקה). שחזור: copy/paste מה-archive חזרה ל-warm דרך ה-UI של Google Sheets.

    הערה לגבי טיימינג: עבור 6M cells סה"כ ה-archive run יכול לקחת 2-4 דקות (הרבה setValues calls). אם נתקע ב-timeout (6 דקות), אפשר להפעיל `archiveOlderThan(18, {dryRun: false})` שוב — הוא יקדם כי כל מה שכבר עבר נשאר ב-archive (idempotent על תאריכים שכבר מחוץ ל-warm).
  </how-to-verify>
  <resume-signal>"approved — archive moved {N} rows successfully" או תיאור הבעיה</resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: dashboard fallback — fetchDailyData קורא מה-archive כשהטווח דורש</name>
  <files>dashboard-web/src/lib/sheets.ts</files>
  <behavior>
    - `fetchDailyData(opts)` בודק האם `opts.range.from` ישן מ-18 חודש (cutoff נקבע ב-`ARCHIVE_FALLBACK_MONTHS = 18`)
    - אם כן, וגם `process.env.ARCHIVE_SPREADSHEET_ID` מוגדר: הפונקציה מבצעת **שתי** קריאות Sheets — אחת ל-warm spreadsheet (כפי שהיום), אחת ל-archive spreadsheet עם אותו tab name + אותו range. ממזגת את התוצאות.
    - אם `from` חדש מ-18 חודש: רק קריאה ל-warm (זול).
    - אם `ARCHIVE_SPREADSHEET_ID` לא מוגדר: רק warm, גם אם הטווח ישן. הדשבורד מציג את מה שיש (פחות שורות → kpi חישוב על מה שיש). הסיכון מוגבל כי הפאזה הזו רק התקין ארכיון; ה-fallback fail-safe.
    - הקובץ מייצא משתנה `ARCHIVE_FALLBACK_MONTHS = 18` שניתן לכוונן.
    - שאר ה-routes (/api/campaigns, /api/products, /api/orders-attribution) **לא** מקבלים את ה-fallback בפאזה הזו — היקף הפאזה (ראה ROADMAP) דורש fallback רק ל-`/api/data` כי זה המקור המרכזי ל-historical analytics tab. הרחבה עתידית — שלב 7.
  </behavior>
  <action>
    עדכן `dashboard-web/src/lib/sheets.ts`:

    1. הוסף בראש הקובץ (אחרי הקבועים הקיימים):

    ```typescript
    /**
     * The dashboard's archive fallback (Phase 5).
     *
     * When the user picks a date range whose `from` is older than this many
     * months, /api/data fetches BOTH the warm spreadsheet AND the archive
     * spreadsheet, then merges. For shorter ranges we read warm only —
     * unchanged from pre-Phase-5 behavior, no perf cost added.
     *
     * Should match the same cutoff used in archiveOlderThan() on the
     * Apps Script side (18 months) so the boundary aligns: archive holds
     * rows older than 18 months, warm holds the rest, the dashboard
     * decides between them based on the requested `from`.
     */
    export const ARCHIVE_FALLBACK_MONTHS = 18;

    function getArchiveSpreadsheetId(): string | null {
      const id = process.env.ARCHIVE_SPREADSHEET_ID;
      return id ? id : null;
    }

    /**
     * Returns YYYY-MM-DD that is `months` months before today (UTC).
     * Used to determine whether a requested range crosses the archive
     * boundary — independent of TZ because we compare ISO strings.
     */
    function monthsAgoUtcStr(months: number): string {
      const now = new Date();
      const dt = new Date(Date.UTC(
        now.getUTCFullYear(),
        now.getUTCMonth() - months,
        now.getUTCDate(),
      ));
      return dt.toISOString().slice(0, 10);
    }
    ```

    2. **עדכן** את `fetchDailyData` (לאחר ה-`opts` שכבר נוסף ב-PLAN 02):

    ```typescript
    export async function fetchDailyData(opts?: { range?: DateRange }): Promise<DailyRow[]> {
      const auth = getAuth();
      const sheets = google.sheets({ version: 'v4', auth });
      const warmId = getSpreadsheetId();

      // Decide whether the archive read is needed. Only when:
      //   1. caller provided a range with `from` older than 18 months, AND
      //   2. ARCHIVE_SPREADSHEET_ID env var is set (otherwise silent no-op
      //      — Phase 5 ships with archive being optional; deployments
      //      without it still work, just won't show very old data).
      const archiveCutoff = monthsAgoUtcStr(ARCHIVE_FALLBACK_MONTHS);
      const archiveId = getArchiveSpreadsheetId();
      const needsArchive = !!archiveId && !!opts?.range && opts.range.from < archiveCutoff;

      const reads: Array<Promise<{ data: { values?: unknown[][] | null } }>> = [
        sheets.spreadsheets.values.get({
          spreadsheetId: warmId,
          range: `${DATA_TAB}!A2:K10000`,
          valueRenderOption: 'UNFORMATTED_VALUE',
          dateTimeRenderOption: 'FORMATTED_STRING',
        }) as Promise<{ data: { values?: unknown[][] | null } }>,
      ];
      if (needsArchive && archiveId) {
        reads.push(
          sheets.spreadsheets.values.get({
            spreadsheetId: archiveId,
            range: `${DATA_TAB}!A2:K100000`,  // archive may be larger
            valueRenderOption: 'UNFORMATTED_VALUE',
            dateTimeRenderOption: 'FORMATTED_STRING',
          }) as Promise<{ data: { values?: unknown[][] | null } }>,
        );
      }

      const responses = await Promise.all(reads);
      const allValues: unknown[][] = [];
      for (const r of responses) {
        const v = r.data.values ?? [];
        for (const row of v) allValues.push(row as unknown[]);
      }

      const rows: DailyRow[] = [];
      for (const row of allValues) {
        const dateStr = parseDate(row[0]);
        if (!dateStr) continue;
        if (opts?.range && !isInRange(dateStr, opts.range)) continue;
        const storeId = String(row[1] ?? '').trim();
        const storeName = String(row[2] ?? '').trim();
        if (!storeId || !storeName) continue;

        const fbSpend = parseNumber(row[3]);
        const gaSpend = parseNumber(row[4]);
        const totalSpend = parseNumber(row[5]) || fbSpend + gaSpend;
        const revenue = parseNumber(row[6]);
        const roas = totalSpend > 0 ? revenue / totalSpend : 0;
        const grossProfit = revenue - totalSpend;
        const cogs = revenue * COGS_RATE_OF_REVENUE;
        const netProfit = revenue - totalSpend - cogs;
        const hasCogs = true;

        rows.push({
          date: dateStr,
          storeId, storeName,
          fbSpend, gaSpend, totalSpend, revenue,
          roas, grossProfit, cogs, netProfit, hasCogs,
        });
      }
      return rows;
    }
    ```

    דבק את ה-`isInRange` import שכבר נוסף ב-PLAN 02. אם הפונקציה הפנימית למיון עדיין מסננת archive duplicates: השורות צריכות להיות שונות בין warm לארכיון (אחרי שה-archive script רץ — ה-warm לא מכיל יותר רשומות מתאריכים ישנים). אם בגלל race או טעות יש חפיפה, המיון הסופי לפי `date+storeId` כפי שכבר נעשה ב-`fetchDailyData` (כל פעם בנייה מחדש מאפס) פותר — אין dedup explicit נדרש בפאזה הזו, ה-cutoff בארכיון ובדשבורד אמורים להתאם.

    הערה: לא משנים את `/api/data/route.ts` — הוא ממשיך לקרוא ל-`fetchDailyData({ range })` כפי שיצא מ-PLAN 02. הלוגיקה החדשה כולה ב-lib.
  </action>
  <verify>
    <automated>cd dashboard-web && npx tsc --noEmit --project tsconfig.json 2>&1 | grep -v "^$" | head -20 && grep -c "ARCHIVE_SPREADSHEET_ID\|ARCHIVE_FALLBACK_MONTHS" src/lib/sheets.ts</automated>
  </verify>
  <done>
    - `ARCHIVE_FALLBACK_MONTHS = 18` מיוצא
    - `fetchDailyData` מבצע 2 קריאות sheets רק כש-(range.from ישן && env.ARCHIVE_SPREADSHEET_ID מוגדר)
    - אם ARCHIVE_SPREADSHEET_ID לא מוגדר: התנהגות זהה לפני PLAN 04 (fail-safe)
    - `npm run build` עובר
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apps Script archiveOlderThan → archive spreadsheet | פעולה הרסנית: כותב ומוחק שורות. dryRun ברירת מחדל. |
| /api/data → archive spreadsheet (read) | service-account scope readonly. אותו gate שכבר מגן על warm. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering / data loss | archiveOlderThan | mitigate | dryRun ברירת מחדל; append-then-delete; checkpoint:human-verify מחייב dry-run לפני production |
| T-05-04-02 | Repudiation | what was archived when | mitigate | Logger.log מפורט של (tab, count, oldest, newest) בכל הרצה; השורות נשמרות ב-archive spreadsheet (persistent log de facto) |
| T-05-04-03 | DoS via giant fetch | dashboard archive read | mitigate | רק כש-`from < 18 חודש`; ברירת מחדל של 90 ימים ב-PLAN 02 לא מפעילה את ה-archive read. ה-`from` תמיד מגיע ממשתמש = bounded. |
| T-05-04-04 | Information disclosure | archive spreadsheet access | mitigate | משוף עם אותו service-account ולא ב-permission רחב יותר. Vercel env var ARCHIVE_SPREADSHEET_ID לא חשוף ל-client (server-only). |
| T-05-04-05 | Race / inconsistency | archive ↔ warm boundary drift | accept | ה-cutoff באפס Script וב-dashboard נקבעים שניהם ל-18 חודש; אם operator מריץ archiveOlderThan(12) אבל הדשבורד עדיין ב-ARCHIVE_FALLBACK_MONTHS=18, יש fenestra של 6 חודשים שלא יראה (חסר ב-warm, אבל הדשבורד לא קורא ל-archive). מתועד; mitigation עתידית: ARCHIVE_FALLBACK_MONTHS גם הוא ב-env. |
</threat_model>

<verification>
- אחרי PLAN 04 (אחרי checkpoint):
  - `archiveOlderThan(18, {dryRun: true})` מדפיס summary סביר; אין כתיבה
  - `archiveOlderThan(18, {dryRun: false})` מעביר שורות (count כמו ב-dry-run); warm קטן, archive גדל
- בדשבורד, אחרי הגדרת `ARCHIVE_SPREADSHEET_ID` ב-Vercel:
  - בחר טווח של 2 שנים אחורה ב-Filters → /api/data חוזר עם payload שכולל גם שורות ישנות (ממוזג מהארכיון)
  - בחר טווח של 6 חודשים → /api/data לא מפעיל את ה-archive read (Network tab של DevTools — fetch אחד אל Google Sheets API)
</verification>

<success_criteria>
- `archiveOlderThan(months, {dryRun: true})` קיים, מדפיס summary מפורט, לא משנה כלום
- `archiveOlderThan(months, {dryRun: false})` מעביר שורות ב-3 שלבים (read warm → write archive → clear warm) בסדר הנכון
- 2 menu items זמינים ב-onOpen
- `fetchDailyData` קורא מהארכיון רק כש-`from < 18 חודש && ARCHIVE_SPREADSHEET_ID set`
- ה-fallback fail-safe: ללא env var, הדשבורד לא נשבר
- `npm run build` עובר
</success_criteria>

<output>
After completion, create `.planning/phases/05-scalability/05-04-SUMMARY.md`
</output>
