---
phase: 05-scalability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - DailyUpdate.gs
  - Main.gs
autonomous: false
requirements: [P5-TRIGGER-SPLIT]

must_haves:
  truths:
    - "שלושה triggers נפרדים מותקנים: runDailyUpdateUzoshop ב-00:05, runDailyUpdateZolplus ב-00:08, runDailyUpdateUsmile ב-00:11"
    - "trigger רביעי refreshAllStoreMeta ב-00:14"
    - "כל פונקציה חדשה (uzoshop/zolplus/usmile) מפעילה updateStoreForDate_ לחנות אחת בלבד"
    - "runDailyUpdate המקורי נשאר כ-entry point ידני לריצה רצופה של כל 3 החנויות (manual menu)"
    - "כל אחת מ-3 הפונקציות החדשות הורצה בהצלחה ידנית מהעורך לפני התקנת ה-triggers"
  artifacts:
    - path: "DailyUpdate.gs"
      provides: "runDailyUpdateUzoshop / runDailyUpdateZolplus / runDailyUpdateUsmile + runUpdateForSingleStore_ helper"
      contains: "function runDailyUpdateUzoshop"
    - path: "Main.gs"
      provides: "installDailyTrigger מתקין 4 triggers (3 חנויות + meta) במקום אחד"
      contains: "runDailyUpdateUzoshop"
  key_links:
    - from: "runDailyUpdateUzoshop"
      to: "updateStoreForDate_"
      via: "runUpdateForSingleStore_ helper שמוצא את החנות לפי id"
      pattern: "STORES.find.*uzoshop"
    - from: "installDailyTrigger"
      to: "runDailyUpdateUzoshop / runDailyUpdateZolplus / runDailyUpdateUsmile / refreshAllStoreMeta"
      via: "ScriptApp.newTrigger(...).atHour(0).nearMinute(...)"
      pattern: "ScriptApp\\.newTrigger\\('runDailyUpdate(Uzoshop|Zolplus|Usmile|)'"
---

<objective>
לפצל את ה-trigger היומי האחד (`runDailyUpdate` ב-00:05) ל-3 triggers נפרדים — אחד לכל חנות — וטריגר רביעי ל-`refreshAllStoreMeta`. כל חנות מקבלת תקציב 6 דקות נפרד; אין יותר quota cascade כשחנות 1 צורכת רוב המכסה והשאר נופלות ב-timeout.

Purpose: מסיר את הסיכון המרכזי שמתועד ב-CONCERNS.md ("Apps Script 6-min Execution Limit") — הוספת חנות 4 או גידול ב-orders-attribution היו מפילים את העדכון היומי.

Output: 3 פונקציות wrapper חדשות + helper משותף ב-`DailyUpdate.gs`, ו-`installDailyTrigger` חדש שמתקין 4 triggers במקום אחד.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
@DailyUpdate.gs
@Main.gs
@Config.gs

<interfaces>
<!-- חוזים שכבר קיימים — לעטוף אותם, לא לכתוב מחדש -->

From Config.gs:
```javascript
const STORES = [
  { id: 'uzoshop',   name: 'uzoshop',   hasGoogleAds: true  },
  { id: 'zolplus',   name: 'Zol Plus',  hasGoogleAds: false },
  { id: 'usmile360', name: '360usmile', hasGoogleAds: false },
];
function getStoreById(id) { return STORES.find(s => s.id === id); }
const TZ = 'Asia/Jerusalem';
const SUMMARY_TAB = 'סיכום';
function yesterdayStr_(): string;  // 'YYYY-MM-DD'
function todayStr_(): string;
```

From DailyUpdate.gs (existing):
```javascript
function runDailyUpdate(): void;         // יישאר manual entry — מריץ את 3 החנויות ברצף
function runLiveUpdate(): void;          // יישאר ללא שינוי
function runUpdateForDate(dateStr): void;
function updateStoreForDate_(ss, store, dateStr, year, month, day, ilsToCad): void;
function runUpdateForDateForStores_(dateStr, stores): void;  // helper קיים — אפשר להשתמש בו!
function notifyError_(dateStr, message): void;
```

From SheetBuilder.gs:
```javascript
function ensureSpreadsheet(): Spreadsheet;
function refreshAllStoreMeta(): void;    // יוקפץ ל-trigger רביעי
```

From Main.gs (existing):
```javascript
function installDailyTrigger(): void;    // יוחלף — יתקין 4 triggers
function removeDailyTrigger(): void;     // יוחלף — יסיר את כל ה-handlers החדשים
function installLiveTrigger(): void;     // נשאר ללא שינוי
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: הוסף 3 wrappers + helper משותף ל-DailyUpdate.gs</name>
  <files>DailyUpdate.gs</files>
  <behavior>
    - runDailyUpdateUzoshop() קורא ל-runUpdateForSingleStore_('uzoshop', yesterdayStr_())
    - runDailyUpdateZolplus() קורא ל-runUpdateForSingleStore_('zolplus', yesterdayStr_())
    - runDailyUpdateUsmile() קורא ל-runUpdateForSingleStore_('usmile360', yesterdayStr_())
    - runUpdateForSingleStore_(storeId, dateStr) מאתר את ה-store ב-STORES, מריץ ensureSpreadsheet + getFxRate + updateStoreForDate_, ומדווח שגיאות דרך notifyError_ עם prefix של שם החנות
    - השגיאות עוברות retain בדיוק כמו ב-runUpdateForDate המקורי (try/catch סביב updateStoreForDate_, errors[] -> notifyError_)
    - runDailyUpdate המקורי נשאר ללא שינוי (manual entry point — menu item או הרצה מהעורך)
  </behavior>
  <action>
    1. הוסף בראש DailyUpdate.gs (אחרי `runLiveUpdate`) שלוש פונקציות wrappers:

    ```javascript
    /**
     * Per-store daily trigger entry points (Phase 5).
     *
     * Each gets its own 6-min budget — eliminates the quota cascade where
     * store 2 + 3 used to time out because store 1 saturated the Sheets API
     * short-window quota. installDailyTrigger installs each at a different
     * minute (00:05, 00:08, 00:11) so they run sequentially but in
     * INDEPENDENT executions (each has its own 6-min cap).
     *
     * The original runDailyUpdate() is retained as a MANUAL entry point
     * for full-sequential runs (e.g., from the editor / spreadsheet menu).
     * Triggers no longer call it directly.
     */
    function runDailyUpdateUzoshop() {
      runUpdateForSingleStore_('uzoshop', yesterdayStr_());
    }
    function runDailyUpdateZolplus() {
      runUpdateForSingleStore_('zolplus', yesterdayStr_());
    }
    function runDailyUpdateUsmile() {
      runUpdateForSingleStore_('usmile360', yesterdayStr_());
    }
    ```

    2. מתחת להן הוסף helper משותף:

    ```javascript
    /**
     * Runs updateStoreForDate_ for a SINGLE store. Used by the per-store
     * trigger entry points (Phase 5 trigger split). Mirrors the error
     * handling shape of runUpdateForDate (per-store try/catch +
     * notifyError_ on failure) but DOES NOT touch the summary tab — the
     * summary is formula-driven and the next store's run (or the
     * next-day's run) will keep it consistent.
     */
    function runUpdateForSingleStore_(storeId, dateStr) {
      if (!/^\d{4}-\d{2}-\d{2}$/.test(dateStr)) {
        throw new Error(`תאריך לא תקין: ${dateStr}. נדרש פורמט YYYY-MM-DD.`);
      }
      const store = getStoreById(storeId);
      if (!store) throw new Error(`חנות לא ידועה: ${storeId}`);

      const ss = ensureSpreadsheet();
      const [year, month, day] = dateStr.split('-').map(Number);
      const ilsToCad = getFxRate('ILS', 'CAD', dateStr);
      Logger.log(`[${store.name}] FX ILS->CAD on ${dateStr}: ${ilsToCad}`);

      const errors = [];
      try {
        updateStoreForDate_(ss, store, dateStr, year, month, day, ilsToCad);
      } catch (e) {
        const msg = `[${store.name}] ${e && e.message ? e.message : e}`;
        errors.push(msg);
        Logger.log(`ERROR ${store.name}: ${e && e.stack ? e.stack : e}`);
      }

      if (errors.length) {
        const fullMsg = `ROAS per-store update ${dateStr} (${store.name}) completed with errors:\n` + errors.join('\n');
        Logger.log(fullMsg);
        notifyError_(dateStr, fullMsg);
      }
    }
    ```

    3. אל תיגע ב-`runDailyUpdate`, `runLiveUpdate`, `runUpdateForDate`, `updateStoreForDate_`, `runUpdateForDateForStores_`, `backfillRange`, `backfillRangeForStores`, `backfillZolUsmileMay1to14`, או `notifyError_` — כולם נשארים ללא שינוי.

    הערה למה לא להשתמש ב-`runUpdateForDateForStores_` הקיים: הוא מקבל array של stores ומכיל לולאה + sleep ביניהן. אנחנו רוצים פונקציה לחנות יחידה ללא ה-sleep הזה (ה-trigger כבר נותן הפרדה ב-3 דקות בין חנויות). שמירה על פונקציה ייעודית נפרדת מגדירה את החוזה בבירור: "trigger אחד = חנות אחת = ריצה אחת".
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('DailyUpdate.gs','utf8');['runDailyUpdateUzoshop','runDailyUpdateZolplus','runDailyUpdateUsmile','runUpdateForSingleStore_'].forEach(fn=>{if(!new RegExp('function '+fn+'\\\\b').test(s))throw new Error('missing '+fn)});if(!/function runDailyUpdate\(\)/.test(s))throw new Error('runDailyUpdate removed by mistake');console.log('OK')"</automated>
  </verify>
  <done>
    - 4 פונקציות חדשות קיימות ב-DailyUpdate.gs
    - `runDailyUpdate()` המקורי עדיין שם (manual entry)
    - אין שינוי בחתימה של פונקציות קיימות אחרות
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: עדכון installDailyTrigger ב-Main.gs להתקין 4 triggers</name>
  <files>Main.gs</files>
  <behavior>
    - installDailyTrigger מתקין 4 triggers (uzoshop@00:05, zolplus@00:08, usmile@00:11, refreshAllStoreMeta@00:14) באזור זמן Asia/Jerusalem
    - removeDailyTrigger מסיר את כל ה-handlers של 4 הפונקציות החדשות (וגם של runDailyUpdate הישן — כדי שניתן יהיה לעבור deployment ישן->חדש)
    - אין הרצה מיידית של 3 הפונקציות בהתקנה (לעומת הגרסה הישנה): ההרצה הראשונה ב-00:05 הקרובה. הוספנו זאת כברירת מחדל כי הרצה מיידית של 3 חנויות ברצף מסכנת timeout במצב התקנה (ה-trigger יסדר את זה ממילא). תיעוד ב-comment.
    - תפריט ה-onOpen לא דורש שינוי — `runDailyUpdate` עדיין קיים כפריט "הרץ עדכון ליום אתמול"
  </behavior>
  <action>
    החלף את הפונקציות `installDailyTrigger` ו-`removeDailyTrigger` ב-Main.gs כך:

    ```javascript
    /**
     * Installs FOUR daily triggers (Phase 5 trigger split):
     *   - runDailyUpdateUzoshop      @ 00:05 IL
     *   - runDailyUpdateZolplus      @ 00:08 IL
     *   - runDailyUpdateUsmile       @ 00:11 IL
     *   - refreshAllStoreMeta        @ 00:14 IL
     *
     * Each per-store function runs in its OWN Apps Script execution, so each
     * gets its OWN 6-min budget and its OWN Sheets API short-window quota
     * window. The 3-min spacing between them gives the quota a chance to
     * refresh between executions.
     *
     * Note: unlike the old installer, this one does NOT run any of the
     * functions immediately. The first per-store run happens at the next
     * scheduled minute (e.g., next 00:05). Rationale: an immediate
     * sequential run of all 3 stores from inside installDailyTrigger would
     * defeat the very quota separation we're trying to set up. If you want
     * to backfill yesterday right now, run `runDailyUpdate` (the manual
     * entry point that retains the original sequential behavior) from the
     * editor.
     */
    function installDailyTrigger() {
      removeDailyTrigger();

      ScriptApp.newTrigger('runDailyUpdateUzoshop')
        .timeBased().atHour(0).nearMinute(5).everyDays(1).inTimezone(TZ).create();
      ScriptApp.newTrigger('runDailyUpdateZolplus')
        .timeBased().atHour(0).nearMinute(8).everyDays(1).inTimezone(TZ).create();
      ScriptApp.newTrigger('runDailyUpdateUsmile')
        .timeBased().atHour(0).nearMinute(11).everyDays(1).inTimezone(TZ).create();
      ScriptApp.newTrigger('refreshAllStoreMeta')
        .timeBased().atHour(0).nearMinute(14).everyDays(1).inTimezone(TZ).create();

      Logger.log('4 daily triggers installed: uzoshop@00:05, zolplus@00:08, usmile@00:11, store-meta@00:14 (Asia/Jerusalem)');
      Logger.log('First run at next 00:05 IL. To backfill yesterday immediately, run runDailyUpdate from the editor.');
    }

    /**
     * Removes ALL handlers that the daily-trigger installer knows about,
     * including the legacy single-trigger handler ('runDailyUpdate') so a
     * re-install after deploying Phase 5 doesn't leave the old trigger
     * orphaned. installLiveTrigger remains untouched.
     */
    function removeDailyTrigger() {
      const HANDLERS = new Set([
        'runDailyUpdate',          // legacy — pre-Phase-5
        'runDailyUpdateUzoshop',
        'runDailyUpdateZolplus',
        'runDailyUpdateUsmile',
        'refreshAllStoreMeta',
      ]);
      const triggers = ScriptApp.getProjectTriggers();
      let removed = 0;
      for (const t of triggers) {
        if (HANDLERS.has(t.getHandlerFunction())) {
          ScriptApp.deleteTrigger(t);
          removed++;
        }
      }
      Logger.log(`Removed ${removed} daily/store-meta trigger(s)`);
    }
    ```

    אל תיגע ב-`setupAll`, `setupCreateSheet`, `installLiveTrigger`, `removeLiveTrigger`, `onOpen`, `promptRunForDate_`, `promptBackfill_`, `showSpreadsheetUrl_`, או `showVerifyConfig_`. הם נשארים ללא שינוי.

    שים לב: `setupAll` ממשיך לקרוא ל-`installDailyTrigger()` הקיים בלי שינוי לוגי. השינוי הוא רק במה שהפונקציה מתקינה.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('Main.gs','utf8');['runDailyUpdateUzoshop','runDailyUpdateZolplus','runDailyUpdateUsmile','refreshAllStoreMeta'].forEach(fn=>{if(!new RegExp(\"newTrigger\\\\('\"+fn+\"'\\\\)\").test(s))throw new Error('trigger missing for '+fn)});if(!/atHour\(0\)\.nearMinute\(5\)/.test(s))throw new Error('00:05 not present');if(!/atHour\(0\)\.nearMinute\(14\)/.test(s))throw new Error('00:14 not present');if(!/'runDailyUpdate',\s+\/\/ legacy/.test(s) && !/'runDailyUpdate'/.test(s))throw new Error('legacy runDailyUpdate not in remove list');console.log('OK')"</automated>
  </verify>
  <done>
    - `installDailyTrigger` מתקין בדיוק 4 triggers
    - `removeDailyTrigger` מנקה את כל 5 ה-handlers (4 חדשים + 1 legacy)
    - אף פונקציה אחרת ב-Main.gs לא השתנתה
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Per-store trigger split: 3 פונקציות חדשות + installer מעודכן. השינוי **לא** מותקן עדיין ב-Apps Script production — צריך upload ידני של 2 הקבצים והרצה ידנית של ה-3 wrappers לפני התקנת ה-triggers.
  </what-built>
  <how-to-verify>
    1. **Upload ה-`.gs`** לעורך Apps Script (DailyUpdate.gs + Main.gs).
    2. **הרץ ידנית** מהעורך, אחד אחרי השני (לא במקביל):
       - `runDailyUpdateUzoshop` → צריך להסתיים תוך ~2 דקות, ולכתוב שורה ל-`uzoshop` ב-data-daily / products-daily / uzoshop-campaigns / uzoshop-ads / uzoshop-orders-attribution לאתמול
       - `runDailyUpdateZolplus` → ~1-2 דקות, שורה ל-Zol Plus
       - `runDailyUpdateUsmile` → ~1-2 דקות, שורה ל-360usmile
    3. **אם כל אחת מסתיימת ב-OK** (אין מייל שגיאה, יש שורה חדשה ב-data-daily לאתמול לכל החנויות) — הרץ `installDailyTrigger`. תקבל ב-log: "4 daily triggers installed".
    4. **בדוק ב-Project Triggers (Apps Script UI)**: צריך להופיע 4 triggers חדשים + הישן `runLiveUpdate` (סה"כ 5). הישן `runDailyUpdate` אמור להעלם.
    5. **לאחר 24 שעות**: בדוק ב-Executions tab שהארבעה רצו בנפרד בין 00:05 ל-00:14, כל אחד תחת 3 דקות.

    אם אחד מ-3 ה-wrappers נכשל ידנית, **אל תתקין את ה-triggers** — תקן ב-DailyUpdate.gs לפני שממשיכים.
  </how-to-verify>
  <resume-signal>"approved — triggers running cleanly" או תיאור הבעיה</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apps Script trigger scheduler → updateStoreForDate_ | פנימי — אין input חיצוני |
| External APIs (Shopify/Meta/Google) → updateStoreForDate_ | כבר מוגן ע"י fetchWithRetry_ + try/catch קיימים |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Denial of Service | per-store trigger | mitigate | 3-min spacing בין triggers + לא מריצים immediately ב-installer (חוסך quota burst) |
| T-05-01-02 | Tampering | trigger registry | accept | רק owners של ה-Apps Script project יכולים לערוך triggers; כבר מנוהל ע"י Google IAM |
| T-05-01-03 | Repudiation | who ran which trigger | accept | Apps Script Executions log מתעד אוטומטית; אין PII |
</threat_model>

<verification>
- אחרי 24 שעות של ריצה אוטומטית: 3 שורות חדשות ב-data-daily לאתמול (אחת לכל חנות), 3 הרצות נפרדות ב-Executions, אף אחת לא חצתה 3 דקות
- אם הרצה נכשלה — מייל הגיע ל-`notification.email` עם prefix `[<store_name>]`
- ה-store-meta tab מתעדכן ב-00:14 (`updatedAt` column)
</verification>

<success_criteria>
- 3 פונקציות wrapper + 1 helper קיימות ב-DailyUpdate.gs
- `installDailyTrigger` מתקין 4 triggers נפרדים
- כל פונקציה רצה תחת 3 דקות באופן עצמאי (אומת בעדויות checkpoint)
- ה-original `runDailyUpdate` עדיין עובד כ-fallback ידני
</success_criteria>

<output>
After completion, create `.planning/phases/05-scalability/05-01-SUMMARY.md`
</output>
