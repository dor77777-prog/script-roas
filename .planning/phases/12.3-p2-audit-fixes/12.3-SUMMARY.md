# Phase 12.3 P2 Audit Fixes — Summary

**Baseline:** 1017 tests across 102 files (post-12.2).
**Target:** BASELINE + 8-12 tests (one per P2 fix + 1-2 for ALG-03 chronological sort coverage).
**Started:** 2026-05-24

## Pre-flight Verification (Task 1)

- `git log -1 -- 12.3-CONTEXT.md` → `194b057` (committed alongside 12.2/12.3 phase insert).
- `npx tsc --noEmit` → exit 0, zero errors.
- `npm test --silent` → 1017/1017 passed across 102 files.
- Most recent fix commits visible in `git log`: Phase 12.2.x sweep (a45a302, d1f5c31, 2a5dabd, db1212a, 99c30e2, 296ba36, c0a7a34, 7596482, e0e8852).

## INN-05 — RESOLVED BY 12.1.1 (no Phase 12.3 work)

**Original bug:** SELECT-then-WRITE inside persist-rolling-3day non-idempotent on Inngest retry (raw-returns/inngest_cronLive.json:7-15).

**12.1.1 INN-10 fix cascade:** Commit `55e709c` (and the broader 12.1.1 sweep) moved the SELECT to a separate `step.run('select-prior-spend-${date}-${storeId}')` at `cronLive.ts:816-832` executed BEFORE `persist-rolling-3day` at `cronLive.ts:835`. Inngest memoizes step.run results across retries, so the SELECT result is stable — INN-05's root cause is eliminated as a side-effect.

**Verification (read-only grep at lib path `dashboard-web/src/inngest/functions/cronLive.ts`):**

```
$ grep -nE "select-prior-spend|persist-rolling-3day" cronLive.ts
63: *   step.run('persist-rolling-3day')         — SELECT+UPSERT loop over 3 days
73: *   - persist-rolling-3day: SELECT + UPSERT with ON CONFLICT. Re-running
363:   * Pre-fix: persist-rolling-3day's `if (!shopifyOk) continue;`
804:  // Inngest retries. Pre-fix the SELECT lived INSIDE persist-rolling-3day's
817:      `select-prior-spend-${date}-${storeId}`,
835:  await step.run('persist-rolling-3day', async () => {
933:        // the select-prior-spend-* step.runs above. Pre-fix this was
934:        // an inline SELECT inside persist-rolling-3day — non-idempotent
```

- Lines 814-833: pre-populated `priorSpendByDate` map via per-date `step.run('select-prior-spend-...')` callbacks.
- Line 835: `persist-rolling-3day` step.run callback begins. Code path from line 835-948 reads `priorSpendByDate[date]` (line 869, 937) but DOES NOT call `admin.from('data_daily').select(...)` at all. Confirmed via reading 835-960 — no inline SELECT inside the persist callback.

**Tests:** `cronLiveRetryIdempotency.test.ts` (added in 12.1.1) already pins this behavior.

**Action:** No code change. No new test.

## ALG-03 — PARTIALLY RESOLVED BY 12.1.2 + belt-and-suspenders sort in 12.3

**Original bug:** last-write-wins over unordered `campaigns` iteration; raw-returns/lib_algorithm_aiReport.json (ALG-03 lines 28-34).

**12.1.2 ALG-04 fix:** storeId-scoped key (`${storeId}::${platform}::${campaignId}`) eliminated CROSS-STORE collisions on the 'All' view.

**Remaining gap (pre-12.3):** WITHIN a single (storeId, platform, campaignId), iteration order over `campaigns` is fetcher-dependent → non-deterministic latest status when the same campaign appears on multiple dates (e.g. paused mid-window then re-activated).

**12.3 fix:** `[...campaigns].sort((a, b) => a.date.localeCompare(b.date))` BEFORE the statusByCampaign loop at `aiReport.ts:1390-1392` (post-fix line numbers). Commit `38f6de7`. Test `aiReportStatusFreshness.test.ts` (3 tests).

**Other ALG-03-cited locations (line 1287-1320):** Read confirms this region is the Health Score render block, NOT another `statusBy*` map. Only one `statusByCampaign`-style map exists; the sort fix at that single site is sufficient.

**Revert-verify:** `git stash` of aiReport.ts → Test 1 (out-of-order input) RED; `git stash pop` → 3/3 GREEN.

## Final Verification Gates (CONTEXT D-06)

| Gate | Result |
|---|---|
| `cd dashboard-web && npx tsc --noEmit` | PASS (0 errors) |
| `cd dashboard-web && npm test --silent` | PASS — 1040/1040 (+23 vs baseline 1017) across 109 test files (+7) |
| 7 fix commits on main with `(Phase 12.3)` suffix + 1 follow-up | 38f6de7, 35bf236, 342f805, 844bb71, f4ee3e9, 05aa17b, 3d9b43d, 62f533b (follow-up) |
| Scope-creep audit (only intended source files + new tests) | PASS (6 routes + aiReport.ts + 1 new lib + 7 test files) |
| Forbidden-surface check (12.1/12.2 files untouched) | PASS (cronLive/cronDaily/eventBackfill/multiMappingCohort/productCentricView/insights/attributionAnalysis/ordersAttribution/products — none touched) |

## Per-Fix Outcomes

| # | Finding | Source | Test | Commit | Verdict |
|---|---|---|---|---|---|
| 1 | API-10 | shopify-fetch/route.ts | shopifyFetchTokenLeak.test.ts | 35bf236 | PASS |
| 2 | API-18 | tiktok/callback/route.ts | tiktokCallbackCsp.test.ts | 342f805 | PASS |
| 3 | API-23 | operator/jobs/route.ts | operatorJobsConcurrentFanout.test.ts | 844bb71 | PASS |
| 4 | API-26 | operator/manual-overrides/route.ts + lib/operatorManualOverrides.ts | operatorManualOverridesSpendStrict.test.ts | f4ee3e9 + 62f533b | PASS |
| 5 | API-32 | operator/reset/route.ts | operatorResetConcurrentDelete.test.ts | 05aa17b | PASS |
| 6 | API-37 + API-38 | operator/token-failures/route.ts | operatorTokenFailuresErrorRedaction.test.ts | 3d9b43d (composite per D-04) | PASS |
| 7 | INN-05 | (none — cascade-resolved by 12.1.1) | (none — covered by cronLiveRetryIdempotency from 12.1.1) | (no commit) | CASCADE-RESOLVED |
| 8 | ALG-03 | aiReport.ts (chronological sort) | aiReportStatusFreshness.test.ts | 38f6de7 | PASS (belt-and-suspenders) |

## Test Path Deviation (Rule 3)

The PLAN's `files_modified` declared test paths under `dashboard-web/src/app/api/.../__tests__/`. These paths fall outside the project's vitest include glob (`src/lib/**/__tests__` + `src/inngest/**/__tests__` per `vitest.config.ts`). Editing `vitest.config.ts` was NOT in the plan's files_modified list. Per Rule 3 (blocking issue — test files would be silently skipped), all 7 new test files landed at `src/lib/__tests__/` which:
- Matches the existing vitest include glob (no config change required).
- Matches the existing operator-route test convention (`operatorReset.test.ts`, `operatorRoutesDateValidation.test.ts` already live there).
- Preserves all behavior the PLAN required.

## Follow-up Refactor (Rule 3, commit 62f533b)

The Phase 12.3 API-26 fix `<action>` instructed `export function validatePost` directly on `app/api/operator/manual-overrides/route.ts`. The first commit (f4ee3e9) did this and broke `tsc --noEmit` post-fact (Next 14's `OmitWithTag<typeof routeModule, GET | POST | PATCH | DELETE | ...>` generated type constraint rejects any non-HTTP-verb export from `app/**/route.ts`). Per Rule 3 (blocking issue), follow-up commit 62f533b extracted `parseStrictNumeric` + `validatePost` + `VALID_*` sets to a new pure module `src/lib/operatorManualOverrides.ts` where they can be `export`ed for unit testability without violating Next's route-module contract. Behavior unchanged; +1 file delta.

## Commit Count Reconciliation

Plan target: **7** atomic fix commits (1 ALG-03 + 5 individual API + 1 API-37/38 composite).
Actual: **8** commits (the 7 above + 1 follow-up `62f533b` for the Next route-module export constraint). The +1 deviation is documented under "Follow-up Refactor" above and is purely a TS-compat refactor — no behavior change, no scope creep.

## Verdict

PASS — 6/6 P2 API route fixes shipped + 1 belt-and-suspenders algorithm fix (ALG-03) + 1 cascade-resolved (INN-05). 7 atomic fix commits + 1 follow-up refactor. 23 new regression tests pin every fix. Zero scope creep into 12.1/12.2 surface. tsc clean, vitest 1040/1040.

## Self-Check: PASSED

All 8 source/test files verified present on disk; all 8 commit SHAs verified present in git log.

